<Page
    x:Class="NewSchool.Board.Pages.PostEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewSchool.Board.Pages"
    xmlns:controls="using:NewSchool.Board.Controls"
    xmlns:appcontrols="using:NewSchool.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="16,8,16,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row0: 메타 정보 한 줄 -->
        <Grid Grid.Row="0" ColumnSpacing="8" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="PageTitle"
                       Grid.Column="0"
                       Text="새 글 쓰기"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"/>

            <ComboBox x:Name="CategoryComboBox"
                      Grid.Column="1"
                      IsEditable="True"
                      PlaceholderText="카테고리"
                      MinWidth="120"
                      SelectionChanged="CategoryComboBox_SelectionChanged"
                      TextSubmitted="CategoryComboBox_TextSubmitted"/>

            <ComboBox x:Name="SubjectComboBox"
                      Grid.Column="2"
                      IsEditable="True"
                      PlaceholderText="주제 선택/입력"
                      MinWidth="180"
                      SelectionChanged="SubjectComboBox_SelectionChanged"
                      TextSubmitted="SubjectComboBox_TextSubmitted"/>

            <Button x:Name="InsertRosterButton"
                    Grid.Column="3"
                    Click="InsertRosterButton_Click"
                    VerticalAlignment="Center"
                    Padding="8,6">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE8A5;" FontSize="14"/>
                    <TextBlock Text="명렬표 삽입"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Row1: 제목 -->
        <TextBox x:Name="TitleTextBox"
                 Grid.Row="1"
                 PlaceholderText="제목을 입력하세요"
                 MaxLength="200"
                 Margin="0,0,0,6"/>

        <!-- Row2: 내용 (Jodit HTML 에디터) -->
        <appcontrols:JoditEditor x:Name="ContentEditor"
                                 Grid.Row="2"
                                 Mode="Full"/>

        <!-- Row3: 첨부파일 -->
        <controls:PostFileListBox x:Name="FileListBox"
                                  Grid.Row="3"
                                  Margin="0,4,0,0"/>

        <!-- Row4: 버튼 -->
        <Grid Grid.Row="4" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1"
                    Content="취소"
                    Click="CancelButton_Click"
                    Margin="0,0,10,0"
                    MinWidth="100"/>

            <Button Grid.Column="2"
                    x:Name="SaveButton"
                    Content="저장"
                    Click="SaveButton_Click"
                    Style="{StaticResource AccentButtonStyle}"
                    MinWidth="100"/>
        </Grid>
    </Grid>
</Page>
