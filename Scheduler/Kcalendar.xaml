<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NewSchool.Scheduler.Kcalendar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewSchool.Scheduler"
    xmlns:Control="using:NewSchool.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border BorderBrush="CornflowerBlue" BorderThickness="0.5" CornerRadius="5,5,5,5"
            Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid Margin="2,2,2,2">
            <Grid.RowDefinitions>
                <RowDefinition x:Name="RowHeader" Height="32"/>
                <RowDefinition x:Name="RowBody" />
            </Grid.RowDefinitions>
            <!--윈도우 타이틀-->
            <!-- 달력 header-->
            <Border Grid.Row="0" >
                <Grid VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="BtnPrevious" Grid.Column="0" ToolTipService.ToolTip="이전 달" FontSize="14" Margin="2,2,2,2" Click="BtnPrevious_Click" Padding="6,2,6,2" Background="{x:Null}" BorderBrush="{x:Null}" HorizontalAlignment="Right">
                        <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE76B;" FontSize="18"/>
                    </Button>
                    <Control:MonthPicker x:Name="PickerMonth" Grid.Column="1" SelectedMonthChanged="PickerMonth_SelectedMonthChanged" />

                    <Button x:Name="BtnNext" Grid.Column="2" ToolTipService.ToolTip="다음 달" FontSize="14" Margin="2,2,2,2" Click="BtnNext_Click" Padding="6,2,6,2" Background="{x:Null}" BorderBrush="{x:Null}" HorizontalAlignment="Left">
                        <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE76C;" FontSize="18"/>
                    </Button>

                    <!-- 열은 0~3 이다. 예전에 Grid.Column="4" 로 적혀 있었는데, WinUI 가 범위를
                         넘는 값을 마지막 열로 클램프해 준 덕에 우연히 의도대로 보였을 뿐이다
                         (열을 하나라도 추가하면 이 버튼이 조용히 딸려 간다). -->
                    <!-- 개인정보처리방침이 "Google 연동 해제" 경로로 가리키는 버튼이다.
                         이름이 없으면 방침을 보고 찾아온 사람이 어디를 눌러야 할지 알 수 없다. -->
                    <Button x:Name="BtnSetup" Grid.Column="3" ToolTipService.ToolTip="캘린더 설정 (Google 연동)" Margin="4,2,4,2" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="4,2,4,2" Click="BtnSetup_Click" Background="{x:Null}" BorderBrush="{x:Null}">
                        <Button.Content>
                            <TextBlock FontFamily="{StaticResource SymbolThemeFontFamily}" Text="&#xE713;" FontSize="18"/>

                        </Button.Content>
                    </Button>
                </Grid>
            </Border>
            <!-- 달력 body-->
            <!-- 달력 body - 기존 GridBody 부분 교체 -->
            <Border Grid.Row="1" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0.5" Margin="2,2,2,2">
                <Grid x:Name="GridBody">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition MaxHeight="60" Height="Auto"/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <!-- 요일 헤더 - 오른쪽과 아래쪽 테두리만 -->
                    <Border Grid.Column="0" Grid.Row="0" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0.5,0.5" Padding="8">
                        <TextBlock x:Name="TxtWeekdaySun" Text="일" Foreground="Red" FontSize="12" FontWeight="Bold" HorizontalTextAlignment="Center" />
                    </Border>
                    <Border Grid.Column="1" Grid.Row="0" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0.5,0.5" Padding="8">
                        <TextBlock x:Name="TxtWeekdayMon" Text="월" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="12" FontWeight="Bold" HorizontalTextAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="2" Grid.Row="0" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0.5,0.5" Padding="8">
                        <TextBlock x:Name="TxtWeekdayTue" Text="화" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="12" FontWeight="Bold" HorizontalTextAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="3" Grid.Row="0" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0.5,0.5" Padding="8">
                        <TextBlock x:Name="TxtWeekdayWed" Text="수" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="12" FontWeight="Bold" HorizontalTextAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="4" Grid.Row="0" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0.5,0.5" Padding="8">
                        <TextBlock x:Name="TxtWeekdayThu" Text="목" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="12" FontWeight="Bold" HorizontalTextAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="5" Grid.Row="0" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0.5,0.5" Padding="8">
                        <TextBlock x:Name="TxtWeekdayFri" Text="금" Foreground="{ThemeResource TextFillColorPrimaryBrush}" FontSize="12" FontWeight="Bold" HorizontalTextAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="6" Grid.Row="0" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0,0.5" Padding="8">
                        <TextBlock x:Name="TxtWeekdaySat" Text="토" Foreground="Blue" FontSize="12" FontWeight="Bold" HorizontalTextAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Border>

</Page>
