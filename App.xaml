<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="NewSchool.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NewSchool"
    xmlns:converters="using:NewSchool.Converters"
    xmlns:helpers="using:NewSchool.Helpers">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Converters -->
            <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
            <converters:InverseStringToVisibilityConverter x:Key="InverseStringToVisibilityConverter"/>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
            <converters:BoolToStrikethroughConverter x:Key="BoolToStrikethroughConverter"/>
            <converters:BoolToStarConverter x:Key="BoolToStarConverter"/>
            <converters:BoolToOpacityConverter x:Key="BoolToOpacityConverter"/>

            <!-- Global TextBox Style: Drag-and-Drop + 한글 IME 기본 -->
            <Style TargetType="TextBox" BasedOn="{StaticResource DefaultTextBoxStyle}">
                <Setter Property="helpers:TextBoxDropHelper.EnableTextDrop" Value="True"/>
                <Setter Property="helpers:KoreanImeHelper.UseHangul" Value="True"/>
            </Style>

            <!-- 대시보드 카드 공용 스타일 (TodayPage·공유 컨트롤 통일 · 라이트/다크 대응) -->
            <!-- 헤더 강조색 옅은 틴트 밴드 (테마 강조색 기반 → 다크모드 자동 대응) -->
            <SolidColorBrush x:Key="DashboardHeaderTintBrush"
                             Color="{ThemeResource SystemAccentColor}" Opacity="0.12"/>
            <Style x:Key="DashboardCard" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultSolidBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
            </Style>
            <Style x:Key="DashboardCardHeader" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource DashboardHeaderTintBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultSolidBrush}"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="Padding" Value="12,9"/>
                <Setter Property="CornerRadius" Value="7,7,0,0"/>
            </Style>
            <Style x:Key="DashboardCardHeaderIcon" TargetType="FontIcon">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Foreground" Value="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
            </Style>
            <Style x:Key="DashboardCardHeaderTitle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <!-- Other app resources here -->
        </ResourceDictionary>
    </Application.Resources>
</Application>
