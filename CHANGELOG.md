# Changelog

## 다음 판 (작업 중)

### 고침 — 누가기록·학생부에서 조용히 사라지던 것들

> 누가기록·학생부 축을 전수로 훑으면서 나온 것들이다. 공통점은 **오류가 뜨지 않는다**는 것 —
> 예외도 경고도 없이 칸이 비어 나가거나, 넣은 값이 아무 데도 도착하지 않았다.

- **학급 일괄 입력 창이 안내만 띄우고 닫히며 입력이 통째로 사라지던 것을 고쳤다.** 학생을
  고르지 않고 [저장]을 누르면 "학생을 선택해주세요" 가 뜬 다음 **창이 그대로 닫혔다** —
  적어 둔 내용이 전부 없어졌다. 교사 정보가 없을 때, 교사 ID 가 등록되어 있지 않을 때도
  같았다. 이제 저장에 실패하면 창을 닫지 않는다.
- **기록 편집 창이 저장 실패를 성공으로 알리던 것을 고쳤다.** 이미 지워진 기록을 고치면
  한 줄도 반영되지 않았는데 창은 "저장됨"으로 닫혔다. 이제 반영 여부를 확인하고, 실패하면
  이유를 알린 뒤 창을 열어 둔다.
- **엑셀의 "동아리" 열이 언제나 비어 있던 것을 고쳤다.** 동아리명이 들어가는 칸이 경로마다
  달랐고(단일 입력은 동아리 칸, 일괄 입력은 활동명 칸) 목록·인쇄·엑셀이 읽는 칸은 또 과목명
  칸이었다. 이제 동아리명은 한 칸에만 넣고 화면·인쇄·엑셀이 같은 값을 본다. 일괄 입력이
  **활동명을 동아리명으로 덮어쓰던 것**도 함께 고쳤다 — 교사가 적은 활동명이 사라졌다.
- **상담기록과 종합의견이 엑셀에서 빈칸으로 나가던 것을 고쳤다.** 입력창에 "기록" 칸과
  "활동 내용" 칸이 따로 있는데, 한쪽이 비면 다른 쪽을 쓰도록 한 코드가 실제로는 한 번도
  물러서지 않았다. 상담 내용을 "기록" 칸에만 적으면 엑셀 상담내용 열이 통째로 비었다.
- **진로활동 희망분야에 "홍길동 진로활동" 이 자동으로 들어가던 것을 고쳤다.** 진로활동은
  희망분야와 특기사항을 **합쳐** 분량 한도를 적용하므로, 자동으로 채운 글자만큼 쓸 수 있는
  분량이 깎였다. 이제 비워 둔다(다른 두 입력 화면과 같다).
- **전출·졸업한 학생에게 그 뒤 날짜로 기록이 조용히 남던 것을 고쳤다.** 학적 확인은 누가기록을
  저장하는 여섯 경로 중 **한 곳에만** 걸려 있었다. 누가기록 전용 화면·동아리활동·수업활동·
  입력 창은 각자 저장 루프를 갖고 있어 검사를 건너뛰었다. 이제 여섯 경로가 같은 검사를 받는다.
- 일괄 입력이 저장 도중 실패하면 오류창이 **두 번** 뜨던 것, 학생 한 명마다 DB 연결을 새로
  열던 것(반 30명이면 30개)을 함께 고쳤다.

### 새로 생김 — 학생부 기록 마감

- **[마감] 버튼을 넣었다.** 학생부 기록 칸(누가기록 화면 오른쪽)과 일괄 입력 창에 자물쇠
  버튼이 생겼다. 마감하면 그 기록은 **수정과 삭제가 잠긴다** — 다른 화면에서 들어와도
  마찬가지다. 언제든 같은 버튼으로 해제할 수 있다.
  마감 여부를 **읽는 곳**은 오래전부터 다섯이었는데(읽기 전용 전환, 목록의 마감 필터,
  엑셀·CSV 의 "마감" 열, 수정 거부) 정작 **마감하는 방법이 없었다**. 그래서 일괄 출력에서
  "마감만" 을 고르면 언제나 "조건에 맞는 기록이 없습니다" 가 떴다.

### 고침 — 저장이 절반만 되던 것

- **학급 일괄 입력이 도중에 실패하면 앞부분만 저장되던 것을 고쳤다.** 학생마다 따로 넣어서,
  열 번째에서 막히면 앞의 아홉 명은 DB 에 남고 뒤는 안 들어갔다. 사용자가 다시 저장을 누르면
  그 아홉 명에게 **같은 기록이 한 벌 더** 생겼다. 이제 한 번에 넣고, 하나라도 실패하면 전부
  되돌린 뒤 "한 건도 저장되지 않았습니다" 라고 알린다.
- **학생부 기록 저장도 같게 맞췄다.** 같은 표를 저장하는데 교과 세특 화면만 전체 되돌리기를
  하고 학생부 기록 화면은 건별로 돌았다. 두 화면이 다르게 동작할 이유가 없다.

### 바뀜 — 느리던 곳

- **학생부 일괄 출력이 학생 수만큼 DB 를 조회하던 것을 한 번으로 줄였다.** 같은 화면의
  목록 조회는 이미 한 번에 읽고 있었는데 출력 쪽만 옛 방식이었다.
- **누가기록 화면이 작업할 때마다 DB 연결을 새로 열던 것을 고쳤다.** 화면이 이미 연결을
  하나 들고 있으면서 정작 저장·삭제·조회는 매번 새로 열었다 — 들고 있던 연결은 열렸다가
  닫히기만 하고 한 번도 쓰이지 않았다.

### 고침 — 화면과 종이가 다르게 보이던 것

- **누가기록 영역 색이 화면과 인쇄물에서 서로 다르던 것을 고쳤다.** 색표가 두 벌이라
  자율활동은 화면에서 하늘색인데 인쇄하면 녹색, 동아리활동은 보라인데 주황, 봉사활동은
  녹색인데 분홍으로 나왔다. 색으로 영역을 구분하던 사람에게는 같은 기록이 두 얼굴이었다.
  이제 화면의 표 하나만 쓴다.
- **역할·기른 능력만 적은 기록이 요약에서 통째로 빠지던 것을 고쳤다.** "구조화된 기록인가"를
  두 곳이 서로 다르게 판단했다 — 한쪽은 일곱 칸을 보고 다른 쪽은 앞의 세 칸만 봤다.
  그래서 역할만 적으면 내보내기·인쇄가 "구조화된 기록"으로 보고 초안을 요청하는데, 정작
  초안은 빈 기록 칸을 돌려줬다. 앞머리가 없을 때 문장이 "자율활동 , 팀장." 처럼 나오던
  것도 함께 다듬었다.

## v1.0.1 (2026-09-01 게시)

> 첫 실행 다음에 무엇을 해야 하는지 알려 주는 데 초점을 둔 판이다. 초기 설정이
> 학교·사용자·학년도까지만 받고 끝나서, 설정을 마친 사용자가 빈 화면 앞에 남았다.

### 새로 생김 — 첫 실행 다음에 무엇을 할지 알려 준다

- **초기 설정에 [4단계: 학사일정]을 넣었다.** 학교를 고르고 [완료]를 누르면 그 학년도 학사일정을
  NEIS 에서 한 번에 받아 저장한다(기본으로 켜져 있고, 체크를 풀면 건너뛴다). 예전에는 초기
  설정이 학교·사용자·학년도까지만 받고 끝나서, 달력을 열어도 학사일정이 비어 있었다.
  가져오기가 실패해도 나머지 설정은 그대로 저장한다 — 이유를 알리고 [설정 → 학사일정 관리]로
  안내한다.
- **학생이나 수업이 하나도 없을 때, 빈 화면 대신 다음 할 일을 띄운다.** 학급 일지·학생 정보·
  누가 기록·학생부 기록·자리 배정에는 **[학생 추가하기]**, 수업홈에는 **[수업 개설하기]**
  버튼이 나와 해당 화면으로 곧장 넘어간다. 학생 관리와 수업 관리 화면의 빈 목록 자리에도 같은
  버튼을 뒀다 — 예전에는 "[수업 추가] 버튼을 눌러 새 수업을 등록하세요" 라고만 적혀 있고 정작
  그 버튼은 화면 맨 위 끝에 있었다.

### 바뀜 — 학사일정 관리 목록

- **한 행을 한 줄 높이로 눌러 담았다.** 날짜(2줄)와 행사명·내용(입력칸 2개)을 세로로 쌓아
  한 행이 70px 을 넘겼는데, 한 화면에 담기는 일정이 두 배 넘게 늘었다.
- **날짜와 요일을 한 줄로 합쳤다** — `2026.03.03. 월`.
- **행사명과 내용을 각각 다른 열로 나눴다.** 한 칸에 위아래로 겹쳐 있던 것을 옆으로 편다.

### 고침 — 학사일정이 한 번 보이고 사라지던 것

- **NEIS 에서 받은 학사일정을 저장하지 않던 것을 고쳤다.** 홈 화면과 학사일정 목록은 받은
  일정을 그리기만 하고 DB 에는 넣지 않은 채 "받았다" 표시만 켰다. 그러면 그 다음부터는
  표시를 믿고 빈 DB 를 읽어서, 학사일정이 딱 한 번 보이고 영영 사라졌다. 이제 처음 한 번은
  **학년도 전체를 받아 DB 에 저장한 뒤에** 표시를 켠다. (달력은 표시를 켜지 않아 달을 넘길
  때마다 NEIS 를 다시 부르면서도 아무것도 남기지 않았다 — 같이 고쳤다.)
- **학사일정 동기화가 실패한 이유를 삼키던 것을 고쳤다.** 인증키가 없거나 네트워크가 끊겨도
  "NEIS에서 가져온 학사일정이 없습니다" 라고만 말했다. 이제 NEIS 가 보낸 사유를 그대로 보여 준다.
- **1·2월에 학년도가 한 해 앞서 잡히던 것을 고쳤다.** 초기 설정이 달력의 연도를 그대로
  학년도로 써서, 1월에 설치하면 "2027학년도 2학기" 처럼 아직 시작하지도 않은 조합이 기본값이
  되고 학사일정 조회가 비었다. 학년도는 3월에 바뀐다.
- 저장하는 동안 초기 설정의 [완료]를 잠근다 — 학사일정 내려받기는 네트워크라 몇 초 걸리는데
  그 사이에 한 번 더 누르면 같은 사람이 교사 명부에 둘로 들어갔다.

### 고침 — 일정·할 일(달력)

- **달력 셀에서 할 일 완료를 눌러도 저장되지 않는 경우가 있었다.** 이미 지워진 할 일이면
  갱신된 행이 0인데 그것을 확인하지 않아, 화면은 완료로 바뀌고 DB는 그대로인 채 다음
  새로고침에 소리 없이 되돌아갔다. (목록 보기는 이미 확인하고 있었는데 달력만 남아 있었다.)
- **이미 지워진 일정·할 일을 편집하면 저장된 척 창이 닫히던 것을 고쳤다.** 이제 창을 열어 둔 채
  알린다.
- **일정 저장·삭제 중 오류가 나면 아무 안내도 뜨지 않던 것을 고쳤다.** 창 안에서 또 다른
  대화상자를 띄우려 해 표시가 막히고 있었다 — 이제 창 안 알림줄로 보여 준다.
- **할 일 ↔ 일정 탭을 옮기면 적어 둔 제목이 사라지던 것을 고쳤다.** 새 항목에서는 제목·메모·
  날짜가 따라간다. **이미 만든 항목은 종류를 바꿀 수 없게 잠갔다** — 예전에는 옮겨서 저장하면
  같은 제목의 항목이 하나 더 생기고 원래 항목은 그대로 남았다(종류가 바뀌지 않았다).
- **새 일정을 쓸 때 날짜·시간 칸이 비어 보이는데 저장하면 값이 들어가던 것을 고쳤다.**
  대화상자가 지금 보이는 탭의 서식만 채워서, '일정' 탭으로 옮기면 칸이 빈 채로 남았다
  (메모리에는 값이 있어 그대로 저장됐다). 이제 두 서식을 모두 채운다.
- **새 일정이 어느 날을 눌러도 9시~10시로 열리던 것을 고쳤다.** 이제 고른 날짜의
  **지금 시각**에서 한 시간짜리로 열린다(할 일과 같은 규칙).
- **오늘 화면의 목록에 지난 날짜가 섞여 나오던 것을 고쳤다.** 여러 날에 걸친 일정을 날짜별로
  펼칠 때 조회 범위 밖까지 펼쳐서, 지난주에 시작한 일정이 목록 맨 위에 지난 날짜로 붙었다.
- **일정의 작성자가 Windows 계정 이름으로 남던 것을 고쳤다.** 게시판·구글 동기화와 셋으로
  갈려 있던 것을 한 벌(설정의 사용자 이름)로 모았다.
- 캘린더를 지우는 경로에서 **기본 캘린더를 넘기면 소속 일정만 지워지고 캘린더는 남던** 문제를
  막았다. 이제 지울 수 없는 캘린더면 아무것도 건드리지 않는다.
- 일정 저장 트랜잭션을 겹쳐 열면 **앞선 작업이 조용히 사라지던 것**을 막았다(오류를 낸다).
- 정리: 쓰이지 않던 색상·취소선 변환기 셋(그중 하나는 다크 테마에서 평일 날짜를 검게 만들던
  코드), 스케줄러 초기화 완료 플래그, 안 쓰는 조회·속성 몇 가지.

### 바뀜 — 게시판 목록 보기

- **페이지 이동을 번호 페이저로 바꿨다.** `‹ 1 … 3 4 [5] 6 7 … 42 ›` 꼴로, 첫 장과 마지막 장은
  언제나 보이고 현재 페이지 둘레로 다섯 칸이 따라다닌다. 총 7페이지 이하면 생략표 없이 전부 편다.
  한 장뿐이면 페이저를 숨기고 전체 건수만 남긴다. **Ctrl+PageUp / Ctrl+PageDown** 으로도 넘긴다
  (Ctrl 없이 쓰면 목록 안 스크롤을 가로채므로 조합키로 뒀다).
- **목록의 날짜에서 시각을 뺐다** — 표·카드·갤러리 세 보기 모두 `2026-08-30` 만 보여 준다.
  글 상세와 댓글은 시각을 그대로 남긴다(댓글은 시각이 있어야 주고받은 순서가 읽힌다).
- **한 화면에 글이 더 많이 들어간다.** 표 보기의 행 높이를 44 → 32 로 줄이고(같은 높이에
  약 1.4 배), 화면 맨 위의 제목 줄을 없앴다 — 왼쪽 메뉴가 이미 같은 말("아카이브"·"업무
  게시판"…)을 하고 있어 한 줄을 되풀이하고 있었다. 날짜 열도 시각을 뺀 만큼 좁혀
  제목에 넘겼다.

### 고침 — 캐시가 자기 객체를 내보내던 것

- **편집 화면에서 고치다 취소해도 목록에 고친 값이 비치던 것을 막았다.** 캐시가 담아 둔
  그 객체를 그대로 돌려주고 있어서, 받은 쪽이 제목·카테고리를 고치면 **저장하지 않아도**
  캐시가 함께 바뀌었다. 댓글 수정이 저장에 실패해도 캐시에는 새 내용이 남았다.
  이제 글·목록·댓글·첨부 조회가 모두 **사본**을 돌려준다.
- 글을 다시 열 때 조회수가 한 박자 늦게 오르던 것을 고쳤다.

### 바뀜 — 목록에 댓글 수 표시

- 표·카드 보기의 💬 아이콘 옆에 **댓글 수**가 붙는다. 목록을 읽을 때마다 개수를 조회하면서도
  어디에도 보여 주지 않아 값을 구해서 버리고 있었다.

### 정리 — 쓰이지 않던 것들

- 호출부가 없던 공개 멤버를 걷어냈다: 게시글 개수 조회(`GetCountAsync`), 게시판 리포지토리의
  연결·트랜잭션 반출 메서드, 첨부 목록/항목의 변경 알림 이벤트 둘, 목록 페이지의 외부 새로고침,
  목록 항목의 중요 글 쓰기 속성, 게시판 초기화 완료 플래그 설정(`Board_Init`).

### 고침 — 작성자 이름과 잔가지

- **설정에서 이름을 지우면 그 뒤 글·댓글의 작성자가 빈칸으로 남던 것을 고쳤다.** 대비해 둔
  `?? "사용자"` 폴백이 네 곳에 있었지만 **하나도 발동하지 않았다** — 설정 값 래퍼에 암시적
  변환이 있어서 `??` 가 값이 아니라 래퍼를 검사하고 있었고, 래퍼는 결코 비어 있지 않았다.
  이제 이름이 없으면 "사용자"로 남는다.
- **자료 글만 작성자가 교사 ID 로 남던 것을 고쳤다.** 다른 글은 이름인데 자료만 ID 여서
  목록의 '작성자순' 정렬이 두 갈래로 섞였다.
- **중요 글(📌) 표시를 카드·갤러리 보기에도 붙였다.** 정렬로는 맨 앞에 오는데 표시가 표 보기에만
  있어서, 다른 보기에서는 왜 그 글이 앞에 있는지 알 수 없었다.
- 첨부 목록을 다시 채울 때 **직전에 고른 '삭제 예정'이 남던 것**을 고쳤다(지금은 화면 하나당
  글 하나라 드러나지 않던 자리).
- 게시판 트랜잭션을 겹쳐 열면 **앞선 작업이 조용히 사라지던 것**을 막았다. 이제는 오류를 낸다.

### 고침 — 메모·자료를 고쳐도 게시판이 옛 내용을 보여 주던 것

- **메모 보드·메모 편집 창·자료 편집 창의 저장/삭제가 캐시를 비우지 않았다.** 메모를 지우고
  게시판 목록을 열면 지워진 글이 그대로 보였고, 눌러 들어가면 상세까지 옛 내용으로 떴다
  (목록 2분·상세 5분·카테고리 콤보 30분). 이제 이 화면들의 쓰기도 캐시 서비스를 탄다.
- **메모 '완료' 표시를 켜도 목록의 ✓ 와 취소선이 최대 2분간 안 바뀌던 것을 고쳤다.**
  게시글을 고치는 메서드 중 완료 표시만 `virtual` 이 아니어서 캐시 계층이 가로채지 못했다.

### 고침 — 메모의 카테고리를 바꾸면 첨부가 끊기던 것

- **메모 편집 창과 메모 보드에서 카테고리를 바꿔도 첨부가 함께 따라간다.** 첨부 실물은
  `Data\Files\{카테고리}\` 에 살고 경로는 언제나 **글의 현재 카테고리**로 만들어지는데,
  실물을 옮기는 손질이 게시글 편집 화면에만 있었다. 메모 쪽에서 카테고리를 바꾸면 첨부가
  옛 폴더에 남아 "파일이 존재하지 않습니다"가 되고, 그 첨부를 지우면 새 폴더의 **엉뚱한
  파일**을 지울 수 있었다. 댓글 첨부도 함께 옮긴다.
- 대상 폴더에 같은 이름이 있으면 `이름 (2).확장자` 로 옮기고 **DB 의 첨부 이름도 함께**
  고친다 — 옛 이름이 남으면 그 자리의 남의 파일을 가리키게 된다.
- 옮기지 못한 첨부가 있으면 무엇이 끊겼는지 알린다(예전에는 조용히 넘어갔다).

### 고침 — 게시판 목록 검색

- **검색 결과에서 페이지를 넘기면 검색이 풀리던 것을 고쳤다.** 목록 조회가 둘로 갈라져 있었고,
  페이지 넘김·정렬 변경·새로고침·뒤로가기를 맡은 쪽이 검색어를 아예 넘기지 않았다. 총 페이지 수는
  검색 결과 기준이라 '다음' 버튼은 켜져 있는데 누르면 전체 목록 2페이지가 나왔고, 검색창에는
  검색어가 그대로 남아 있어 "검색이 안 먹는다"로만 보였다. 조회를 한 곳으로 모았다.
- **검색어를 치고 곧바로 Enter 를 누르면 직전 검색어로 검색되던 것을 고쳤다.** 검색창의
  TwoWay 바인딩이 포커스를 잃을 때만 값을 넘기고 있었다(WinUI `TextBox.Text` 의 기본 동작).
  버튼 클릭은 포커스가 옮겨가며 우연히 동작해, 증상이 "Enter 만 안 된다"로 보였다.
- **다른 페이지를 보고 있을 때 검색하면 그 페이지 번호로 검색되던 것을 고쳤다.** 3페이지에서
  검색하면 검색 결과의 3페이지를 가져오면서 화면에는 "1 / N 페이지"가 찍혔다.
- 글을 열고 **뒤로 돌아올 때 보던 페이지와 검색을 그대로 유지**한다. 새로고침(F5)도 제자리에서
  다시 읽는다 — 예전에는 둘 다 1페이지로 되돌리고 검색을 풀었다.
- 카테고리·주제·정렬·페이지 크기를 바꾸면 **1페이지부터** 다시 읽는다. 5페이지를 보다 글이 적은
  주제로 바꾸면 빈 화면이 나오던 것이 사라진다.
- 글이 지워져 마지막 페이지가 통째로 사라진 경우, 빈 목록 대신 **남아 있는 마지막 장**으로 물러난다.

### 고침 — 수업·시간표·교과 (전수 감사, 2026-08-31)

- **학급일지가 화면에서 고른 학년도가 아니라 설정의 작업 학년도로 저장되던 것을 고쳤다.**
  같은 화면에서 명렬과 누가기록만 위쪽 피커를 따르고, 정작 일지와 그 옆 학급 시간표는
  설정값을 봤다. 지난 학년도를 펼쳐 놓고 적은 알림장·출결이 **올해 날짜의 일지에 들어갔고**,
  그 해의 일지는 열 방법도 없었다. 이제 일지·시간표·일지 목록·누가기록이 모두 피커를 따른다.
- **수업 홈의 [오늘의 수업]만 그날 변경(휴강·교체·보강·대강)을 반영하지 않던 것을 고쳤다.**
  휴강한 수업이 '예정'으로 남아 "N시간 중 M건"의 N까지 부풀었고, 보강은 아예 나오지 않았다.
  바로 옆 [내 시간표] 카드는 반영하고 있었으므로 한 화면이 같은 질문에 두 답을 내놓았다.
  이제 휴강 줄은 `(휴)` 표식과 함께 흐리게 남고 집계에서 빠지며, 일지 쓰기 버튼이 꺼진다.
- **손으로 고친 주차별 시수가 다른 주에 가서 붙던 것을 고쳤다.** 조정을 주차 *번호*로 찍어
  두었는데, 2학기를 관례값(9월 1일 시작)으로 보다가 학사일정을 내려받으면 학기 시작이
  여름방학 다음 첫 수업일로 당겨지면서 번호가 통째로 밀렸다. 이제 **주 시작일**로 찍는다.
  이미 저장된 조정은 앱을 열 때 자동으로 옮겨진다(같은 주에 두 줄이 쌓여 있으면 나중에
  적은 값을 남긴다).
- **강의실을 다시 정할 때 딸린 기록이 반쯤만 지워질 수 있던 것을 고쳤다.** 배치·시수·진도·
  배정을 각각 따로 지워서, 중간에 실패하면 진도와 시수는 사라졌는데 강의실은 그대로였다.
  이제 넷을 한 트랜잭션으로 묶는다 — 실패하면 아무것도 지우지 않는다.
- **학급 시간표를 못 읽어 왔을 때 "시간표가 없다"와 똑같이 보이던 것을 고쳤다.** 학급일지의
  시간표 제목 줄이 실패를 알리고, 사유는 툴팁으로 보여 준다.

### 바뀜 — 하루 교시 수 상한을 8로 맞췄다

- 설정의 [요일별 교시 수]는 **12**까지 올릴 수 있었는데 시간표를 그리는 쪽은 **7**까지만
  다뤘다. 8교시에 수업을 배치하면 저장은 되고 배치판·주별 시간표·시수에는 나오지만
  **내 시간표·오늘 화면·학급일지에서만 사라졌다**(같은 8교시라도 보강은 보였다).
  이제 상한이 코드 한 곳(`PeriodCounts.MaxSupported` = 8)에서 나오고, 설정·격자·학급 시간표
  편집이 모두 그 값을 따른다. 현실의 7교시에 예비 한 칸을 더한 값이다.
- 상한을 넘겨 저장돼 있던 설정은 기본값(월·수 6, 화·목·금 7)으로 되돌린다.

## v1.0.0 — 첫 공개 릴리스 (2026-08-26 게시)

> **버전 재조정 안내**
> 2026-03 ~ 08 사이에 `v1.0.0`~`v1.3.0` 태그로 배포한 빌드가 있었다. 공개 출시 시점을
> 1.0.0 으로 맞추기 위해 버전을 되돌렸고, 그 태그와 릴리스는 모두 삭제했다.
>
> 삭제 전 자산 다운로드는 v1.0.3 이 1회, v1.0.4 가 7회로 합계 8회였다(나머지는 0회).
> 개발자 본인이 설치·확인한 것으로 판단해 되돌리기를 강행했다. 즉 **그 8회 중 외부
> 사용자가 있었다면 그 설치본은 1.0.0 을 자기보다 낮은 버전으로 보고 갱신 알림을
> 받지 못한다** — 이 대가를 알고 내린 결정이다.
> (되돌리기를 결정할 당시에는 "다운로드 0회" 로 기록해 두었으나, 릴리스 직전 재확인에서
>  사실이 아님이 드러나 여기 바로잡는다.)
>
> **게시 보류와 해소 (2026-08-16 → 2026-08-24)**
> 기존 태그·릴리스를 지운 뒤로도 v1.0.0 게시를 미뤄 두었다. 설치 파일이 품는
> Windows App SDK 런타임이 2.3 에 멈춰 있어(앱은 2.4 필요) 깨끗한 PC 에서 앱이
> 시작되지 않았기 때문이다. **2026-08-24 에 런타임 2.4.0 으로 교체하고 게시했다.**
> 개발 PC 에는 2.4 가 이미 있어 이 불일치가 로컬 테스트로는 드러나지 않았고,
> 인스톨러가 설치된 런타임을 먼저 검사해 번들을 건너뛴 탓에 더 오래 묻혀 있었다.
>
> **자체 포함으로 다시 내며 재게시 (2026-08-26)**
> 8/24 자산은 내려받은 사람이 없어(3회 전부 개발자 본인) 태그와 릴리스를 지우고
> 같은 번호로 다시 낸다. 그동안 고친 것과 배포 방식 변경을 첫 배포에 함께 담기 위해서다.
>
> 배포 방식을 **런타임 번들(프레임워크 의존) → 자체 포함**으로 바꿨다. 실측 —
> **설치 파일 117.1MB → 26MB(−78%)**, 설치 폴더 46파일·4폴더·96.5MB.
> 무엇보다 **런타임 설치 단계가 통째로 사라져**, 위에 적은 "번들 2.3 vs 요구 2.4" 부류가
> 원천 소멸한다. 설치 시 내려받는 방식(약 16MB)은 이제 검토할 이유가 없다.
>
> 앱 파일은 모두 `{app}\bin\` 아래로 내렸다(루트에는 그 폴더와 언인스톨러만).
> 포터블로 쓰려고 설치 폴더를 통째로 옮기는 경우를 위해, 포터블 표식을 **실행 파일 폴더의
> 부모까지** 찾도록 고쳤다 — 그러지 않으면 `Data\` 를 `bin\` 안으로 밀어 넣어야 해서
> 사용자 자료가 프로그램 파일 46개 사이에 섞인다. 실제 설치로 확인했다.
>
> **같은 1.0.0 으로 다시 낸다 (2026-08-30)**
> 첫 공개 뒤 나흘간 잡은 것을 모아 **번호를 올리지 않고** 같은 1.0.0 을 새 자산으로 교체한다.
> 아직 첫 배포를 다듬는 중이고, 여기 담긴 것은 새 기능이라기보다 **처음부터 그랬어야 하는
> 것들**이기 때문이다 — 주당 시수 합계가 학급 수를 안 세던 것, 방학 주차가 표에 남던 것,
> 방학식이 휴업일로 잡히던 것, 급식 상자에 날짜 이동이 겹쳐 있던 것.
> 눈에 띄게 달라진 곳은 **주별 시간표를 가로로 세운 것** 하나다.
>
> 내려받은 사람이 있다면 **자동 갱신 알림은 뜨지 않는다**(번호가 같다). 이 점을 알고 내린
> 결정이며, 다음 변경부터는 번호를 올린다.
>
> 아래 **개발 이력**은 그 기간의 작업 기록이며, 내용은 손대지 않고 그대로 보존한다.
> 괄호 안의 "구 vX.Y.Z" 는 되돌리기 전에 쓰던 번호다.

### 주당 시수 합계가 학급 수를 세지 않았다 (2026-08-30)
[수업 시간표 입력] 아래 **"주당 시수 합계"** 가 `Σ 주당 시수` 였다. 배치판의 한 칸은 "어느
학급의 몇 교시" 하나이므로, 주당 4시간짜리 과목을 세 학급에 가르치면 판에는 **12칸**이 들어간다.
합계는 4만 세고 있었다.

더 나빴던 것은 **같은 숫자가 배치 상한이기도 했다**는 점이다. 4칸을 놓으면 "이미 4시간
배치됐습니다" 로 막혀 **두 번째 학급부터는 아예 놓을 수 없었다**. 셋을 한 규칙으로 모은다:

| 자리 | 전 | 후 |
|---|---|---|
| 합계 | `Σ Unit` | `Σ (Unit × 강의실 수)` |
| 수업 카드 배지 | `배치/Unit` | `배치/(Unit × 강의실 수)` |
| 배치 상한 | `Unit` | `Unit × 강의실 수` |

강의실을 적지 않은 수업은 한 학급으로 본다 — 강의실을 안 쓰던 사용자에게는 숫자가 그대로다.
막을 때의 안내도 근거를 밝힌다: "주당 4시간 × 3개 학급 = 12시간인데 이미 12시간 배치됐습니다".

### 수업 시수 탭이 옛 학사일정을 세션 내내 붙들고 있었다 (2026-08-30)
`CourseHoursView` 가 학사일정을 <b>학년도가 바뀔 때만</b> 읽었다(`_schedulesYear` 가드).
그런데 학사일정은 [설정 → 학사일정] 에서 **앱을 켜 둔 채로** 내려받고, 손으로 고치기도 한다.
그러면 이 탭만 옛 목록을 들고 그 세션 내내 버틴다 — 방금 받아 온 겨울방학이 닿지 않는다.

가드를 걷어 **탭이 열릴 때마다 다시 읽는다**. 백여 행짜리 조회 한 번이고 탭이 낡았을 때만
오므로 캐시할 값이 아니었다. 학기 기간(`_range`)도 같은 이유로 매번 다시 유추한다 —
새로 읽어 놓고 옛 기간을 쓰면 방학 경계가 그대로다.

> **⚠ 이 수정은 "방학 주에 수업일이 5일로 잡힌다" 의 원인이 아니었다.** 그 화면을 조사하다
> 곁가지로 찾은 것이다. 실제 원인은 코드가 아니라 **자료**였다 — 그 학교의 2026학년도
> 학사일정에 **겨울방학이 한 행도 등록돼 있지 않았다**(여름방학은 `휴업일` 로 정상 등록).
> 휴업일이 없으면 1~2월 평일이 전부 수업일로 잡히고, 방학을 찾을 근거도 없다.
> 자료가 비면 화면도 빌 수밖에 없다는 것을 여기 적어 둔다 — 같은 증상을 다시 만나면
> **코드보다 그 학교의 학사일정부터** 볼 것.

### 휴업일 판정에서 행사명 짐작을 걷는다 — 방학식은 수업일이다 (2026-08-30)
`SchoolCalendar.IsNonTeachingDay` 가 수업공제일자명(`SBTR_DD_SC_NM`)을 본 **뒤에**, 행사명에
"휴업·공휴·방학" 이 들어가는지도 함께 봤다. 그 짐작이 **`"여름방학식"` 을 휴업일로 만들었다** —
방학식은 수업공제일자명이 `해당없음` 인 엄연한 수업일인데, **시수에서 하루가 조용히 빠졌다.**

실제 학사일정(154행)을 열어 세어 보니 짐작이 필요한 자리가 아니었다:

| 확인한 것 | 결과 |
|---|---|
| 수업공제일자명이 비어 있는 행 | **0건** (NEIS 자료든 손으로 넣은 것이든 늘 채워진다) |
| 이름 규칙이 <b>추가로</b> 잡아내던 행 | **1건 — `여름방학식`**, 그것도 틀린 쪽으로 |
| 휴업일인데 이름이 비어 잡히지 않던 행 | 0건 |

**근거를 그 칸 하나로 줄였다.** 앱에서 손으로 넣을 때도 같은 셋(해당없음·휴업일·공휴일)에서
고르므로 빈 칸이 생기지 않는다. 대신 `IsHoliday` 는 같은지가 아니라 **포함**으로 본다 —
교육청에 따라 `"토요휴업일"` 처럼 앞뒤가 붙은 값이 올 수 있다.

실측 — 1학기 마지막 주의 수업일이 3일 → **4일**이 됐다(7/16 방학식이 되돌아왔다).

### 수업 시수 표를 방학에서 끊는다 (2026-08-30)
학기 구간은 **일부러 방학을 품는다**(그래야 1학기와 2학기 경계가 겹치지도 비지도 않는다).
그걸 그대로 주차로 펼치니 표에 **수업일 0인 방학 주가 5~6줄** 붙고 "n주" 통계도 그만큼 부풀었다.

**어디서부터가 방학인지는 학사일정이 이름으로 말해 준다.** 처음에는 "수업일이 2주 연속
끊기는가" 로 유추하려 했는데, 실제 NEIS 자료를 열어 보니 그럴 이유가 없었다 — 방학 기간의
**모든 날**에 `EVENT_NM = "겨울방학"/"여름방학"` 과 `휴업일` 이 붙어 있다. 그 첫 날이 든
주부터 끊는다.

| | |
|---|---|
| ① 방학 첫 휴업일 | `EVENT_NM` 에 "방학" + `휴업일`. 그 **다음 주**부터 지운다 |
| ② 남긴 구간의 끝 | 빈 주가 붙어 있으면 턴다 (방학이 월요일에 시작한 경우 · 이름이 없는 학교) |

**방학이 시작한 주는 남긴다** — 실제 자료에서 여름방학은 금요일, 겨울방학은 목요일 시작이라
그 주 앞머리는 멀쩡한 수업일이다. 그리고 **이름만 보면 안 된다**: `"여름방학식"` 도 "방학" 을
품지만 그 날은 수업일(`해당없음`)이라, 거기서 끊으면 한 주가 통째로 사라진다. `IsHoliday` 로
휴업일임을 함께 본다(위 항목 참고).

실제 학사일정으로 확인했다:

| | 자르기 전 | 뒤 | 마지막 주 |
|---|---|---|---|
| 1학기 | 23주 | **20주** | 7/13~7/19 (수업일 4) |
| 2학기 | 29주 | **21주** | 12/28~1/3 (수업일 3) |

2월은 하나도 남지 않는다. 다섯 갈래(방학 이름 · 방학식 함정 · 학기 중간 빈 주 · 학사일정 없음 ·
겨울방학+2월 개학) 모두 테스트로 못박았다.

### 주별 시간표를 가로로 세운다 — 교시 × 날짜 (2026-08-30)
날짜를 세로로 세우고 있었다. 기초 시간표(`CourseTimetableBoard`)와 행·열을 반대로 두면
"그 주만" 바꿀 것을 "매주" 바꾸는 사고를 막는다고 봤기 때문이다. **그 대가가 더 컸다** —
3주치 15일이 아래로 흘러 한 주를 한눈에 볼 수 없었고, 시간표를 시간표답게 읽지 못했다.

이제 **교시가 행, 날짜가 열**이고 가로로 흐른다. 헷갈릴 위험은 모양이 아니라 **표시**로 막는다:
주 구분 띠, 날짜(요일)·오늘 표시, 변경 칸의 (휴)(교)(보)(대) 표식, "평소 시간표는 그대로입니다" 안내.

- **왼쪽 교시 열은 고정** — 바깥은 세로만, 안쪽은 가로만 맡는 이중 스크롤이다. 오른쪽으로
  밀어도 몇 교시인지 남아 있고, 세로로 밀면 교시 열도 함께 따라 올라간다.
  두 표의 행 높이는 코드가 같은 값으로 함께 만든다 — 한쪽만 `Auto` 로 두면 한 칸씩 어긋난다
- **마우스 휠 = 가로**, Shift+휠 = 위아래. 넘길 축이 가로이기 때문이다
  (⚠ 디스플레이 배율이 100% 가 아니면 WinUI 가 휠을 다른 요소로 hit-test 하는
  프레임워크 버그에 함께 걸린다 — 앱에서 고칠 수 있는 문제가 아니다)
- **←→ 는 날짜, ↑↓ 는 교시**로 바꿨다. 커서가 화면 밖으로 나가면 그 열이 보이도록 민다
- **내 수업 칸 배경을 파스텔로** — 강조색 원색이라 15열이 늘어서면 화면이 파랗게 덮였다.
  강조색을 그대로 쓰되 알파로 옅게 깔아 라이트에서는 하늘색, 다크에서는 짙은 남색 기가 돈다
  (밝은 색을 고정으로 박으면 다크 테마에서 글자를 삼킨다)

**한 화면에 하루라도 더** — 날짜 열을 118 → 88px 로 줄이고 칸 좌우 여백도 8 → 3px 로 좁혔다.
그만큼 벌려면 윗줄에서 "· 오늘" 을 떼어내야 했다: 날짜는 **`8/31(월)` 한 줄**만 두고, "오늘"·
휴업 사유는 **아랫줄**로 내렸다(오늘은 글자 대신 강조색으로도 알린다). **주 구분 띠와 날짜는
가운데 정렬**이다. 좁아서 잘리는 글자는 툴팁에 전문을 남긴다.

**배치판(수업 시간표 입력) 칸 색도 같이 눅였다** — 같은 강조색 원색이라 과목명이 바탕에 묻혔다.
두 표가 따로 놀면 안 되므로 같은 규칙(알파 0.16/테두리 0.45)을 쓴다.

**학년도·수업 필터도 걷었다.** 이 탭이 보는 대상은 날짜다 — 학년도는 화면에 뜬 주가 이미
말해 주고, 수업 하나로 거르면 그 주에 실제로 있는 다른 수업이 표에서 사라져 오히려 사실과
멀어진다. 학기만 남긴다(학기가 바뀌면 기초 시간표 자체가 다른 표다). 딸려 나온 둘:

- **Enter** 가 "위에서 고른 수업" 을 넣었는데, 정작 이 탭에는 무엇이 골라져 있는지 보이지
  않았다. 이제 **그 칸의 메뉴를 연다**(오른쪽 클릭과 같은 것) — 넣을 것을 고르는 자리가 하나가 됐다
- **학년별 행사 판정**이 "고른 수업의 학년" 기준이었다. **내가 가르치는 학년들** 기준으로
  바꾸고, 학교 전체 휴업과 달리 날짜를 흐리게 만들지는 않는다 — 1학년만 수련회인 날에도
  2학년 수업은 그대로 있기 때문이다. 대신 "1학년 수련회" 처럼 학년을 밝혀 적는다

### 급식 상자의 날짜 이동을 걷는다 — 날짜 기준을 홈 화면 하나로 (2026-08-30)
홈 화면 머리에 `◀ 날짜 ▶ 오늘` 이 있고, 그 아래 급식 상자에도 `◀ 달력 ▶` 이 또 있었다.
같은 일을 하는 두 조작이라 중복인 데다, **기준이 둘이라 어긋날 수 있었다** — 상자에서
날짜를 옮기면 헤더는 어제를 말하는데 급식만 내일을 보여 준다. 상자 쪽을 걷어
날짜는 화면이 정하고 상자는 `LoadMealsAsync(날짜)` 로 받은 것만 그리게 했다.

딸려 나온 것들:

- **`SelectedDate`·`INotifyPropertyChanged`** — 이 컨트롤에서 값이 바뀌는 것을 알릴 대상이
  `SelectedDate` 하나뿐이었다. 그것이 사라지자 인터페이스째로 갈 곳을 잃었다
- **상자 안 실패 안내(`TxtMealStatus`)** — 띄우는 유일한 경로가 "사용자가 상자 안에서
  날짜를 바꿨을 때" 였다
- **`LoadMealsForUserAsync`** — 그 안내를 띄우려고 예외를 삼키던 우회로. 이제 실패 경로는
  **"`LoadMealsAsync` 가 던지고 홈 화면이 전역 InfoBar 로 모은다" 하나뿐**이다

헤더는 학사일정 카드와 같은 "아이콘 + 제목" 모양이 됐다. 도움말의 홈 설명에 그동안 빠져
있던 **날짜 이동 자체**를 적어 넣었다 — 조작이 한 곳뿐이 되었으니 그 한 곳은 설명이 있어야 한다.

빌드 경고 0, 테스트 609개 통과. 실행해 홈 화면 표시를 확인했다.

### 게시본에서 아무도 열지 않는 파일 12개를 걷는다 (2026-08-30)
게시 폴더 **55개 → 43개, 약 534KB** 를 줄였다. 크기보다 중요한 것은 **고른 방법**이다 —
"안 쓸 것 같다" 로 지우면 다른 PC 에서만 터지는 부류가 나온다.

자체 포함 게시본에서 WinRT 활성화가 실제로 참조하는 목록은 **`NewSchool.exe` 안에 박혀 있는
regfree-COM 매니페스트**(`<asmv3:file>` 27줄) 하나뿐이다. 그것을 뽑아내고, 따로 `dumpbin
-dependents` 로 PE 35개의 역참조 표를 만들어 대조했다. 지운 것은 **매니페스트에 없거나,
있어도 그 클래스를 부르는 코드가 앱에 없고, 남는 어떤 파일도 정적으로 임포트하지 않는** 것들이다.

| 걷어낸 것 | 왜 닿지 않는가 |
|---|---|
| `Microsoft.UI.Designer.dll` | 클래스가 `DesignerOutputHost` 하나. Visual Studio 디자이너가 띄운다 |
| `Microsoft.Graphics.Imaging.dll` | Windows AI 의 `ImageBuffer` 네이티브. 투영은 이미 빠져 있었다 |
| `SessionHandleIPCProxyStub.dll` | AI 세션 핸들 COM 프록시. `Microsoft.Windows.AI.*` 가 전부 없다 |
| `PushNotificationsLongRunningTask.ProxyStub.dll` | 매니페스트에도 없다. 푸시 알림 API 미사용 |
| `…Background.UniversalBGTask.dll` | `BackgroundTaskBuilder` 전용. 앱에 백그라운드 작업이 없다 |
| `workloads*.json` 6개 | 읽는 `Microsoft.Windows.Workloads.dll` 을 이미 지웠다 |
| `Microsoft.UI.Xaml\Assets\map.html` | `MapControl` 이 WebView2 로 띄우는 지도. 둘 다 안 쓴다 |

같은 표를 만들다 **지우면 안 되는 것 둘**도 확인했다. `RestartAgent.exe` 는 DB 손상 복원 뒤
`AppInstance.Restart` 가 쓰고, `Microsoft.UI.Xaml.Phone.dll` 에는 `Pivot`·`DatePicker`·
`TimePickerFlyout`·네비게이션 전환이 들어 있어 앱이 전부 쓴다(`.mui` 도 함께 남긴다).
`Microsoft.UI.Xaml\Assets\` 의 `NoiseAsset_256x256_PNG.png` 도 `AcrylicBrush` 가 쓴다.

정리 후 게시본으로 앱을 실제로 띄워 시작을 확인했다.

### 서비스·컨트롤 계층을 훑고, 남은 잔가지를 친다 (2026-08-30)
서비스 34개(10,253줄)와 컨트롤 25개(10,409줄)를 모델 검토와 같은 축으로 훑었다.
**둘 다 실사용 결함이 없었다.**

| 축 | 결과 |
|---|---|
| 미참조 공개 멤버 | 서비스 0 · 컨트롤 3 (모두 사소) |
| 반복문 안 DB 호출(N+1) | 0. 내보내기 묶음에도 없다 — 배치 조회가 이미 되어 있다 |
| 수명(연결 누수) | 0. 서비스를 든 화면 8곳이 **전부** 언로드·닫힘에서 놓는다 |
| 이벤트 구독 누수 | 0. 바깥 것에 거는 5곳은 모두 짝이 맞고, `PageSeats` 는 재호출마다 먼저 뗀다 |
| 의존 속성 | 6개 전부 등록 이름·소유 타입 일치, 문자열 리터럴 0 |
| 다크 테마 고정색 | XAML 0. 코드 5곳은 알파 96 반투명이라 밑바탕과 섞인다 |
| `async void` 예외 | 전역 처리기가 로그·flush·안내 후 `e.Handled = true` 로 앱을 살린다 |

걷어낸 것 넷은 다 합쳐 20줄 남짓이다.

- **`PhotoCard.CardHeight`** — 좌석은 늘 너비로 정한다(`PageSeats` 가 `CardWidth` 만 넣는다).
  높이는 사진 비율과 이름칸에서 따라 나온다. 이걸 걷으니 `SetSize` 의 높이 갈래도 유일한
  진입점을 잃어 함께 정리했다 — **기준이 하나면 둘이 어긋날 일도 없다**
- **`SchoolScheduleListControl.ScheduleGroups` 세터** — 바깥에서 안 부른다. 필드와 그룹화는
  화면에 실제로 그려지므로 그대로다(뷰모델에서 지운 `SchoolEventGroups` 와 다르다)
- **`TodayPage.ViewModel_PropertyChanged`** — **본문이 비어 있었다.** 구독·해제만 하고
  아무 일도 안 했다. 셋을 함께 걷었다
- **`MonthPicker` 의 `PreviousMonth`** — 실어 보내기만 하고 받는 쪽이 없었다. 딸린 지역
  변수도 함께. ⚠ 남은 `SelectedMonth` 도 **지금은 아무도 인자에서 읽지 않는다**(소비자가
  컨트롤 속성을 읽는다) — 그래서 이 인자 클래스가 통째로 필요 없어질 수 있다는 것을
  주석에 남겼다. 이벤트의 공개 형태를 바꾸는 일이라 여기서는 손대지 않았다

**다섯 계층을 다 본 결과, 피해는 계층 안이 아니라 경계에 몰려 있었다** — 뷰모델이 서비스를
만드는 자리, 리포지토리가 연결을 여는 자리. 그 사이를 지나가기만 하는 서비스·컨트롤은
멀쩡했다.

### 리포지토리에 남은 잔가지를 친다 (2026-08-30)
리포지토리 검토의 나머지 셋. 둘은 **"대신 이걸 쓰라"고 가리키던 메서드**였는데, 정작
가리키는 쪽도 아무도 안 썼다.

- **`StudentRepository.SearchByNameAsync` 제거.** 2026-08-19 에 깨져 있던
  `SearchAsync(keyword)` 를 지우며 "대신 쓸 것" 으로 남겼는데 그것도 끝내 호출부가 0이었다.
  실측해 보니 **이 앱은 이름을 SQL 로 찾지 않는다** — 배정 화면들이 이미 읽어 둔 명단을
  메모리에서 거른다. 명단이 한 학급~한 학년 규모라 왕복이 없는 그편이 낫다. 그 사실을
  주석에 남겨, 다음에 이름 검색이 필요한 사람이 SQL 부터 만들지 않게 했다
- **`EnrollmentRepository.GetBySchoolAndYearAsync` 제거.** 39차에 `GetAllBySchoolAsync` 를
  지우며 "대신 쓸 것" 으로 가리켰던 메서드다. 그런데 이쪽도 호출부가 0이고, 무엇보다
  **`GetEnrollmentsAsync` 와 겹쳤다** — 같은 조회이고, `includeInactive: true` 를 주면
  전출·졸업까지 포함하는 것도 같다. 학년·반으로 더 좁히는 것도 그쪽이 인자로 받는다.
  **그 "같다" 를 회귀 테스트로 못박았다** — 주석의 주장을 코드가 지키게
- **`KCalendarListRepository` 의 `catch { /* ignore */ }` 에 이유를 붙였다.**
  이 계층에서 유일하게 이유가 안 적힌 삼킴이었다. 정렬 순서를 못 읽어도 달력 만들기를
  막지 않는 것이 맞다(새 달력이 맨 앞에 꽂힐 뿐이고, 순서는 사람이 바꿀 수 있다).
  다만 **조용히 넘기지는 않게** 경고를 남겼다 — 그 조회가 실패한다면 DB 쪽에 다른 문제가
  있다는 뜻이고, 신호까지 지우면 "왜 새 달력이 자꾸 맨 앞에 오지" 로만 보인다

### 트랜잭션을 겹쳐 열면 던진다 — 조용한 롤백을 막는다 (2026-08-30)
리포지토리 계층 검토. 이 계층은 상태가 좋았다(SQL 주입 0, 학년도 필터 누락 0,
미참조 공개 멤버 2개뿐, 연결 수명 119곳 중 96이 `using`). 대신 **한 자리가 위험했다.**

```csharp
public void BeginTransaction()
{
    Transaction?.Dispose();          // ← 커밋도 롤백도 없이 버린다
    Transaction = Connection.BeginTransaction();
}
```

`SqliteTransaction` 은 커밋되지 않은 채 `Dispose` 되면 **조용히 롤백된다.** 오류도 경고도
없이 앞선 작업이 사라지므로, 무엇이 왜 저장되지 않았는지 알아낼 방법이 없다.

- 특히 위험한 조합은 **`UnitOfWork`** 다. 공유 트랜잭션 하나를 여러 리포지토리에
  `SetTransaction` 으로 나눠 주는데, 그중 하나가 자기 트랜잭션을 열면 **나머지의 작업까지
  함께 버려지고** UnitOfWork 는 이미 Dispose 된 것을 계속 들고 있게 된다
- **지금은 닿지 않는다.** 세 경로를 다 확인했다 — `Scheduler/UnitOfWork.Schedules` 는
  아무도 안 쓰고, 게시판 리포지토리는 protected `ExecuteInTransactionAsync` 를 쓰지 않으며,
  `CourseSectionRepository` 는 UnitOfWork 에 들어가지 않는다. **지뢰이지 폭발이 아니다**
- 그래도 막아 두는 이유는 실패 방식이 조용한 데이터 손실이기 때문이다. 이제 던진다

**곁들여 충돌 경로 하나를 아예 없앴다.** `Scheduler/UnitOfWork.Schedules` 를 걷어냈다 —
아무도 안 쓰는데 `SchoolScheduleRepository` 는 스스로 `ExecuteInTransactionAsync` 를 쓰므로,
쓰지도 않는 속성이 위험한 조합을 열어 두고 있었다.

**`BaseRepository` 두 벌은 합치지 않고 각각 고쳤다.** 지금은 갈라지지 않았지만(PRAGMA·
`Dispose`·`_ownsConnection` 동일) 이 결함은 양쪽에 다 있었다. 그래서 두 파일 주석에
"같은 결함이 저쪽에도 있었고 함께 고쳤다 — 고칠 때 양쪽을 같이 볼 것" 을 서로 가리키게 적었다.

**테스트 3건.** 던지는 것뿐 아니라 **정상 흐름을 막지 않는 것**도 함께 고정했다 —
열고 닫기를 되풀이하는 것은 겹쳐 여는 것이 아니고, 가드가 거기까지 막으면 일괄 저장 화면이
통째로 멎는다. 옛 동작으로 되돌려 두 테스트가 실제로 실패하는 것을 확인하고 원복했다.

### 뷰모델의 INPC 를 기반 클래스 하나로 모은다 (2026-08-30)
뷰모델 검토 마지막. 열 개 중 **여덟이** `PropertyChanged`·`OnPropertyChanged`·
`SetProperty` 를 저마다 손으로 들고 있었다. 모델 계층은 거의 전부
`NotifyPropertyChangedBase` 를 상속하는데 뷰모델은 반대였다.

- **`SetProperty` 복제본이 셋이었고, 기반 클래스와 글자 그대로 같았다.**
  옮기기 전에 세 벌을 대조해 확인했다 — 하나라도 달랐다면(예: `Equals` 대신
  `EqualityComparer<T>.Default`) 동작이 조용히 바뀐다
- 열 개 **전부** 기반 클래스로 모았다. `SetProperty` 복제본 0, 직접 구현 0
- `Board` 쪽 둘과 `StudentListItemViewModel` 은 `using NewSchool.Models;` 가 없어
  함께 넣었다. 기반 클래스가 `Models` 네임스페이스에 있는 것은 자리가 어색하지만,
  옮기는 것은 이번 범위 밖이라 두었다
- 남은 `PropertyChanged?.Invoke` 열둘은 뷰모델이 아니라 컨트롤·모델·Scheduler 쪽이다

**테스트 5건 추가.** 이 정리는 **깨져도 빌드가 통과한다** — 상속만 바꾸면 컴파일은 되고,
알림이 안 나가는 것은 화면에서 값이 안 바뀌는 모습으로만 드러난다. 그래서 대표 뷰모델의
이벤트가 실제로 오는지, 같은 값에는 안 오는지, 그리고 `StudentCardViewModel` 이 감싼 모델의
변경을 흘려보내는 사슬(열 개 중 유일하게 모델을 구독한다)이 끊기지 않았는지 확인한다.

### 뷰모델의 죽은 멤버를 걷어낸다 — 그중 셋은 헛조회였다 (2026-08-30)
뷰모델 검토 3·4단계. 미참조 공개 멤버를 훑었는데, 단순한 잔재가 아니라
**결과를 아무도 안 보는데 DB·계산을 돌리는 것**이 셋 나왔다.

- **`ClassDiaryViewModel.StudentLogs`** — 채우기만 하고 읽는 곳이 0건인데, 채우려고
  `LoadDiaryAsync` 가 **일지를 열 때마다** `GetStudentLogsByDateAsync` 로 DB 를 한 번 더
  쳤다. 컬렉션·메서드와 함께 그 서비스 메서드도 걷었다(유일한 호출자였다).
  학급 일지 화면의 학생 로그는 `LogListViewer` 가 직접 읽는다
- **`TodayPageViewModel.SchoolEventGroups`** — 같은 무늬. 읽는 곳이 없는데 채우려고
  목록을 읽을 때마다 `GroupSchedules` 로 묶기까지 했다. 그룹화가 진짜 필요한 곳
  (`SchoolScheduleListControl`·`GoogleSyncService`)은 각자 직접 부르므로 그 헬퍼는 남는다
- **`IsEmpty`·`HasPosts` 게터의 `Debug.WriteLine`** — 알림이 5곳에서 오는 속성이라
  평가할 때마다 찍혔다

미참조 멤버 정리 — `ClassDiaryViewModel` **15개**(`Diary` 계열 표시 속성 대부분),
`SchoolScheduleViewModel` 2, `StudentCardViewModel`·`PostListViewModel` 등 각 1~2.
딸린 알림 18줄과 주석으로 꺼진 알림 7건도 함께 걷었다
(`LogByteInfo` 계열은 **속성 자체가 없고** XAML 쪽도 주석 블록 안이었다).

**⚠ 조사 중 두 번 헛짚었다 — 지우지 않고 남긴 것들.**

- `PostListViewModel.IsEmpty` 를 "바인딩 0건" 으로 보고 죽었다고 판단했는데,
  `PostListPage` 가 **`PropertyChanged` 스위치에서** 읽고 있었다. XAML 만 봐서 틀렸다
- `CurrentPage`·`EditingComment` 는 형제 속성(`PageInfo`·`HasPreviousPage`·`IsEditing`)이
  외부에서 쓰여 **내부적으로 살아 있다**
- `CommentCount` 는 표시하는 화면이 없지만 **지우지 않았다.** 그 값을 구하는
  `GetCommentCountsAsync` 에 "글마다 조회하던 N+1 제거" 라고 적혀 있다 — 누군가 보여 줄
  작정으로 다듬어 둔 자리다. 대신 **지금 헛조회가 돌고 있다**는 것과, 정말 안 쓸 것이면
  조회까지 함께 걷어야 한다는 것을 속성 주석에 남겼다

### ViewModel 을 만드는 것만으로 DB 연결이 열리던 것을 막는다 (2026-08-30)
뷰모델 계층 검토에서 나온 **실사용 결함**. `BaseRepository` 는 **생성자에서**
`Connection.Open()` 을 하는데, ViewModel 둘이 서비스를 미리 만들고 있었다.

- **`ClassDiaryViewModel`** — 일지 목록이 `foreach` 로 **줄마다** 하나씩 만든다.
  줄마다 연결 하나. 목록은 표시용 속성만 읽으므로 **그 연결을 쓰지도 않는다.**
  게다가 `_diaries.Clear()` 는 그냥 버려서 **새로고침할 때마다 쌓였다**
- **`StudentCardViewModel`** — 인자 없는 생성자가 **서비스 셋**을 만든다
  (`Student`·`StudentDetail`·`Enrollment`). 그런데 내보내기·인쇄가 이걸
  **학급 인원수만큼 루프로** 만들고(`StudentCardPrintService.LoadClassStudentsAsync`·
  `UnifiedExportService`·`PageStudentLog`), 그 경로들은 `LoadFromModels` 로 이미 읽어 둔
  모델을 넣어 주므로 역시 **쓰지 않는다.** 30명이면 연결 90개
- 둘 다 `IDisposable` 인데 **부르는 곳이 한 곳도 없었다.** `Pooling=true` 라 Dispose 되어야
  풀로 돌아가는데, GC 가 SafeHandle 을 정리할 때까지 물려 있었다

고침은 셋이다.

- **서비스를 지연 생성으로.** 처음 쓸 때 만든다 — 목록·내보내기 경로는 닿지 않는다.
  호출부를 하나도 안 고치고 세 군데 루프가 모두 해결된다
- **Dispose 를 실제로 부르게.** `ClassDiaryBox`·`StudentCard` 는 언로드에서, 목록은 줄을
  버리기 전에. 둘 다 **저장이 끝난 뒤**에 놓아준다 — 그 저장이 서비스를 쓴다
- **소유권을 구분한다.** DI 생성자로 받은 서비스는 수명이 준 쪽 것이라 건드리지 않는다
  (`_ownsServices`·`_ownsViewModel`). 지연 생성이라 컨트롤이 다시 붙어도 알아서 되살아난다

회귀 테스트 4건. 이 성질은 **눈에 보이는 증상이 없어서**(연결이 새도 화면은 멀쩡하다)
되돌아가기 쉽다. 일부러 되돌려 테스트가 실제로 무는 것을 확인하고 원복했다.

### 변동 일자를 날짜로 다루고, 네임스페이스 스타일을 빌드가 강제한다 (2026-08-30)
모델 검토의 마지막 둘.

**① `Enrollment.ChangeDate` 가 `string` → `DateTime?`**

- 읽는 쪽이 **모두 날짜로 되돌려 쓰고 있었다**(`EnrollmentGuard.ParseDate`·
  `StudentEditDialog.ParseDate`). 형식이 어긋난 글자가 들어와도 아무도 몰랐다
- **`DateTime` 이 아니라 `DateTime?` 다.** `null`("날짜를 모른다")이 진짜 상태이고 컬럼도
  NULL 을 받는다. `DateTime.MinValue` 같은 특수값을 쓰면 그 학생이 아주 오래전에 떠난 것으로
  잡혀 **경고가 늘 뜬다**. `Student.BirthDate`·`LessonProgress.CompletedDate` 가 이미 같은 꼴이다
- 컬럼은 TEXT 그대로다 — 글자↔날짜 변환을 **리포지토리 한 곳**으로 모았다.
  읽을 때는 `TryParse` 로 받는다: 이 매퍼는 명렬표 조회가 모두 지나는 길목이라 여기서 던지면
  날짜 하나가 아니라 **학생 목록이 통째로 안 뜬다**
- 표시용 `StudentManagementViewModel.ChangeDate` 는 문자열로 남겼다 — 목록에 글자로 찍을 뿐이다
- 회귀 테스트 3건(왕복·NULL·**깨진 글자를 만나도 안 터진다**)

**② 네임스페이스 스타일 통일**

- **`.editorconfig` 에 규칙이 이미 있었다** — `csharp_style_namespace_declarations =
  file_scoped:warning`. 결정은 되어 있었는데 **빌드에서 검사되지 않아** 지켜지지 않았고,
  Models 14개가 아니라 저장소 전체 **59개**가 어긋나 있었다
- **손으로 고치지 않았다.** 들여쓰기를 걷어내는 일괄 재작성은 LF 를 깨뜨린 전력이 있다.
  Roslyn 기반 `dotnet format style --diagnostics IDE0161` 로 그 규칙만 적용했다.
  변경 150개 파일 전부 CRLF 유지 실측, LF 경고 0
- **다시 어긋나지 않게 `EnforceCodeStyleInBuild` 를 켰다.** 일부러 한 파일을 되돌려
  **빌드가 실제로 무는 것을 확인**하고 원복했다 — 안 물면 넣은 뜻이 없다
- 그걸 켜면 "IDE0005 를 검사하려면 `GenerateDocumentationFile` 을 켜라" 는 **권유가 경고로**
  올라온다. 그 권유는 따르지 않는다(XML 문서 파일은 배포물 군더더기다) — 규칙이 아니라
  권유이므로 그것만 `NoWarn` 했다. **빌드 경고 0 유지**
- 이제 열 폴더 전부 file-scoped 0/100

### 학급일지 출결은 "교사가 손으로 적는 칸" 으로 확정한다 (2026-08-30)
모델 검토에서 유일하게 설계 결정이 필요했던 항목. `ClassDiary` 의 `Absent`·`Late`·
`LeaveEarly` 가 이름을 쉼표로 이어 적은 문자열이라 **동명이인을 구분할 수 없고, 학생 이름을
고치면 지난 일지가 어긋난다**고 보고했었다. **결론: 그건 고칠 문제가 아니라 그 칸의 성격이다.**

- **결정을 코드가 말하게 했다.** 세 칸에 "교사가 손으로 적는다" 를 달고, 왜 학생을 가리키지
  않는지와 **학생별 출결이 필요해지면 학적을 가리키는 표를 새로 만들 것**을 함께 적었다
- **그 성격과 어긋나던 API 를 걷어냈다** — `HasAttendanceIssue(name)`·
  `GetAttendanceStatus(name)`·`Listed(csv, name)`·`AttendanceIssueCount`·
  `ClearAttendance()`·`ClearAll()`, 그리고 `ClassDiaryViewModel` 의 통과 래퍼 넷.
  **부르는 곳은 한 곳도 없었다**(ViewModel 래퍼뿐이고 그 래퍼도 호출 0)
- **이름으로 묻는 것 자체가 답할 수 없는 질문이었다.** 예전에 `Contains` 로 검사해 부분
  일치로 엉뚱한 학생을 잡았고(김하 ⊂ 김하늘), `Split(',')` 으로 고쳤지만 **동명이인은 여전히
  못 막는다.** 고쳐 쓸 수 있는 종류가 아니라 없애야 할 종류였다
- `AttendanceIssueCount` 는 "학생 수" 라는 이름과 달리 **적힌 토막의 수**였다 —
  쉼표를 안 쓰면 1, 빈칸이 섞이면 늘어난다. 메모칸에서는 그 둘이 같지 않다
- 딸려서 죽은 **`AttendanceStatus` 상수**(출석·지각·조퇴·결석·결과·질병·기타)도 지웠다.
  쓰던 곳이 `GetAttendanceStatus` 하나뿐이었다. 되살릴 때를 위해 **NEIS 는 결석 사유를
  질병·미인정·기타·인정으로 가른다**는 것(옛 목록과 맞지 않았다)을 남겨 두었다
- **남긴 둘** `HasAttendanceIssues`·`AttendanceSummary` 는 **글자가 있나 / 뭐라고 적혔나** 만
  본다. 학생을 찾지 않으므로 메모칸이라는 성격과 어긋나지 않고, 화면이 실제로 쓴다
- 빌드 경고 0, 테스트 **593개** 통과

### `x:Bind` 만 쓴다는 것을 규칙으로 못박고, 죽은 바인딩 특성 13개를 걷는다 (2026-08-30)
모델 검토의 `[Bindable]` 항목. **처음에 "`[Bindable]` 인데 POCO다 — 변경 알림 수단이 없다"
라고 적었는데 틀렸다.** 이 특성들은 알림과 아무 상관이 없다. 고전 `{Binding}` 이 반사로
속성을 찾을 수 있게 하는 표식이다.

- **실측 둘.** ① 저장소 전체 XAML 에서 `{Binding}` **0건** — 전부 `x:Bind` 다.
  `DisplayMemberPath`·`SelectedValuePath`·`TextMemberPath` 도 0건, 코드에서 문자열 경로로
  거는 바인딩(`new Binding`·`PropertyPath`)도 0건. ② 그런데 이 프로젝트는
  `PublishTrimmed`·`PublishAot` 를 켜고 낸다
- **그래서 특성 13개는 전부 죽어 있었다.** `Post`·`PostFile`·`PostListViewModel`·
  `MaterialEditDialog`·`SchoolMeal`·`Course`·`Enrollment`·`SchoolSchedule`·
  `SchoolScheduleGroup`·`SettingsPage`·`KAgendaControl`·`KEvent`·`StudentListItemViewModel`
- **지우기 전에 규칙부터 테스트로 세웠다.** 트리밍·AOT 아래에서 `{Binding}` 은 특성이 없으면
  **빌드는 통과하고 실행할 때 값만 조용히 안 나온다** — 오류도 경고도 없다. 그냥 특성만
  지우면 나중에 누가 `{Binding}` 을 습관적으로 썼을 때 그 함정에 그대로 빠진다.
  이제 테스트가 먼저 걸리고, 정말 필요하면 특성을 함께 되살리면 된다
- 생성기가 만들던 파트가 사라져 짝을 잃은 `partial` 넷도 함께 걷었다
  (`SchoolMeal`·`SchoolScheduleGroup`·`Course`·`CourseEnrollment`)
- 빌드 경고 0, 테스트 591 → **593개** 통과

### 계산 속성이 자기 입력을 따라 돈다 — 규칙을 정하고 테스트로 못박는다 (2026-08-29)
모델 검토에서 남겨 둔 항목. `DisplayName => $"{Grade}학년 {Subject}"` 같은 계산 속성이
`Grade`·`Subject` 가 바뀌어도 알림을 내지 않았다. `LessonChange` 만 혼자 알리고 있어
**한 저장소에 규칙이 둘**이었다.

- **지금 증상은 없었다.** `x:Bind` 는 `Mode` 를 빼면 **OneTime** 이라 알림이 없어도 화면이
  멀쩡해 보인다. 문제는 그래서 **어긋난 줄도 모른다**는 것이다 — 나중에 누가 `Mode=OneWay` 를
  붙이는 순간 오류도 경고도 없이 값만 안 바뀐다
- **실제로 이미 걸려 있었다.** `ClassTimetableEditDialog` 이 계산 속성 `DayName` 에
  `Mode=OneWay` 를 걸어 두었다. `DayOfWeek` 가 생성 뒤 안 바뀌어 드러나지 않았을 뿐이다
- **규칙: 계산 속성은 자기 입력이 바뀔 때 알림을 낸다.** 반대 방향(모델은 안 알리고 화면이
  목록을 다시 만든다)도 가능했지만, 그러면 `Mode=OneWay` 가 언제 안전한지를 화면마다 따져야
  한다. 알리는 쪽이면 **"OneWay 를 붙이면 그냥 된다"** 가 성립한다.
  규칙은 `NotifyPropertyChangedBase` 머리에 적었다 — 모델이 전부 지나는 자리다
- `Notify(params string[])` 헬퍼를 그 기반 클래스에 두고, **모델 일곱**에 적용했다 —
  `Course`·`ClassTimetable`·`Lesson`·`ClassDiary`·`CourseSection`·`StudentLog`, 그리고
  이미 하고 있던 `LessonChange` 도 같은 헬퍼로 옮겨 표현을 하나로 맞췄다
- **`StudentLog` 은 하마터면 놓칠 뻔했다.** 표현식 본문(`=>`)만 훑어서는 안 걸린다 —
  `Summary`·`DraftSummary` 가 블록 본문이라 따로 훑어 찾았다
- **테스트로 못박았다(8건).** 이 규칙은 깨져도 증상이 없어서 주석만으로는 지켜지지 않는다.
  같은 값을 다시 넣으면 **알리지 않는다**는 것도 함께 고정했다 — `SetProperty` 가 거짓일 때
  `Notify` 를 부르면 값이 그대로인데 화면이 다시 그려지고, 목록이 길면 그대로 버벅임이 된다
- 곁들여 `Base.cs` 머리의 없는 파일명(`SchoolDB.cs`)을 실제 내용으로 고치고 쓰지 않는
  `using` 6개를 걷었으며, `Course._type` 의 `"Class"` 리터럴을 `CourseTypes.Class` 로 바꿨다
- 빌드 경고 0, 테스트 583 → **591개** 통과

### 동아리 목록에 스크롤바를 되찾고, 좌석 축 정리는 닫는다 (2026-08-29)
`FEATURES.md` 에 남아 있던 차후 과제 둘을 끝냈다.

- **동아리 배정 목록의 스크롤바.** 원인이 목록이 아니라 **다이얼로그에 높이 상한이 없던 것**
  이었다. 높이를 묶지 않으면 안쪽 `ListView` 가 "높이 무한" 으로 측정돼 항목을 전부 펼치고,
  스크롤이 필요 없다고 판단해 스크롤바를 그리지 않는다. 같은 모양인 수업 배정 다이얼로그에는
  `ContentDialogMaxHeight`·`MinHeight` 가 처음부터 있었다 — 그걸 맞춰 넣었다
- **좌석 축 정리(학적 재설계 3b)는 하지 않기로 했다.** `StudentID` → `EnrollmentNo` 전환이다.
  수업·동아리를 바꾼 이유는 *전출한 학생이 명단에 남아서* 였는데, **좌석은 그렇지 않은 것을
  실측했다** — 복원 루프가 현재 명단과 대조해 짝이 없으면 빈 자리로 두고, 그 상태로 한 번
  저장하면 스스로 아문다. 고칠 증상이 없는데 표 넷 + `SeatService`(560줄) +
  `PageSeats`(1,300줄) + 짝 제외 로직 + 데이터 이관을 건드리는 것은 남는 장사가 아니다.
  근거를 `docs/enrollment-redesign.md` 7.5 에 적어 닫았다
- **다만 진짜 문제 하나는 있었다.** 좌석 표 셋이 `StudentID` 를 들고 있으면서 **FK 가 없어**,
  학생을 지워도 좌석·짝 이력·자리 이력이 영영 남았다. 초기화기의 고아 정리에 넣었다 —
  좌석은 미사용·숨김·고정 같은 **배치 상태를 함께 들고 있어 `StudentID` 만 비우고**(지우면
  자리 모양이 무너진다), 이력 둘은 지운다
- ⚠ 이 정리는 **앱을 켤 때마다** 돈다. 조건이 한 글자만 넓어도 멀쩡한 배치가 사라지므로,
  "살아 있는 학생의 좌석과 이력은 건드리지 않는다" 를 회귀 테스트로 못박았다
- 테스트 580 → **583개** 통과

### 강의실은 정하고 나면 잠긴다 — 바꾸려면 초기화다 (2026-08-29)
course 축 점검 4단계이자 마지막. `Course.Rooms` 는 교사가 치는 **자유 텍스트**인데 그 조각
하나가 네 표의 키 노릇을 한다(`Lesson.Room`·`CourseWeeklyHours.Room`·`LessonProgress.Room`·
`CourseEnrollment.Room`). DB 에 이 넷을 `Course.Rooms` 와 잇는 제약은 하나도 없다 —
문자열이 우연히 같아서 이어져 있었다.

- **⚠ 진짜 트리거는 교사의 개명이 아니었다.** `CourseEditDialog` 의 학년·유형 콤보 핸들러에서
  `TxtRooms.Text = template` 이 **`switch` 밖**에 있었다. 그래서 유형을 "선택 과목"·"동아리" 로
  바꾸면 `template` 이 빈 문자열인 채 대입돼 **강의실 목록이 통째로 지워졌고**, 학년을 바꾸면
  직접 적어 둔 "음악실" 이 자동 목록으로 갈아치워졌다. 시수나 비고를 고치러 들어왔다가
  콤보를 한 번 건드렸을 뿐인데 그랬다
- **정한 규칙: 강의실은 정하고 나면 바뀌지 않는 것이 기본.** 딸린 기록이 하나라도 있으면
  칸을 **잠근다.** 잠금이 경고보다 중요하다 — 잠긴 동안에는 위 사고 경로가 **손댈 대상 자체를
  잃는다.** 딸린 기록이 없는 새 수업은 잠그지 않는다
- **바꾸려면 [강의실 다시 정하기].** 무엇이 지워지는지 세어서 보이고, 확인해야 칸이 열린다.
  실제 삭제는 **저장할 때** 한다 — 취소하면 아무 일도 없어야 한다.
  세는 것과 지우는 것은 `Services/CourseRoomReset.cs` 한 곳에 뒀다
- **초기화 범위** — 앞의 셋은 강의실이 정해져야 뜻이 생기는 파생 기록이라 지운다. 특히
  `Lesson` 은 `Room` 이 **그 칸이 어느 학급 수업인지 말하는 유일한 값**이다(격자는 요일·교시당
  한 칸이고 학년·반 열은 채우는 코드가 없어 걷어냈다). 이름이 뜻을 잃으면 칸도 뜻을 잃는다.
  한 수업치라 다시 놓기 싸다

  | 표 | 처리 |
  |---|---|
  | `Lesson` · `CourseWeeklyHours` · `LessonProgress` | **지운다** |
  | `CourseEnrollment` | **행은 살리고 `Room` 만 비운다** |

- **수강 배정만 살리는 이유**는 성격이 다르기 때문이다. `Room` 이
  `UNIQUE(EnrollmentNo, CourseNo)` 에 **들어 있지 않다** — 학생 한 명은 한 수업에 한 번
  배정되고 강의실은 거기 달린 속성이다. 강의실 이름이 바뀌었다고 수강생을 명단에서 뺄 이유가
  없다. 학급 공통 수업이면 이 값은 학적에서 만든 파생값(`"{학년}-{반}"`)이라 [일괄 배정]
  한 번으로 되돌아오고, 선택 과목·동아리만 분반을 다시 지정하면 된다 — 경고가 그 차이를 말한다
- **`CourseRoom` 테이블은 만들지 않는다.** 초기화가 고아 행이 생길 길을 없애므로 필요가 없다.
  이름이 안 바뀌면 문제도 없다는 지적이 옳았다 — 표를 하나 더 두는 건 과한 공사다
- 순서·공백만 다른 목록은 **같은 목록**으로 본다(`SameRooms`). 스치기만 해도 기록이 날아가면 안 된다
- 새 것: `LessonProgressRepository.DeleteByCourseAsync`(`CourseSection` 을 거치는 JOIN),
  `CourseEnrollmentRepository.ClearRoomsByCourseAsync`, `Course.ParseRooms`(미리보기·판정이
  인스턴스 없이 같은 규칙을 쓰게)
- 빌드 경고 0, 테스트 570 → **580개** 통과

### 아무도 부르지 않던 모델 네 개를 걷어낸다 (2026-08-29)
`Models/Base.cs` 의 `ClassAssignment`·`Subject`·`CourseAssignment`·`Classroom`. 모델 계층을
스키마와 전수 대조하다 나왔다 — 넷 다 저장소 전체(`.cs`·`.xaml`)에서 **정의부 말고는 참조가
한 군데도 없었다.**

- 학급 배정은 `Enrollment` 가, 수강 배정은 `CourseEnrollment` 가 이미 맡는다. 같은 일을
  하는 모델이 두 벌 있으면 다음에 이 폴더를 여는 사람이 어느 쪽이 진짜인지부터 물어야 한다
- `Subject` 는 **테이블 쪽이 먼저 없어졌다** — 읽고 쓰는 코드가 없던 아홉 개를 걷어낼 때
  함께 지웠는데(2026-08-28) 모델만 남아 있었다. 과목명은 `Course.Subject` 에 문자열로 들어간다
- `Classroom` 은 학년·반 한 쌍을 담던 그릇이다. 그 한 쌍을 함께 넘길 일이 생기면
  `Enrollment` 나 `ClassTimetable` 처럼 맥락을 가진 형이 이미 있어 되살리지 않는다
- 비게 된 `#region Core Models` 도 함께 지우고, 그 자리에 왜 없어졌는지를 남겼다
- 코드 59줄이 빠졌다. 빌드 경고 0, 테스트 566개 그대로 통과
- 모델 대조에서 **모델↔스키마 불일치는 0건**이었다. 이름이 어긋난 둘
  (`CourseWeeklyHours.WeekStart`↔`WeekStartDate`, `StudentSpecial.IsFinalized`↔`IsActive` 반전)은
  모두 매퍼에 명시된 의도된 매핑이다
- 곁들여 테스트 쪽 경고 하나(xUnit2031)를 없앴다. `Assert.Single(roster.Where(…))` 는
  걸러 낸 뒤 개수만 세어 실패했을 때 "몇 개였는지" 만 알려 준다.
  `Assert.Single(roster, …)` 로 바꾸면 걸린 항목을 함께 찍어 준다.
  **이제 앱·테스트 두 프로젝트 모두 경고 0.**

### 작년 수업이 "오늘의 수업" 에 섞이던 것을 막고, `Lesson` 을 시간표 한 칸으로 줄인다 (2026-08-29)
course 축 점검 3단계. `Lesson` 15열 중 **여섯이 만들어진 뒤로 줄곧 기본값**이었다.
그런데 "아직 안 쓴 칸" 이 아니라 **이미 다른 곳이 하기로 정해진 일의 잔해**였다.

| `Lesson` 의 칸 | 그 일을 실제로 하는 곳 |
|---|---|
| `Topic`·`IsCompleted` | 게시판 수업 일지 — 완료 판정은 "그 교시 글이 있는가"(일지 일원화, 2026-08-21) |
| `IsCancelled` | `LessonChange` (휴강) |
| `Date` + `IsRecurring=0` | `LessonChange` (보강) |
| `Class` | `Room` 문자열과 `ClassTimetable` |

- **먼저 진짜 버그 하나.** `GetByDateAsync` 가 교사와 요일만 보고 **학년도·학기를 안 걸렀다.**
  다른 두 조회는 거른다. 하필 이게 "오늘의 수업" 을 먹여서, **학년도가 바뀌면 작년 같은 요일
  수업이 섞인다** — 과목 목록은 올해 것만이라 과목명이 빈 유령 행으로 뜨고 "N시간 중 M건" 의
  N 도 부풀려진다. 배포 첫 해라 안 드러났을 뿐 첫 롤오버에 나타날 자리였다. 회귀 테스트 3개 추가
- **여섯 열을 걷어냈다.** 딸린 것도 함께 — 늘 참인 `IsRecurring=1`·늘 거짓인 `IsCancelled=0`
  으로 거르던 SQL 조건 넷, 한 줄도 못 걸러 내던 `CourseHoursView` 의 필터, 한 번도 참이 된 적
  없는 `LessonHomePage` 의 휴강 건너뛰기, 늘 `Room` 쪽만 타던 `Lesson.ClassDisplay` 삼항
- **`Grade` 는 남겼다.** 유일하게 값이 들어가는 칸이라 성격이 다르다. 읽는 곳은 없지만
  `Room` 문자열 축을 손볼 때 함께 볼 자리다
- **⚠ `DatabaseInitializer` 의 `Lesson(Date, Period)` 인덱스도 함께 지워야 했다.**
  없는 열에 `CREATE INDEX` 를 걸면 **새 DB 의 초기화가 통째로 멎는다** — `CourseSchedule`
  인덱스를 지울 때 겪은 것과 같은 자리다
- **마이그레이션은 두지 않았다**(스키마 세대도 2 그대로). 기존 DB 는 그 여섯 열을 그대로
  안고 가되 읽지도 쓰지도 않는다. 옛 스키마 DB 에 새 `INSERT`/`UPDATE`/`SELECT` 를 실제로
  돌려 통과를 확인했다 — 빠진 열이 전부 NULL 허용이거나 `DEFAULT` 가 있어 그대로 통한다
- `LessonChange` 의 경고 주석이 없어진 `Lesson.IsCancelled` 를 가리키게 되어, 근거는 살리고
  사실만 고쳤다
- 빌드 경고 0, 테스트 567 → **570개** 통과

### 배정 다이얼로그가 후보를 조회 한 번으로 읽는다 (2026-08-29)
course 축 모델을 살펴보다 나온 것이다. 동아리 배정이 후보 학생을 **`1~3학년 × 1~15반`
이중 루프**로 모으고 있었다 — 한 번 열 때마다 DB 에 45번을 물었고, 대부분이 없는 학급을
묻는 헛물이었다. 두 배정 화면 모두 학년 인자를 빼고 조회 한 번으로 바꿨다.

- **빈 `catch` 를 없앴다.** "학급에 학생이 없으면 무시" 라고 적혀 있었지만 실제로는 **모든**
  예외를 삼켰다. DB 가 잠겨 조회가 실패해도 학생이 조금 모자란 목록과 구별되지 않았다.
  조회가 하나면 실패가 실패로 드러난다
- **반 필터를 명부에서 만든다.** 예전에는 늘 `1~15반` 이 나와서, 3반까지인 학교에서도
  4~15반을 고를 수 있었고 골라도 목록은 비었다. 학년 칸도 같은 규칙으로 맞췄다 —
  한쪽만 고치면 두 칸이 서로 다른 말을 한다. `CourseEnrollmentDialog` 는 이미 이 방식이었다
- **⚠ 학년 `1~3` 은 버그가 아니었다.** 처음에 이것을 "초등학교 4·5·6학년이 빠진다" 로 잘못
  보고했다. **이 앱은 중·고등학교용**이라 3학년까지가 맞고, 그 전제로 손댄 `PageSeats`
  (자리 배치의 학년 콤보)와 새로 만든 `Helpers/GradeOptions.cs` 는 되돌렸다. 남긴 변경은
  학교급과 무관하게 성립하는 것들뿐이다 — 조회 45번 → 1번, 삼켜지던 예외, 없는 반이 보이던 필터
- `FEATURES.md` 의 "동아리 배정 다이얼로그 — 학년·반 필터" 항목이 이걸 가리킨 것이다
- 회귀 테스트 1개 추가(567개). "학년도만 주면 전 학년·전 학급이 온다" — 두 화면이 이제
  이 계약에 기댄다. 여기 조건이 슬쩍 들어가면 후보 목록에서 학생이 조용히 사라지는데,
  그건 "원래 명단에 없나 보다" 로 보여 알아채기 어렵다

### 빈 목록만 돌려주던 학급 시간표 조회를 걷어낸다 (2026-08-29)
course 축 점검 2단계. `Lesson` 을 건드리는 죽은 API 셋을 서비스·리포지토리 양쪽에서 지웠다.

- **`GetClassScheduleAsync`** — 호출부가 없었고, **있었다면 늘 빈 목록을 돌려줬다.**
  조건에 `Class = @Class` 가 있는데 `Lesson.Class` 를 채우는 코드가 한 곳도 없다
  (유일한 생성 지점인 `CourseTimetableBoard` 가 `Grade` 만 넣는다). 늘 0 이니 어떤 반
  번호로도 걸리지 않는다. 이름이 "학급 시간표" 라 다음에 그게 필요한 사람이 반드시 집어 들
  자리였다 — 죽은 채로 두면 무해한 게 아니라 **함정**이다. 학급 시간표는 `ClassTimetable`
  이 맡고, `Lesson` 은 교사 관점이라 애초에 다른 표다
- **`MarkCompletedAsync`·`MarkCancelledAsync`** — 부르는 곳이 없어 `Lesson.IsCompleted`·
  `IsCancelled` 는 테이블이 생긴 뒤로 줄곧 0 이었다. 그 둘로 거르는 조회들이 사실상 조건
  없이 돌고 있었다는 뜻이다. (`Scheduler` 의 동명 메서드 `KEventRepository.MarkCancelledAsync`
  는 다른 타입이라 그대로다 — 이름만 같다)
- **지운 자리에 되살리기 전 읽을 것을 남겼다.** 휴강을 `IsCancelled` 로 하면 안 된다 —
  `Lesson` 은 정기 시간표라 행 하나가 "매주 그 교시" 를 뜻하므로 취소를 세우면 그 수업이
  **매주** 사라진다. 날짜 하나의 예외는 `LessonChange` 가 맡는다
- 빌드 경고 0, 테스트 567개 그대로 통과

### `LessonService` 가 정체를 하나로 줄이고 이름을 얻는다 (2026-08-29)
위 정리를 하다 드러났다. 이 서비스를 쓰는 곳이 **화면 셋뿐**이고 각각 한 메서드만 부르는데,
`Lesson` 을 만지는 나머지 **일곱**은 리포지토리로 직행하고 있었다. 취향 문제가 아니라
**타입이 정체를 둘로 광고하고 있어서**였다.

- **CRUD 통과 래퍼 여섯을 걷어냈다** — `Create`·`Update`·`Delete`·`GetById`·
  `GetTeacherSchedule`·`GetByCourse`. 전부 리포지토리를 그대로 부르기만 했고 호출부도 0.
  이것들이 붙어 있으니 "범용 CRUD 파사드" 로 보였고, 그렇게 읽으면 굳이 거칠 이유가 없다
- 딸려서 죽은 `LessonRepository.GetByIdAsync` 도 지웠다(유일한 호출자가 그 래퍼였다).
  시간표 화면들은 언제나 묶음으로 읽는다 — 교사별·과목별·날짜별
- **남은 셋은 통과 래퍼가 아니다.** `Lesson` 과 `Course` 를 엮어 격자를 채우고(과목을
  일괄 로드해 N+1 을 피한다), `Settings` 의 "나·올해·이번 학기·오늘" 을 묶는다.
  리포지토리 하나로는 못 하는 일이고, 그래서 서비스를 없애는 선택지는 버렸다 —
  없애면 그 조립이 화면 셋으로 흩어진다
- **`LessonService` → `TeacherTimetableService`.** 잘라 내고 나니 이 타입은 교사 시간표
  전용이고, 마침 `TimetableService` 는 이미 **학급** 시간표 전용이었다(교사 쪽은 39차에
  빠졌는데 클래스 주석만 "교사별/학급별" 로 남아 있었다 — 함께 고쳤다). 이제 둘이
  관점으로 갈리는 짝이고, 그 갈림이 이름에 드러난다. 호출부가 셋뿐이라 개명이 쌌다
- 리포지토리의 옛 주석 셋이 없어진 타입 이름을 가리키고 있어 함께 손봤다. `FEATURES.md`
  파일 표도 두 서비스가 무엇으로 갈리는지 적도록 고쳤다
- 빌드 경고 0, 테스트 567개 그대로 통과

### 학년 상한을 1~3 으로 되돌린다 (2026-08-29)
위 오보를 정리하다 드러났다. 예전에 **초등학교(6학년)를 염두에 두고 1~6 으로 넓혀 둔** 자리가
넷 있었다. 이 앱은 중·고등학교용이므로 전부 1~3 으로 되돌린다.

- `ClassPicker` 의 폴백 목록 — 학생을 넣기 전 **첫 실행**에서 없는 4·5·6학년이 콤보에 떴다
- `AddStudentsPage` 셋 — 엑셀 가져오기 판정, 수동 추가 검증, 학년 입력 상자(안내 문구 포함)
- **넓은 쪽이 안전한 게 아니었다.** 학년 선택기들이 이제 **명부에서** 목록을 만들기 때문에,
  오타로 들어온 `5학년` 학적 한 줄이 그 뒤로 화면마다 "5학년" 항목을 만들어 낸다.
  들어오는 자리에서 막는 편이 낫다
- `SchoolCalendar.GradeCountFor` 의 `초등 → 6` 은 **그대로 둔다.** 그건 이 앱의 정책이 아니라
  NEIS 가 주는 `School.SchoolType` 을 옮긴 것이고, 학사일정의 "그 학년만 빠지는 행사" 판정이
  쓰는 순수 함수다. 바깥 세계를 기술하는 값까지 앱 범위에 맞춰 고치면 그 함수가 거짓말을 한다

### 수업·동아리 배정이 학생이 아니라 학적을 가리킨다 (2026-08-28)
학적 재설계 3단계. `CourseEnrollment`·`ClubEnrollment` 가 `StudentID` 로 학생을 직접
가리켜, **"어느 학년도의 배정인가" 가 관계에 담기지 않았다.** 그래서 전출한 학생이 그 해
수업·동아리 명단에 계속 남았고, 막는 장치가 조회 필터밖에 없었다.

- **`StudentID` → `EnrollmentNo`.** 백필은 `Course.Year`·`Club.Year` 로 학년도를 잡아
  풀었고 **짝을 못 찾는 행이 0**(344 + 9 행 전부 유일하게 대응). 지금이 가장 싼 시점이었다 —
  학년도가 하나뿐이라 대응이 유일하다
- **`ON DELETE CASCADE`** 라 학적을 지우면 그 해 배정도 함께 사라진다. 예전에는 고아가 됐다
- **`CourseEnrollmentFull`·`ClubEnrollmentFull` 뷰**가 학적을 거쳐 `StudentID`·`Name` 과
  함께 `IsActive` 를 내준다. "전출한 학생을 명단에서 뺀다" 가 조회 한 줄이 됐고,
  `includeInactive: true` 로 그 해에 실제로 들었던 기록까지 볼 수 있다
- `CreatedAt`·`UpdatedAt` 도 함께 걷어냈다 — 학적에서와 같이 읽는 코드가 없었다
- **⚠ 조용히 깨질 뻔했다.** `StudentID` 가 모델에 속성으로 남아 있어
  `new ClubEnrollment { StudentID = ... }` 가 **컴파일은 된다**(저장은 안 되고 FK 위반이
  날 뿐). 두 배정 다이얼로그가 그렇게 되어 있었고, 빌드가 통과해서 생성 지점을 전수
  검색하지 않았으면 놓쳤을 것이다
- **좌석 두 표(`SeatAssignment`·`SeatPosHistory`)는 미뤘다.** `SeatAssignment` 는
  `SeatArrangement` 를 거쳐 이미 학년도 범위가 잡혀 있어 사정이 다르다 — 고쳐야 할
  문제가 아니라 일관성 정리이므로 좌석 축을 통째로 볼 때 함께 한다
- 테스트 560 → **566개**

### 전출한 학생에게 기록을 남기려 하면 알려 준다 (2026-08-28)
학적 재설계 2단계. `ChangeDate` 에 처음으로 읽는 코드가 생겼다 — 그동안 날짜 칸이 빈 채였던
것은 쓰는 사람이 없어서였다.

- **판정은 `Services/EnrollmentGuard.cs` 한 곳**에 뒀다. 부르는 화면이 여럿이라 조건을
  화면마다 적으면 값이 늘 때 어긋난다
- **"떠났다" 로 보는 변동은 넷** — 전출·졸업·자퇴·퇴학. 비활성 전부가 아니다:
  휴학·유예·정원외는 학적이 살아 있어 그 사이에도 기록할 일이 있다
- **막지 않고 묻는다**(계속/취소). 막으면 전출일을 뒤늦게 넣었을 때 이미 적어 둔 기록을
  못 고치게 된다
- **반대 방향도 본다** — 학적 변동을 저장하면 그 날짜 뒤에 이미 남은 기록을 세어 알린다.
  전출은 늦게 입력되는 쪽이 흔해 실제로는 이쪽이 더 자주 걸린다. 세어서 알릴 뿐 지우지
  않는다 — 그때는 우리 학생이었을 수도, 날짜를 잘못 넣었을 수도 있다
- **덮는 범위**: `LogListViewer` 한 곳을 고쳐 학급일지·동아리활동·수업활동·학생정보·
  누가기록 다섯 화면이 한꺼번에 걸렸고, 학생부는 `StudentSpecPage` 에 따로 걸었다.
  `StudentLogDialog`·`CourseSpecPage` 는 **일부러 안 걸었다** — 학생을 명부에서 고르는데
  명부가 이미 재적만 주므로 떠난 학생이 목록에 없다
- 경계를 촘촘히 고정했다 — **떠난 당일까지는 우리 학생**이고(당일은 조용), 변동일자를
  모르면 조용하다(근거 없는 경고를 띄우면 사람이 경고를 무시하는 법을 배운다)
- 테스트 544 → **560개**

### 학적을 다시 설계했다 — 23열에서 12열로 (2026-08-28)
설계 경위와 근거는 **`docs/enrollment-redesign.md`** 에 남겼다. 원설계 의도는 추정이 아니라
읽을 수 있었다 — 구설계 모델(`ClassAssignment`·`CourseAssignment`·`Subject`)이 `Models/Base.cs`
에 그대로 남아 있고, `Enrollment.cs:10` 이 "기존 `ClassAssignment` 대체 — A안의 핵심 테이블!"
이라고 스스로 밝힌다. `ClassAssignment` 7열에서 `Enrollment` 23열로 늘 때 더한 16열이
**NEIS 학적 테이블을 옮겨오려던 의도**를 드러내는데, 그 대부분이 실현되지 않은 채
컬럼만 남아 있었다.

| 뺀 것 | 실측 근거 |
|---|---|
| `Semester` | 173행 전부 `1`. **2학기 행을 만드는 경로가 앱에 없다.** 학기는 명부가 아니라 기록(`StudentLog.Semester` 등)이 각자 들고 있다 |
| `Name` · `Sex` · `Photo` | `Student` 의 사본. 정본이 둘이라 학생을 고칠 때마다 학적을 따라 고쳐야 했고, 어제 그 동기화를 빠뜨려 한 번 물렸다 |
| `Status` + 날짜 6열 + 학교명 2열 | → `IsActive` · `ChangeType` · `ChangeDate`. 날짜 6열은 역할이 둘("언제부터"·"언제까지")뿐이었고 **전부 빈 채**였다 |
| `CreatedAt` · `UpdatedAt` | **읽는 코드가 0건**(쓰기만 함). `ChangeDate` 가 "언제 바뀌었나" 를 대신한다 |
| `IsDeleted` | `IsDeleted=1` 인 행 **0건**, 복원 경로 없음. 그런데 삭제 확인 문구는 "되돌릴 수 없습니다" 였다 — 앱이 하는 말과 동작이 달랐다. 이제 실제로 지운다(안전망은 ZIP 백업) |

살린 것은 `SchoolCode`(전근 대비 — `TeacherSchoolHistory` 가 실재한다)와 `TeacherID`(다중
사용자 대비) 둘뿐이다.

- **`IsActive` 는 파생 가능하지만 컬럼으로 뒀다.** 계산이 비싸서가 아니라(문자열 비교
  하나다) **SQL 에서 `WHERE IsActive = 1` 로 거를 수 있기 때문**이다. 상태 목록을 `WHERE`
  절에 나열하면 값이 늘 때마다 두 곳이 어긋나지만 불리언 하나는 안정적이고 인덱스도 탄다.
  대신 **`IsActive` 를 인자로 받는 함수를 아예 만들지 않았다** — `ChangeType` 에서만
  채워지므로 두 값이 갈라질 길이 없다(불변식을 테스트로 고정)
- **학적 변동 10종**: 입학·진급·전입(명단에 들어감) / 전출·졸업·휴학·유예·정원외·자퇴·퇴학
  (빠짐). 기본값은 학년이 정한다 — 1학년은 입학, 그 위는 진급
- **`EnrollmentFull` 뷰를 뒀다.** 조회가 열 곳이라 `Student` JOIN 을 되풀이하지 않도록
  한 번만 적는다. 읽기는 뷰, 쓰기는 표
- **이관 실측**: 173행 유지, 입학 172·전입 1, `IsActive` 와 `ChangeType` 이 어긋난 행 0,
  `ChangeDate` 빈 행 0, `integrity_check` ok, 외래키 위반 0. 손대기 전 백업을 떠 두었다
- 테스트 533 → **544개**

### 아무도 읽지 않던 테이블 아홉 개를 걷어냈다 (2026-08-28)
모델을 손보기 전에 실측부터 했다. `school.db` 에 테이블이 **34개**였는데 그중 9개가
**행도 0이고 읽고 쓰는 코드도 한 줄 없었다.** 모델 클래스조차 없는, 옛 스키마의 잔해다.

| 걷어낸 것 | 행 |
|---|---|
| `Subject` · `CourseSchedule` · `Evaluation` · `Attachment` | 0 |
| `SubjectYearPlan` · `WeeklyUnitPlan` · `WeeklyLessonHours` | 0 |
| `ScheduleUnitMap` | 4 |
| `UndoHistory` | 3 |

- **34개 → 25개.** `integrity_check` 통과, 외래키 위반 0, `Enrollment` 173행·`StudentLog`
  552행 그대로. 지우기 전 `Backups\school_before_table_cleanup_20260828.db` 로 떠 두었다
- `ScheduleUnitMap`·`UndoHistory` 만 행이 있었는데 둘 다 2026-07-30 자 시간표 실험의
  잔해이고, 읽는 코드가 없어 앱에서 닿을 수 없는 값이었다
- `CourseWeeklyHours` 는 행이 0이어도 **남겼다** — `CourseHoursView` 가 실제로 쓴다.
  "비었으니 죽었다" 로 판단하면 안 되는 자리다
- 초기화 코드에서 `Subject`·`CourseSchedule` 생성문과 `CourseSchedule` 인덱스·고아 정리
  구문을 함께 지웠다. 안 지우면 다음 실행에 되살아나거나, 없는 테이블에 인덱스를 걸다
  초기화가 멎는다(실제로 테스트 90개가 `no such table: CourseSchedule` 로 무너져 드러났다)

남은 25개도 겹치는 축이 있다 — 좌석 하나에 테이블이 넷(`SeatArrangement`·`SeatAssignment`·
`SeatHistory`·`SeatPosHistory`), 수업/시수/계획 축도 값이 실린 것은 셋뿐이다. 그건 다음 순서다.

### 학생 관리를 목록 전용으로, 전출한 학생은 명단에서 빠지게 (2026-08-28)
학생 관리 페이지가 행마다 `TextBox` 를 깔고 상단 저장 버튼으로 한꺼번에 커밋하는
구조였다. 저장하지 않은 편집이 화면에 쌓이다가 다른 반을 조회하는 순간 통째로 사라졌다.

- **목록 전용으로 바꾸고 편집을 다이얼로그(`StudentEditDialog`)로 옮겼다.** 확인을 누르는
  순간 그 학생만 커밋되므로 "저장 안 한 변경" 이라는 상태 자체가 없어졌다. 저장 버튼과
  `IsModified` 를 걷어냈다. 위쪽 **수정** 버튼(체크한 한 명)과 **행 클릭** 두 입구가 있다
- **학적 상태·전입·전출에 드디어 입구가 생겼다.** `Enrollment` 에 원래 있던 칸들인데
  값을 넣을 화면이 없어 늘 비어 있었다. 스키마 변경은 없다
- **"전학" 을 전입·전출로 갈랐다.** 둘은 명부에 남는지가 정반대다. 그리고 코드가 상태를
  두고 묻는 질문을 **`EnrollmentStatus.IsOnRoll` 하나로 못박았다** — 빠지는 넷(전출·졸업·
  자퇴·퇴학)만 거짓이고 나머지는 전부 참이다. 빈 값도 옛 "전학" 도 모르는 값도 참으로 본다.
  잘못 숨겨 멀쩡한 학생이 사라지는 손해가 반대쪽보다 크기 때문이고, 그 규칙을 테스트로 고정했다
- **전출한 학생이 명렬표·좌석표·수업 배정·동아리 배정에 그대로 섞여 들어오고 있었다.**
  명단 조회가 `IsDeleted = 0` 만 보고 상태를 아예 안 봤다. `EnrollmentService.GetEnrollmentsAsync`
  가 깔때기라 거기 한 곳에 `includeNotOnRoll = false` 기본값을 달아 전부 해결했다.
  참으로 켜는 곳은 **학생 관리 하나뿐**이며, 거기서만 빠진 학생을 흐리게 보여 준다
  (`총 20명 (재적 19 · 전출 등 1)`). 거르기를 SQL 이 아니라 C# 에서 하는 이유는 판정을
  `Enrollment.IsOnRoll` 한 곳에만 두기 위해서다 — WHERE 절에 상태 목록을 또 적으면 어긋난다
- **학생 생성 경로를 하나로 모았다**(`StudentCreationService`). 트랜잭션용 연결 공유는 두 벌로
  두면 반드시 한쪽이 어긋나는 코드다. `AddStudentsPage` 는 위임만 하도록 212줄 줄었다
- **머리 부분을 눌러 목록에 한 명이라도 더 보이게 했다** — 제목을 필터 줄에 얹고 여백을
  줄여 세로 90~100px 을 돌려줬다. 학생 관리와 학사일정 관리에 **현재 학교 이름**을 넣었다.
  조회가 `Settings.SchoolCode` 하나로 걸리는데 화면에 그 사실이 없어서, 목록이 비면
  "학생이 없는 것" 인지 "학교가 안 잡힌 것" 인지 구분할 수 없었다

**고치면서 밟은 함정 둘** — 둘 다 조용히 실패해서 "눌러도 반응이 없다" 로만 보였다.

- **`dialog.ShowAsync()` 를 직접 부르면 안 된다.** 이 앱의 대화상자는 전부
  `MessageBox.ShowDialogAsync` 의 세마포어(`_dialogGate`)를 거친다. 직접 부르면 겹칠 때
  예외가 나고 `async void` 라 그 예외가 사라진다. 전수 검색해 보니 앱 전체에서 내 코드
  두 곳만 규칙을 어기고 있었다
- **`ItemsRepeater` 는 실체화한 요소에 `DataContext` 를 채워 주지 않는다**(`ListView` 와 다름).
  핸들러에서 `DataContext` 를 보면 늘 null 이다. `Tag="{x:Bind}"` 로 항목을 실어 보내야 한다.
  이 때문에 **원래 있던 `OnStudentCheckBoxClick` 도 통째로 헛돌고 있었다** — 체크 자체는
  `x:Bind` TwoWay 가 해 줘서 티가 안 났지만, 개별 체크를 해도 위쪽 전체 선택 체크박스가
  중간 상태로 바뀌지 않았다
- `x:Bind` 는 `Mode` 를 빼면 OneTime 이라, 다이얼로그로 고친 뒤 행을 제자리 갱신해도
  화면이 옛 값 그대로 남는다. 목록의 바인딩에 `Mode=OneWay` 를 붙였다

### 달력이 엉뚱한 school.db 를 새로 만들어 읽고 있었다 (2026-08-28)
달력을 열면 날짜 셀에 학사일정이 하나도 뜨지 않고, **실행 파일 옆에 빈 `school.db` 가
생겼다.** `Data\school.db` 는 멀쩡한데 그것을 안 보고 있었다.

`SchoolScheduleService` 를 만드는 곳이 여덟 군데인데 여섯 곳은 `SchoolDatabase.DbPath`
(`Data\school.db`)를 넘기고 **두 곳만 `Settings.SchoolDB.Value`** 를 넘겼다. 그 값은
`"school.db"` 라는 **파일 이름뿐**이라 상대 경로가 되고, SQLite 가 프로세스 작업 폴더
(실행 파일 옆)에서 찾다가 없으면 빈 DB 를 새로 만든다.

- `Kcalendar.xaml.cs` 두 곳(DB 로드·NEIS 내려받기) — 신고된 증상이 여기다
- **`GoogleSyncService.cs` 한 곳이 더 있었다.** 여기가 더 나쁘다 — `GetScheduleTitlesAsync`
  는 구글 종일 이벤트가 이미 있는 학사일정과 겹치는지 판정하는데, 빈 DB 를 읽으니 제목
  집합이 늘 비어서 중복 판정이 **전부 "안 겹침"** 이 된다. 동기화할 때마다 같은 학사일정이
  구글 캘린더에 다시 쌓일 수 있었다. 달력 화면이 아니라 구글 쪽에서 드러날 증상이라
  따로 신고되지 않았다
- 같은 부류(`SchedulerDB`·`Board_DB` 포함)를 전수 검색해 남은 곳이 없음을 확인했다.
  나머지는 모두 `Path.Combine(Settings.UserDataPath, ...)` 을 거친다
- 그동안 실행 파일 옆에 생긴 빈 `school.db` 는 지워도 된다. 진짜 자료는 `Data\` 에 있다

### 소스를 MIT 로 공개한다 — 라이선스가 없는 것은 "마음대로 쓰세요" 가 아니었다 (2026-08-27)
저장소에 `LICENSE` 파일이 없으면 깃허브 기준으로 **모든 권리 유보**다. 소스가 눈에 보이는
것과 쓸 수 있는 것은 별개라, 지금까지는 누가 포크하거나 일부를 가져다 쓰는 것이 법적으로
막혀 있었다. 남이 쓰는 데 거부감이 없다면 그 상태가 오히려 뜻을 거스른다.

- **`LICENSE` (MIT) 를 추가했다.** 이미 쓰던 관례를 따랐다 — 직접 만들어 NuGet 에 올린
  `WinUIRichEditor` 가 MIT 이고, 담고 있는 제3자 구성 요소도 대부분 MIT 다. 깃허브가
  라이선스를 알아보도록 **순수 MIT 원문만**(ASCII·BOM 없음) 두었다. 설명은 README 로 뺐다
- **바로 앞 항목에서 쓴 제한 조항과 충돌해 범위를 좁혔다.** 사용권 계약 제2조와 이용약관
  제7조에 "역설계 금지·판매 금지" 를 넣어 두었는데, 소스가 MIT 면 앞뒤가 안 맞는다
  (MIT 는 판매를 허용하고, 소스가 공개돼 있으면 역설계 금지는 의미가 없다). 조항을 없애는
  대신 **적용 대상을 제3자 구성 요소로 한정**했다 — Windows App SDK 와 QuestPDF 는 여전히
  역설계·분리 배포·소스 형태 재배포를 금지하므로 그 제한 자체는 반드시 남아야 한다.
  보증 부인·책임 제한·Microsoft 보호 조항은 MIT 와 충돌하지 않아 그대로 두었다
- **부수 효과** — OSI 라이선스가 붙어 QuestPDF Community 자격 5항(오픈소스 프로젝트)이
  추가로 성립한다. 기존 근거인 1항(개인·연 매출 100만 달러 미만)과 두 갈래가 됐다
- **포크하는 사람을 위한 경고를 README 에 넣었다.** QuestPDF 자격은 쓰는 주체마다 따로
  판정된다 — 이 저장소가 MIT 라고 QuestPDF 까지 자유로워지지 않는다. 매출 100만 달러 이상
  기업·공공기관·상장사가 가져다 쓰면 유료 라이선스가 따로 필요하다
- 홈페이지의 "GitHub 에 공개" 도 "MIT 라이선스로 공개" 로 고쳤다. 코드 서명 없이
  SmartScreen 경고를 감수하는 근거로 소스 공개를 들고 있는 자리라, 조건이 분명할수록 낫다

### 저작권 — 남의 것을 담아 나르면서 고지를 안 하고 있었다 (2026-08-27)
배포물이 담고 나가는 제3자 구성 요소와 그 라이선스를 하나씩 대조했다.

- **제3자 라이선스 고지가 어디에도 없었다.** MIT·Apache-2.0·BSD 계열은 바이너리 배포에도
  저작권 문구와 라이선스 전문을 함께 보낼 것을 요구하는데, 저장소에도 설치본에도 앱 안에도
  한 장이 없었다. `THIRD-PARTY-NOTICES.txt` 를 만들어(212KB) 게시본에 실었다 —
  설치본은 `{app}\bin\`, 포터블은 게시본을 복사하므로 양쪽에 함께 간다.
  전문은 **각 패키지가 동봉한 원문을 그대로 이어 붙였다**(손으로 옮겨 적으면 조항이 바뀐다).
  특히 `QuestPdfSkia.dll` 은 QuestPDF 가 `ExternalDependencyLicenses\` 에 12종(Skia·libpng·
  zlib·HarfBuzz 등)을 일부러 동봉해 두었는데 그 12장이 통째로 빠져 있었다
- **`HtmlAgilityPack` 이 exe 에 들어 있는 것을 몰랐다.** 직접 참조가 없어 목록에 없었는데
  `WinUIRichEditor` 가 끌어온다. 반대로 `WinUIEdit.dll` 은 제3자가 아니라 WinAppSDK 의 파일이었다
- **QuestPDF 라이선스가 더 이상 MIT 가 아니다**(v3.0, 2026-07-06 시행). OSI 승인 오픈소스가
  아닌 소스 공개형 상업 라이선스로 바뀌었다. 자격은 그대로 충족하지만(개인·연 매출 100만 달러
  미만) 고지에 MIT 로 적으면 틀린다. `README` 와 고지 파일에 사실대로 적었다
- **PDF 를 만드는 경로 하나가 라이선스 설정을 빠뜨리고 있었다.** 서비스마다 각자
  `Settings.License` 를 넣었는데(6곳) `GenerateClassInfoPdfFromDbAsync` 에만 없었다. 정적
  전역이라 다른 PDF 를 먼저 만들어 본 적이 있으면 넘어가지만, 앱을 켜고 그 메뉴를 가장 먼저
  누르면 QuestPDF 가 예외를 던진다. **시작 시 한 번**(`App.xaml.cs`)으로 옮겨 순서 의존을 없앴다
- **최종 사용자가 아무 조건에도 동의하지 않고 있었다.** 함께 나르는 Windows App SDK 의
  라이선스 §3(b)(ii) 는 "외부 최종 사용자가 Microsoft 를 이 계약만큼 보호하는 약관에
  동의하도록 할 것" 을 재배포 조건으로 거는데, 설치 마법사에 동의 페이지가 없고 앱 안에서
  약관·방침으로 가는 링크도 **한 곳도 없었다**. 사용권 계약(`Installer\LICENSE_ko.txt`)을 만들어
  마법사 동의 페이지로 띄우고, 같은 파일을 게시본에도 실어 설치 뒤와 포터블에서도 읽게 했다
- **이용약관에 보증 부인과 책임 제한이 없었다.** 면책이 천재지변·외부 API 장애·이용자 귀책
  세 가지뿐이었다. 지식재산권·제3자 구성 요소·보증 부인(AS-IS)·책임 제한·준거법을 더해
  12개 조로 고쳤다. 도움말에도 약관·방침·오픈소스 고지로 가는 자리를 만들었다
- **배포 바이너리에 저작권 표시가 아예 없었다.** `LegalCopyright` 가 빈 문자열이었다.
  `.csproj` 에 `Copyright`·`Company`·`Product`·`AssemblyTitle` 을 넣어 채웠다
  (`FileDescription` 은 `Description` 이 아니라 `AssemblyTitle` 에서 온다). 홈페이지 하단의
  `© NewSchool` 도 연도와 권리자를 넣어 `© 2026 Centwon` 으로 고쳤다

### 포터블 표식을 어디에 두라는 안내가 틀려 있었다 (2026-08-27)
앱 파일을 `{app}\bin\` 아래로 내리면서(위 항목) 코드는 표식을 **실행 파일 폴더의 부모까지**
찾도록 고쳤는데, 안내 문서 세 곳은 여전히 "실행 파일 옆" 이라고만 적혀 있었다. 그대로 따라
하면 `portable.txt` 가 `bin\` 안으로 들어가고, 가까운 쪽이 이기는 규칙에 따라 `bin\` 이 루트가
되어 **`Data\`·`Backups\`·`Logs\` 가 프로그램 파일 50개 사이에 섞인다** — `bin\` 으로 내린
이유가 그대로 무너지는 자리였다. 코드는 옳고 안내만 틀린 경우다.

- `README.md`·`FEATURES.md`·`Assets\help.html` 을 "실행 파일 폴더 **또는 그 부모**" 로 고치고,
  표식을 `bin\` 안에 두면 어떻게 되는지 함께 적었다. README 의 폴더 그림에도 `bin\` 과
  `portable.txt` 를 넣었다
- 곁다리로 `README.md` 의 `secrets.json` 설명도 고쳤다 — "실행 파일 옆" 이라고만 적혀 있었으나
  실제로는 빌드 시 어셈블리에 내장되어 배포본에 파일이 없고, 옆에 두는 것은 재빌드 없이 키를
  바꾸는 override 경로다(`Services/SecretsService.cs`)
- 게시 경로도 함께 맞췄다 — VS "게시" 버튼이 쓰는 `FolderProfile.pubxml` 의 `PublishDir` 이
  옛 기본값(`bin\Release\Publish`)에 멈춰 있어, `NewSchoolSetup.iss` 가 읽는 폴더와 달랐다.
  아직 터지지는 않았지만(두 폴더 내용이 우연히 같았다) 소스를 고친 뒤 게시 버튼만 누르면
  설치 파일에 **옛 빌드**가 조용히 담길 자리였다. 같은 주석이 "ReadyToRun 은 AOT 와 상호배타라
  끈다" 고 해 놓고 값은 `true` 이던 것도 함께 바로잡았다(AOT 아래에서 무시되어 산출물은 동일)

### 안내 문서가 사실과 다르던 곳 (2026-08-26)
홈페이지·개인정보처리방침·이용약관·README 가 하는 말을 코드와 하나씩 대조했다.
**이용약관과 앱 안 도움말은 정확했고**(수치 여덟 개가 모두 코드와 일치), 나머지에서 나왔다.

- **개인정보처리방침이 없는 메뉴를 가리키고 있었다.** Google 연동을 끊는 방법을
  "설정 → 캘린더" 라고 안내했는데 설정에는 캘린더 항목이 없다. 실제로는 **달력 화면
  오른쪽 위 톱니바퀴 버튼**이 유일한 입구다. 연동을 끊으려는 분이 못 찾을 자리였다
- **그 톱니바퀴 버튼에 이름이 없던 것을 고쳤다.** 마우스를 올려도 무엇인지 알 수 없었다.
  달력의 이전·다음 달 버튼에도 함께 붙였다
- **방침이 주고받는 정보를 실제보다 좁게 적고 있었다.** 일정의 확정·취소 상태를 받아
  저장하고, 할 일로 등록한 항목은 완료 여부와 완료 시각을 구글 일정에 함께 기록하는데
  목록에 없었다. 사실대로 적었다
- **"구글 말고는 어디로도 보내지 않는다"는 설명을 바로잡았다.** 나이스에는 학교 코드와
  조회 날짜를, 학교를 찾을 때는 입력한 학교 이름을 보낸다(학생 정보는 보내지 않는다).
  새 버전 확인 때 GitHub 도 부른다. 이용약관에는 이미 적혀 있던 것이 방침에만 빠져 있었다
- **홈페이지가 내보내기 형식을 넓게 적고 있었다.** 좌석배정과 학생카드는 표가 아니라
  CSV 로 나오지 않는데 다섯 종류 모두 되는 것처럼 읽혔다(앱은 원래부터 막고 있었다)
- **README 가 "학생 민감 정보가 암호화된다"고 했다.** 실제로 그렇게 저장되는 칸은
  주민번호 하나뿐이고 그것도 넣을 화면이 없어 늘 비어 있다. 전화번호·주소·보호자
  정보는 평문이다. 사실대로 고쳤다
- **README 라이선스를 실제에 맞췄다.** "사내 배포용, 외부 공개 시 협의" 라고 되어 있었지만
  이미 공개 저장소·공개 홈페이지로 배포 중이다. **개인과 학교에서 자유롭게 사용**으로 바꿨다

### 화면 표시 다듬기 (2026-08-26)

- **삭제 버튼에 설명을 붙였다.** 교과·동아리·단원·시간표 줄을 지우는 버튼이 휴지통 그림만
  있고 아무 설명이 없어, 마우스를 올려도 무엇인지 알 수 없고 화면 낭독기로도 읽히지 않았다.
  되돌릴 수 없는 동작인데도 그랬다. 뒤로가기·연도 이동 화살표에도 함께 붙였다
- **게시글 날짜가 목록과 상세에서 다른 형식으로 보이던 것을 고쳤다** (상세 화면만 컴퓨터
  기본 형식이라 "오후 2:30:00" 처럼 나왔다)
- 편집기를 바꾼 뒤에도 옛 이름(Jodit)을 가리키던 주석 넷을 사실대로 고쳤다

### 화면이 갱신되지 않거나 어긋나던 곳 (2026-08-26)

- **누가기록을 고쳐도 목록에 반영되지 않던 것을 고쳤다.** 편집 창에서 학년도·학기·날짜·영역·
  과목을 바꿔 저장해도 목록에는 고치기 전 값이 그대로 남았다. 목록이 값 변화를 듣지 않는
  방식으로 묶여 있어서, 저장 후 목록을 갱신하려고 준비해 둔 코드가 아무 일도 하지 못했다
- **게시판에서 [제목]·[내용]을 둘 다 끄면 검색어가 조용히 무시되던 것을 고쳤다.** 조건이
  하나도 없으면 검색 자체가 실행되지 않아 전체 목록이 그대로 나왔는데, 오류도 안내도 없어
  "검색이 안 된다"로만 보였다. 이제 범위를 고르라고 알려준다
- **학사일정 목록에서 헤더와 값이 어긋나던 것을 고쳤다.** 열 간격이 헤더에만 빠져 있어
  오른쪽 열로 갈수록 밀렸다
- **교과 단원 목록의 열이 헤더와 미세하게 어긋나던 것을 고쳤다** (맨 오른쪽 칸 너비가
  헤더는 고정, 행은 자동이라 가운데 칸 폭이 서로 달라졌다)
- **게시판 목록에서 마우스를 올린 행이 어두운 테마에서 사라지던 것을 고쳤다**

### 어두운 테마에서 보이지 않던 화면들 (2026-08-26)
글자색은 테마를 따라가는데 그 **배경만 밝은 색으로 고정**돼 있어, 어두운 테마로 쓰면
밝은 배경에 밝은 글씨가 겹쳐 내용이 통째로 사라지던 곳들을 고쳤다.

- **학급 시간표가 어두운 테마에서 아무것도 안 보이던 것을 고쳤다.** 셀 배경이 흰색으로
  박혀 있었고 과목명에는 색을 주지 않아, 흰 배경에 흰 글씨가 됐다. 다른 시간표 화면
  세 곳은 처음부터 테마 색을 쓰고 있었는데 이 화면만 셀을 코드로 그리면서 빠져 있었다
- **달력의 평일 날짜와 학사일정 이름이 안 보이던 것을 고쳤다.** 검정으로 박혀 있어서
  공휴일·토·일(제 색이 있다)만 남고 평일만 사라졌다. 오늘 칸 강조·마우스 올린 칸·
  할 일 개수 배지의 배경도 함께 테마 색으로 바꿨다
- **월 선택 창에서 고르지 않은 달이 안 보이던 것을 고쳤다.** 선택한 달만 강조색이라
  살아남고 나머지 열한 달이 배경에 묻혔다. 상단의 연·월 버튼도 흰 배경 고정이었다
- **일정 추가 창의 [할 일]/[일정] 선택 탭이 안 보이던 것을 고쳤다**
- **학생 정보 일괄 가져오기의 요약 안내가 안 보이던 것을 고쳤다** — "몇 명이 바뀐다"는
  핵심 문구가 밝은 배경에 묻혔다
- **학생부 입력칸을 비운 직후 바이트 안내가 안 보이던 것을 고쳤다.** 글자를 치면 제 색으로
  돌아왔는데, 비운 상태에서만 검정으로 박혀 있었다
- **초기 설정 창만 저장된 테마를 따르지 않던 것을 고쳤다** (창 여덟 중 하나만 빠져 있었다)
- 누가기록 목록의 열 숨김이 실제로는 열 너비를 0으로 만드는 것에만 기대고 있었다.
  숨김 지정 자체가 목록 항목 안에서는 해석되지 않는 방식이라 늘 무시됐다 — 학년도·학기
  두 열이 이미 쓰던 올바른 방식으로 나머지 여섯 열도 맞췄다
- 달력 헤더의 설정 버튼이 존재하지 않는 열 번호를 가리키고 있었다(마지막 열로 밀려나
  우연히 제자리에 보였을 뿐이라, 열을 하나만 추가해도 어긋난다)

### 같은 것을 두 곳이 다르게 판정하던 규칙 (2026-08-26)

- **7·8월과 1·2월에 누가기록의 학기가 뒤집히던 것을 고쳤다.** 학기는 3~8월이 1학기,
  9월~다음해 2월이 2학기인데 누가기록 입력 상자만 "6월까지가 1학기" 로 알고 있었다.
  그래서 **여름방학 무렵에 쓴 1학기 기록이 2학기로, 학년말에 쓴 2학기 기록이 1학기로**
  기본 선택됐다. 기록은 학기로 걸러 보여주므로 그렇게 저장된 기록은 제 학기 목록에서
  사라진다. 규칙을 한 곳(`DateTimeHelper.SemesterOf`)으로 모으고 열두 달을 테스트로 고정했다
- **학교급을 한 번 못 읽으면 영영 다시 읽지 않던 것을 고쳤다.** 학년 수(학사일정의
  "우리 학년만 빠지는 행사" 판정에 쓴다)를 조회하다 실패하면 그 실패까지 기억해 두는 바람에,
  학교를 바꾸기 전에는 다시 시도하지 않았다. 이제 성공했을 때만 기억한다
- **학급일지 출결 판정이 이름의 일부만 같아도 걸리던 것을 고쳤다.** 결석자 칸은 "김하늘, 박지민"
  처럼 이어 적은 글이라, 이름을 그대로 찾으면 **김하 학생이 김하늘 때문에 결석으로 잡혔다**.
  이름 단위로 갈라서 대조한다

### 겹쳐서 도는 작업 — 두 번 눌렀을 때 (2026-08-26)
오래 걸리는 일이 도는 동안 화면이 멀쩡히 살아 있어서 생기던 문제들을 고쳤다.
버튼을 또 누를 수 있고, 창을 여러 개 띄울 수 있고, 뒤에서 자동으로 도는 일과 겹칠 수 있었다.

- **구글 캘린더에 같은 일정이 두 개 생기던 것을 고쳤다.** 자동 동기화(기본 15분 주기)가 도는
  중에 설정에서 [지금 동기화] 를 누르면, 서로를 막지 못한 채 양쪽이 "아직 안 올린 일정"
  목록을 각각 읽어 각각 올렸다. 먼저 올라간 쪽은 앱과 연결이 끊겨 **구글 캘린더에서 손으로
  지워야 하는 일정**으로 남았다. 이제 겹치면 뒤엣것이 기다렸다가 안내를 낸다
- **학생부 [일괄 입력] 으로 넣은 내용이 덮어써지던 것을 고쳤다.** 일괄 입력 창은 곧바로
  저장하는데 뒤에 있는 목록 화면은 그것을 모르고 열기 전 내용을 그대로 들고 있었다.
  그 상태로 [저장] 을 누르면 방금 넣은 내용이 옛 내용으로 되돌아갔다. 이제 창을 닫으면
  목록을 다시 읽는다
- **알림장이 다른 날짜에 저장되던 것을 고쳤다.** 알림장 편집창은 떠 있어도 뒤의 일지 화면을
  막지 않는다. 편집하는 사이에 날짜나 학급을 바꾸고 [확인] 을 누르면 고친 알림장이 **엉뚱한
  날짜의 일지**에 들어가고 원래 있던 알림장은 사라졌다. 이제 대상이 바뀌었으면 적용하지 않고
  어디로 돌아가야 하는지 알려준다
- **자리 짜기 중에 [자리 짜기] 를 또 누를 수 있던 것을 고쳤다.** 흩뿌리기·정위치 애니메이션이
  30명 기준 6초쯤 걸리는데 그동안 버튼이 살아 있어서, 다시 누르면 두 배정이 같은 카드를
  번갈아 옮겨 제자리에 앉지 못했다. 학생들 앞에서 돌리는 시연에서 특히 눈에 띄었다
- **백업을 연달아 누르면 "백업 중 오류" 가 뜨던 것을 고쳤다.** 백업 파일 이름이 초 단위라
  같은 초에 두 번 시작되면 뒤엣것이 실패했다. 백업·복원 버튼을 작업 중에 잠그고, 그래도
  겹치면(시작할 때 도는 자동 백업과 만나는 경우) 이름 뒤에 번호를 붙인다
- **통합 내보내기 중에 [클립보드 복사] 를 누르면 진행 표시가 꺼지던 것을 고쳤다.** 복사가
  먼저 끝나면서 아직 돌고 있는 내보내기의 표시를 끄고 [내보내기] 버튼까지 도로 켰다
- **학사일정 화면에서 NEIS 동기화 중에 다른 버튼을 누를 수 있던 것을 고쳤다.** 여섯 버튼이
  진행 표시 하나를 함께 쓰면서 서로의 표시를 껐고, 동기화가 끝나며 목록을 다시 읽어
  그 사이의 삭제 결과와 섞였다
- **업데이트 확인 창이 안 닫힐 수 있던 것을 고쳤다.** 다른 알림창이 떠 있는 상태에서 업데이트를
  확인하면 "확인하고 있습니다..." 창이 뒤늦게 떠서 그대로 남았고, 그것이 결과 창까지 막았다

### 실패를 실패라고 말한다 — NEIS 오류와 엑셀 저장 (2026-08-25)
아무것도 없는 상태(갓 설치, 인증키 없음, 자료 없음)에서 화면이 하던 말을 사실대로 고쳤다.

- **학교 검색이 "검색 결과가 없습니다" 로 둘러대던 것을 고쳤다.** NEIS 는 인증키 오류·호출 한도
  초과·서비스 점검도 정상 응답에 코드로 실어 보내는데, 초기 설정의 학교 검색은 그 코드를
  읽고도 버린 뒤 **결과 0건**으로 보여줬다. 학교를 못 고르면 앱을 아예 쓸 수 없는 첫 관문이라
  사용자는 학교 이름을 잘못 쳤다고 믿고 계속 헤맸다. 이제 NEIS 가 준 사유를 그대로 보여준다
- **급식이 인증키 오류를 "오늘 급식 없음" 으로 삼키던 것을 고쳤다.** 오류 판정이 급식·학사일정·
  학교검색 세 곳에 제각각이었고, 그중 급식만 `INFO` 로 시작하는 코드를 전부 정상으로 넘겨
  **인증키 오류(INFO-300)를 통과**시켰다. 판정 규칙을 한 벌로 모았다
- **인증키가 없을 때 "설정에서 입력해주세요" 라고 하던 안내를 고쳤다.** 설정 화면에 인증키
  입력칸은 없다 — 키는 프로그램에 내장된다. 사용자가 할 수 있는 일을 말하도록 바꿨다
- **[모든 설정 초기화] 안내에서 "NEIS 인증키" 를 뺐다.** 실제로는 초기화 직후 내장값으로
  다시 채워져 지워지지 않는다
- **엑셀 내보내기가 실패해도 아무 말이 없던 것을 고쳤다.** [학생 목록 내보내기] 는 실패를
  조용히 삼켰고, [학생 정보 엑셀 저장] 은 "취소되었거나 실패했습니다" 로 뭉뚱그렸다.
  이제 취소는 알리지 않고 실패만 로그 안내와 함께 알린다. 중간에 만든 임시 파일도
  실패했을 때 남지 않는다

### 앱 설정 — 말만 하고 아무 일도 안 하던 것들을 정리 (2026-08-25)
설정 화면에 있지만 저장만 되고 **읽는 곳이 한 군데도 없던** 항목들을 찾아 정리했다.

- **테마가 이제 유지된다.** 다크로 바꿔도 앱을 껐다 켜면 라이트로 돌아왔다 — 고른 값을 시작할 때
  다시 읽는 곳이 없었다. 이제 다음 실행에도, **편집기·수업 일지·학생부 일괄 같은 보조 창에도**
  같은 테마가 걸린다(예전에는 메인 창에만 걸려 창마다 색이 달랐다)
- **[언어] 를 없앴다.** English 를 고르면 "다시 시작한 후 적용됩니다" 라고 했지만, 앱에 번역 자료가
  아예 없어 재시작해도 영원히 한국어였다
- **[캐시 사용]·[페이지당 항목 수] 를 없앴다.** 캐시는 꺼도 그대로 돌았고, 페이지당 항목 수는
  목록이 따르지 않았다. 게시판 목록의 개수는 목록 화면 아래 콤보로 바꾼다
- **[설정 초기화] 가 무엇까지 지우는지 미리 알린다.** 학교 정보·NEIS 인증키·구글 계정 연결·
  시정표·담임 학급까지 함께 지워져 다시 시작하면 초기 설정을 처음부터 다시 해야 한다는 사실을
  누르기 전에 말한다(학생·수업·게시글 등 저장된 데이터는 그대로다)

### 백업 — 무엇이 담기고 무엇이 안 담기는지 (2026-08-25)
- **백업 ZIP 에는 데이터베이스만 담긴다**는 안내를 설정 화면에 넣었다. 학생 사진과 게시판·수업 일지
  첨부는 용량 때문에 빠지므로, 그것까지 지키려면 [데이터 폴더 열기] 로 열리는 폴더를 통째로
  복사해 두어야 한다. 예전에는 이 사실이 도움말에만 있어 복원한 뒤에야 알게 됐다
- **[지금 백업]·[복원] 중에 창이 얼지 않는다.** 두 작업 모두 백그라운드로 옮겼다
- **복원이 중간에 실패하면 원래대로 되돌린다.** 예전에는 DB 를 하나씩 덮어쓰다 실패하면
  "복원 실패" 라고만 알려서, 학생 데이터는 백업 시점이고 게시판은 현재 시점인 상태로 남는데도
  사용자는 아무것도 안 바뀐 줄 알았다. 설정(Settings.db) 복원 실패도 이제 결과에 반영된다

### 게시판 — 카테고리를 옮기면 첨부가 남의 파일로 바뀔 수 있었다 (2026-08-25)
첨부 실물은 카테고리 폴더에 살고, 첨부를 찾을 때는 **글의 현재 카테고리**로 경로를 만든다.
그런데 글을 다른 카테고리로 옮길 때 대상 폴더에 같은 이름의 파일이 이미 있으면 **아무 말 없이
건너뛰었다**. 그러면 그 글의 첨부를 열 때 그 자리에 있던 남의 파일이 열리고, 첨부를 지우면
남의 파일이 지워졌다.

- 이제 새 첨부를 붙일 때와 같은 규칙으로 처리한다 — 이름이 겹치면 `이름 (2).확장자` 로 옮기고
  기록된 이름도 함께 바꾼다
- 그래도 못 옮긴 첨부가 있으면 무엇이 남았는지 알린다

### 게시판 — 댓글·첨부를 더해도 목록 표시가 그대로이던 문제 (2026-08-25)
글에 댓글을 달거나 첨부를 붙인 뒤 목록으로 돌아오면 💬·📎 표시가 **최대 2분간 안 붙었다**.
목록 캐시를 비우지 않아서다. 이제 바로 반영된다.

### 좌석 — "회차"가 자리바꿈 횟수를 뜻하게 됐다 (2026-08-25)
좌석 회차는 저장 버튼을 누른 횟수로 세고 있었다. 자리를 한 번 짜면서 안내 문구를 고치고
고정 좌석을 잡느라 여러 번 저장하면, **"최근 3회차의 짝 회피"가 오늘 만든 같은 배치 세 벌로
차버려** 지난달 짝을 전혀 회피하지 못했다.

- 직전 회차와 자리·짝이 모두 같으면 회차를 올리지 않는다
- 옵션 창의 "누적된 배치 회차" 안내도 이제 실제 자리바꿈 횟수를 보여 준다

### 학생 등록 — 명단 템플릿이 받아지지 않던 문제 (2026-08-25)
갓 설치한 PC 에서 [템플릿 다운로드] 를 누르면 **아무것도 받아지지 않고 "취소되었습니다" 라고만**
나왔다. 템플릿을 만들 폴더가 아직 없어 실패한 것인데 그 실패가 취소로 둔갑했다.
같은 날 두 번째로 받을 때도 같은 증상이 있었다.

- 폴더가 없으면 만들고, 받을 때마다 새 임시 파일을 쓴다
- 취소와 오류를 갈라 말한다 — 오류일 때는 로그를 볼 수 있는 곳을 함께 안내한다

### 내보내기 — 이름·제목 때문에 저장이 막히던 문제 (2026-08-25)
- 누가기록을 학생별로 내보내거나 인쇄할 때 파일명에 학생 이름이 그대로 들어가, 이름에
  파일명으로 쓸 수 없는 문자가 있으면 저장이 실패했다. 학생카드에는 이미 있던 정리를 맞췄다
- 학생정보 엑셀 내보내기의 제목 입력칸도 마찬가지로 정리한다("국어/문학 명렬표" 등)
- 단원·진도 CSV 내보내기가 쓰던 인용 규칙이 나머지와 달라, 값 안의 캐리지리턴과
  `=`·`+`·`-`·`@` 로 시작하는 값(엑셀이 수식으로 읽는다) 처리가 빠져 있었다 — 한 벌로 맞췄다

### 학생 정보 — 자동 저장이 실패해도 아무 말이 없었다 (2026-08-25)
학생 카드는 입력이 멎고 3초 뒤 자동으로 저장합니다. **앱을 그냥 닫아도 잃지 않게** 하려고 넣은
장치인데, 정작 그 저장이 실패하면 개발자용 로그에만 남고 사용자에게는 아무 말도 하지 않았습니다.
저장된 줄 알고 앱을 닫으면 결국 잃습니다.

- 자동 저장이 실패하면 알리고, 고친 내용은 화면에 그대로 두어 [저장] 으로 다시 시도할 수 있게 합니다
- 같은 실패로 대화상자가 반복해 뜨지는 않습니다 (학급일지와 같은 처리)

### 오늘 화면 — 같은 말을 두 번 하던 변경 표시 (2026-08-24)
휴강·교체·보강·대강이 걸린 교시는 과목명 앞에 **(휴)·(교)·(보)·(대)** 표식이 붙는데, 그 뒤에 다시
**[보강]** 같은 배지가 따라붙어 `(보)영어 1-1 [보강]` 처럼 두 번 읽혔다.

- 배지를 없앴다. 구분은 과목명 앞 표식으로 그대로 알 수 있다
- 배지에 달려 있던 **변경 사유 메모**는 그 줄 전체의 툴팁으로 옮겼다 — 줄에 마우스를 올리면 보인다

### 수업 홈 — 시간표가 왜 비었는지 알려 준다 (2026-08-24)
[수업 시간표 입력] 탭에서 수업을 한 칸도 배치하지 않으면 내 시간표는 **빈 격자**만 남아서, 아직 안
넣은 것인지 못 읽어 온 것인지 구분되지 않았다.

- 비어 있으면 격자 대신 **어디서 넣는지 짚어 주는 안내**를 보여준다

### 학생 정보를 고치다 앱을 닫으면 사라지던 문제 (2026-08-24)
학생 카드의 편집 내용은 **다른 학생을 고르거나 화면을 떠날 때** 저장됐다. 그래서 고쳐 놓고 그 화면에
그대로 둔 채 앱을 닫으면 저장이 일어나지 않고 **그대로 사라졌다**.

- 이제 **입력이 멎고 3초 뒤에 자동 저장**한다. 학급일지 알림장이 쓰던 방식과 같다
- 창을 닫을 때 저장하는 방식은 쓰지 않는다 — 저장은 시간이 걸리는 작업이라 프로그램이 끝나는
  속도를 못 따라가서, 결국 같은 자리에서 또 사라진다
- 기존의 저장 시점(다른 학생 선택·화면 이탈·창 닫기)은 그대로 남아 있다

### 수업 홈이 못 불러온 것을 "없음"처럼 보여주던 문제 (2026-08-24)
수업 홈의 과목·내 시간표·수업 할일은 불러오기에 실패해도 **아무 표시 없이 빈 화면**이었다. 정말
비어 있는 것과 구분할 방법이 없었다.

- 이제 못 불러온 항목의 이름을 모아 화면 위쪽에 안내한다(오늘 화면과 같은 방식)
- 카드 하나가 실패해도 나머지는 그대로 보여준다

### 게시판 중요 글 — 항상 맨 앞으로 (2026-08-24)
글을 쓰거나 고칠 때 **중요 글**에 체크하면 그 글이 목록에서 항상 맨 앞에 온다. 목록에서는 제목
앞에 압정 표시가 붙는다.

- **지금 보고 있는 목록 안에서만** 앞으로 온다 — 카테고리·주제 필터나 검색을 걸면 그 결과 안에서
  위로 올라가고, 다른 카테고리 목록까지 밀고 들어오지 않는다
- 정렬 기준(최신순·오래된순·제목순·조회순)을 무엇으로 바꿔도 중요 글이 먼저 온다. 중요 글이
  여럿이면 그 안에서는 고른 정렬 기준을 따른다
- 여러 쪽으로 나뉜 목록에서도 중요 글은 첫 쪽에 모인다

### 큰 화면 정독에서 나온 결함 다섯 (2026-08-24)
그동안 패턴 검사만 거치고 통독하지 않았던 큰 화면들(좌석·학생 정보·학생 추가·주간 시간표·
진도표·좌석 출력·명렬표)을 한 줄씩 읽었다.

- **좌석 — 미사용 자리에 앉힌 학생이 두 자리를 차지했다.** 미사용으로 표시한 자리에도 클릭·
  드래그로 학생을 놓을 수 있었는데, 그 경우에만 "같은 학생이 다른 자리에 있으면 지우는" 처리가
  돌지 않았다. 원래 자리와 미사용 자리에 같은 학생이 남고 저장까지 됐으며, 다시 불러올 때도
  같은 순서를 밟아 계속 되살아났다. 이제 미사용 자리에는 놓이지 않고 안내가 뜬다
- **학생 정보 — 초기화가 실패해도 "초기화되었습니다"라고 알렸다.** 화면을 비우는 것과 DB에
  쓰는 것이 다른 단계인데 저장 결과를 보지 않았다. 실패하면 이유를 알리고, 목록도 새로고침하지
  않는다(사진 파일은 이미 지워진 뒤라 이 사실도 함께 안내한다)
- **학생 정보 — 번호가 겹치는 학급에서 일괄 입력이 통째로 죽었다.** 번호는 학급 안에서 유일하지
  않은데(전입생 번호를 잘못 넣는 등) 번호를 열쇠로 학생을 찾다가 내부 오류로 끝났다. 이제 겹치는
  번호를 먼저 알려 주고 멈춘다
- **학생 정보 — 엑셀에서 날짜 칸으로 넣은 생년월일이 조용히 사라졌다.** 날짜 칸은 "2010-05-03
  오전 12:00:00" 같은 형태로 읽히는데 날짜만 있는 형식으로만 맞춰 보고 어긋나면 그냥 넘겼다.
  미리보기에는 "생년월일 변경"으로 세어 두고 저장은 "완료"라고 알리면서 값만 빠졌다
- **주간 시간표 — 되돌리기가 화면에서만 이루어졌다.** 삭제 결과를 보지 않아 지우지 못했는데도
  칸은 평소 수업으로 돌아갔고, 다시 열면 변경이 되살아났다. 바로 옆 저장 경로와 기준을 맞췄다

### 한 번도 쓰지 않은 코드 전면 정리 (2026-08-24)
앱 어디에서도 부르지 않는 공개 메서드·속성을 기계로 훑어 **약 170개**를 지웠다. 지우면 그것만
쓰던 코드가 다시 죽으므로, 새로 죽은 것이 없어질 때까지 반복했다. 합쳐서 **3,500줄 남짓**이
줄었고 동작에는 변화가 없다.

- **학교 관리**: 이 앱은 학교 한 곳만 다루는데 여러 학교를 전제한 등록·수정·폐교·검색·통계가
  통째로 남아 있었다. 초기 설정이 쓰는 저장·조회 셋만 남겼다
- **학적·학생 상세**: 항목별 부분 저장(부모·보호자·가족·교우·진로·건강), 물리 삭제, 완성도 체크
- **수업·시간표**: 쓰이지 않는 시간표 생성·충돌 확인 경로, 교사 시간표 조회 한 벌
- **일정·할 일**: 캘린더 생성/삭제, 캘린더별 조회
- **게시판**: 어느 화면에서도 묶지 않은 명령(Command) 계층 여덟 개와 그 구현체, 캐시 통계
- **내보내기**: 개인 단위 HTML·PDF 생성(학급 단위만 쓴다), 여러 시트 저장, CSV→Excel 변환
- **모델 편의 메서드**: 재학 기간, 상태 확인, 학교 코드 검증, 근무 연수 등
- **표시용 속성 32개**: 어느 화면도 묶지 않은 단원·학생·시간표 표시 문자열, 구글 응답에서
  받기만 하고 읽지 않던 항목 일곱, 컨트롤이 밖으로 열어 두었지만 아무도 안 보던 상태값
- 쓰이지 않던 출결 모델(`Attendance`)과 파일 크기 포맷터(`Tool`) 두 클래스는 통째로 삭제

지운 자리마다 "왜 지웠는지, 대신 무엇을 쓰는지"를 주석으로 남겨 두었다.

### 한 번도 쓰지 않은 DB 유지보수 코드 정리 (2026-08-24)
게시판·달력 DB 에 각각 붙어 있던 검증·최적화·백업·복원·초기화 다섯 벌(합 10개)은 앱 어디에서도
부르지 않는 코드였다. school.db 쪽 같은 메서드들은 2026-08-16 에 이미 지웠고, 사용자에게 보이는
백업·복원은 설정의 백업 ZIP 한 경로뿐이다. 딸려 있던 헬퍼(검증기·최적화기, WAL 체크포인트,
매개변수 없는 질의 실행기)까지 함께 걷어내 약 390줄이 줄었다. 동작에는 변화가 없다.

### 구글 일정 수정이 참석자·알림을 지우던 문제 (2026-08-24)
앱에서 일정을 고쳐 구글로 올릴 때 **이벤트를 통째로 대체**(PUT)하고 있었다. 올리는 본문에는
앱이 다루는 항목(제목·내용·장소·시간·색·반복)만 담기므로, 구글 웹에서 넣어 둔 **참석자·알림·
Meet 링크·첨부·공개 범위**가 그 순간 전부 사라졌다. 제목만 고쳐도 마찬가지였다.

- 수정은 **부분 갱신**(PATCH)으로 보낸다 — 보내지 않은 항목은 구글에 그대로 남는다
- 대신 앱이 값을 가진 **내용·장소**는 비었을 때 빈 값을 실어 보내, 앱에서 지운 내용이
  구글에도 반영되게 했다. 색만은 빈 값이 유효하지 않아 앱에서 지워도 구글 색은 유지된다
- 방금 구글에서 받아온 일정을 같은 회차에 다시 구글로 되돌려 보내던 것도 멈췄다
  (내용이 같은 되쓰기라 호출만 늘고 구글 쪽 수정 시각이 계속 갱신됐다)

### 학사일정 중복 판정이 첫 동기화에 굳던 문제 (2026-08-24)
구글에서 받은 종일 일정이 이미 있는 학사일정과 겹치는지 보는 판단이 **앱을 켠 뒤 첫 동기화
때 읽은 내용에 그대로 묶여** 있었다. 자동 동기화를 켜 두면 그 뒤에 추가한 학사일정은 중복으로
걸러지지 않았고, 조회가 한 번 실패한 날짜는 그 뒤로 영영 판정되지 않았다. 동기화 회차마다
새로 읽는다.

### 할 일 목록이 반복·시리즈 삭제를 따라가지 못하던 문제 (2026-08-24)
학교 업무·오늘 화면의 할 일/일정 목록에서

- **반복 할 일을 만들면 첫 건만 보였다.** 저장은 전부 됐지만 목록에는 대표 1건만 얹혔다
- **"이후 반복 항목 모두 삭제"를 하면 누른 줄만 사라졌다.** 남은 줄을 다시 누르면 이미 없는
  항목을 편집하는 셈이었다

목록에 손으로 끼워 넣는 대신, 저장·삭제 뒤에는 목록을 다시 읽도록 고쳤다.

### 메모 목록을 조금 조밀하게 (2026-08-24)
메모 카드의 한 줄 목록에서 위아래 여백을 8 → 4 로 줄였다. 줄 최소 높이(40)는 그대로라
한 줄의 크기는 유지되고, 여백만 줄어 같은 높이에 메모가 더 들어간다.

### 학생 정보 화면을 카드 셋으로 정리 (2026-08-23)
왼쪽 학생 목록은 이미 카드 면(배경·테두리·둥근 모서리)을 갖고 있는데 가운데 **학생카드만
면이 없어** 배경에 그대로 얹혀 있었다. 아래 누가기록은 카드이긴 한데 색을 직접 적어
(`CornflowerBlue`·`AliceBlue`·`#E3F2FD`) 어두운 테마에서 깨졌다.

- 학생카드에 앱 공용 카드 스타일(`DashboardCard`)을 입혔다 — 대시보드·오늘 화면이 쓰는 그것이다
- 누가기록도 같은 스타일로 바꾸고 머리 줄은 `DashboardCardHeader`(강조색 12% 틴트)로 통일했다.
  손으로 적은 세 색이 사라져 밝게/어둡게를 따라간다
- 페이지 배경이 지정돼 있지 않아 카드가 배경과 구분되지 않았다. 다른 페이지와 같은
  `ApplicationPageBackgroundThemeBrush` 를 지정했다

### 좁은 창·긴 내용에서 잘리던 화면 셋 (2026-08-23)
- **학생 정보 — 상세 정보를 펼치면 아래가 잘렸다.** 학생카드는 스스로 스크롤을 갖고 있는데도
  놓인 자리가 `Height="Auto"` 행이라 재는 동안 높이를 **무한**으로 받았다. 그러면 스크롤은
  "내용만큼 크면 된다"고 판단해 걸리지 않고, 카드가 화면 밖까지 자란다. 남은 공간에 맞춰
  높이 상한을 주도록 고쳤다 — 넘치면 카드 안에서 스크롤되고, 접혀 있을 때는 예전처럼
  필요한 만큼만 쓴다. 두 칸이 나눠 갖는 몫은 **비율**로 정한다(누가기록 4할, 최소 220px):
  고정값으로 남기면 창을 키워도 아래쪽 누가기록은 그대로라 몇 줄밖에 보이지 않았다.
  누가기록 목록 자체는 원래부터 스크롤을 갖고 있어 자리만 확보되면 된다
- **학생 정보 출력 — 학생 전화와 보호자 전화를 함께 고르면 출력이 실패했다.** 두 항목의
  이름이 똑같이 "전화" 라 같은 이름의 열을 두 번 만들려다 터진 것이다. 보호자 쪽을
  **"보호자 전화"** 로 바꿨고, 코드도 *"이름이 겹칠 리 없다"* 는 가정을 버리고 겹치면
  뒤엣것에 번호를 붙여 **둘 다** 만들도록 했다(항목을 건너뛰면 값이 엉뚱한 칸에 들어간다)
- **학생 정보 출력 — 창이 좁으면 뒤쪽 출력항목이 보이지 않았다.** 항목이 한 줄에 가로로만
  놓여 있어 잘렸다. 줄바꿈되는 배치로 바꿔 어떤 너비에서도 전부 보인다. 한 줄에 섞여 있던
  '건강' 은 자기 묶음으로 분리했다. 사용자 정의 항목 칸도 같이 줄바꿈된다

### 할 일·일정 카드에서 지연과 오늘을 찾을 수 있게 (2026-08-23)
이 목록은 **지난 미완료 할 일부터 두 달 뒤 일정까지 한 줄로** 이어진다. 그런데 날짜 열이
모두 같은 회색이라, 3일 지난 할 일과 40일 뒤 일정의 무게가 똑같아 보였다.

- **기한이 지난 할 일**은 날짜를 경고색·굵게 쓰고 줄 배경에 옅은 경고 틴트를 깐다.
  목록에서 가장 급한 항목인데 그동안 가장 눈에 안 띄었다. 그 자리에서 완료로 바꾸면
  경고 표시도 바로 풀린다
- **오늘** 항목은 날짜를 강조색·굵게 쓰고 옅은 강조 틴트를 깐다. 오늘 시간표의
  '지금' 교시 강조와 같은 규칙이다
- 지연과 오늘은 날짜가 겹치지 않고, 지난 날짜로 남는 것은 미완료 할 일뿐이다
  (일정은 오늘부터 담는다) — 지난 일정이 경고색으로 뜨는 일은 없다
- 배경만 쓰지 않은 이유: 줄 왼쪽에 이미 분류 배지라는 짙은 색이 있어 배경까지 칠하면
  "이 항목의 색" 으로 오해되기 쉽고, 목록이 선택·마우스오버도 배경으로 표시하는 ListView 라
  진한 배경은 그 표시를 덮는다. 그래서 글자를 주 신호로 두고 배경은 반투명으로 거들게 했다
- 곁들여 날짜·시간 열의 고정 회색(`#777`)을 테마 브러시로 바꿨다 — 어두운 테마에서 묻히던 색이다

### 학적 INSERT 두 벌을 한 벌로 (2026-08-23)
학생을 추가하면 **Student 행과 Enrollment 행이 함께** 만들어져야 한다. 그런데 학생 추가 화면은
학적 INSERT 를 리포지토리에 맡기지 않고 **손수 SQL 로 적어** 갖고 있었다. 두 리포지토리가
각자 연결을 열면 트랜잭션이 공유되지 않아 한쪽만 저장될 수 있어서, 연결을 공유하려고 그렇게
우회한 것이다 — 목적은 맞았지만 대가로 22개 컬럼짜리 INSERT 문이 두 벌이 됐다.

- 학적 리포지토리를 **학생 리포지토리와 같은 연결로** 만들어 쓰도록 바꾸고, 손수 적은
  INSERT(65줄)를 지웠다. 바로 위 항목에서 교사 등록을 고친 방식과 같다
- 두 INSERT 는 컬럼·값이 글자까지 같은 복사본이었고, 담임이 비었을 때 NULL 로 넣는
  외래키 가드도 리포지토리 쪽에 이미 똑같이 들어 있어 동작은 그대로다
- 회귀 테스트 2건 — 함께 저장되는지, 학적이 실패하면 학생도 롤백되는지.
  연결을 나눠 쓰도록 되돌리면 첫 번째가 실패하는 것을 확인했다

### 쓸 수 없던 진급 기능을 걷어냈다 (2026-08-23)
`EnrollmentService.PromoteStudentsAsync`(학년 전체 진급)는 **화면에서 부를 방법이 없었다**.
호출처가 회귀 테스트뿐이라 한 번도 실사용된 적이 없고, 그 사이 결함이 하나 자라 있었다 —
테스트가 현실에 없는 데이터(2학기 학적)를 심어 통과시키는 바람에 실제로는 늘 0명이 진급하는
상태였다(7월에 발견해 고쳤다). 코드 주석 스스로도 "UI 미노출" 과 "실사용 전에 반·번호 재배정
화면이 필요하다" 를 적어 두고 있었다.

- 진급 처리와 그 회귀 테스트 4건을 지웠다. 학년도가 바뀔 때 학적을 잇는 기능이 필요해지면
  그때 만든다 — **되살릴 때는 반·번호를 어떻게 재배정할지부터 정해야 한다**(옛 코드는 이전
  반·번호를 그대로 복사했고, 그래서 화면에 못 내놓고 있었다)
- 학적 상태값 `EnrollmentStatus.Graduated` 와 `GraduationDate` 컬럼은 과거 데이터 호환을
  위해 그대로 둔다
- `EnrollmentService` 는 이제 이름 그대로 **학적 조회**만 한다. 학적을 만드는 곳은
  `AddStudentsPage`, 그 해 학적을 고치는 곳은 `StudentManagementPage` 다

### 교사 쪽 죽은 코드 정리 — 1,600줄 (2026-08-23)
바로 아래 38차 검사에서 드러난 것들을 한 번에 걷어냈다. 교사를 다루는 코드는 화면 하나 없이
서비스·리포지토리만 두껍게 자라 있었고, 그중 손대지 않은 부분이 **실제로 썩어 있었다**
(연결을 공유하지 않아 부르면 락 대기 끝에 실패하는 메서드가 둘).

- **`Dialogs/InitialSetupDialog.xaml(.cs)` 삭제** (563줄) — 어디서도 열지 않는 화면이었다.
  초기 설정은 `InitialSetupWindow` 가 한다. `.csproj` 항목과 문서 표에서도 지웠다
- **`TeacherService` 12개 → 1개** — 남긴 것은 첫 실행이 쓰는 `RegisterTeacherAsync` 뿐이다.
  전보·퇴직·상태 변경·조회·수정과 헬퍼 클래스 둘을 지웠다
- **`TeacherRepository` 12개 → 3개**, **`TeacherSchoolHistoryRepository` 10개 → 2개** —
  위 서비스가 사라지며 함께 죽은 것들. 남은 것은 실제로 쓰는 등록·조회뿐이다
- `SchoolService.GetTeacherCountAsync` 도 호출처가 없어 함께 정리했다

지운 것은 git 에 그대로 남아 있으니, 나중에 교사 관리 화면을 만들 때 되살리면 된다.
다만 **그때는 되살린 코드가 도는지 반드시 확인해야 한다** — 이번에 확인해 보니 안 돌았다.

빌드 경고 0, 테스트 471개 통과(변동 없음).

### 첫 실행 교사 등록이 반쪽으로 남을 수 있었다 (2026-08-23)
전수 검사 38차. 그동안 한 번도 정독하지 않고 남겨 둔 자리
(`SpecListViewer` · `TeacherRepository`/`TeacherService` · 인쇄 조판 본문)를 마저 읽었다.

- **트랜잭션이 걸린 척만 하고 있었다** — 교사 등록·전보·퇴직은 교사 행과 근무 이력을 함께
  건드리는데, 두 Repository 가 **각자 연결을 열고** 한쪽 트랜잭션을 다른 쪽에 넘겨 주는
  모양이었다. `BaseRepository` 는 "이 연결에서 시작된 트랜잭션인가" 를 확인하고 아니면
  **조용히 무시**하므로, 이력 쓰기는 트랜잭션 밖 다른 연결에서 돌았다. 같은 파일을 두 연결이
  동시에 쓰려다 락 대기에 걸려 **5초쯤 멈춘 뒤 "database is locked" 로 실패**한다
  (회귀 테스트로 재현 확인: 고치기 전에는 34초 만에 실패)
- **첫 실행 초기 설정은 아예 그 안전한 경로를 쓰지 않고 있었다** — 교사와 근무 이력을 따로
  저장해서, 이력 저장이 실패하면 **근무 이력 없는 교사 행만 DB 에 남았다**. 재시도하면
  TeacherID 를 시각+난수로 새로 만들기 때문에 그 고아 행은 영영 지워지지 않는다.
  이제 한 트랜잭션으로 함께 저장한다(연결을 공유하도록 고친 그 경로다)
- **학생부 목록에서 손대지 않은 기록이 저장 대상이 되던 것** — 내용 칸의 `TextChanged` 가
  무조건 그 행을 '선택됨' 으로 켰다. 목록은 행을 재활용하므로 **스크롤만 해도**(처음 그릴 때도)
  바인딩이 값을 다시 넣으며 이 이벤트가 떠서, 고치지 않은 기록까지 체크됐다. 실제로 내용이
  바뀐 경우는 원래부터 ViewModel 이 스스로 표시하므로 그 한 줄만 걷어냈다

**이상 없음 확인**: 좌석표 인쇄 3경로(`jul`·`jjak` 보정이 세 곳 모두 적용됨, 짝 이력은
`jjak < 2` 에서 미리 빠져나가 0 나눗셈 없음), 학생카드 PDF·엑셀·명렬표(파일명은 숫자뿐이라
경로 안전), `TeacherRepository` 매핑·쿼리.

**남은 죽은 코드(미제거, 별도 정리 대상)**: `Dialogs/InitialSetupDialog.xaml(.cs)` 은 어디서도
열지 않는다(초기 설정은 `InitialSetupWindow` 가 한다). `TeacherService` 12개 중 10개,
`TeacherRepository.GetByIdsAsync` 도 호출처가 없다.

테스트 469 → 471개 통과, 빌드 경고 0.

### 안내 창이 엉뚱한 창에 뜨던 문제 (2026-08-23)
전수 검사 37차. 대화상자가 **어느 창 위에 뜰지**를 앱이 딱 한 번 정해 놓고 계속 쓰고 있었다.

- **보조 창의 안내가 메인 창 뒤에서 열렸다** — `ContentDialog` 는 `XamlRoot` 가 가리키는 창의
  안쪽에 그려진다. 그런데 `MessageBox` 는 XamlRoot 를 앱 전역에 하나만 두고 사실상 메인 창으로
  고정해, 수업 일지·메모·누가기록·학생부 일괄·학급일지 목록 창에서 부른 안내가 **메인 창 위에**
  떴다. 앞에 있는 보조 창('항상 위에'면 더욱)에 가려 사용자에게는 저장을 눌러도 아무 반응이
  없는 것처럼 보였고, 그동안 대화상자 게이트가 잡혀 뒤따르는 안내까지 줄줄이 멈췄다
- **초기 설정 창의 오류는 아예 뜨지 않았다** — 그 시점에는 메인 창이 아직 없어서 띄울 창을
  찾는 데 반드시 실패했고, 폴백이 Debug 출력만 남기고 조용히 "확인" 을 돌려줬다. 그래서
  **학교 검색 실패와 초기 설정 저장 실패가 첫 실행 사용자에게 한 번도 보이지 않았다**
- 고친 방식: 창이 자기를 `MessageBox.TrackWindow` 로 등록하고, 대화상자를 띄울 때마다
  **지금 활성인 창**을 새로 찾는다(닫힌 창은 자동으로 놓고 메인 창으로 돌아간다).
  캐시해 두던 XamlRoot 와, 메인 창만 돌려주던 죽은 창 목록 헬퍼는 지웠다

곁들여 같은 검사에서 나온 것들:

- **수업 일지 제목에 강의실이 반영되지 않던 경우** — 강의실 칸은 직접 적을 수 있는데
  목록 선택만 듣고 있어서, 목록에 없는 강의실을 타이핑하면 제목은 옛 강의실 그대로 저장됐다.
  시간표 칸이 알려 준 강의실도 교과의 첫 강의실에 덮여 **화면에 보이는 강의실과 제목의
  강의실이 갈릴** 수 있었다. 제목은 이제 칸에 보이는 값을 따른다
- **첨부 반영 실패가 무음이었다** — 첨부 삭제·등록 결과를 보지 않고 넘어가, 지운 줄 알았는데
  남아 있거나 붙인 줄 알았는데 없는 첨부가 생길 수 있었다. 반영하지 못한 파일을 이름과 함께 알린다
- **로드 실패 문구가 영구히 남던 것** — 최근 수업 일지 카드와 오늘의 수업은 실패할 때 안내
  문구를 갈아 끼우고 되돌리지 않아서, 나중에 정말 0건인 날에도 "불러올 수 없습니다" 가 남았다

테스트 469개 통과(강의실 왕복 1개 추가), 빌드 경고 0.

### 수업 일지를 게시판 한 곳으로 모았다 (2026-08-21)
"수업 일지" 라는 이름의 화면이 **서로 다른 DB 에 둘** 있었다. 메뉴의 수업 일지는 게시판
(`board.db` 의 `Post`)이고, 수업 홈의 카드는 `school.db` 의 `LessonLog` 테이블이었다.
8/16 에 게시판 쪽을 만들면서 옛 경로를 내리지 않았고, 같은 날 먼저 들어간 "이름 통일" 항목이
*"수업 홈의 카드는 메뉴의 수업 일지와 같은 것"* 이라고 적어 둔 탓에 하나로 합쳐진 것처럼
보였다(그 문장은 몇 시간 뒤 메뉴가 게시판으로 바뀌면서 사실이 아니게 됐다). 이름만 같고
데이터는 오가지 않아서, 게시판에 쓴 일지가 수업 홈에 보이지 않고 반대도 마찬가지였다.

- **옛 `LessonLog` 경로 제거** — 모델·리포지토리·서비스·목록 컨트롤·편집 다이얼로그를 통째로
  지웠다. 스키마 세대를 2 로 올려 테이블도 버린다(3월에 나흘 쓰고 멈춘 8건이 전부였다).
  `DatabaseInitializer` 에 세대 변환 자리(`MigrateSchemaAsync`)를 만들어 두었다 — 지금까지는
  버전을 찍기만 하고 변환을 도는 곳이 없었다
- **수업 홈의 "최근 수업 일지" 카드가 게시판 글을 읽는다** — 새 컨트롤 `LessonJournalList`.
  카드는 프레임을 모르므로 이동은 이벤트로 올려 보내고 페이지가 처리한다
- **수업 일지를 한 창에서 끝낸다** — 머리 정보(날짜·교시·교과·강의실·단원)와 제목·본문·첨부를
  한 창(`LessonJournalWindow`)에 놓고 저장까지 거기서 한다. 처음에는 머리 정보만 받는
  다이얼로그를 띄우고 게시판 편집기 **페이지로 넘어가게** 만들었는데, 한 줄 적자고 화면을
  갈아 끼우는 구조였다. 형제인 메모 편집 창(`MemoEditDialog`)이 이미 같은 모양이라 그것을 따랐다
    - 제목은 머리 정보를 따라 자동으로 만들어지고, **직접 고치는 순간 따라다니기를 멈춘다**.
      고쳐 둔 일지를 다시 열면 제목에서 머리 정보를 되읽어 채운다 — 그 제목이 머리 정보로
      그대로 되만들어질 때만 자동 추적을 다시 켠다
    - 단원은 본문이 비어 있을 때만 첫 줄로 들어간다. 이미 쓴 본문에 끼워 넣으면 커서 위치에
      따라 엉뚱한 자리에 들어가므로 손대지 않고, 그 사실을 창 아래에 적어 준다
    - 작성일시는 **새 글일 때만** 찍는다. 고칠 때마다 밀면 '언제 쓴 일지'인지가 사라진다
- **시간표 칸과 오늘의 수업이 수업 일지로 가는 문이 됐다** — 수업 홈의 내 시간표 한 칸,
  수업 홈의 오늘의 수업 한 줄, 오늘 화면의 내 수업 한 줄, 그리고 수업 일지 게시판의 새 글
  버튼과 최근 수업 일지 카드. 전부 같은 창을 연다. 시간표에서 시작하면 그 칸이 아는
  날짜·교시·교과·강의실이 채워진 채 열리므로 남는 선택은 단원뿐이다.
  이미 써 둔 일지가 있는 교시는 새로 쓰지 않고 **그 글을 연다**
    - 날짜는 **보고 있는 날짜**를 따른다 — 지난 주를 펼쳐 놓고 눌렀으면 그 주의 그 날짜,
      오늘 화면에서 날짜를 옮겨 놨으면 그 날짜다
    - 휴강 칸은 누를 수 없다. 하지 않은 수업의 일지를 쓸 일은 없다
    - `TimetableControl` 의 클릭은 기본이 꺼짐이다(`IsSlotClickable`). 이 컨트롤은 학급 일지의
      학급 시간표에도 쓰이므로 동작을 컨트롤 안에 넣지 않고 이벤트만 올린다
- **완료 배지는 제목 규칙으로 맞춘다** — 게시글에는 날짜·교시를 담을 칸이 없어서
  (`Post` 는 Category·Subject·Title·Content 뿐) 제목 `8/21 3교시 영어 1-1` 이 유일한 단서다.
  만드는 쪽과 되읽는 쪽이 갈라지지 않게 규칙을 `LessonJournalTitle` 한자리에 두고 왕복을
  테스트로 묶었다. **제목을 손으로 고치면 그 교시는 다시 '예정' 으로 보인다** — 글이 사라지는
  것은 아니고 목록에는 그대로 남는다. board.db 에 날짜·교시 칸을 더하는 쪽이 더 튼튼하지만,
  마이그레이션 장치가 없는 DB 에 그것부터 만들어야 해서 이번에는 접었다
- **곁들여 고친 것** — 수업 홈의 현재 교시 판정이 8:50~16:20 을 하드코딩한 복제본
  (`LessonLogService.GetCurrentPeriod`)이라 **학교 교시 설정을 무시하고 있었다**. 오늘 화면이
  쓰는 설정 기반 계산(`Functions.GetPeriodNow`)으로 통일했다. 도움말에 남아 있던 "수업 시간표"
  절도 지웠다 — 8/16 에 메뉴와 페이지가 함께 사라진 기능이다

- **첨부 저장 코드를 한자리로** — 게시글 편집 페이지와 메모 창이 파일 복사·삭제 로직을 그대로
  복사해 갖고 있었다. 수업 일지 창까지 세 번째 사본을 만들 참이라 `PostAttachments` 로 모았다
  (동작 그대로 옮기기만 했다)
- 게시판 편집기의 머리말 주입 경로(`PostEditPageParameter.SeedTitle`·`SeedFirstLine`)는
  쓰는 곳이 없어져 함께 지웠다

순 -1,400줄 남짓, 테스트 468개 통과(제목 규칙 15개 추가, `LessonLog` 스키마 3개 제거).

### 학급일지 죽은 코드 정리 — 중복 로직은 지우는 대신 되살렸다 (2026-08-19)
`ClassDiaryService` 의 공개 메서드 9개 중 4개가 호출처 0건이었다(2026-07 감사에서 넘어온 항목).
넷을 다 지우려다 하나는 성격이 다르다는 것을 확인했다 — 나머지 셋은 쓸 데가 없는 기능인데,
`GetOrCreateDiaryAsync` 는 **쓸 데가 있고 실제로 쓰이는데** 화면이 같은 로직을 복사해 간 경우였다.

- **`GetOrCreateDiaryAsync` 는 남기고 화면이 그것을 쓰게 했다** — `ClassDiaryViewModel.LoadDiaryAsync`
  가 "조회하고 null 이면 새 일지 만들기" 를 인라인으로 들고 있었다. 서비스로 되돌리면서 날짜를
  일 단위로 자르는 처리도 서비스 안으로 넣었다(일지는 하루에 하나라 시각이 섞이면 조회한 날과
  만들어 준 일지의 날짜가 어긋난다). 내부적으로는 `GetDiaryAsync` 를 거치게 해 리포지토리를
  부르는 자리를 하나로 모았다
- **셋은 삭제** — `GetMonthDiariesAsync`(기간 조회가 상위 호환이고 화면도 그쪽을 쓴다),
  `UpdateMemoAsync`(화면이 일지를 통째로 저장하는 방식이라 부분 갱신이 필요 없다),
  `GetClassDiariesAsync`. 셋째는 불렸다면 오히려 버그였다 — `semester` 를 받아 놓고 리포지토리에
  넘기지 않아, 학기로 걸렀다고 믿는 쪽에서 학년도 전체를 받았을 것이다
- **고아가 된 리포지토리 둘도 삭제** — `ClassDiaryRepository` 의 `GetByMonthAsync`·`GetByClassAsync`.
  `ClassDiaryRepository` 를 직접 쓰는 코드는 앱에 없어서(전부 서비스를 거친다) 위 셋을 지우면
  닿을 수 있는 경로가 사라진다

합계 156줄 제거, 30줄 추가.

### 수업 일지 삭제 버튼 자리 정리 · 누가기록 학기 필터 제거 · ReaderColumnCache 전면 적용 (2026-08-19)

- **수업 일지의 삭제를 대화상자 버튼 줄로 옮겼다** — 본문 안에 따로 놓인 '이 기록 삭제' 버튼을
  없애고 `SecondaryButton` 으로 뺐다. 이제 저장·삭제·취소가 한 줄에 선다(형제인 `UnifiedItemDialog`
  와 같은 배치다). 확인은 **'삭제' 를 한 번 더 누르는** 방식이다 — 첫 누름은 경고 InfoBar 를 펴고
  창을 닫지 않는다. 경고를 닫거나 저장을 누르면 예고가 풀려서, 나중에 무심코 누른 '삭제' 가
  곧바로 지우지 않는다
- **통합 내보내기가 학기 0 기록을 빠뜨리던 것도 함께 고쳤다**(감사 2.4) — 1학기·2학기를 따로 불러
  합치는 방식이라, 학기가 0 으로 저장된 기록(학년 단위·방학 중)이 어느 쪽에도 안 걸려 조용히
  빠졌다. 화면과 같은 기준(학년도 전체)으로 한 번에 조회한다. 쿼리도 2회 → 1회
- **누가기록 페이지에서 학기 필터를 뺐다** — 이 화면은 학년도 단위로 모아 보는 자리다. 학기로
  거르면 앞 학기 기록이 통째로 사라져 보이는데, 이는 명부를 학년 단위로 두기로 한 것과 같은 이유다.
  목록은 항상 학년도 전체를 조회하고, 새 기록을 쓸 때 붙는 학기는 `Settings.WorkSemester` 를 따른다
- **`ReaderColumnCache` 를 리포지토리 전체로 넓혔다**(감사 3.2) — 감사 문서는 4개 파일을 지목했으나
  같은 패턴이 **18개 파일**에 있었다. 매퍼가 행마다 `reader.GetOrdinal("컬럼명")` 으로 이름을 찾던
  것을 리더당 한 번 캐시해 인덱스로 바꿨다. 매퍼가 부르는 보조 메서드(`GetStringOrEmpty` 등 8개)
  까지 캐시를 넘겨 이름 조회가 남지 않도록 했다 — `Repositories/` 에 `reader.GetOrdinal` 은 이제 0건이다
- 그 과정에서 **죽은 코드 셋**을 지웠다 — `CourseSectionRepository.GetStringOrDefault`(호출처 0건),
  `StudentRepository.MapStudent(reader)`(호출처 0건, 캐시판과 내용 동일),
  `EnrollmentRepository.MapEnrollment(reader)`(호출부를 모두 캐시판으로 옮겨 불필요해짐)

> 이 항목은 결함이 아니라 최적화다. 눈에 띄는 속도 변화를 기대하고 넣은 것은 아니고,
> 리포지토리 사이에 매퍼 작성 방식이 갈려 있던 것을 하나로 맞추는 데 목적이 있다.

### 대화상자 표시를 게이트로 일원화 — 43곳 + 중첩 안내 InfoBar 전환 (2026-08-19)
WinUI 3 는 `ContentDialog` 를 한 번에 하나만 허용하고, 이미 열려 있는데 또 띄우면 예외를 던진다.
`MessageBox` 는 이를 막으려고 세마포어 게이트(`ShowDialogAsync`)를 두고 있는데, 정작 앱 곳곳에서
`dialog.ShowAsync()` 를 직접 부르고 있었다. 게이트가 그 대화상자를 모르니 직렬화가 새는 셈이다.
배경 작업의 오류 알림이 편집 대화상자와 겹치면 안내가 사라지고 엉뚱한 오류창이 떴다.

- **34곳을 `MessageBox.ShowDialogAsync(dialog)` 로 바꿨다** — 감사 문서는 22곳이라 했으나 실제로는
  43곳이었다(주석 처리된 2곳 제외)
- **43곳 전부를 `MessageBox.ShowDialogAsync(dialog)` 로 바꿨다** — 감사 문서는 22곳이라 했으나
  실제로는 43곳이었다(주석 처리된 2곳 제외)

다만 그중 9곳은 그대로 바꾸면 **멈춘다**. `UnifiedItemDialog`·`LessonLogEditDialog` 가 열려 있는
동안 내부에서 `MessageBox` 를 부르기 때문이다(제목 미입력 검증, 삭제 확인). 게이트는 재진입이
안 되는 세마포어라, 바깥에서 잡은 채 띄우면 안쪽 호출이 바깥이 닫힐 때까지 대기한다.
**감사 문서의 "모든 ShowAsync 를 교체" 지시를 그대로 따르면 이 9곳이 그렇게 멈춘다.**

그런데 실제로 눌러 보니 그 중첩 호출은 **이미 망가져 있었다**. WinUI 는 ContentDialog 를 한 번에
하나만 허용하므로 안쪽 `ShowAsync` 가 막히고, 게이트의 재시도 루프가 250ms 간격으로 헛돌며
디버그 로그만 쌓았다 — 화면에는 아무것도 뜨지 않았다. 제목을 비우고 저장을 누르면 반응이
없는 것처럼 보였다.

- **중첩 안내를 InfoBar 로 바꿨다** — `UnifiedItemDialog` 에 안내용 InfoBar 를 넣고 제목 검증·삭제
  실패 안내를 그리로 돌렸다. `LessonLogEditDialog` 의 삭제 확인은 경고 InfoBar 의 동작 단추로
  받는다(이 대화상자는 이미 오류용 InfoBar 를 쓰고 있었다 — 같은 방식으로 맞췄다)
- 중첩 호출이 사라지자 남겨 뒀던 9곳도 안전해져 **함께 게이트로 옮겼다**. 직접 `ShowAsync` 를
  부르는 자리는 이제 주석 처리된 2곳 말고는 없다

> 이 패턴은 `CourseSectionDialog.ShowError` 가 이미 쓰고 있던 것이다 — 대화상자 안의 안내는
> 대화상자 안에서 한다. 앞으로 새 대화상자를 만들 때도 같은 규칙을 따를 것.

### 두 학년이 함께 빠지는 행사를 수업일에서 제외 (2026-08-19)
`1·2학년 수련회` 처럼 **여러 학년이 함께** 빠지는 날을 그 학년들의 수업일로 세고 있었다.
판정이 "표시된 학년이 하나뿐인가"(`marked == 1`)였기 때문이다. 3학년제 학교에서 두 학년이
빠지면 `marked == 2` 라 걸러지지 않았고, 그만큼 주차별 시수가 부풀려졌다.

감사 문서는 `marked < flags.Length` 를 제안했는데 이건 더 나쁘다 — NEIS 플래그 배열은 학교급과
무관하게 **항상 6칸**이라, 3학년제의 전교 행사(3개 학년 표시)를 `3 < 6` 으로 보고 학년 전용이라
오판한다. 남의 학년 시수까지 깎인다.

제대로 풀려면 그 학교에 학년이 몇 개인지 알아야 하는데, 그 값이 이미 있었다 —
NEIS 학교 검색이 채우는 `School.SchoolType` 이다.

- **학년 수를 판정에 넣었다** — `IsGradeOnlyEvent(schedule, grade, gradeCount)`.
  기준은 "표시된 학년이 <b>전부는 아닌가</b>" 다. 1·2학년 수련회(3학년제)는 두 학년 모두 제외되고,
  세 학년이 다 표시된 전교 행사는 종전대로 휴업 판정에 맡긴다
- **학년 수를 모르면 종전 기준으로 물러난다** — 특수학교·각종학교는 유·초·중·고 과정이 섞여
  단정할 수 없고, 학교급이 비어 있는 구데이터도 있다. 이때 `gradeCount = 0` 으로 두면
  `marked == 1` 규칙이 그대로 돈다. 모르는 채 "전부는 아님" 을 적용하는 쪽이 더 위험하다
- **학교급 조회를 한곳에 모았다**(`Helpers/SchoolProfile.cs`) — 화면마다 DB 를 다시 뒤지면
  `SchoolCalendar` 가 경고하는 "규칙을 화면마다 복붙" 이 그대로 재현된다. 캐시 열쇠를 학교 코드로
  둬서, 설정에서 학교를 바꾸면 자동으로 다시 읽는다(무효화 호출이 필요 없다)
- 시수 계산기(`WeeklyHoursCalculator.Calculate`)는 리포지토리를 들고 있지 않다는 설계를 지켜,
  학년 수를 **인자로** 받는다. 기본값 0 이라 기존 호출부는 그대로 컴파일된다

곁들여 **구글 종일 이벤트의 `End` 널 방어**도 넣었다(감사 2.6). 규격상 늘 오는 값이지만 외부
응답을 널 허용 무시(`!`)로 받고 있어, 비면 NullReference 가 나고 그 캘린더의 Pull 전체가 멈춘다.
없으면 하루짜리 일정으로 본다.

테스트 444 → **456개**.

### 좌석 배정 줄 수 하한 도입 — 짝 없음 4줄·짝 있음 3줄 (2026-08-19)
좌석표 PDF 는 행이 많아지면 셀 높이가 사진(20pt)+이름(20pt) 아래로 내려가 QuestPDF 가
레이아웃 예외로 터진다. 계산상 임계는 17행. 행 수는 설정값이 아니라 `ceil(학생수 / (줄 × 짝))`
으로 나오므로, 막을 지점은 행이 아니라 **줄**이다.

- **줄 수 하한을 짝 설정에 묶었다** — 짝 없음 4줄, 짝 있음 3줄. 이러면 짝 없는 4줄에서도
  65명까지, 짝 있는 3줄에서는 97명까지 안전하다. 현실에서 임계에 닿지 않는다
- **저장된 배치는 건드리지 않는다** — 불러올 때 하한을 끌어올리면 격자 행 수가 줄고,
  복원 루프가 `Row/Col` 로 카드를 못 찾은 배정을 **말없이 버려서** 뒷줄 학생들의 자리가 사라진다.
  하한 미만으로 저장된 배치도 인쇄상 안전하므로(30명·2줄 = 15행) 그대로 연다.
  사용자가 줄이나 짝을 직접 건드리는 순간부터 하한이 적용된다
- **출력 경로의 격자 보정을 편집 화면과 맞췄다** — `SafeGrid` 가 하한을 1 로만 잡아,
  구데이터의 `Jul=0·1` 이 열 하나짜리 격자가 되어 30명이 30행이 됐다. 편집 화면과 같은
  2~8(줄)·1~2(짝)로 자른다
- **보정값이 실제 렌더까지 닿게 했다** — 세 출력 경로(PDF·HTML·엑셀)가 보정값을 `totalCols`
  계산에만 쓰고 정작 아래 렌더 루프는 원본을 그대로 썼다. `jul=0` 이면 **행은 있는데 칸이
  하나도 없는 표**가 나왔다. 회귀 테스트 3건 추가(고치기 전 3건 모두 실패 확인)

테스트 441 → **444개**.

### 감사 문서 전수 대조 후 결함 16건 수정 + 죽은 코드 정리 (2026-08-19)
외부 감사 문서(`docs/CODE_AUDIT_AND_OPTIMIZATION_SPEC_20260817.md`) 27건을 코드와 전수 대조해
실재하는 것만 골라냈다. 등급은 믿지 않았다 — "Critical 6건" 중 셋은 호출되지 않는 죽은 코드였고
하나(엑셀 열 밀림)는 아예 사실이 아니었다. 정작 유일한 데이터 유실은 Phase 2 맨 앞에 묻혀 있었다.

- **동아리 정보가 저장되지 않던 것을 고쳤다** — `StudentLog` 테이블에는 `ClubNo`·`ClubName` 이
  있고 `AddLogParameters` 도 값을 채우는데, 정작 INSERT·UPDATE 문 양쪽에서 두 컬럼이 빠져 있었다.
  SQLite 는 쓰이지 않는 파라미터를 조용히 무시하므로 **오류 한 줄 없이 동아리 정보만 사라졌다**.
  동아리 활동 기록을 아무리 저장해도 다시 열면 동아리가 비어 있었다.
  회귀 테스트를 붙였다(고치기 전 실행 시 `Expected: 7, Actual: 0` 으로 실패하는 것을 확인)
- **한글 IME 자동 전환이 되살아났다** — Win32 `INPUT` 구조체를 `KEYBDINPUT` 하나만 얹어
  선언한 탓에 x64 에서 크기가 32바이트로 잡혔다. Win32 가 요구하는 값은 가장 큰 멤버
  (`MOUSEINPUT`) 기준 40바이트라 `SendInput` 이 `ERROR_INVALID_PARAMETER(87)` 로 0 을 돌려주고
  **키를 하나도 보내지 못했다**. 폴백(`VK_HANGUL`)도 같은 구조체를 써서 함께 죽어 있었다.
  크기를 40 으로 못 박는 대신 공용체를 제대로 선언했다 — 32비트의 `INPUT` 은 28바이트라
  상수로 박으면 x86 게시본에서 같은 증상이 재현된다. 실측으로 x64=40 / x86=28 을 확인했다
- **구글 '가짜 연결됨' 표시를 없앴다** — `IsAuthenticated` 만 저장된 문자열의 존재 여부를 봤다.
  토큰은 DPAPI 로 PC·사용자에 묶여 암호화되므로 포터블로 다른 PC 에서 열면 문자열은 남아 있어도
  복호화가 실패한다. 그래서 화면은 "연결됨" 인데 동기화는 전부 조용히 실패했다.
  토큰을 실제로 쓰는 다른 여섯 곳처럼 복호화 결과를 보게 했다
- **누가기록 목록의 DB 연결 폭증을 잡았다** — `StudentLogViewModel` 생성자가
  `StudentLogService`·`EnrollmentService`·`StudentService` 를 필드로 들고 있었다. 이 ViewModel 은
  기록 **한 줄마다** 만들어지므로 목록을 한 번 여는 것만으로 SQLite 연결이 기록 수 × 3 개 열리고
  각각 PRAGMA 7 개를 실행했다(기록 200건 → 연결 600개). 게다가 `_logService` 는 어디서도
  쓰이지 않았다. 서비스는 `InitializeAsync` 안에서만 열고 닫도록 옮겼고, 목록 경로는 이미
  `CreateManyAsync` 가 배치로 처리하므로 대부분의 인스턴스는 연결을 하나도 열지 않는다.
  더 이상 들고 있을 자원이 없어 `IDisposable` 도 뗐다(호출처 0건 확인)

이어서 한 줄짜리 넷도 함께 고쳤다.

- **세특에 담당 교사를 비워 두면 저장이 실패하던 것** — 미지정 `TeacherID` 를 `""` 로 넣었는데
  빈 문자열은 NULL 이 아니다. `StudentSpecial.TeacherID` 는 `Teacher(TeacherID)` FK 이고 연결마다
  `foreign_keys=ON` 이라 `SQLITE_CONSTRAINT_FOREIGNKEY` 로 저장이 통째로 막혔다. NULL 로 바꿨다.
  회귀 테스트를 붙였다(고치기 전 `SQLite Error 19: FOREIGN KEY constraint failed` 로 실패 확인)
- **게시판 검색 캐시가 오염되던 것** — 캐시 키에 `searchTitle`·`searchContent` 가 빠져 있어
  같은 검색어로 "제목에서 찾기" 와 "본문에서 찾기" 가 **같은 캐시를 공유했다**. 먼저 한 검색의
  결과가 그대로 다시 나왔다. 키 생성을 `CacheKeys.Posts` 안으로 모으고 두 플래그를 넣었다
- **초등학교 4~6학년 엑셀 등록이 막혀 있던 것** — 학년 입력을 `1~3` 으로 검사했다. 초등학교는
  6학년까지다. 안내 문구도 `1~6` 으로 고쳤다
- **다크 테마에서 학사일정 날짜가 보이지 않던 것** — 휴업일이 아닌 날짜 글자색에 `Colors.Black`
  이 박혀 있어 어두운 배경에 묻혔다. 테마 리소스를 찾아 쓰고 못 찾으면 회색으로 물러난다.
  같은 파일의 `BoolToTodayBackgroundConverter`(RGB 240,240,240 하드코딩)는 어떤 바인딩에서도
  쓰이지 않는 죽은 컨버터라 선언째 지웠다


마지막으로 안정성·성능 일곱 건을 손봤다.

- **손상된 DB 를 안내 없이 죽던 순서를 바로잡았다** — 무결성 점검이 DB 초기화 *뒤*에 있어서,
  깨진 파일에 `CREATE TABLE` 을 걸다 예외가 나면 복구 안내를 띄우기도 전에 앱이 조용히 닫혔다.
  점검을 초기화 앞으로 옮겼다. 여기에 **`Settings.db` 점검을 새로 넣었다** — 이 파일이 깨지면
  `Settings.Initialize()` 가 먼저 죽어서 손상 안내가 아예 뜰 수 없었다. 경로가 설정값과 무관하게
  정해지므로 초기화 전에 볼 수 있다
- **설정 복원이 열린 연결 위에 덮어쓰던 것** — 폴더 복원 경로는 `PrepareForRestore` 로 풀을 끊고
  잔여 `-wal`·`-shm` 을 지운 뒤 덮어쓰는데, `SettingsDb.Restore` 만 그 준비 없이 `File.Copy` 를 했다.
  같은 준비를 거치게 했다
- **임시 인쇄 PDF 가 무한히 쌓이던 것** — 임시 폴더의 인쇄용 PDF 를 만들기만 하고 아무도
  지우지 않았다. 현재 배포 구성에서는 인쇄가 **항상** 이 경로를 타므로 인쇄할 때마다 하나씩
  늘었다. 뷰어가 방금 연 파일을 잡고 있을 수 있어 하루 지난 것만 지운다
- **누가기록·세특 목록의 O(N²) 글자 크기 루프** — 컬렉션이 바뀔 때마다 목록 전체를 다시 돌았다.
  행을 하나씩 `Add` 로 채우는 화면에서는 N 번째 추가마다 N 개를 훑어 기록 200건이면 대입이
  2만 번이었다. 추가된 행만 손대게 했다(전체 교체는 종전대로 전부 훑는다)
- **게시판 진입 시 같은 조회가 세 번 돌던 것** — 카테고리·주제 setter 가 각각 자동 새로고침을
  걸고 호출부가 또 한 번 로드했다. 억제 플래그를 카테고리 setter 에도 적용하고
  `SetScopeWithoutReload` 로 한 번에 설정하게 했다 (조회 3회 → 1회)
- **N+1 쿼리 3건** — 교사 시간표는 과목마다 수업을 따로 읽었고(과목 12개면 쿼리 12회),
  학교 교사 목록은 재직 이력마다 교사를 한 명씩 읽었으며(교사 40명이면 40회), 구글 동기화는
  Pull 되는 **이벤트마다** `SchoolScheduleService` 를 새로 만들어 하루치 학사일정을 조회했다.
  앞의 둘은 `IN` 절 일괄 조회를 추가해 각각 1회로, 구글 쪽은 서비스를 한 번만 열고 날짜별
  결과를 기억하게 바꿨다. **필터 조건은 종전과 똑같이 두었다** — 시간표 쪽은 취소된 수업까지
  보고 판단하고 있어서 여기서 조건을 바꾸면 화면 동작이 조용히 달라진다
- **SQLite 공유 캐시 제거** — 연결 6곳(+`Settings.db`)이 `Cache=Shared` 를 썼다. WAL 위에 공유
  캐시를 얹으면 같은 프로세스의 연결들 사이에 테이블 락이 생겨 WAL 이 주는 읽기/쓰기 동시성이
  깎이고, 그 락은 `SQLITE_BUSY` 가 아니라 `SQLITE_LOCKED` 로 즉시 실패해 `busy_timeout=5000`
  도 듣지 않는다. 인메모리 DB 를 쓰지 않으므로 공유 캐시로 얻는 것이 없다

**고치지 않기로 한 것 둘.** 둘 다 실제 앱에서 확인한 뒤 내린 결론이다.

`ListStudent` 의 `ItemsSource = null` 재할당 제거(감사 3.4-3). 표시 모드·선택 모드를 바꿀 때만
도는 코드고, 실제로 모드를 토글해 봐도 거슬리는 점이 없었다. 없애려면 열 너비를 항목 템플릿에
바인딩으로 물리는 재작성이 필요한데, 얻는 것에 비해 화면이 틀어질 위험이 크다.

좌석표 사진 오버플로(감사 2.5). 실제 PDF 출력물로 확인한 결과 통상 배치(6행 × 5열)는 멀쩡하다.
계산상 `seatAreaMax(678pt) / 행수 < 40pt`, 즉 **17행부터** 사진 하한(20pt)+이름(20pt)이 셀 높이를
넘어 QuestPDF 레이아웃 예외로 터진다. 줄 수 하한을 넣은 뒤로는 새로 만들거나 줄·짝을 수정한
배치에서 17행에 닿으려면 **65명 이상**(짝 없음 4줄)이 필요해 사실상 도달할 수 없다.
하한 이전에 2줄로 저장해 둔 배치가 남아 있고 학생이 33명 이상이며 사진까지 켠 경우에만
이론상 재현되는데, 그런 배치는 편집 화면에서 세로로 17줄 늘어선 모양이라 눈에 안 띌 수 없다.
실제 사용 범위는 최대 7행(셀 96.9pt, 임계값의 두 배 이상)이다.

끝으로 "죽은 코드" 로 분류했던 셋을 지우려다 하나는 **살아 있는 버그**임을 뒤늦게 확인했다.

- **단원 수정이 100% 실패하던 것** — `CourseSectionRepository.UpdateAsync` 의 `UPDATE` 문 마지막
  필드 뒤에 후행 쉼표가 있어 `near "WHERE": syntax error` 로 예외가 났다. 호출처를 찾을 때
  `Controls/`·`Pages/`·`Services/` 만 보고 `Dialogs/` 를 빠뜨려 "호출처 0건" 으로 잘못 판정했는데,
  실제로는 단원 편집 다이얼로그가 저장할 때마다 부른다. 쉼표를 지우고 회귀 테스트를 붙였다
  (고치기 전 `SQLite Error 1: near "WHERE": syntax error` 로 실패 확인)
- **미사용 메서드 둘 삭제** — `CourseEnrollmentRepository.GetByStudentAndPeriodAsync`(`Course` 에 없는
  `c.Class` 를 `ORDER BY` 에 걸고 있었다), `StudentRepository.SearchAsync(keyword)`(`Student` 에 없는
  `ID` 컬럼을 `WHERE` 에 걸고 있었다). `.cs`·`.xaml` 전수 검색으로 호출처 0건을 확인했다.
  둘 다 부르는 순간 깨졌을 코드다 — 아무도 안 불렀기에 드러나지 않았을 뿐이다

테스트 438 → **441개**.

감사 문서의 나머지 항목 판정은 아래와 같다. 실재하지만 죽은 코드(1.1~1.3), 사실이 아닌 것
(1.5 엑셀 열 밀림 · 2.9 · 2.13 · 2.15), 제안된 수정이 오히려 틀린 것(2.12 — `flags.Length` 가
항상 6 이라 3학년제 학교의 전교 행사를 학년 전용으로 오판한다). 나머지 실재 항목은 후속으로 남긴다.

### 엑셀 가져오기 필터에서 .xls 제거 (2026-08-19)
파일 선택 창이 `.xls` 를 고를 수 있게 내주고 있었다. 그런데 읽기를 맡은 MiniExcel 은 xlsx·csv 만
다룬다 — 구버전 `.xls` 를 고르면 그대로 "Excel 파일 읽기 오류" 로 끝났다. 고를 수 없게
막는 편이 고르고 실패하는 것보다 낫다.

- 학생 추가 페이지와 공통 선택 다이얼로그 둘 다 `.xlsx` 만 남겼다
- 다시 넣는 일이 없도록 이유를 주석으로 박아 두었다
- 엑셀 라이브러리를 Sylvan.Data.Excel 로 갈아끼우면 `.xls` 도 읽을 수 있지만,
  그쪽 쓰기는 서식을 전혀 다루지 못해 모든 내보내기 문서가 민무늬가 된다. 교체는 접었다

### 데이터 폴더 정리 — Data 하위로 모으고, 포터블 판정을 표식 파일로 (2026-08-16)
프로그램 파일과 데이터가 한 폴더에 섞여 있었다. 실사용 폴더를 열어 보니 DB 옆에 exe·dll 이
나란히 있고, 그 사이에 동기화 충돌본과 수동 `.bak` 이 끼어 무엇이 진짜 데이터인지 눈으로
가릴 수 없었다. 첫 공개 릴리스가 배치를 바꿀 마지막 기회라 지금 정리했다.

- **사용자 자산을 `Data\` 한 곳으로** — DB 4종·`Photos\`·`Files\`. 이 폴더만 옮기면 데이터가
  통째로 따라온다. 포터블은 실행 파일 폴더 아래, 설치본은 `%USERPROFILE%\NewSchool` 아래로
  **두 모드가 같은 배치**를 쓴다
- **산출물은 루트에 남겼다** — `Backups\`·`Exports\`·`Prints\`·`Logs\`. 특히 백업을 `Data\` 안에
  두면 백업이 자기 자신을 담고, 폴더째 복사할 때 용량이 배로 불어난다
- **포터블 판정을 `portable.txt` 표식으로 바꿨다** — 옛 방식은 `Settings.db` 가 exe 옆에 있는지로
  판정했다. 그래서 DB 가 사라지거나 OneDrive 가 파일 이름을 바꾸면 **조용히 사용자 폴더 모드로
  넘어가 빈 화면**을 띄웠다. 모드와 데이터 상태를 떼어 놓았다. 표식이 없어도 옛 배치면
  포터블로 인정하고 표식을 만들어 주므로 기존 폴더는 그대로 열린다
- **옛 배치의 이관은 자동화하지 않았다** — 이전 릴리스는 내려받은 사용자가 없어 이관할 폴더가
  사실상 하나뿐이다. 한 번 쓰고 영영 안 도는 코드를 짊어지느니 손으로 옮기는 편이 낫다고 봤다.
  옮긴 뒤에는 `Data\Settings.db` 를 보고 포터블로 알아보므로 표식을 따로 만들 필요가 없다
- **백업 대상을 화이트리스트로** — `*.db` 를 전수로 훑던 탓에 동기화 충돌본까지 ZIP 에 담고
  복원 때 되살렸다. 복원 경로도 같이 좁혔다
- 통합 내보내기의 학생정보 xlsx 만 `Prints\` 로 가던 것을 `Exports\` 로 바로잡았다
- 죽은 백업 경로 제거 — `SchoolDatabase.BackupDatabaseAsync`/`RestoreDatabaseAsync`(호출처 0건)가
  정리 정책도 복원 경로도 없는 `Backup\` 폴더에 사본을 쌓게 되어 있었다
- 테스트 5개 추가(`DataLayoutTests`) — 표식 판정. 433 → **438개**

### 문서를 현재 코드에 맞춤 (2026-08-16)
1.0 정리와 수업 관리 재구성으로 사라진 것들이 문서에 그대로 남아 있었다. 파일을 찾아가면
없는 목록은 없느니만 못하다 — `FEATURES.md` 가 가리키는 경로가 실제로 있는지 전수 대조했다.

- **내비게이션 표를 실제 메뉴 구조로 다시 썼다** — 일곱 갈래(홈·달력·학급·수업·업무·아카이브·설정)와
  `Tag` → 페이지 매핑. 옛 표에는 없는 `ClubHome`·`Settings_General` 이 있었고,
  게시판 넷(학급·수업·업무·아카이브)과 수업 일지·도움말·업데이트 확인은 아예 빠져 있었다
- **없는 파일을 걷어냈다** — `Attachment`·`Evaluation`·`Interfaces` 계열, 1.0 정리에서 사라진
  리포지토리 6종(`Schedule`·`SubjectYearPlan`·`WeeklyLessonHours`·`WeeklyUnitPlan`·
  `ScheduleUnitMap`·`UndoHistory`), `ReportExportService`, `UI/`·`Validation/`·`Events/` 세 폴더
- **새로 생긴 것을 넣었다** — `LessonChange`·`CourseWeeklyHours` 모델과 리포지토리,
  `TimetableChangeMerger`, 수업 관리 여섯 탭 컨트롤, 새 다이얼로그 넷
- `README` 의 Windows App SDK 2.2 → **2.4**, 존재하지 않는 `installer.iss` 참조 제거,
  테스트 실행 절차 추가. 게시 때만 나는 `obj\x64\Release` 옛 `*.g.cs` 오류의 처방도 적어 뒀다
- 테스트 수 표기 421 → **433** (`TEST_PLAN.md`)
    - 죽은 페이지 둘(`ClubHomePage`·`HomeroomSettingsPage`)은 내비게이션에도 다른 호출부에도
      없어 **차후 과제로 적어만 뒀다** — 지우는 것은 문서 작업이 아니다

### 날짜 이동 — 오늘 화면과 수업 홈 시간표 (2026-08-16)
"내일 뭐 하지" 를 보려고 다른 화면을 찾아갈 일이 없게 했다.

- **오늘 화면**: 헤더에 `[◀] 날짜 [▶] [오늘]`. 날짜를 옮기면 **내 수업 · 우리 반 · 급식 · 그날 행사**
  넷만 따라온다 — 학사일정 목록·할 일·메모는 원래 "앞으로"를 보는 카드라 오늘 기준으로 둔다.
  오늘이 아니면 그 사실을 헤더에 알리고 **현재 교시 배지를 감춘다**(다른 날짜에서 "3교시"는 참이 아니다)
    - 주말도 그대로 보여준다. 건너뛰면 등교하는 토요일의 행사·급식을 볼 수 없다
    - 달력 선택기는 두지 않았다 — 며칠 뒤를 한눈에 보는 일은 [주별 시간표 확인 및 변경] 탭이
      3주치로 이미 답한다. 역할이 겹치면 어느 쪽을 봐야 할지 흐려진다
    - 앱을 켜둔 채 자정을 넘겨도 **다른 날짜를 보고 있는 중이면 끌고 가지 않는다** — 보던 화면이 멋대로 바뀐다
- **수업 홈의 내 시간표**: 요일 아래에 날짜가 붙고 `[◀] [이번 주로] [▶]` 로 주를 옮긴다.
  그 주의 휴강·교체·보강·대강이 얹혀 보이고(칸 색·표식·툴팁), 오늘 날짜는 굵게 표시된다.
  읽기 전용이다 — 넣고 고치는 곳은 수업 관리의 주별 탭 하나로 둔다
    - 얹는 규칙은 오늘 화면과 같은 `TimetableChangeMerger` 를 쓴다. 두 화면이 서로 다른 답을 내면 안 된다
    - 범위 옆에 늘 **이번 주 / 다음 주 / 지난 주**를 붙인다. 날짜만 있으면 그게 어느 주인지 매번 세어야 한다
    - ⚠ 버튼을 `이번 주` 라고만 쓰면 옆의 날짜 범위를 설명하는 **라벨로 읽힌다**
      (8/17~8/21 이 이번 주인 것처럼). `이번 주로` 로 바꿔 동작임을 드러냈다
    - 주말에는 **다가오는 주**를 연다 — 일요일에 이미 끝난 주를 펼쳐 봐야 쓸모가 없다
    - 시간표 칸 폭을 넓혔다(좌측 열 360 → 470). 월~금 5열이 들어가는데 좁아서 과목명이 두 줄로 접혔다

### 이름 통일 — "수업 기록" → "수업 일지" (2026-08-16)
수업 홈의 카드는 `LessonLogService`/`LessonLogList` 를 읽으므로 메뉴의 **수업 일지**와 같은 것인데,
화면마다 "수업 기록" 으로 불려 서로 다른 기능처럼 보였다(옆에 진짜로 다른 **누가 기록** 메뉴가 있어
더 헷갈렸다). 사용자에게 보이는 문구를 전부 "수업 일지" 로 맞췄다 —
목록 머리글·빈 상태 안내·편집 창 제목, 그리고 FEATURES 표기까지.

### 수업 시간표 메뉴 제거 (2026-08-16)
`수업 → 수업 시간표`(`TeacherTimetablePage`)를 내렸다. 수업 홈의 내 시간표가 같은 것을 보여주면서
**주 이동과 그 주 변경까지** 얹으므로 남겨 둘 이유가 없다. 메뉴만 지우면 닿을 수 없는 페이지가
죽은 코드로 남으므로 페이지 파일도 함께 지웠다.

### 시수·배치 다듬기 (2026-08-16)
- **주당 시수 초과를 배치할 때 막는다.** 넘겨서 놓으면 시수 탭의 계산이 조용히 부풀어 오른다.
  지금 배치 수와 주당 시수를 함께 알려 주고, 늘리려면 [수업 개설] 에서 먼저 고치게 안내한다.
  주당 시수를 안 적어 둔 수업(0)은 넘을 기준이 없으므로 막지 않는다
- **시수 표의 합계열을 합계행으로 바꿨다.** 가로 합계는 학급이 여럿일 때
  "이 주에 내가 몇 시간 들어가나" 밖에 답하지 못한다. 정작 필요한 숫자는
  "이 학급을 학기 동안 몇 시간 만나나" 라서, 맨 아래 줄에 학급별 학기 총 시수와 총 수업일을 둔다
- **학기 기간을 학사일정에서 유추하고, 기간 고르는 줄은 없앴다.**
  학기는 방학을 품는다 — 1학기는 `3월 1일 ~ 여름방학 마지막날`, 2학기는 `그 다음날 ~ 학년도 끝(2월 말)`.
  그래서 유추할 것은 **여름방학의 끝** 하나뿐이고, 그 지점이 두 학기의 경계다.
  8월 중순에 개학하는 학교의 8월 하순 수업이 이렇게 해야 2학기로 제대로 들어간다
    - 방학은 행사명이 아니라 **수업일이 14일 이상 끊기는 구간**으로 찾는다 —
      이름은 학교마다 다르지만 공백은 다르지 않다. 연휴에 재량휴업이 붙어도 열흘을 넘기기 어렵다
    - 유추할 근거(학사일정)가 없으면 관례값을 쓰되 **화면에 그렇다고 표시한다** —
      그때는 방학이 섞여 있어 숫자를 믿으면 안 된다
    - 유추에 <b>학년 행사는 보지 않는다</b> — 학기의 경계를 정하는 일이라 학년을 타면 안 된다
      (1학년만 빠지는 행사로 경계가 밀리면 다른 학년의 계산까지 어긋난다)
    - 설정에 학기 기간을 넣지 않았다. 학사일정은 이미 매년 내려받지만 설정은 매 학기 손으로
      넣어야 하고, 안 넣으면 틀린 값으로 조용히 계산된다 — 연간계획이 죽은 그 패턴이다

### fix(배치): 학년 필터가 다른 학년 배치를 감춰 겹쳐 넣을 수 있던 문제 (2026-08-16)
학년 필터는 **수업 개설 탭의 카드 목록**을 좁히는 것인데, 그 걸러진 목록이 다른 탭에도
그대로 갔다. 배치판과 주별 표는 받은 수업 목록으로 `Lesson` 을 거르므로,
학년을 걸면 **다른 학년의 배치가 화면에서 사라졌다.** 빈 것처럼 보이는 칸에 수업을 놓으면
같은 교시에 두 수업이 겹쳐 들어간다(중복 검사도 안 보이는 배치는 못 본다).

- 개설 탭을 뺀 나머지 탭에는 **거르지 않은 전체 목록**을 준다
- 학년 콤보는 목록을 실제로 거르는 **수업 개설 탭에만** 남겼다(`CourseScopeBar.ShowGrade`).
  대상이 수업 하나인 탭에서는 수업 콤보만 있으면 되고, 있어 봐야 하는 일이 없었다

### 마이그레이션 코드 제거 — 정의 한 벌로 (2026-08-16)
1.0 을 첫 배포로 잡았으므로 그 이전 모양을 위한 마이그레이션은 두지 않는다는 방침을
`LessonChange.SubjectText`·`CourseWeeklyHours.Room` 에도 적용했다. 두 열은 `CREATE TABLE`
정의에만 있고, `ALTER TABLE` 로 붙이던 코드는 걷어냈다. 테스트 DB 는 초기화기가 매번
새로 만드므로 언제나 현재 정의와 같다.

> ⚠ 배포 전 개발 DB 에 옛 모양이 남아 있으면 **그 두 테이블만 지우면** 된다 —
> 담긴 것은 손으로 고친 주차 시수와 앞날 수업 변경뿐이다:
> `DROP TABLE CourseWeeklyHours; DROP TABLE LessonChange;`

### fix(시수): 학급 열 추가가 영영 실행되지 않던 문제 (2026-08-16)
주차별 시수가 `SQLite Error 1: 'no such column: Room'` 으로 계산되지 않았다.
화면에는 오류와 함께 **"아직 배치된 시간표가 없습니다"** 로 보였는데, 예외가 학급 계산 앞에서
끊는 바람에 배치가 있는데도 없는 것처럼 나타난 것이다.

원인은 스키마 정본이 **테이블과 인덱스를 한 배치로** 실행한 것이다. 그 유일 인덱스가
학급별 시수를 위해 나중에 붙인 `Room` 열을 쓰는데, 옛 DB 에는 그 열이 없으니 배치가 통째로 터졌고
**바로 뒤에 있던 `ALTER TABLE … ADD COLUMN Room` 이 실행되지 못했다.** 생성자의 `catch` 가
그 예외를 삼켜서, 그 뒤로는 조회마다 같은 오류만 반복됐다.

- 뒤이어 마이그레이션 자체를 걷어내고 정의 한 벌로 정리했다(아래 항목) — `Room` 은
  이제 `CREATE TABLE` 에만 있으므로 인덱스와 순서가 엉킬 일이 없다
- 유일 인덱스가 `(CourseNo, Room, Week)` 인지 테스트로 못박았다 — `Room` 이 빠지면
  같은 주차의 둘째 학급이 조용히 저장되지 않는다

### 탭 이름 정리 (2026-08-16)
`시수 관리` → **수업 시수**, `수업시간표 관리` → **수업 시간표 입력**,
`주별 시간표` → **주별 시간표 확인 및 변경**. 무엇을 하는 탭인지 이름만으로 갈리게 했다 —
"관리" 는 여섯 탭 중 넷에 붙어 있어 구분에 보탬이 되지 않았다.

### 시간표 변경(휴강·대체·보강)을 오늘 화면에서 확인 (2026-08-16)
학교 시간표는 행사·시험·출장으로 종종 바뀌는데, 오늘 화면은 **정기 시간표만** 보고 있어서
변경이 들어올 자리가 없었다. 앞날의 변경을 미리 등록해 두면 그날 오늘 화면에 나타난다.

- **새 표 `LessonChange`** — 날짜·교시 단위 예외. 정기 시간표(`Lesson`)는 건드리지 않으므로
  변경을 아무리 넣어도 교사 시간표·시수 계산의 "평소" 기준은 흔들리지 않는다
    - ⚠ `Lesson.IsCancelled` 로는 이 일을 할 수 없다 — 그건 행 단위 플래그라 정기 수업에 걸면
      그 수업이 **매주** 사라진다(`GetByDateAsync` 의 조건이 날짜를 가리지 않는다)
    - 한 장으로 넷을 덮는다 — 표식을 과목명 앞에 붙여 한눈에 갈린다:
      **(휴)휴강** · **(교)교체**(원래 있던 시간이 옮겨감) · **(보)보강**(내 수업을 새로 넣음) ·
      **(대)대강**(남의 수업에 대신 들어감). 교체냐 보강이냐는 저장하지 않고 볼 때 판단한다 —
      정기 시간표가 나중에 바뀌면 저장해 둔 판정은 그 순간 거짓이 된다
    - 휴강을 `CourseNo = 0` 으로 두지 않고 NULL 로 둔다. `Course(No)` 에 0 인 행이 없어
      FK 가 걸리는데, SQLite 는 NULL 인 FK 만 통과시킨다
- **등록**: 새 **[주별 시간표] 탭** — `날짜 | 1 | 2 | … | 7` 표에 한 번에 3주치를 펼친다.
  칸을 오른쪽 클릭해 휴강·대체·보결을 넣고, 칸끼리 끌면 맞바꾸기가 된다(변경 두 줄로 표현된다)
    - **기초 시간표 탭과 행·열을 일부러 반대로 뒀다.** 같은 모양이면 "그 주만" 바꿀 것을
      "매주" 바꾸는 사고가 난다. 기초는 `요일 × 교시`, 주별은 `날짜 × 교시`다
    - 휴업일·공휴일·그 학년만 빠지는 행사인 날은 날짜 칸에 사유가 붙고 흐려진다
    - 칸을 고쳐 결과가 평소와 같아지면 **변경 행을 지운다** — 남겨 두면 나중에 기초를 고쳤을 때
      옛 내용이 그 날에만 고정으로 버틴다
    - **대강** — 남의 수업에 대신 들어가는 경우는 내 수업 목록에 없다. `Course` 로 가리킬 수 없으므로
      `SubjectText` 에 과목명을 그대로 적는다(`CourseNo` 는 비운다)
    - **[내 수업 넣기] 는 수업과 강의실을 함께 고른다.** 강의실을 첫 번째로 자동 선택해 주면
      학급이 여럿인 수업에서 엉뚱한 반이 조용히 들어간다
    - 칸 높이를 키웠다 — 과목명 줄과 강의실 줄이 함께 들어가는데 낮아서 아래 줄이 잘렸다
- **[변경 목록] 창**은 읽기 전용이다 — 오늘 이후로 걸린 변경을 한 번에 훑고 되돌리기만 한다.
  넣고 고치는 입구는 주별 탭 하나뿐이다(같은 표에 쓰는 입구가 둘이면 어느 쪽이 진짜인지 헷갈린다)
    - ⚠ 콤보의 `SelectionChanged` 에서 **그 콤보의 `Items` 를 다시 만들지 말 것.**
      자기 SelectionChanged 콜스택 안에서 자기 목록을 지우면 WinUI 가 터지는데,
      핸들러가 `async void` 라 **메시지 하나 없이 앱이 멈춘다**(옛 등록 폼에서 실제로 났다).
      그 자리에서 고른 값이 곧바로 되돌아가는 증상도 같은 재생성 탓이다
    - 이 화면들의 `async void` 핸들러는 예외를 `UserErrorReporter` 로 넘긴다 — 조용히 죽는 대신 알린다
- **확인**: 오늘 화면 [내 수업] 칸에 바로 — 휴강은 취소선+흐리게, 대체·보강·보결은 배지(사유는 툴팁).
  **휴강도 칸을 지우지 않고 남긴다** — 조용히 사라지면 원래 없던 교시인지 오늘만 없는 교시인지
  구분할 수 없다. 휴업일이라 평소 칸이 없는 날에도 보강은 보인다
- 병합 규칙은 `TimetableChangeMerger` 로 떼어 내 테스트로 고정했다(맞교환·정렬·휴강 보존 포함)

### 수업 관리 5탭 재구성 — 시수·진도 되살리기 + 시간표 배치판 (2026-08-16)
흩어져 있던 수업 관련 화면을 **수업 관리 한 페이지의 탭**으로 묶었다:
`수업 개설 · 단원 관리 · 시수 관리 · 진도 관리 · 수업시간표 관리 · 주별 시간표`.
다섯 탭이 같은 대상을 두고 이어진다 — "수업을 열고 → 단원을 넣고 → 시간표에 배치하면 →
그 배치가 시수가 되고 → 진도를 단원별로 남긴다". 탭 내용은 처음 열 때 채운다.

- **페이지 헤더를 없앴다** — 제목 "수업 관리" 는 상단 내비게이션의 [수업]과 겹쳤고,
  필터(학년도·학기·학년·수업)는 탭이 쓰는 것이라 탭 **위**에 있으면 위아래 순서가 뒤집혀 보였다.
  새 `CourseScopeBar` 를 탭마다 하나씩 두고, 페이지가 값을 맞춘다. 어느 탭에서 바꾸든
  나머지 탭이 같은 값을 따라가므로 탭을 옮길 때마다 다시 고를 일은 없다
    - 목록(학년도·학년)은 바가 스스로 읽지 않는다. 페이지가 한 번 읽어 다섯 바에 나눠 준다 —
      바마다 읽으면 같은 질의가 다섯 번 돌고 바들끼리 목록이 어긋날 여지도 생긴다
    - 학년 목록은 학생 명부가 아니라 **실제 개설된 수업**에서 뽑는다. 이 화면의 관심사는
      "내가 이 학기에 들어가는 학년" 이다
    - Pivot 머리글은 기본 24pt 라 다섯 개가 화면 위쪽을 다 먹어 16pt 로 낮췄다

- **수업시간표 관리 (신규)** — 왼쪽에 교사의 수업과 강의실을 펼쳐 두고, 오른쪽 주간 칸에
  끌어다 놓는 배치판. 저장 대상은 `Lesson`(정기 수업)이라 교사 시간표·수업일지에 바로 반영된다
    - 마우스: 수업 카드 → 빈 칸(배치) / 배치된 칸 → 다른 칸(이동) / 강의실 카드 → 배치된 칸(강의실 변경).
      오른쪽 클릭으로 강의실 바꾸기·배치 삭제
    - 키보드: 방향키 이동 · `Enter` 배치 · `Delete` 삭제 · `R` 강의실 순환 · `1`~`9` 수업 고르기
    - 요일별 교시 수(설정의 시정)를 따라 칸을 만들고, 없는 교시·이미 찬 칸은 애초에 놓이지 않는다
    - 수업 카드마다 `배치/주당 시수` 뱃지를 달아 남은 시수가 바로 보인다
    - 카드의 **[시간표 배치] 다이얼로그는 제거**했다 — 같은 `Lesson` 표에 쓰는 입구가 둘이면 헷갈린다
    - ⚠ **강의실만 끌어다 놓으면 먹히지 않던 문제** — 강의실 목록은 항목이 `string` 이라
      ListView 가 DataPackage 에 텍스트를 **이미** 채워 둔다. 거기에 `SetText` 를 한 번 더 부르면
      예외가 나고, `DragItemsStarting` 안에서 난 예외는 끌기를 통째로 취소시킨다
      (수업 카드는 커스텀 객체라 자동으로 채워지는 게 없어서 멀쩡했다).
      표식 싣기를 실패해도 되는 일로 낮췄다 — 실제 payload 는 필드에 있다
- **시수 관리 (되살림)** — 옛 탭은 연간계획(`SubjectYearPlan`)에 매달려 있어서 계획을 다 세우기
  전에는 숫자가 하나도 안 나왔다. 이제 시간표 배치와 학사일정만 있으면 바로 계산된다
    - `주차 | 기간 | 일수 | 학급들… | 합계 | 비고(학사일정)` — **학급(강의실)별로 열이 갈리고
      칸마다 손으로 고칠 수 있다.** 같은 과목이라도 학급마다 요일이 다르고 빠지는 날도 다르다
    - 휴업일·공휴일과 **그 학년만 빠지는 행사**를 빼고 센다. 여러 학년이 함께 표시된 행사는
      전교 행사에 가까우므로 빼지 않는다 — 그러지 않으면 남의 학년 행사로 내 시수가 깎인다
    - 위쪽 카드 셋: 시간표 정보(학급별 배치 요일) · 학사일정 요약 · 통계(총 주차·수업일·총 시수·단원 차시)
    - 자동값은 저장하지 않는다 — 시간표나 학사일정이 바뀌면 저장된 자동값은 그 순간 거짓이 된다.
      **손으로 고친 칸만** `CourseWeeklyHours(수업, 학급, 주차)` 에 남고,
      자동값과 같은 값을 넣으면 조정을 지운다(안 그러면 옛 숫자가 고정으로 버틴다)
- **진도 관리 (되살림)** — 단원 × 학급(강의실) 매트릭스. 칸을 골라(Ctrl 로 여러 개)
  완료 · 미완료 · 보강 · 병합 · 건너뜀 · 결강으로 처리한다. 격차 분석과 CSV 내보내기 포함
    - `LessonProgress` 를 되살리되 **`ScheduleId` 는 빼고** 만들었다. 부모였던 `Schedule` 테이블이
      1.0 정리에서 사라졌고, 없는 테이블을 가리키는 FK 는 `foreign_keys=ON` 에서 INSERT 를
      준비 단계부터 막는다
    - **일정 동기화·엑셀 내보내기는 되살리지 않았다** — 각각 `Schedule` 테이블과
      `ReportExportService` 가 원천이었고 둘 다 없다. 대신 결과를 밖으로 빼는 길은 CSV 로 열었다
- **단원 관리** — 별도 페이지(`CourseSectionPage`)에서 탭 안의 컨트롤(`CourseSectionView`)로 옮겼다.
  기능은 그대로(목록 CRUD · 드래그 정렬 · CSV 입출력 · 전체 삭제), 뒤로가기 버튼만 없앴다
- **`SchoolCalendar` 가 다시 쓰인다** — 1.0 정리 뒤 프로덕션 호출부가 없던 학사일정 판정 헬퍼를
  시수 계산이 쓴다. 학년 행사 판정(`IsGradeOnlyEvent`)과 수업일 판정(`IsTeachingDayFor`)을 여기 모았다 —
  규칙을 화면마다 복붙하면 예전처럼 두 곳의 기준이 조용히 어긋난다

### 연간 수업 계획·진도 관리 제거 (2026-08-15)
실사용 데이터로 두 기능의 효용을 확인한 결과 **둘 다 걷어냈다.** 순 -15,440줄.

- **근거** — `SubjectYearPlan`·`WeeklyLessonHours`·`WeeklyUnitPlan` 전부 **0행**.
  `LessonProgress` 는 10행인데 **전부 2026-03-25 하루에 만들어졌고 이후 5개월 무수정**.
  단원도 7개 과목 중 1개에만 25개 입력된 채 멈췄다. 같은 기간 학급일지는 5개월 연속,
  학생부는 6~7월에 100건 — 쓰는 기능과 안 쓰는 기능의 차이가 뚜렷하다
    - 안 쓰인 이유가 구조적이다. 교과 → 단원 전체 → 주당 시수 → 주차 배분을 모두
      입력해야 비로소 진도율 숫자가 나온다. 반면 학급일지·학생부는 입력한 것 자체가
      결과물(NEIS 붙여넣기·출력물)이다. 투입과 보상이 뒤집혀 있다
- **함께 사라진 것** — 두 페이지가 수업일정 자동배치 계열의 유일한 진입점이었다.
  `SchedulingEngine`·`ScheduleShiftService`(밀기/당기기)·`UndoHistory`·
  `Schedule`/`ScheduleUnitMap` 모두 여기서만 구동됐다
    - ⚠ **진짜 시간표는 그대로다** — `ClassTimetable` 계열(교사 시간표·학급 시간표·
      오늘 화면·학급일지)과 학사일정(`SchoolSchedule`)은 무관하다. 여기서 지운
      `Schedule` 은 "언제 어느 단원을 나갈지" 자동 배정하던 연간계획 전용 테이블이다
- **단원 관리는 살렸다** — 연간계획 페이지의 첫 탭에만 있어서 그대로 지우면 단원을
  만들 방법이 사라진다(수업일지가 `CourseSectionNo` 로 참조). 수업 관리의 교과 카드에
  "단원 관리" 버튼을 붙여 독립 페이지로 분리했다. 목록 CRUD·드래그 정렬·CSV 입출력 유지
- **`CourseSection` 20 → 12컬럼** — 실사용 25행에서 `LessonPlan`·`IsPinned`·
  `PinnedDate`·`LearningObjective`·`MaterialPath`·`MaterialUrl`·`Memo` 는 **전부 공백**,
  `SectionType` 은 **전부 기본값**이었다. 이들을 쓰던 기능이 사라졌으므로 함께 정리했다
  (편집 다이얼로그 입력 UI·CSV 열·인덱스 2개 포함)
- 삭제된 기능 전용 테스트 7파일 제거. 테스트 421 → 390개, 빌드 경고 0
- 검증: 초기화기만 돌린 새 DB 의 `CourseSection` 이 정확히 12컬럼, 테이블 31 → 24개

### 배포 전 마이그레이션 정리 (2026-08-15)
1.0 을 첫 배포로 잡았으므로, **배포된 적 없는 스키마를 위한 하위호환 코드**를 전부 걷어냈다.
컬럼 추가 마이그레이션은 지우기 전에 `CREATE TABLE` 정의로 접어 넣었다 — 빠뜨리면
기존 개발 DB 에는 컬럼이 남아 있어 모른 채 지나가고 **새로 만드는 DB 에서만** 사라진다.

- `CREATE TABLE` 로 접어 넣은 컬럼 11개
    - `CourseSection` 8개(LessonPlan·SectionType·IsPinned·PinnedDate·LearningObjective·
      MaterialPath·MaterialUrl·Memo) — `EndPage` 는 CREATE 와 ALTER 양쪽에 중복돼 있었다
    - `ClassDiary` CreatedAt·UpdatedAt, `KCalendarList` SyncToken
- 제거한 마이그레이션 — `LessonLog` 컬럼 7종, `Post` IsCompleted·PlainText,
  `Post` Memo→Category 이관 2종, `Comment` ReplyOrder→ParentNo 개명,
  `KEvent`/`KCalendarList` 컬럼 5종, `StudentSpecial.Semester` 추가+백필,
  `LessonProgress` 외래키 재작성
- `PRAGMA user_version` 기록은 **남기고 1(= v1.0.0 출시 스키마)로 되돌렸다.**
  1.0 이 나간 뒤 스키마를 바꾸려면 "이 파일이 어느 세대인지" 판별할 수단이 필요하다
- **검증** — 실사용 DB 3개(school·board·scheduler)의 스키마를 기준선으로 뜬 뒤,
  초기화기만 돌린 새 DB 와 컬럼 집합을 대조했다: **36개 테이블 전부 일치, 불일치 0건**
  (순서만 다른 곳이 둘 있으나 SQLite 는 이름으로 조회하므로 무관)
    - 예전에 ALTER 로만 붙던 컬럼 18개를 `SchemaOwnershipTests` 에 못박아 회귀 테스트로 남겼다
- 함께 제거한 테스트 — `StudentSpecialSemesterMigrationTests`(마이그레이션 자체를 검증),
  `LessonProgressCascadeTests.구스키마_DB_는_초기화_시_외래키가_교정된다`
- 참고: 실사용 DB 에는 코드가 더 이상 만들지 않는 잔여 테이블이 있다
  (school: Attachment·Evaluation·TodoItem·WorkLog / scheduler: Ktask·KtaskList).
  **코드 참조 0건**이며 이번 변경과 무관한 옛 흔적이다

### 자리 배정 — 저장된 줄·짝이 헤더 도트에 안 뜨던 문제 (2026-08-15)
지난 배치를 불러오면 좌석은 제대로 그려지는데 헤더의 도트 패턴만 늘 "5줄·짝 없음" 이었다.
NumberBox 는 복원된 줄 수를 보여주고 있어서 둘이 서로 어긋났다.

- 원인은 `InitializeData()` 의 **순서**였다. 콤보박스에 학년을 채우면서 `SelectedItem` 을
  대입하는데, 이게 `SelectionChanged`(async void) 사슬을 발동시켜
  `LoadStudentsAsync` -> `TryLoadSavedArrangementAsync` 까지 이어진다.
  `Microsoft.Data.Sqlite` 의 `*Async` 는 대개 완료된 Task 를 돌려줘 `await` 가 양보하지 않으므로,
  **그 사슬이 대입 지점에서 동기적으로 끝까지 실행되며 저장값을 복원**한다.
  그런데 기본값 `_jul = 5; _jjak = 1;` 대입이 그 **뒤에** 있어서 복원 결과를 곧바로 덮어썼다
    -> 기본값을 콤보박스 채우기 앞으로 옮겼다. 어느 쪽이 먼저 끝나든 결과가 같아진다
- 저장값 방어 추가 — `Jjak` 이 0 인 배치가 실제로 존재하는데(구 데이터·초기화 실패),
  그대로 쓰면 `new string('●', 0)` 이 빈 문자열이라 **헤더에서 점이 사라지고**
  `InitSeats` 도 `_jjak <= 0` 가드에 걸려 좌석을 아예 안 그린다.
  이제 `Jjak` 은 1·2 로, `Jul` 은 NumberBox 범위(2~8)로 맞춰서 받는다
    - 같은 원인(DB 의 Jul·Jjak = 0)으로 인쇄 경로가 이미 한 번 깨져 `SeatGridGuardTests` 가
      있다(26·36차 전수조사). 이번은 화면 로드 경로였다
- `Rows` 는 저장돼 있지만 `InitSeats` 가 학생 수로 다시 계산하므로 쓰지 않는다는 점을
  주석에 명시(기존 주석은 "Rows 반영" 이라고 적혀 있었으나 반영한 적이 없다)

### 게시판 — 첨부 유실·조회수·죽은 캐시 (2026-08-15)
- **첨부 파일이 조용히 덮어써지던 문제** — 이름이 같은 파일을 한 글에 둘 붙이면 뒤엣것이
  앞엣것을 지웠다(DB 에는 첨부 2건, 실물은 1개). 저장 이름의 유일성이 **초 단위 타임스탬프**
  에만 걸려 있었고, 새 첨부는 붙일 때가 아니라 **저장 버튼을 눌렀을 때 한 루프에서 한꺼번에**
  복사돼 전부 같은 초를 받는다 -> 다른 폴더의 동명 파일 두 개는 반드시 충돌했다
    - 충돌 해소를 OS 에 맡기고(`GenerateUniqueName`), `CopyAsync` 가 돌려준 **실제 저장 이름**을
      DB 에 넣는다. 게시글·댓글·메모 첨부 3곳 모두 수정
    - `PhotoService` 도 `ReplaceExisting` 을 쓰지만 학생 사진을 같은 경로에 갈아끼우는
      **의도된 교체**라 제외. 이미 덮어써진 파일은 복구할 수 없다
- **조회수가 DB 보다 항상 1 작게 보이던 문제** — 조회수를 올리기 전에 읽은 객체를 그대로
  돌려주고 있었다(자기 열람이 반영 안 된 숫자). 회귀 테스트 2개 추가
- 한 번도 저장된 적 없는 캐시 키(`CacheKeys.PostCount`)와 그것을 지우던 무효화 코드 제거
- 게시글 답글 3필드(`RefNo`·`ReplyOrder`·`Depth`)에 미사용 경위 주석 — 값을 넣는 코드도
  답글 UI 도 없다. 1인용 메모·자료 보관함이라 되살리지 않기로 했고(덧붙이기는 댓글 +
  1단계 대댓글이 담당), DB 컬럼은 `DEFAULT 0` 이라 그대로 둔다
- 테스트 421개 통과

### 스케줄러 — 표시 순서·필터·트랜잭션 (2026-08-15)
- **종일 항목이 오전·오후 일정 사이에 끼어 보이던 문제** — `KEvent.Start` 에 형식이 두 가지
  섞여 있다(종일=로컬 날짜 `yyyy-MM-dd`, 시간=UTC `…THH:mm:ss.fffZ`). `WHERE` 는 `IsAllday`
  로 분기하는데 `ORDER BY Start` 에는 그 분기가 없어 두 형식이 한 축에서 바이트 비교됐다.
  KST 기준 8/15 08:00 은 `2026-08-14T23:00…` 이라 종일 `2026-08-15` 보다 앞서고,
  8/15 20:00 은 뒤에 온다
    -> 로컬 `DateTime` 기준으로 메모리에서 재정렬(`SortForDisplay`). 하루 안에서 종일 먼저,
       그다음 시각순, 동시각이면 제목순. 회귀 테스트 6개 추가
- **달력 셀의 할 일 필터가 아무것도 거르지 못하던 것을 제거** — `!t.IsDone || Settings.ShowTasks`
  는 `ShowTasks`("할 일 표시/숨김")의 의미를 잘못 읽은 조건이라 어느 상태에서도 무효였다
  (켜져 있으면 전부 통과, 꺼져 있으면 `Kcalendar` 가 이미 상류에서 걷어내 목록이 비어 있음).
  표시 여부 판정을 조회 단계 한 곳으로 모았다
- **셀 표시 경로가 둘로 갈려 있던 것을 하나로** — `UpdateDayDisplayAsync` 가
  `UpdateDayDisplaySync` 를 복제해 갖고 있었고 `UpdateColorDisplay` 호출만 빠져 있었다.
  이 갈림 때문에 이미 한 번 버그가 났던 자리다 -> 비동기 쪽은 UI 스레드 보장만 하고
  실제 갱신은 동기 메서드에 위임
- **반복 일정 "이후 모두 삭제"가 트랜잭션 없이 돌던 문제** — 중간에 실패하면 시리즈가 반만
  지워진 채 남았다(생성 경로는 이미 `UnitOfWork` 로 원자적이었다). 항목마다 `GoogleId` 를
  보려고 던지던 재조회도 제거(조회 결과에 이미 들어 있다)
  -> 365개 시리즈 기준 쿼리 731회 -> 366회
- **달력 셀 채우기 O(42 × N) -> O(N)** — 42개 셀마다 전체 목록을 다시 훑던 것을 날짜별 버킷
  1회 생성으로 대체(다중일 이벤트는 표시 창 안에서만 전개)
- `SchedulerService` 의 try/catch 위임 20곳 제거 — 리포지토리가 이미 `LogError` 후 rethrow 해
  로그만 두 번 남았다. 같은 파일의 나머지 절반이 쓰던 한 줄 위임 스타일로 통일
- 스키마·DB 무변경. 순 -148줄, 테스트 419개 통과

### 기록 리포지토리 기반 통일 (2026-08-15)
"학생기록·학생부·수업일지·학급일지를 게시판으로 통합할 수 있나"를 검토한 결과
**모델 통합은 하지 않기로 하고**, 실제 중복이 있던 리포지토리 층만 정리했다.

- 통합을 접은 이유 — Board 는 별도 DB(`board.db`)라 `Student`/`Teacher`/`Course` FK 가 전부
  끊기고(SQLite 는 파일 간 FK 불가), 4종의 구조화 필드·`ClassDiary` 의 하루 1건 UNIQUE 제약·
  NEIS 바이트 한도 로직이 `Category` 문자열 하나로 뭉개진다
- **`LessonLogRepository` 를 `BaseRepository` 로 편입** — 형제 3종(StudentLog·StudentSpecial·
  ClassDiary)은 이미 상속 중인데 이것만 `IDisposable` 을 직접 구현하고 맨 연결을 열어서
  **WAL·`foreign_keys=ON`·busy_timeout 이 적용되지 않았다**
    - FK 가 켜지므로 `TeacherID` 빈 문자열을 NULL 로 바꿔 넣는다(빈 문자열은 NULL 이 아니라
      제약 위반이 된다 — 형제 리포지토리와 같은 처리). `Lesson` FK 는 값을 넣는 곳이 없어 무관
- `StudentSpecialRepository` 의 수동 reader 루프 10곳을 같은 base 의 `ExecuteListAsync` 로
  통일 — 컬럼 인덱스 캐싱을 타게 되어 `GetOrdinal` 반복 호출이 사라진다
- **잠복 버그 2건 수정** — `LessonLog.TeacherID`·`Subject` 와 `StudentSpecial.TeacherID`·`Tag`
  를 NULL 검사 없이 `GetString` 하고 있었다. `CleanupOrphansAsync` 가 교사 삭제 시
  `TeacherID` 를 NULL 로 만들기 때문에 실제로 도달 가능한 `InvalidCastException` 이었다
- **소비처가 없던 모델 인터페이스 6종 제거**(`Models/Interfaces.cs` 삭제) — `IEntity`·
  `IDeletable`·`IYearSemesterEntity`·`ITeacherRecord`·`IDailyRecord`·`IStudentRecord`.
  25개 모델이 구현만 하고 있었고 이 타입으로 동작하는 코드는 없었다(제네릭 제약은 전부
  `where T : class`, 리플렉션·XAML 참조 없음)
    - 미사용에 그치지 않고 **틀린 사실을 만들고 있었다** — `IDailyRecord` 때문에 `LessonLog` 에
      "ClassDiary 와 공통 기반 공유" 주석이 붙어 있었지만 공유되는 코드는 없었다
- 스키마·DB·UI 무변경. 순 -122줄(29파일), 테스트 413개 통과

### 구글 캘린더 권한 최소화 (OAuth 검증 대응) (2026-08-13)
Google OAuth 민감 스코프 검증을 앞두고, 요청 권한을 **실제로 호출하는 API 범위까지 좁혔다**.

- 전체 권한 `auth/calendar` 하나 -> 세분화 3종
  (`calendar.calendarlist.readonly` + `calendar.calendars` + `calendar.events`)
    - 앱은 캘린더 공유 설정(ACL)을 바꾸거나 캘린더 자체를 삭제하지 않는데,
      전체 권한은 그것까지 허용한다. 코드 전수 조사로 실사용 API 6개
      (`calendarList.list`, `calendars.insert`, `events.list/insert/update/delete`)를
      확인하고 거기에 맞췄다 -> **앱 동작은 달라지지 않는다**
    - 기존 사용자는 재연동이 필요 없다. 이미 받아둔 전체 권한 토큰이 세분화 3종을 포함한다
- **부분 동의(granular consent) 처리 추가** — 스코프가 여러 개면 구글 동의 화면에
  체크박스가 항목별로 뜨고 사용자가 일부만 허용할 수 있다. 그대로 두면 앱은 "연동됨"인데
  동기화만 403 으로 조용히 실패한다
    -> 토큰 교환 응답의 `scope` 를 검사해 부족하면 **토큰을 저장하지 않고 즉시 revoke**,
       빠진 권한이 무엇인지("일정 조회·등록·수정·삭제" 등) 이름으로 알려 재연동을 유도한다
    - `GoogleTokenResponse.Scope` 는 그동안 파싱만 하고 아무도 읽지 않던 필드였다
    - 인증 실패 사유를 `LastAuthError` 로 노출해, 설정 화면이 "인증에 실패했습니다" 대신
      실제 이유(권한 취소·시간 초과·부분 동의)를 보여준다
- 개인정보처리방침(`Privacy.html`) 1~4항 교체
    - **요청하지도 않는 데이터를 수집한다고 적혀 있었다** — "이메일 주소, 사용자 이름,
      프로필 이미지 URL". 앱은 `openid`·`userinfo.*` 스코프를 요청하지 않는다.
      심사는 등록 스코프와 방침을 대조하므로 그대로 두면 반려 사유다
    - 스코프 목록, 로컬 저장·DPAPI 암호화, 삭제 경로 3가지, Limited Use 준수 문구 추가
    - Calendar 는 restricted 가 아니므로 "Restricted Scopes 포함" 문구는 뺐다
- 검증 신청 자료를 `docs/oauth-calendar-verification.md` 에 정리
  (스코프 결정 근거·코드 인용, 스코프별 목적 설명 국문/영문, 데모 영상 촬영 체크리스트)

### WinUIRichEditor 1.0.0 -> 1.1.0 (2026-08-08)
- 게시 실행파일이 28.51MB -> 28.82MB 로 **+312KB(+1.09%)** 늘었다. 대부분 이 상승분이다
  (편집기 IL 이 463KB -> 513KB 로 +50KB, Native AOT 는 IL 을 네이티브로 펼치며 몇 배가 된다).
  나머지는 QuestPDF 마이너 상승과 이번 세션 코드
- **배포 모델은 그대로다** — 1.1.0 의 `Microsoft.WindowsAppSDK.WinUI` 하한선은 여전히 2.2.1 이라
  메타패키지 2.3.1 이 주는 WinUI 2.3.0 을 넘지 않는다. 게시 폴더도 8개 파일 그대로.
  (하한선이 2.3.0 을 넘으면 프레임워크 의존 게시가 깨진다 — 편집기 버전을 올릴 때마다 확인할 것)


### 학기 저장 불가 + 구글 캘린더 등록 실패 무음 (2026-08-08)
사용자 제보 2건. 테스트 401 -> 402개, 빌드 경고 0.

- **학교 설정에서 학기를 바꿔도 저장되지 않았다** — XAML 의 `Tag="1"` 은 **문자열**인데
  코드가 `(int)item.Tag` 로 언박싱하고 있었다. 학기를 고를 때마다
  `InvalidCastException` 이 나고 **그 줄에서 멈춰 바로 다음의 저장이 실행되지 않았다**
    -> `int.TryParse(item.Tag?.ToString(), ...)` 로 읽는다
    - 같은 함정을 막는 회귀 테스트 추가 — XAML 이 `Tag` 를 문자열로 쓰는 화면에서
      코드가 `(int)` 로 언박싱하면 실패한다. 컴파일러가 못 잡는 종류다
    - `LessonLogEditDialog` 도 한 파일 안에 두 방식이 섞여 있어 같이 정리했다
      (교시 콤보는 XAML 문자열 `Tag`, 단원 콤보는 코드에서 int — 오늘은 우연히 안전했다)
- **할 일·일정에서 "구글 캘린더에 등록"을 체크해도 아무 일이 없었다** — Push 실패를
  통째로 삼키고 있어서, 등록이 안 됐다는 사실조차 알 수 없었다
    -> 실패하면 이유와 함께 알린다(로컬 저장은 그대로 유지되고 다음 자동 동기화에서 재시도)
    - 캘린더가 구글과 연결되지 않은 경우, 인증 정보가 없는 경우를 각각 구분해 안내
    - 구글이 **오류 본문**을 돌려주면 `GoogleEvent` 로 역직렬화돼도 `Id` 가 비어 있는데,
      예전에는 이 경우를 그냥 넘겨 조용히 성공처럼 끝났다 -> 실패로 처리
- **반복 할 일이 첫 건만 구글에 올라갔다** — `ResultEvent` 는 대표 1건만 담는데
  그것만 Push 했다 -> 생성된 반복분 전부를 올린다
- **저장 버튼을 누르면 한참 멈춰 있었다** — `args.GetDeferral()` 이 대화상자를 붙들고 있는데
  그 안에서 구글 왕복을 `await` 했다. 반복 항목 수만큼 순차 왕복하고, 실패하면
  재시도 백오프(2·4·8초)까지 겹쳐 더 길어졌다
    -> 로컬 저장이 끝나면 창을 바로 닫고 업로드는 배경에서 진행한다.
       실패는 모달 대신 전역 InfoBar 로 알린다(이 시점엔 창이 이미 닫혀 있다)
    - 배경 처리로 바꾸면서 생긴 경합도 함께 막았다. 업로드가 끝날 때
      전체 행을 쓰는 `UpdateEventAsync` 로 되돌려 쓰면, 그동안 사용자가 같은 항목을
      수정한 내용이 조용히 사라진다 -> `GoogleId`·`Updated` 두 열만 갱신한다.
      회귀 테스트 2건(전체 행을 쓰게 되돌리면 실패하는 것까지 확인)
    - 업로드 중 앱을 닫아도 `GoogleId` 가 비어 있어 다음 자동 동기화가 집어 간다


### 배치 검사가 오류를 못 잡던 문제 (2026-08-08)
28차에 관찰만 하고 넘어간 건을 처리했다. 테스트 398 -> 401개, 빌드 경고 0.

- **배치 검사의 ❌ 분기가 한 번도 실행되지 않았다** — `ValidationResult.Errors` 를 채우는
  코드가 한 군데도 없어서 `IsValid` 가 늘 true 였다. 검사 화면은 ❌(고쳐야 함)와
  ⚠️(확인만 하면 됨)를 구분하도록 만들어 놓았는데, 실제로는 경고만 나왔다
    -> 실제로 배치가 성립하지 않는 두 가지를 오류로 보고한다
        - **이 수업의 단원이 아닌 배치** — 단원을 옮기거나 지운 뒤 매핑이 남은 경우.
          배치 목록에는 나오는데 단원 목록에는 없어서 시수만 먹고 진도에도 안 잡힌다
        - **휴일에 배치된 수업** — 배치한 뒤 학사일정이 바뀌면(개교기념일 추가 등)
          멀쩡하던 배치가 휴일에 걸린다. 그 날은 수업이 없으므로 배치가 성립하지 않는다
    -> 오류가 있으면 대화상자 제목에 건수를 넣고, ❌ 의 의미를 한 줄 덧붙인다
- **학기 마지막 날의 휴일이 무시되고 있었다** — 슬롯 생성은 종료일을 **포함**해서 도는데
  휴일 조회(`GetByDateRangeAsync`)는 `[시작, 종료)` 반개구간이라 **마지막 날만 빠졌다**.
  그래서 학기 마지막 날이 휴일이어도 수업이 배치됐다.
  자동배치·미리보기·밀기/당기기 모두 같은 어긋남이었다 -> 하루를 더해 포함시킨다
- `SchedulingEngine` 이 휴일 조회에 쓰는 학교 코드를 인자로 받게 했다. 전역 설정을
  직접 읽으면 설정이 초기화되지 않은 환경에서 조회가 통째로 실패해 "학사일정을 읽지 못했다"는
  경고로만 나타나고, 휴일 관련 동작을 검증할 수 없었다
    - 단, 설정 해석은 종전대로 `try` 안에서 한다 — 밖으로 빼면 예외가 새어 "배치 오류"로 둔갑한다
      (실제로 그렇게 만들어 회귀 테스트 5건이 깨지는 것을 확인하고 되돌렸다)


### 시연용 자리 배정 (2026-08-08)
**자동배정** 버튼을 **오른쪽 클릭**하면 **앞자리 우선 · 짝 고정 · 짝 분리를 적용하지 않고**
배정한다. 학생들 앞에서 자리를 뽑아 보일 때 쓰는 기능이다 — 이 세 조건은 교사가 지정한 것이라
결과에 패턴으로 남아 무작위로 뽑은 것처럼 보이지 않는다.

- 지난 짝 회피·지난 자리 회피는 시연에서도 그대로 적용된다. 결과만 보고는 알 수 없어
  무작위 인상을 해치지 않기 때문이다
- **화면에는 아무 단서도 남기지 않는다** — 툴팁도, 배정 후 안내도, 저장 확인도 없다.
  학생들이 보는 화면이라 "조건이 존재한다"는 사실 자체가 드러나면 안 된다.
  기능의 존재는 안내서에만 적었다
- 왼쪽 클릭(최종 배정)은 종전과 완전히 동일하다


### 36차 전수조사 - 캐시·좌석출력·로깅 (2026-08-08)
마지막까지 남아 있던 미정독 코드(캐시, 인쇄 서비스 본문, 로거, 컨트롤, 유틸)를 읽었다.
테스트 393 -> 398개, 빌드 경고 0.

- **게시글 수정이 반영되지 않아도 성공으로 보고했다** — `BoardService.SavePostAsync` 가
  `UpdateAsync` 의 결과를 버리고 무조건 글 번호를 돌려줬다. 갱신된 행이 없어도
  (이미 지워진 글 등) 편집 화면은 성공으로 보고 창을 닫았다 — 편집이 유실됐다
    -> 반영 여부를 확인하고, 안 되면 실패로 던진다. 메모 자동 저장도 이제 실패를 알린다
- **줄·짝 수가 0 인 좌석 배치를 인쇄하면 출력이 깨졌다** — PDF·HTML·Excel 세 경로가 모두
  `jul * jjak` 으로 나누는데 이 값은 DB 에서 그대로 온다. 26차에 확인했듯 구데이터나 초기화
  실패로 0 이 들어 있을 수 있고, 그러면 0 으로 나뉘어 좌석표가 깨지거나
  QuestPDF 안쪽에서 이유를 알 수 없는 오류로 터졌다
    -> 세 곳 모두 최소 1 로 보정. 회귀 테스트 5건(수정 전 3건 실패 확인)
- **로그 보관본이 영원히 지워지지 않았다** — 로그가 10MB 를 넘으면 `Archive` 폴더로 옮기는데,
  30일 정리는 최상위 폴더만 훑고 있었다. 보관본이 무한히 쌓였다 -> 하위 폴더까지 정리
- `DateTimeHelper.FromDateString` 의 마지막 폴백만 현재 문화권으로 파싱하고 있었다
  (파일의 다른 파싱은 전부 `InvariantCulture`). 로캘에 따라 "01/02/2026" 의 월·일 해석이
  달라질 수 있어 통일

- 확인만 하고 넘어간 것
    - `CacheManager` 회계는 정확하다(교체 시 이전 크기 차감, LRU 축출·만료 제거 모두 반영).
      다만 싱글턴인데 `Dispose()` 가 공개라, 누가 `using` 으로 받으면 정리 태스크가
      영구히 멈춘다 — 지금은 필드로만 보관해서 문제 없다
    - `PhotoCard` 의 사진 로딩은 토큰으로 stale 응답을 막고 있어 이상 없음
    - `KEvent`·`Post` 캐시 무효화는 write-through 로 맞물려 있음
    - `DateTimeToStringConverter.ConvertBack` 도 현재 문화권을 쓰지만 단방향 바인딩에서만
      쓰여 호출되지 않는다 — 건드리지 않았다


### 35차 전수조사 - 캘린더·인쇄·내보내기 (2026-08-08)
한 번도 정독한 적 없던 영역(캘린더 본체, 인쇄 3종, `Settings`, 교사 관리, 컨트롤)을 읽었다.
테스트 373 -> 393개, 빌드 경고 0.

- **진로활동 희망분야가 출력물에서 통째로 빠졌다** — 화면 목록은 "과목/분야" 칸에
  희망분야(진로)와 학기(교과 "국어 (2학기)")를 보여주는데, **인쇄 PDF 2종·엑셀·HTML 4곳은
  `SubjectName` 만** 찍었다. 그런데 분량(Byte)은 희망분야까지 합산해 세므로
  **인쇄물만 보면 글자 수가 안 맞았다**
    -> 규칙을 `NeisHelper.BuildSubjectDisplay` 한 곳으로 모으고 목록·인쇄·내보내기가 함께 쓴다.
       표 머리글도 "과목" -> "과목/분야". 회귀 테스트 12건
    - 27차에 바이트 **기준**은 통일했는데 **표시 내용**은 통일하지 않아 남아 있던 짝이다
- **달력에서 일정이 통째로 안 보여도 알려주지 않았다** — 학사일정·일정/할 일 조회가 실패하면
  `Debug` 로그만 남기고 빈 목록으로 넘어가, "그 달에 아무 일도 없는" 것처럼 보였다
    -> 홈 화면과 같은 방식으로 전역 InfoBar 에 "빈 달력이 아니라 조회 실패"라고 표시
- **달력 초기화가 한 번 실패하면 영구히 무반응이었다** — `_isInitialized` 가 false 로 남는데
  `Loaded` 구독은 해제돼서, 이전/다음 달 버튼과 월 선택이 전부 조용히 먹통이 되고
  되살릴 방법이 없었다 -> 실패해도 조작은 살려 둔다(달을 옮기면 다시 시도)
- **학생 이름·과목명이 그대로 파일명에 들어갔다** — 둘 다 사용자가 입력하는 값이라
  "국어/문학" 같은 과목명이면 엉뚱한 경로가 되어 내보내기가 이유 없이 실패했다
    -> `FileNameHelper.Sanitize` 로 7곳 정리. 회귀 테스트 8건
- **초등학교에서 학년 선택이 1~3 만 보일 수 있었다** — 학년 목록 조회가 비었거나 실패하면
  1~3 으로 폴백했다. 학생 추가 화면은 29차에 1~6 으로 넓혔는데 이 선택기만 남아 있었다

- 확인만 하고 넘어간 것
    - `KEventRepository` 는 이상 없음(전 컬럼 `NOT NULL DEFAULT` 라 매핑도 안전,
      종일/시간 이벤트의 저장 형식별 경계 비교도 일관)
    - `Settings` 의 초기화·백업·복원 경로 이상 없음(초기화는 `LoadAll` 로 메모리 값까지 되돌리고,
      복원은 재시작을 안내한다)
    - **`TeacherService` 는 12개 중 10개가 죽은 코드**(초기 설정의 `CreateAsync`·`AddHistoryAsync` 만
      쓰인다). 그 둘은 트랜잭션 없이 이어져 있어, 이력 저장이 실패하면 교사 행만 남고
      재시도 시 새 ID 로 한 건 더 생긴다 — 초기 설정 1회성이라 이번엔 손대지 않았다


### 죽은 코드 제거 - 600줄 (2026-08-08)
34차에서 "호출부 0건"으로 확인해 둔 것들을 지웠다. 테스트 375 -> 373개(지운 코드만 검증하던
`TimetableServiceTests` 2건이 함께 빠졌다), 빌드 경고 0.

- **`TimetableService` 의 쓰기 5개**(Course 생성/수정/삭제, 학급 시간표 일괄 등록/삭제) —
  화면들이 같은 일을 직접·불안전하게 하고 있어서, 정작 이 **안전한(트랜잭션) 구현이 죽은 채**였다.
  화면 쪽을 트랜잭션화한 지금은 중복이므로 지운다. 조회 2개는 그대로 쓰인다
    - 이것만 쓰던 `TimetableUnitOfWork`(175줄)도 함께 제거
- **`Scheduler` 의 정적 헬퍼 5개**(`GetTasksAsync`·`InsertTaskEventAsync`·`UpdateTaskEventAsync`·
  `DeleteTaskEventAsync`) — 화면들은 `CreateService()` 로 `SchedulerService` 를 직접 쓴다
- **`ClassDiaryViewModel` 의 학생 로그 메서드 5개**와 그것만 쓰던 `ClassDiaryService` 의 2개 —
  학생 로그의 저장·삭제는 화면이 `LogListViewer` 를 통해 직접 한다.
  쓰이지 않게 된 `_logRepo` 필드와, 예전 정리에서 남았던 빈 region 도 함께 정돈

필요해지면 커밋 `decf101` 이전 이력에서 되살릴 수 있다.


### 34차 전수조사 - 진도·수업·일정 (2026-08-08)
아직 정독한 적 없던 **진도 매트릭스·수업 관리·일정 대화상자**를 훑었다.
테스트 364 -> 372개, 빌드 경고 0.

- **진도 기록이 있는 수업은 삭제가 영영 실패했다** — `LessonProgress` 의 외래키 두 개에
  `ON DELETE` 절이 없어 NO ACTION 이었다. 그래서 진도 매트릭스에서 완료/보강/건너뛰기를
  한 번이라도 누른 수업은
    - 수업 삭제(`Course` -> `CourseSection` CASCADE 가 자식을 지우는 순간 위반),
    - 단원 삭제, 단원 전체 삭제,
    - "일정 동기화"를 쓴 뒤의 **시간표 일정 삭제**

  가 모두 `FOREIGN KEY constraint failed` 로 막혔다. 화면에는 이유를 알 수 없는 오류만 떴다
    -> 단원은 `ON DELETE CASCADE`(단원이 사라지면 그 진도도 의미가 없다),
       일정은 `ON DELETE SET NULL`(일정은 진도의 부가 정보일 뿐이라 삭제를 막으면 안 된다)
    -> 기존 DB 는 앱을 다시 켜면 자동으로 교정된다(`PRAGMA user_version` 2 -> 3,
       표를 새로 만들어 옮겨 담는다). 회귀 테스트 4건 — 구스키마 DB 를 재현해
       고쳐지는지까지 확인
- **보강·건너뛰기·병합이 실패해도 성공 메시지가 떴다** — 리포지토리 반환값을 버리고
  무조건 건수를 세는 데다, 페이지도 결과를 보지 않아 한 건도 저장되지 않았는데
  "0개 단원 보강 처리 완료"라고 알렸다(완료/미완료 처리는 21차에 고쳤는데 형제들이 남아 있었다)
    -> `MarkAsMakeupAsync`/`MarkAsSkippedAsync` 가 반영 여부를 돌려주고,
       시도 건수와 반영 건수가 다르면 "N개 중 M개만" 이라고 알린다. 회귀 테스트 4건
- **격차 분석이 실패하면 "최대 격차 0단원"으로 보였다** — 실패해도 통계가 0 인 결과를 그대로
  띄워서, 모든 학급 진도가 같다는 **정반대의 안심**을 줬다 -> 실패를 실패로 표시
- 진도 매트릭스 로드 실패도 마찬가지로 빈 화면이 돼 "단원이 없는 수업"과 구분되지 않았다 -> 표면화
- **저장 도중 실패하면 기존 시간표가 통째로 사라졌다** — 학급 시간표 편집·수업 시간표 배치·
  주차별 시수 계산이 모두 "전체 삭제 -> 하나씩 다시 저장"인데 **트랜잭션 밖**이었다.
  중간에 실패하면 원본은 이미 없고 새것은 일부만 남았다(창은 열려 있지만 되돌릴 것이 없었다)
    -> 세 곳 모두 한 트랜잭션으로 묶어 실패 시 원래대로 되돌린다.
       주차별 시수 계산은 실패를 `Debug` 로만 남겨서 시수표가 왜 비었는지 알 수 없었는데,
       이제 사용자에게 알린다
- **수강생 배정은 부분 실패 후 다시 저장할 수 없었다** — 등록/해제/강의실 변경 세 단계가
  건별로 즉시 반영돼서, 중간에 실패하면 앞부분만 저장된 채 창이 남았다. 그런데
  `UNIQUE(StudentID, CourseNo)` 때문에 다시 저장을 누르면 이미 들어간 학생에서 또 실패해
  **영영 저장할 수 없었다** -> 한 트랜잭션으로 묶었다
- **일정 삭제가 실패해도 창이 그냥 닫혔다** — 결과도 예외도 보지 않아, 지워지지 않았는데
  지운 것처럼 보였다(목록을 다시 읽으면 되살아났다) -> 삭제 결과를 확인하고 실패하면 창을 유지
- **일정 저장 실패는 아무 말도 없었다** — 창만 남아서 저장 버튼이 안 먹는 것처럼 보였다 -> 안내 추가
- **목록 보기의 할 일 완료 토글이 무음으로 어긋났다** — 화면은 완료인데 DB 는 미완료로 갈라진 채
  다음 새로고침에 소리 없이 되돌아갔다(달력 셀은 20차에 고쳤는데 목록 보기만 남아 있었다)
    -> 실패 시 체크 상태를 되돌리고 알린다
- 확인만 하고 넘어간 것
    - `ON DELETE` 절이 없는 나머지 외래키는 전부 `SchoolCode -> School` 뿐이다.
      학교 행은 지우지 않는 데다 초기화기가 없으면 만들어 주므로 무해
    - `TimetableService` 의 쓰기 메서드 5개는 **호출부가 0건**이다. 위에서 고친 대화상자들이
      같은 일을 직접·불안전하게 하고 있었다(안전한 구현이 죽은 코드로 있었던 셈).
      `Scheduler` 의 `UpdateTaskEventAsync`/`InsertTaskEventAsync`/`DeleteTaskEventAsync` 도 호출부 0건
    - `Microsoft.Data.Sqlite` 는 공유 캐시가 아닌 연결에서 외래키를 자동으로 켠다 —
      `LessonProgressRepository` 가 `PRAGMA foreign_keys` 를 직접 켜지 않는데도 제약이 도는 이유


### 34차 후속 - "저장했다고 말하지만 저장 안 된" 지점 전수 (2026-08-08)
같은 결함을 앱 전체에서 훑었다. **기록이 조용히 사라지는** 부류라 특히 학생부·누가기록에 위험했다.
테스트 372 -> 375개, 빌드 경고 0.

- **학생부 기록** — 개별 입력 상자(`StudentSpecBox`)·목록(`학생부 기록`)·교과별 화면·일괄 입력이
  모두 저장 결과를 버리고 무조건 성공 처리했다. 갱신된 행이 없어도(예: 다른 화면에서 이미 지운 기록)
  **변경 표시가 사라져서**, 사용자는 저장됐다고 믿고 창을 닫았다
    -> 반영된 항목만 저장 표시하고, 남은 것은 "N개 중 M개만 저장됐습니다"로 알린다.
       삭제도 실제로 지워진 것만 센다
- **일괄 저장의 "하나라도 실패하면 전체 롤백"이 지켜지지 않았다** — 예외에만 걸려 있고
  0행 갱신은 그대로 커밋됐다 -> 반영 안 된 기록이 있으면 롤백. 회귀 테스트 추가
- **수업 기록·학생 누가기록**의 저장/삭제도 같은 문제였다. 특히 삭제는 DB 결과와 무관하게
  목록에서 지워서, 새로 고치면 기록이 되살아났다 -> 지워진 것만 목록에서 뺀다
    - 학생을 바꾸기 직전의 "저장할까요?" 확인 저장도 실패 시 선택을 풀어 편집을 버렸다 -> 표시 유지
- **학생 정보 일괄 저장**이 실패 집계를 갖고도 무의미했다 — 저장 결과를 안 봐서
  0행 갱신이 성공에 포함됐다. `StudentDetailService.CreateOrUpdateAsync` 가 갱신 결과와 무관하게
  기존 번호를 돌려주고 있어서, 호출부가 반환값을 검사해도 실패를 알 수 없었다 -> 둘 다 정정
- **학생 관리에서 이름만 안 바뀔 수 있었다** — 정본(`Student`) 갱신 결과를 안 봐서,
  학적(`Enrollment`)만 새 이름이 되면 **화면마다 이름이 다르게** 보일 수 있었다
- **초기 설정·설정 화면의 학교 저장**이 실패해도 그대로 넘어갔다(설정>학교정보는 계속 옛 정보) -> 정정
- **단원 추가·수정**이 결과를 안 봤다(형제인 수업 편집 대화상자는 원래부터 확인하고 있었다)
- **구글 캘린더 매핑·동기화 설정**이 저장 안 돼도 "완료"라고 알렸다 -> 반영된 개수만 보고
- **자료 첨부 삭제**가 DB 결과와 무관하게 목록에서 지워, 창을 다시 열면 첨부가 되살아났다

- **테스트가 간헐적으로 실패할 수 있었다** — 전체 실행 중 1회 실패를 봤는데 이후 9회 연속
  실행에서는 재현되지 않았다. 원인은 타이밍이다. `SqliteTestFixture` 가 임시 DB 파일을 지우려고
  `SqliteConnection.ClearAllPools()` 를 부르는데 이건 **프로세스 전역**이라, 마침 병렬로 돌던
  다른 테스트 클래스의 풀 연결까지 끊는다
    -> 테스트를 순차 실행으로 바꿔 원인을 없앴다(전체 5초 -> 22초). 결과를 믿을 수 있는 편이 낫다

- 확인만 하고 넘어간 것
    - 좌석 저장(`SeatService.SaveAsync`)은 트랜잭션 + 예외 전파라 이상 없음
    - 학급일지 저장은 31차에 이미 정리됨. `ClassDiaryViewModel.SaveSelectedLogsAsync`/
      `AddNewLogAsync` 는 호출부 0건(죽은 코드)
    - 게시판 글·댓글 저장은 22차에, 학생카드는 21차에 이미 정리됨


## 개발 이력 — 2026-07-25 ~ 07-31 (구 v1.3.0)

### 33차 전수조사 - 이번 세션 변경분 자체 검토 (2026-07-31)
이번 세션에서 손댄 곳(학적 모델·급식 예외·죽은 코드 삭제·자동 저장)을 되짚었다.
테스트 363 -> 364개, 빌드 경고 0.

- **진급 처리가 실제 데이터에서는 늘 0명이었다** - `PromoteStudentsAsync` 가 진급 대상을
  "**2학기** 재학생"으로 찾는데, 앱은 2학기 학적을 만들지 않는다(학적은 학년 단위).
  회귀 테스트가 2학기 행을 **직접 심어** 통과시키고 있어서 드러나지 않았다
  -> 학년도·학년 기준으로 찾도록 고치고, 테스트 도우미도 앱이 실제로 만드는 1학기 행을 심도록
  현실화했다. 예전에 2학기로 등록된 구데이터도 진급되는지 검증하는 테스트를 추가
- **학기로 거를 수 있는 마지막 통로를 막았다** - `EnrollmentRepository.GetEnrollmentsAsync` 의
  `semester` 인자와 `GetByGradeAsync`(학기 필수 인자)가 아무도 안 쓰는 채로 남아 있었다.
  같은 결함을 다시 부르는 통로라 제거하고, 이유를 주석으로 남겼다
- 확인만 하고 넘어간 것
    - `GetEnrollmentsAsync` 호출부 21곳 전수 재확인 — 위치 인자로 학기를 넘기던 곳은 모두 정리됨
    - 삭제한 타입·메서드가 XAML 에서 참조되는 곳 없음(컴파일러가 못 잡는 경로)
    - `TodayPageViewModel.LoadDataAsync` 는 호출부가 없다(홈 화면은 컨트롤별로 직접 로드).
      그 안의 급식 로드도 함께 죽어 있음


### 알림장 헤더 정리 + 급식 조회 실패 처리 (2026-07-31)
테스트 354 -> 360개, 빌드 경고 0.

- **알림장 헤더를 표에서 두 줄 텍스트로** 바꿨다 — "알림장"(16px 굵게 가운데)과
  날짜(14px 굵게 오른쪽)
- **헤더를 붙였다 떼는 방식을 그만두었다.** 예전에는 편집창을 열 때마다 헤더를 붙이고 저장할 때
  정규식으로 떼어냈는데, 그 정규식이 어긋나면 헤더가 본문에 눌러앉아 편집할 때마다 중복되거나
  반대로 본문 첫 줄이 잘렸다. 이제 **처음 작성할 때만** 헤더를 넣고 그 뒤로는 본문의 일부로 둔다
    - 제거 정규식 4종을 통째로 삭제했다. 일지는 (학년도·학년·반·날짜)로 고정된 행이라
      헤더의 날짜가 나중에 어긋날 일도 없다
    - 사용자가 헤더를 지우면 다시 붙이지 않는다(의도를 거스르지 않는다)
    - 접힌 상태의 미리보기에서는 헤더를 건너뛴다 — 안 그러면 50자 요약의 앞부분을 매번
      같은 글자가 차지한다
    - 삽입 규칙에 회귀 테스트 9건
    - 헤더 뒤에 **빈 문단**(왼쪽 정렬, 14px)을 하나 붙여 바로 입력할 수 있게 했다.
      없으면 커서가 날짜 줄 끝에 놓여 타이핑이 날짜 뒤에 이어 붙는다
- **인터넷이 끊긴 채 급식 상자에서 날짜를 바꾸면 앱이 죽던 문제(회귀)** - 급식 조회 실패를
  예외로 올리도록 바꾸면서, 급식 상자 자체의 날짜 변경 경로를 놓쳤다. `async void` 핸들러라
  미처리 예외가 됐다 -> 사용자 조작 경로는 예외를 잡아 **상자 안에 안내 문구**로 표시한다
  (홈 화면 첫 로드는 종전대로 하단 전역 InfoBar 가 맡는다)
- **인터넷이 끊기면 홈 화면에 경고창이 뜨던 문제** - 급식 조회 함수가 네트워크 없음을
  **직접 모달 대화상자로** 알리고 빈 목록을 돌려줬다. 데이터 조회 함수가 UI 를 띄우는 것도,
  실패를 빈 결과로 감추는 것도 문제였다(그래서 20차에 만든 전역 InfoBar 는 뜨지 않았다)
    - 대화상자를 걷어내고 실패를 예외로 올린다. 타임아웃·NEIS 오류코드도 마찬가지 —
      빈 목록으로 돌려주면 "오늘 급식 없음"과 구분되지 않는다
    - `SchoolMealBox` 도 예외를 삼키고 있어 한 겹 더 막혀 있었다. 화면은 비우되 실패는 올린다
    - 결과적으로 홈 화면은 모달 대신 하단 InfoBar 로 "일부 정보를 불러오지 못했습니다 — 급식"


### 전수조사 후속 정리 (2026-07-30)
32차까지의 전수조사에서 "판단이 필요하다"고 남겨둔 3건을 처리했다. 테스트 354개 유지, 빌드 경고 0.

- **학급일지가 앱을 그냥 닫으면 사라지던 문제** - 저장이 날짜 변경·목록 이동·화면 언로드에서만
  일어나서, 알림장을 쓰고 창을 닫으면 내용이 남지 않았다. 창 닫기 훅은 답이 되지 못한다 —
  핸들러가 동기라 비동기 DB 저장이 프로세스 종료와 경합한다. 대신 **저장 시점을 앞당겼다**:
  ① 입력칸에서 포커스가 빠질 때 ② 입력이 멎고 3초 뒤(디바운스). 둘 중 하나만으로는 구멍이 남는다
- **`ClassDiaryService` 죽은 코드 제거** - 호출부 0건인 메서드 17개와 전용 결과 타입 3종을
  삭제했다(**384줄**). 출결 통계·생활기록 자동 생성·일지 검색/완성도 통계·월간 일괄 생성이
  들어 있었지만 어느 화면도 쓰지 않았고 테스트도 없었다. 남은 공개 API 는 11개
- **홈 화면에서 일부 정보를 못 불러와도 조용했다** - 급식·시간표 로드가 실패하면 Debug 로그만
  남아서 화면상 "오늘은 없음"과 구분되지 않았다 -> 실패한 섹션 이름을 모아 전역 InfoBar 로
  한 줄 안내한다. 20차에 만든 InfoBar 에 재시도 버튼 없는 일반 경고용 진입점을 추가


### 32차 전수조사 - 업데이트·초기 설정 (2026-07-30)
`UpdateService` 와 업데이트 확인 화면 2곳, `InitialSetupWindow` 를 점검했다.
테스트 354개 유지, 빌드 경고 0.

- **업데이트 진행 대화상자가 미관측 태스크였다** - `_ = dialog.ShowAsync()` 로 버려서, 다른 대화상자가
  열려 있을 때 나는 예외가 아무에게도 잡히지 않았다. 게다가 22차에 넣은 **대화상자 직렬화 게이트를
  우회**해 바로 뒤의 결과 대화상자와 충돌할 수 있었다 -> 업데이트 관련 대화상자 4개를 모두
  `MessageBox.ShowDialogAsync` 경유로 바꾸고, 진행 표시는 실패를 로그로 관측한다
- **다운로드 링크가 외부 문자열을 셸에 그대로 넘겼다** - 앱 설정의 다운로드 버튼이
  `Process.Start(..., UseShellExecute = true)` 로 GitHub 응답값을 실행했다. URL 이 아닌 값이 오면
  셸이 그걸 실행한다 -> **http/https 만** 열고, 그마저도 `Launcher` 로 연다(홈 화면과 동일한 방식).
  실패 시 안내도 추가
- 확인만 하고 넘어간 것: `UpdateService` 의 버전 비교(`<Version>1.2.0</Version>` -> 1.2.0.0 과
  태그 `v1.2.0` 비교)는 의도대로 동작한다. `InitialSetupWindow` 는 저장 실패를 예외로 올려
  사용자에게 알리고 있어 이상 없음


### 31차 전수조사 - 학급일지·오늘·학교일정 (2026-07-30)
`ClassDiaryService`·`ClassDiaryBox`·`ClassDiaryPage`·`TodayPage`·`SchoolScheduleService` 를 점검했다.
테스트 354개 유지, 빌드 경고 0.

- **학급일지 저장이 반영 여부를 확인하지 않았다** - 대상 행이 없어 0행 갱신이 나도 "저장 완료"로
  돌아갔고, 호출부는 변경 표시를 지운다. 즉 알림장·메모가 조용히 사라질 수 있었다
  -> 반영을 확인하고 실패면 예외로 알린다
- **자동 저장 실패가 통째로 묻혔다** - 학급일지는 날짜 변경·목록 이동·화면 언로드에서 자동 저장되는데
  그 호출부가 전부 `async void` 라 예외가 아무에게도 잡히지 않았다. 사용자는 저장된 줄 안다
  -> `ClassDiaryBox.SaveDiaryAsync` 가 실패를 직접 처리한다. **변경 표시를 유지**해 다음 기회에
  다시 저장되게 하고 사용자에게 알린다
- 확인만 하고 넘어간 것: `SchoolScheduleService` 는 모든 메서드가 `(Success, Message, ...)` 를
  돌려주고 호출부도 24차에서 정리돼 있어 이상 없음


### 30차 전수조사 - 동아리 (2026-07-30)
`ClubService`·`ClubRepository`·`ClubEnrollmentRepository` 와 동아리 4개 화면을 점검했다.
결함은 전부 **"실패했는데 성공했다고 말하는"** 부류였다. 테스트 354개 유지, 빌드 경고 0.

- **동아리 활동 기록 저장이 결과를 세지 않았다** - 반영 건수와 무관하게 "N건 저장되었습니다"로
  보고했다(0행 갱신도 성공). -> 실제 반영 건수를 세고, 실패한 항목은 선택 상태로 남겨 다시
  저장할 수 있게 했다
- **삭제도 마찬가지** - DB 결과와 무관하게 목록에서 지우고 성공 보고. 삭제가 실패해도 사라진 것처럼
  보이고 새로고침하면 되살아났다 -> 실제로 지워진 것만 화면에서 내린다
- **동아리 편집 저장 실패 시에도 창이 닫혔다** -> 입력 내용을 잃지 않도록 창을 유지하고 안내
- **부원 배정 저장이 트랜잭션 없이 건별로 반영**되고 결과도 확인하지 않았다. 중간에 실패하면
  "일부만 반영된 채 저장 완료"가 됐다 -> 추가·해제를 단일 트랜잭션으로 묶고 실패 시 롤백
- **동아리 삭제 안내가 사실과 달랐다** - "등록된 부원 정보도 함께 삭제됩니다"라고 했지만
  실제로는 논리 삭제라 캐스케이드가 일어나지 않는다 -> 안내를 사실대로 정정
- `ClubService` 에서 만들어놓고 쓰지 않던 리포지토리 필드 제거


### 죽은 코드 제거 - StudentService / EnrollmentService (2026-07-30)
29차에서 보고한 호출부 0건 메서드들을 제거했다. **547줄 삭제**, 테스트 354개 유지, 빌드 경고 0.

- `StudentService` 11개 제거: `RegisterNewStudentAsync`, `GenerateStudentID`, `GetFullInfoAsync`,
  `GetDetailInfoAsync`, `SearchByNameAsync`, `UpdateDetailInfoAsync`, `UpdateContactInfoAsync`,
  `UpdateGuardianContactAsync`, `DeleteStudentAsync`, `GetTotalStudentCountAsync`,
  `CheckCompletenessAsync`(+`StudentInfoCompleteness` 클래스). 남은 것은 조회 2개와 수정 1개뿐
    - 그중 `RegisterNewStudentAsync` 는 **트랜잭션이 가짜**였다 — studentRepo 에만 트랜잭션을
      걸고 enrollment·detail 은 다른 연결로 즉시 커밋돼, 되살려 쓰면 바로 깨질 코드였다
    - `GenerateStudentID` 는 `Student.GenerateStudentID` 와 **같은 규칙을 중복 구현**하고 있었다
      (실제로 쓰이는 쪽은 모델의 것)
- `EnrollmentService` 8개 제거: `AssignToClassAsync`, `BulkAssignAsync`, `TransferInAsync`,
  `TransferOutAsync`, `GetYearListAsync`, `GetStudentHistoryAsync`, `GetTeacherStudentsAsync`,
  정적 `UpdateAsync`
    - 전학 처리는 상태값을 `"전학(전출)"` 리터럴로 써서 `EnrollmentStatus.Transferred`(`"전학"`)와
      어긋나 있었다 — 살렸다면 상태로 거르는 코드와 맞지 않았을 것이다
- 두 서비스에서 쓰이지 않게 된 리포지토리 필드와 검증 메서드도 함께 정리
- **`PromoteStudentsAsync`(진급)는 남긴다** — 호출부는 없지만 학년을 넘겨 누가기록·특기사항을
  잇는 유일한 경로이고 회귀 테스트가 붙어 있다. 실사용 전에 반·번호 재배정 화면만 있으면 된다


### 29차 전수조사 - 학생·재적 관리 (2026-07-30)
학생 등록(`AddStudentsPage`), `StudentService`·`EnrollmentService`·`StudentRepository` 를 점검했다.
결함은 전부 **실제로 쓰이는 유일한 등록 경로**인 학생 추가 화면에 몰려 있었다. 테스트 350 -> 353,
빌드 경고 0.

- **학적을 학년 단위로 확정** (가장 큰 건). 학적 테이블은 (학년도, 학기) 단위 행인데
  **2학기 학적을 만드는 경로가 앱에 없다** - 학생 추가 화면이 등록 시점의 행 하나만 만든다.
  그런데 조회하는 쪽은 학기로 걸러서, **1학기에 등록한 학생이 2학기에는 명부·학급 누가기록·
  반 목록·명렬표·수강생 배정에서 통째로 사라졌다**. 학급 편성은 한 학년도 내내 유지되므로
  (사용자 확인) 명부 조회에서 학기를 걷어내고, 두 학기 행이 다 있으면 학생당 최신 한 건만 쓴다
    - `EnrollmentService.GetEnrollmentsAsync` 에서 **학기 인자 자체를 제거**해 다시 걸 수 없게 했다
    - 학기로 거르던 호출부 정리: 학급 누가기록, 학생 관리, 반 필터(`ClassPicker`), 명렬표,
      학생정보 내보내기, 과목 필터, 수강생 배정 다이얼로그
    - 학생 등록은 학적 학기를 1 로 고정한다. `UNIQUE(StudentID, SchoolCode, Year, Semester)` 가
      "한 학년도에 학적 한 건"을 그대로 강제해 준다
- **번호 중복 검사도 학기를 보지 않는다** - 같은 학년도·학년·반에 같은 번호면 중복이다
- **삭제한 학생의 ID 가 재사용 가능으로 판정**됐다. 학생 삭제는 논리 삭제(행 유지)인데
  `StudentID` 에는 UNIQUE 제약이 있어, 저장 단계에서 실패한다 -> `IsDeleted` 를 보지 않는다
- **저장 실패 이유가 사용자에게 보이지 않았다** - 실패 목록에 이름만 나오고 원인은 Debug
  로그에만 남아, 무엇을 고쳐야 할지 알 수 없었다 -> 실패 사유를 함께 표시
- **담임 ID 가 빈 문자열이면 저장이 통째로 실패**할 수 있었다(Teacher FK 위반) -> 비어 있으면 NULL
- **수동 추가만 학년 1~3 으로 막혀** 있었다(엑셀 가져오기는 1~6) -> 1~6 으로 통일
- DB 불변식 회귀 테스트 3건 추가(논리삭제 ID 재사용 불가, 학년도·학기 학적 유일성, 학기별 분리)


### 28차 전수조사 — 시간표·스케줄링 (2026-07-30)
자동 배치(`SchedulingEngine`), 밀기/당기기·Undo(`ScheduleShiftService`), 시간표 조회
(`TimetableService`) 및 관련 페이지를 점검했다. 테스트 337 → 350개, 빌드 경고 0.

- **당기기 취소(Undo)가 항상 실패하던 버그** — 되돌리는 순서가 원래 날짜 오름차순이라
  "아직 비켜나지 않은 뒷 수업의 자리"로 먼저 되돌리려다 슬롯 UNIQUE 제약에 걸렸다.
  밀기는 계획이 역순이라 우연히 맞았고 당기기는 정순이라 100% 깨졌다. → **적용의 역순**으로
  되돌리도록 수정. 되돌리기·다시실행도 밀기/당기기와 같이 **단일 트랜잭션**으로 처리해
  중간 실패 시 "일부만 되돌아간" 상태가 남지 않는다. 회귀 테스트 2건 추가
- **학사일정 조회 실패가 완전히 묻히던 문제** — 빈 `catch` 라서 학사일정을 못 읽으면
  공휴일·방학에도 수업이 배치되는데 사용자는 정상 배치로 받아들였다. → 실패를 경고로
  올려 배치 결과와 미리보기 확인창에 표시
- **휴일 판정·요일 변환 규칙이 두 곳에 복붙**돼 있어 한쪽만 바뀌면 조용히 어긋났다(실제 이력 있음).
  → `Helpers/SchoolCalendar` 로 단일화. 행사명이 null 이어도 안전하다(예전에는 여기서 나는
  NullReference 를 바깥 catch 가 삼켜 **휴일 목록 전체**가 비었다). 요일은 .NET(일=0)과
  시간표 규약(일=7)이 달라 일요일만 어긋나던 것도 함께 정리
- **시간표 조회 실패와 "수업 없음"이 구분되지 않던 문제** — `TimetableService` 가 예외를 삼키고
  빈 시간표를 돌려줘, 페이지의 오류 처리가 **영영 실행되지 않았다**. → `LoadFailed`/`ErrorMessage`
  로 구분해 교사 시간표 화면이 오류를 안내한다
- **교사 시간표 페이지의 `async void` 초기화** — DB 를 읽는데 try/catch 가 없어 실패하면
  앱이 그대로 죽었다. → awaitable + 오류 안내. 초기화 완료 플래그도 실제 완료 후에 세운다
  (이전에는 콤보가 차기 전에 true 였다). 남아 있던 디버그 출력 제거


### 글자 크기 조절이 엉뚱한 곳을 키우던 문제 (2026-07-30)
누가기록·학생부기록 페이지의 "글자 크기" 슬라이더가 **읽으려는 기록 내용은 그대로 두고
헤더 라벨만** 키우고 있었다. 원인은 목록 컨트롤의 `FontSize` 를 바꾸는 방식이었다 —
행의 각 칸(학년도·이름·과목·기록 내용…)에 `FontSize="12"` 가 **명시**돼 있어 상속값이 무시되고,
크기가 지정되지 않은 헤더 라벨에만 값이 내려갔다.

- `LogListViewer`·`SpecListViewer` 에 `ContentFontSize` 속성 추가 — 이 값만 **기록 내용 칸**이
  직접 바인딩한다. 나머지 칸과 헤더는 고정 크기를 유지한다
- 세 페이지(학급 누가기록·학급 학생부기록·수업 학생부기록)가 `FontSize` 대신 이 속성을 쓴다
- 목록에 나중에 추가되는 행에도 현재 글자 크기가 적용되도록 컬렉션 변경을 감시한다
- **수업 누가기록·동아리 활동 페이지에 글자 크기 버튼 추가** — 같은 목록 컨트롤을 쓰는데
  이 두 페이지에만 버튼이 없었다. 이제 활동 기록 다섯 페이지가 모두 동일하게 동작한다
- **행의 학생 정보 칸(이름·번호·과목·일시·주제)을 12 → 14 로** — 헤더(14)·기록 내용(14)보다
  작아 목록에서 학생을 짚어내기 불편했다. 바이트 표시(10)와 영역 라벨(9)은 그대로 둔다
- **글자 크기 기본값을 14 로 통일** — 목록 기록 내용은 12, 학생부 박스는 14, 슬라이더는 12 로
  제각각이라 슬라이더를 처음 건드리는 순간 글자가 한 번 튀었다. 기준값을
  `ViewModel.DefaultContentFontSize` 상수로 두고 네 페이지의 슬라이더 기본값을 맞췄다
- **누가기록 페이지의 학생부 기록 박스도 함께 커진다** — 목록과 나란히 보며 옮겨 적는 화면이라
  한쪽만 커지면 오히려 읽기 나쁘다. `StudentSpecBox.ContentFontSize` 추가(특기사항 입력칸만 적용,
  헤더·버튼·바이트 표시는 고정)


### 과목 필터에 학년도·학기 추가 (2026-07-30)
교과 세특과 수업 기록은 **학기별**인데, 과목 필터(`CoursePicker`)가 `Settings.WorkSemester` 로 고정돼
있어 **다른 학기 기록에 접근할 방법이 아예 없었다**. 필터를 `학년도 · 학기 · 과목 · 강의실` 로 바꿨다.
테스트 337개, 빌드 경고 0.

- **`CoursePicker` 에 학년도·학기 선택 추가**: 학년도 콤보를 새로 만들지 않고 기존
  `YearSemesterPicker` 를 **조합**했다(학년도 목록을 DB에서 조회하는 로직이 이미 있어 중복을 피함).
  `ShowYear`·`ShowSemester` 옵션은 내부 피커에 위임하고, `SelectedYear`·`SelectedSemester` 를 노출한다
- **호출부가 필터 선택값을 따르도록 정정**(중요): `LessonActivityPage` 가 조회·저장 곳곳에서
  `Settings.WorkYear/WorkSemester` 를 직접 쓰고 있었다. 필터로 다른 학기를 골라도 **현재 학기 기준으로
  조회·저장돼 기록이 엉뚱한 학기에 들어갈** 수 있었다. → 필터 이벤트에서 받은 학년도·학기를 보관해
  쓴다(누가기록 조회·로그 생성·세특 조회·세특 생성·로그 다이얼로그 5곳)
- **새로고침이 선택을 초기화하던 문제 수정**: 두 페이지의 새로고침이 `Settings` 값으로 다시 로드해
  사용자가 고른 학년도·학기를 되돌렸다. → 현재 선택값으로 재로드
- `YearSemesterPicker.TrySelect(year, semester)` 추가 — 외부에서 값을 지정할 때 표시만 맞추고
  변경 이벤트는 쏘지 않아 중복 로드를 막는다


### 학생부 목록 헤더가 카테고리 변경에 따라오지 않던 문제 수정 (2026-07-30)
학생부 기록 화면에서 카테고리 콤보를 바꿔도 목록 헤더가 맞게 바뀌지 않았다. 원인은 **같은 컬럼을
두 곳에서 각각 건드리고 있었던 것**이다. 테스트 337개, 빌드 경고 0.

- `ChangeCategory()`(카테고리 변경 시)는 진로활동을 `default` 분기에서 **폭 0·헤더 숨김**으로 처리했고,
  목록 로드 쪽은 로드된 항목을 보고 **폭 130·"희망분야" 라벨**을 설정했다. 호출 순서에 따라 결과가
  엇갈려, 실제로 **입력칸은 보이는데 헤더는 비어 있는** 상태가 나왔다
- → 과목/분야 칸의 라벨·폭을 결정하는 곳을 **`UpdateSubjectColumn()` 한 곳으로 통합**했다.
  카테고리 변경과 목록 로드가 모두 이 메서드를 호출한다
- 판단 기준도 정리했다: **선택된 카테고리가 우선**이고(조회 전에도 헤더가 맞게 바뀐다),
  '전체'일 때만 로드된 목록에서 단일 영역을 추론한다. 라벨 문구는 `NeisHelper.Areas` 정의표에서
  가져오므로 영역을 추가해도 화면이 따라온다
  (진로활동 → "희망분야", 교과활동 → "과목/학기", 개인별세특 → "과목", 동아리활동 → "동아리",
  자율·봉사·종합의견 → 칸 접기, 전체 → "세부영역")


### 학생부 목록의 과목/분야 칸을 이름 오른쪽으로 이동 (2026-07-30)
과목·희망분야 칸이 번호·이름보다 **왼쪽**에 있어 학생을 눈으로 따라가기 불편했고, 진로활동일 때
헤더에 무엇을 입력하는 칸인지 표시되지 않았다. 테스트 337개, 빌드 경고 0.

- **컬럼 순서 변경**: `학년도 · 유형 · 학년 · 반 · 번호 · 이름 · 과목/분야` 로, 과목/분야를
  **학생 이름 바로 오른쪽**에 두었다(헤더·행 템플릿·폭 동기화 매핑을 함께 재배치)
- **헤더 라벨이 영역에 따라 바뀐다**: 진로활동 목록에서는 그 칸이 희망분야 입력칸이므로 헤더도
  **"희망분야"** 로 표시된다(문구는 `NeisHelper.Areas` 의 `TitleLabel` 에서 가져오므로 정의표만 고치면
  따라온다). 영역이 섞여 있으면 "과목/학기"
- **필요 없을 때 칸을 접는다**: 자율활동처럼 과목도 희망분야도 없는 영역은 폭을 0으로 줄여
  기록 내용에 공간을 넘긴다


### 진로활동 희망분야 입력·합산 (2026-07-30)
진로활동은 **희망분야와 특기사항으로 구성되고 둘을 합쳐** 연간 500자다. 그런데 희망분야를 입력할
곳이 없고 분량 계산도 특기사항만 세고 있어, 실제 NEIS 한도보다 느슨하게 판정됐다.
테스트 337개(+11), 빌드 경고 0.

- **정의표에 Title 의 역할을 명시**(`NeisHelper.SpecArea`): `TitleLabel`(그 영역에서 `Title` 이 담는 것)
  과 `TitleCountsInBytes`(분량에 합산되는가)를 추가했다. 진로활동은 `희망분야`·합산, 교과 세특은
  `과목명`·합산 안 함(교과목에서 자동으로 채워지는 값이라 분량에 들어가지 않는다).
  `Title` 은 영역마다 의미가 다른 필드인데 그 의미가 코드에 흩어져 있지 않도록 정의표에 모았다
- **합산 계산을 한 곳으로**(`NeisHelper.CountSpecBytes`): 입력 화면·일괄 입력·뷰모델·엑셀·HTML·PDF
  **6곳이 모두 이 메서드**를 쓰므로 기준이 어긋날 수 없다
- **희망분야 입력칸 추가**: 개별 입력(`StudentSpecBox`)·일괄 입력(`StudentSpecBatchDialog`)·목록
  (`SpecListViewer`) 세 곳 모두에서 입력·수정할 수 있다. 일괄 입력에서는 학생마다 값이 다르므로
  전역 툴바가 아니라 **학생 정보 줄**에 두었다. 목록에서는 "과목/분야" 칸을 공유해, 진로활동 행만
  입력칸이 되고 나머지는 표시 전용이다
- 입력·수정 시 **수정 표시와 바이트 정보가 특기사항과 동일하게** 갱신되고, 저장 경로는 기존 그대로다
  (모델의 `Title` 을 쓰므로 `UPDATE` 문에 이미 포함되어 있었다)


### 학생부 목록·입력 화면에 학기 노출 (2026-07-30)
교과 세부능력 및 특기사항은 학기별인데 화면에 학기가 보이지 않아, 1·2학기 기록을 눈으로 구분할 수
없었다. 테스트 326개, 빌드 경고 0.

- **입력 화면의 학기 표시를 살렸다**(`StudentSpecBox`): 학기 표시 UI 와 "교과활동일 때만" 조건이
  이미 있었지만 **값을 채우는 코드가 없어 빈칸**이었다(모델에 `Semester` 가 없던 시절의 잔재).
  이제 학기가 표시되고, 표시 여부 판단도 `NeisHelper.Areas` 정의표를 따른다
- **목록의 과목 칸이 학기까지 함께 보여준다**(`SpecListViewer`): 열을 새로 끼우면 이후 `Grid.Column`
  인덱스가 모두 밀리므로, 기존 "과목" 칸을 **"과목/학기"** 로 바꿔 `"국어 (2학기)"` 처럼 한 칸에
  묶었다. 학기별이 아닌 영역(개인별세특 등)은 과목명만, 해당 없으면 빈칸이다
  (`StudentSpecialViewModel.SubjectDisplay`)


### 학생부 글자 제한 기본값 정정 + 학교 설정으로 이동 (2026-07-30)
교육부 지침 표(2026·2027·2028~ 입시)와 대조한 결과 **기본값 3개가 실제와 달랐다**. 현재 코드값은
2026 입시 기준이었고, 봉사활동은 어느 기준과도 맞지 않았다. 테스트 326개, 빌드 경고 0.

| 영역 | 기존 | 정정(2027·2028~) | 지침 |
|---|---|---|---|
| 봉사활동 | 1500 | **150** | 실적 50자 |
| 진로활동 | 2100 | **1500** | 연간 500자 (2026 입시는 700자) |
| 종합의견 | 1500 | **900** | 연간 300자 (2026 입시는 500자) |

교과활동·개인별세특·자율활동·동아리활동은 500자(1500바이트)로 기존과 동일하다.
2026 입시에 해당하는 기록을 다루려면 **학년도별 오버라이드**로 예전 값을 지정하면 된다.

- **한도 설정을 앱 설정 → 학교 설정으로 이동**: 학생부 글자 제한은 앱 동작이 아니라 학교·학년도
  기준이라 학교 설정이 맞는 자리다. 입력칸이 정의표에서 코드로 생성되는 구조라 이동이 단순했다
- **기본값을 리터럴로 박지 않도록 테스트 정리**: 우선순위·저장 규칙 테스트가 진로활동 기본값
  2100 을 하드코딩하고 있어 지침이 바뀔 때마다 깨졌다. → `NeisHelper.GetMaxBytes(type)` 를 참조하게
  바꿔, **표 값은 전용 테스트 한 곳에서만 고정**한다(지침 변경 시 고칠 데가 한 곳)


### 학생부에 학기 정보 도입 (2026-07-30)
교과 세부능력 및 특기사항은 **학기별**, 나머지 영역(개인별세특 포함)은 **학년 단위**다. 그런데
`StudentSpecial` 에는 학기 컬럼이 없어 학기를 `CourseNo → Course.Semester` 로 **유도**해야 했다.
`CourseNo` 는 `ON DELETE SET NULL` 이라 **교과목을 삭제하면 그 세특이 어느 학기 것인지 영구히
알 수 없어지는** 구멍이 있었다. 스키마에 학기를 직접 저장하도록 바꿨다.
테스트 324개(+15), 빌드 경고 0, `PRAGMA user_version` 1 → 2.

- **`StudentSpecial.Semester` 추가**: `0 = 학년 단위(연간)`, `1·2 = 해당 학기`. 교과활동만 1·2 를
  갖고 개인별세특·자율·동아리·봉사·진로·종합의견은 0 이다. 학기별 여부는 `NeisHelper.Areas`
  정의표의 `IsSemesterScoped` 플래그 하나로 판단한다(규칙이 흩어지지 않게)
- **기존 DB 자동 마이그레이션**: 컬럼이 없으면 `ADD COLUMN` 후, 교과활동 중 **교과목이 살아 있는
  행만** `Course.Semester` 로 백필한다. 학년 단위 기록과 이미 교과목이 삭제된 행은 0 으로 남는다
  (후자는 복원 불가 — 이 변경을 한 이유 자체가 앞으로 그 유실을 막기 위함이다).
  두 번 실행해도 안전하고, 기존 데이터는 그대로 보존된다
- **수업 기록 화면의 세특 매칭도 정정**(`LessonActivityPage`): 교과 세특을 **과목명(`Title`)으로**
  찾고 있어, 1·2학기에 같은 과목을 가르치면 다른 학기 기록을 집어와 덮어썼다(일괄 입력에서 고친
  것과 같은 결함). → `CourseNo` 로 매칭하고 `CourseNo` 없는 구 기록만 과목명으로 보조 매칭
- **회귀 테스트 15건**: 영역별 학기 적용 규칙, 학기 저장·수정 왕복, "같은 과목이라도 학기가 다르면
  별개 기록", 그리고 **구버전 DB 마이그레이션**(컬럼 추가·백필·재실행 안전성·버전 기록). 마이그레이션
  테스트는 실제 초기화기로 스키마를 만든 뒤 `Semester` 컬럼만 떼어내 구버전을 재현한다

### 학생부 글자 제한을 학년도별로 관리 (2026-07-29)
교육부 지침이 학년도마다 바뀌는데, 한도가 **전역 1개**라 새 지침을 적용하면 지난 학년도 기록까지
갑자기 "초과"로 표시됐다. 또 영역 목록이 **5곳에 하드코딩**돼 서로 어긋나 있었다(실제로 "봉사활동"이
내보내기 필터에만 있고 한도·설정 화면에는 빠져 있었다). 테스트 302개(+22), 빌드 경고 0.

- **영역 정의를 단일 정의표로 통합**(`NeisHelper.Areas`): 키·표시이름·기본 바이트를 한 곳에 모았다.
  설정 화면의 입력칸, 내보내기 필터의 영역 목록, 일괄 입력의 영역 목록이 **모두 이 표에서 생성**되어
  영역 추가·삭제가 **한 줄**이 됐다. 흩어져 있던 5곳(`GetMaxBytes` 스위치, 설정 XAML 6개 고정 컨트롤,
  설정 코드 6줄, 내보내기 필터 XAML, 일괄 입력 목록)을 제거. 누락됐던 **봉사활동도 자동 포함**
- **학년도별 한도 오버라이드 지원**: 저장 형식을 `2026:진로활동=2100` 으로 확장하고
  우선순위를 **해당 학년도 → 학년도 무관 → 코드 기본값** 으로 정했다. 새 지침을 올해에만 적용해도
  지난 학년도 기록의 판정이 바뀌지 않는다. **구버전 설정(`진로활동=2100`)은 학년도 무관 값으로
  그대로 동작**한다(하위 호환)
- **설정 화면에 적용 학년도 선택 추가**: "모든 학년도(기본)" 또는 특정 학년도를 고르고 영역별 한도를
  입력한다. 한도 판정은 **기록 자신의 학년도**(`spec.Year`)를 기준으로 하므로, 2025년 기록은
  2025년 지침으로 본다
- **한도 초과 판정을 한 곳으로 모음**(`NeisHelper.IsOverLimit`): 경계 규칙은 **"이하 허용"** 이다 —
  한도와 정확히 같은 바이트는 초과가 아니고(500자=1500바이트를 온전히 사용), 1바이트라도 넘을 때만
  초과다. 예전에는 이 비교가 입력·일괄입력·HTML·PDF **6곳에 흩어져** 있어 한 곳만 `>=` 로 바뀌어도
  알아채기 어려웠다. 회귀 테스트 7건으로 경계를 고정
- **한도를 글자 수로 입력**: 교육부 지침은 "500자·700자"처럼 **글자 수**로 나오는데 설정은 바이트만
  받아 매번 ×3을 암산해야 했다. → 지침 숫자를 그대로 넣도록 **글자 수 입력칸 하나**로 바꾸고,
  실제 판정 단위인 바이트는 옆에 `= 1,500 Byte` 로 표시한다(칸을 두 개 두는 것은 중복).
  저장·판정 기준은 **바이트를 유지**했다 — NEIS 가 바이트로 제한하고, 영문·숫자·공백(1바이트)이
  섞이면 표시된 글자 수보다 더 많이 입력할 수 있다(NEIS 와 같은 동작)
- **교과 세특이 1·2학기에 섞이던 문제 수정**(`StudentSpecBatchDialog`): 교과 세특은 학기별이고
  교과목(`Course`)도 학기별로 등록되는데, 일괄 입력만 세특을 **과목명 문자열**로 찾았다.
  조회가 학년도 전체를 훑기 때문에, 1·2학기에 같은 과목을 가르치면 **2학기에 열어도 1학기 기록을
  집어와 그 위에 덮어썼다**. → `CourseNo` 로 매칭한다(`CourseSpecPage` 는 이미 이 방식).
  `CourseNo` 가 없는 구 기록은 과목명으로 보조 매칭
- **회귀 테스트 22건 추가**: 우선순위 규칙, 하위 호환, 잘못된 설정값 무시, "학년도 지정값이 기본값과
  같아도 학년도 무관 오버라이드가 다르면 항목을 유지"(그러지 않으면 그 학년도만 기본값으로 되돌리는
  의도가 뒤집힌다) 등. 저장소와 분리된 순수 함수(`ResolveSpecMaxBytes`/`ApplySpecByteOverride`)로
  테스트 가능하게 리팩터링

### 27차 — 전수 조사: 학생부/과세특 내보내기 (2026-07-29)
학생부 특기사항의 입력·일괄 입력·내보내기(엑셀/HTML/PDF) 경로를 정독했다. 핵심은 NEIS 바이트
한도인데, **입력 화면과 내보낸 파일이 서로 다른 기준**을 쓰고 있었다. 빌드 경고 0, 테스트 280개 유지.

- **바이트 한도가 입력 화면과 내보내기에서 달랐던 문제 수정**: 입력 화면(`StudentSpecBox`)·일괄
  입력(`StudentSpecBatchDialog`)·설정 화면은 **사용자 오버라이드를 반영하는** `Settings.GetSpecMaxBytes`
  를 쓰는데, **엑셀·HTML·PDF 내보내기와 뷰모델은 하드코딩된** `NeisHelper.GetMaxBytes` 를 썼다.
  설정에서 한도를 조정하면 **편집기에서는 "초과(빨강)"인데 내보낸 파일에는 정상으로 표기**되어,
  한도를 넘긴 원고를 그대로 NEIS 에 붙여넣게 된다. → 6개 호출부를 설정 반영 버전으로 통일
  (`HtmlExportService` 2곳, `StudentSpecPrintService` 2곳, `StudentSpecExportService`,
  `StudentSpecialViewModel`). 오버라이드가 없으면 내부적으로 같은 기본값이라 동작은 동일하다
- **"저장 후 닫기"가 저장을 기다리지 않던 문제 수정**(`StudentSpecBatchDialog.BtnClose_Click`):
  미저장 변경이 있을 때 뜨는 확인창에서 "저장 후 닫기"를 골라도, `async void` 저장 핸들러를
  **`await` 없이 호출**하고 곧바로 `Close()` 했다(주석은 "저장 완료 후 닫기"였지만 실제로는
  기다리지 않음). 저장이 끝나기 전에 창이 닫혀 **편집이 유실**될 수 있었다. → 저장 로직을
  `SaveAllAsync()` 로 분리해 `await` 하고, 저장에 실패해 변경분이 남으면 **닫지 않고** 재시도를 안내

**관찰(미수정)**: 내보내기 필터는 영역 7종(자율·진로·동아리·**봉사**·교과·개인별세특·종합의견)을
제공하는데 `NeisHelper.GetMaxBytes` 와 설정 화면에는 **봉사활동이 없어** 기본값 1500 으로 처리된다.
실제 NEIS 기준을 확인한 뒤 명시하는 편이 안전하다.

### 26차 — 전수 조사: 좌석 배치 (2026-07-29)
좌석 배치의 배치 알고리즘·제약 검증·이력을 정독했다. `SeatService`(저장·이력)는 견고했고, 결함은
전부 **제약 검증(`PageSeats`)** 에 몰려 있었다 — 특히 "조건을 켜면 배치가 아예 성공하지 못하는"
경로 두 건. 빌드 경고 0, 테스트 280개 유지.

- **1인석 모드에서 나눗셈 0 크래시 수정**(`AreNeighbors`): 짝 그룹을 `Col / _jjak` 로 계산하는데
  `_jjak` 은 **저장된 배치에서 그대로 로드**된다(구 데이터·초기화 실패 시 0 가능). 0 이면
  `DivideByZeroException` 으로 앱이 죽는다. → 짝 모드가 아니면(`_jjak < 2`) 나눗셈 전에 false 반환
- **1인석 모드에서 고정 짝을 쓰면 배치가 영원히 실패하던 문제 수정**(`HasPairViolation`): 1인석에는
  '짝'이 없어 `AreNeighbors` 가 항상 false 인데, 고정 쌍 검사는 "짝이 아니면 위반"이라
  **고정 쌍이 하나라도 있으면 매 시도가 위반**으로 판정됐다. 시도를 모두 소진하고 실패 경고만
  반복됐다. → 짝 모드일 때만 고정 쌍을 검사한다
- **"남녀 교차 우선"이 사실상 불가능 조건이던 문제 수정**(`ArrangeSeatAsync`): 옵션 이름은 "우선"인데
  **모든 짝이 남녀여야만** 통과하는 필수 조건이었다. 성비가 불균형한 학급(예: 남 20·여 10)에서는
  수학적으로 불가능해 **매번 실패 경고**가 떴고, 화면에는 조건을 어긴 배치가 남았다.
  → 앞쪽 시도는 종전대로 남녀 교차를 요구하고, 못 찾으면 **그 조건만 풀어** 다시 찾는다(분리·고정·
  지난 짝은 계속 지킴). 이 경우 실패가 아니라 "남녀 교차 일부 적용"으로 안내한다
- **누적 배치 회차가 1인석 학급에서 항상 0으로 표시되던 문제 수정**(`CountRoundsAsync`
  ·`SeatService.GetRoundCountAsync`): 회차를 **짝 이력만** 세는 간이 구현이라 (1) 0/1 로만 나오고
  (2) 1인석은 짝 이력이 아예 없어 아무리 저장해도 "누적 없음"으로 보였다(위치 이력은 쌓이는데도).
  → 짝·위치 두 이력의 실제 최대 Round 를 사용(이미 `GetNextRoundAsync` 가 쓰던 방식과 통일)

**이상 없음 확인**: `SeatService` 저장(트랜잭션·upsert·이력 누적)·이력 조회 정상, `AreNeighbors` 의
짝 판정이 `InsertPairHistoryAsync` 와 동일 규칙(같은 Row·같은 `Col/jjak` 그룹·인접 Col)이라
"지난 짝 배제"가 실제로 맞물림, 저장 실패는 이미 사용자에게 보고됨.

### 25차 — 시수 배분 죽은 코드 정리 + 편집기 NuGet 전환 (2026-07-26)
시수 배분/연간지도계획 서브시스템을 정독한 결과, 라이브 경로(새 계획 생성 시 자동 계산·단원 배치)는
정상이었고 결함은 대부분 **미배선 죽은 코드**였다. 살아있는 로직은 건드리지 않고 죽은 코드만 제거했다.
빌드 경고 0, 테스트 280개 유지.

- **시수 조정 죽은 코드 제거**: 참조 0으로 확인된 미배선 코드 정리.
  - `Services/WeeklyHoursHelper.cs` 전체 삭제(408줄) — 이 클래스는 죽은 VM 메서드 2곳에서만 생성됐다.
    비율 배분·시뮬레이션(미리보기 합계가 실제와 어긋나는 버그 포함)·리셋 3종·다중 수정·통계 등 전부 미사용.
  - `WeeklyHoursCalculator`: `RecalculateAsync`(편집 보존 재계산), `GetWeekNumberForDate` 제거.
  - `AnnualLessonPlanViewModel`: `AddHoursEvenlyAsync`, `UpdateWeekHoursAsync`, `GenerateUnitPlanAsync`(빈 스텁) 제거.
- **WinUIRichEditor 를 NuGet 패키지 참조로 전환**: 로컬 `ProjectReference`(`..\..\WinUIRichEditor`) →
  **`PackageReference`(nuget.org)**. 로컬 편집기 저장소 없이도 빌드되고, 의존성 버전이 패키지에 고정된다.
  ⚠ **버전 선택에 제약이 있다**: 편집기가 선언하는 `Microsoft.WindowsAppSDK.WinUI` **하한선이 앱
  메타패키지(`Microsoft.WindowsAppSDK` 2.3.1)가 주는 WinUI 2.3.0 을 넘으면 안 된다.** 넘으면 NuGet 이
  위로 통일하면서 `.Runtime` 2.3.1 의 컴포넌트 버전 검사에 걸려 **빌드가 실패**한다. 편집기 0.9.0 은
  하한선이 2.3.2 라 부적합했고, 하한선을 2.2.1 로 내린 **0.9.1** 로 확정했다
  (해석 결과: `WinUIRichEditor 0.9.1` + `WinUI 2.3.0` + `Runtime 2.3.1` — 충돌 없음).
  · 배포 모델은 **종전 그대로 프레임워크 의존** 유지 — 런타임은 설치 프로그램의
  `prerequisites\WindowsAppRuntimeInstall-x64.exe` 가 설치하고 게시 폴더는 **9개 / 44MB**.
  (`WindowsAppSDKSelfContained=true` 로 돌리면 런타임이 게시 폴더로 들어와 70개 / 97MB 가 된다.)
  · 단일 파일 게시는 Native AOT 와 상호 배타라 원래 불가(설계상 유지).
- **게시 언어 리소스 필터 추가**(`RemoveUnusedWinAppSdkAssets`, 안전망): self-contained 로 전환할 경우
  WinAppSDK 의 ~85개 언어 폴더(`af-ZA` … `zh-TW`)가 게시 폴더로 딸려온다. 한국어 앱이므로 `.mui` 를
  `ko`/`en` 외 걸러낸다(현재 프레임워크 의존에서는 `.mui` 자체가 게시되지 않아 무동작).

### 24차 — 전수 조사: NEIS 조회·파싱 (2026-07-26)
NEIS(교육행정정보시스템) 연동 경로—학교 검색·학사일정·급식—를 정독했다. 파싱 자체는 견고했고,
결함은 대부분 "저장/삭제 결과를 확인하지 않고 성공처럼 처리하던" 학사일정 관리 화면에 몰려 있었다
(21차 학생/누가기록과 같은 성격). 빌드 경고 0, 테스트 280개 유지.

- **학사일정 저장의 거짓 성공 집계 수정**(`SchoolScheduleManagementPage.SaveSelectedAsync`): 신규 항목은
  `CreateScheduleAsync` 결과를 확인하지 않고 **무조건 저장 성공으로 셌다**(수정 경로만 `.Success` 확인).
  게다가 생성 후 반환된 새 PK 를 모델에 반영하지 않아, 같은 항목을 다시 저장하면 **중복 생성**됐다.
  → 성공한 건만 집계하고 새 PK(`No`)를 반영한다. 성공한 항목만 수정 표시(✏️)를 해제해 실패분은 재시도
  가능하게 남기고, 일부 실패 시 "N개 중 M개 저장"으로 알린다
- **학사일정 삭제의 표시/DB 불일치 수정**(`SchoolScheduleManagementPage.DeleteSelectedAsync`):
  `DeleteBulkScheduleAsync` 결과를 버리고(`_ =`) 선택 항목을 **UI에서 전부 지운 뒤 "N개 삭제됨"** 을
  띄웠다. 실제로 0개만 지워져도 화면에서만 사라졌다. → 전건 성공일 때만 UI에서 제거하고, 부분/전체
  실패면 개수만으로 어떤 행이 남았는지 알 수 없으므로 DB 에서 다시 읽어 화면을 실제 상태와 맞춘다
- **NEIS 동기화 플래그·집계 오류 수정**(`SchoolScheduleManagementPage.SyncFromNeisAsync`): DB 저장
  **전에** `IsNeisEventDownloaded` 플래그를 세워, 저장이 실패해도 "다운로드됨" 으로 남았다. 또 일괄
  저장이 예외로 실패하면 `Count=-1` 이 그대로 흘러 "총 N개 중 -1개 신규 저장" 같은 집계가 나왔다.
  → 저장 성공을 먼저 확인하고, 성공 후에만 플래그를 세운다. 실패는 그대로 알린다
- **학교 검색 로그의 API 키 노출 수정**(`SchoolSearchDialog`): 요청 URL 을 `KEY=` 값째 디버그 로그에
  찍었다(급식·연간계획 로그는 키를 가림). → 키를 `***` 로 가려 출력(빈 키 예외도 방어)
- **잘못된 필드 매핑 죽은 코드 제거**(`SchoolSearchDialog`): `COEDU_SC_NM`(남녀공학 구분)을
  "교장명" 으로 주석 단 채 읽어두고 어디에도 쓰지 않았다. → 제거

**이상 없음 확인**: 급식 파싱(`GetSchoolMealsAsync`/`GetMenuStringOptimized`—알레르기·`<br/>` 처리),
학사일정 다운로드(`SchoolScheduleService`—Content-Type·NEIS 오류코드·타임아웃·날짜 InvariantCulture),
학교 코드→설정 배선, `NeisHelper` 바이트 계산 모두 정상.

### 23차 — 전수 조사: Google 동기화·인증 서브시스템 (2026-07-26)
게시판 모듈을 마친 뒤 그동안 안 훑던 **Google 캘린더 동기화/인증**을 정독했다. 겉으론 "연결됨"인데
속으로는 조용히 실패가 굳어지는 두 지점을 찾아 수정했다. 빌드 경고 0, 테스트 280개 유지.

- **쓰기 요청 재시도가 항상 깨지던 문제 수정**(`GoogleCalendarApiClient.SendWithRetryAsync`): 재시도
  분기가 **이미 전송된 `HttpContent` 를 그대로 다시 붙여** 보냈다. HttpClient 는 전송 후 요청 본문을
  폐기하므로, 본문이 있는 요청(이벤트·캘린더 생성/수정)의 401·429·5xx·네트워크 재시도는 재사용된
  Content 때문에 `ObjectDisposedException` 으로 **사실상 전부 실패**했다(코드 주석도 "재사용 시도"로
  불확실성을 인정). → 본문을 한 번만 바이트로 버퍼링하고 재시도마다 새 `Content` 를 만들어 보낸다
- **죽은 리프레시 토큰이 "연결됨"으로 남던 문제 수정**(`GoogleAuthService.RefreshTokenAsync`): 토큰
  갱신이 `invalid_grant`(6개월 미사용·비번 변경·재발급 한도 초과로 리프레시 토큰이 취소됨)로 실패해도
  저장된 토큰을 지우지 않아, `IsAuthenticated` 가 계속 true 였다. 앱은 **"연동됨"으로 보이는데 모든
  동기화가 영원히 조용히 실패**하고 재인증 안내도 없었다. → `400 invalid_grant` 일 때만 저장 토큰을
  지워 연동 해제 상태로 되돌린다(설정에 "연동 필요"로 표시 → 재인증 유도). 일시적 네트워크 오류나
  OneDrive 포터블의 DPAPI 복호화 실패로는 지우지 않는다(그 경우 HTTP 호출 전에 반환됨)

### 22차 — 전수 조사: 게시판 모듈 무음 실패·파일 정합성 (2026-07-25)
20·21차가 학생·진도·로그 쪽의 "실패가 안 보이던 지점" 이었다면, 이번엔 그동안 정독하지 않았던
**게시판(Board) 모듈**을 훑어 같은 성격의 결함 10건(무음 실패 6 + 후속 3 + 죽은 코드 1)을 찾아
수정했다. 주제는 여전히 "실패했는데 사용자에게 안 보이거나, 물리 파일만 어긋나던 지점". 빌드 경고 0.

- **게시글 삭제의 파일/DB 삭제 순서 역전 수정**(`BoardService.DeletePostAsync`): 형제 메서드
  (`DeleteCommentAsync`·`DeletePostFileAsync`)는 DB 삭제가 커밋된 뒤에만 물리 파일을 지우는데,
  게시글 삭제만 **물리 파일을 먼저 지우고** 나서 행을 삭제했다. `DeleteAsync` 가 예외로 실패하면
  파일은 이미 사라졌는데 게시글 행은 그대로 남는 역-고아 상태가 됐다. → 삭제 대상 파일명을 먼저
  수집만 하고, DB 삭제가 확정된 뒤에 파일을 지우도록 순서를 통일
- **댓글 등록 실패가 무음이던 문제 수정**(`PostDetailViewModel.AddCommentAsync`): `CreateCommentAsync`
  가 0(실패)을 반환해도 아무 안내가 없었다. 게다가 첨부파일이 이미 디스크에 저장된 뒤 댓글 생성이
  실패하면 **고아 파일**이 남았다. → 실패 시 방금 저장한 첨부 파일을 정리하고 사용자에게 알린다.
  첨부 저장 자체가 실패해 파일 없이 등록되는 경우도 명시적으로 안내(첨부된 줄 오인 방지)
- **댓글 수정 실패가 무음이던 문제 수정**(`PostDetailViewModel.UpdateCommentAsync`): 0행 반영(대상
  없음)이나 예외가 나도 조용히 넘어가 수정이 저장된 것처럼 보였다. → 실패 시 알린다
- **댓글 삭제 실패가 무음이던 문제 수정**(`PostDetailViewModel.DeleteCommentAsync`): 삭제 0행이면
  목록에는 그대로 남는데 사용자는 삭제된 줄 오인했다. → 실패 시 알린다
- **완료 토글의 미관측 실패 수정**(`PostDetailViewModel.IsCompleted`): 완료 상태 DB 반영이
  fire-and-forget 이라, 실패하면 화면만 바뀌고 DB 는 안 바뀐 채 예외가 관측되지 않았다.
  → 실패 시 토글을 원복하고 알린다
- **메모 완료 처리의 무음 실패 수정**(`MemoBoard.CompleteMemoAsync`): 완료 저장이 실패해도 체크박스는
  켜진 채로 남고 알림이 없었다. → DB 반영을 먼저 확정한 뒤에 상태·목록을 바꾸고, 실패하면 체크
  상태와 목록을 원복하고 알린다
- **메모 편집창의 거짓 성공 보고 수정**(`MemoEditDialog.BtnSave_Click`): `SavePostAsync` 결과가 0(실패)
  이어도 실패 분기 없이 `Result=true` 로 창을 닫아, **저장이 안 됐는데 편집이 조용히 사라졌다**
  (게시글 편집창엔 있던 실패 안내가 여기엔 없었다). → 0 이하면 창을 열어둔 채 실패를 알린다
- **최상위 댓글 삭제 시 답글이 사라지고 DB 에 고아로 남던 문제 수정**(`BoardService.DeleteCommentAsync`
  ·`PostDetailViewModel`): `Comment.ParentNo` 에는 CASCADE 가 없어(부모 없는 답글은 화면 로직에서
  누락), 답글 달린 댓글을 지우면 **답글이 목록에서 사라지지만 DB·댓글 수에는 남았다**. → 최상위
  댓글을 지울 때 그 답글(과 첨부)도 같은 트랜잭션에서 함께 정리하고, 삭제 후 목록을 다시 읽어 화면을
  DB 와 맞춘다. 표시 로직은 이미 생긴 고아 답글을 최상위로 승격해 잃지 않게 보강
- **첨부 실행파일 차단 목록 확장·일원화**(`Board.IsBlockedAttachment`): 첨부는 `Process.Start`
  (ShellExecute)로 열리는데, 차단 목록이 실행형 13종뿐이라 **`.lnk`(바로가기)·`.url`·`.hta`·`.pif`
  ·`.scf`·`.reg`·`.msc`·`.cpl`·`.jar` 등 클릭 실행 확장자가 통과**했고, 같은 목록이 게시글·댓글
  두 곳에 따로 있어 어긋날 위험도 있었다. → `Board` 공용 목록/헬퍼 한 벌로 통합하고 위험 확장자를 보강
- **죽은 코드 제거**(`PostPrintService`): HTML 콘텐츠(Jodit) 시절의 게시글 PDF 생성 서비스(186줄)가
  어디서도 참조되지 않았다. 인쇄는 에디터의 `PrintAsync` 경유로 대체된 지 오래다. → 파일 삭제
  (QuestPDF 패키지는 좌석·학생카드·누가·과세특 인쇄가 계속 쓰므로 유지)

### 21차 — 전수 조사: 미저장 편집 유실·거짓 성공 보고 (2026-07-23)
20차가 "실패가 안 보이던 지점" 이었다면, 이번 주제는 한 단계 안쪽인 **"실패했는데 성공했다고
말하던 지점"** 이다. 저장 결과를 확인하지 않고 성공 메시지를 띄우거나, 미저장 편집을 저장하지 않은
채 화면을 비우는 경로 8건을 찾아 전건 수정했다. 테스트 268개(+5), 빌드 경고 0.

- **학적 갱신이 미저장 편집을 지우던 문제 수정**(`StudentCardViewModel`): `OnModelPropertyChanged` 가
  어떤 모델이 바뀌든 일단 `IsChanged = true` 로 올린 뒤, 읽기 전용인 Enrollment 분기에서 다시
  `false` 로 되돌렸다. 그래서 학생 정보를 편집하는 도중 학년·반·번호 변경 알림이 한 번이라도 끼면
  **미저장 편집 표시가 통째로 사라져** 자동 저장도 저장 버튼도 아무 일을 하지 않았다.
  → 편집 대상(Student/Detail)이 바뀔 때만 플래그를 세운다. 회귀 테스트 3건
- **저장 실패를 성공으로 보고하던 문제 수정**(`StudentCardViewModel.SaveAsync`): 리포지토리의
  `UpdateAsync` 결과(갱신 0행)를 버리고 무조건 `IsChanged = false` + `true` 를 반환했다.
  정본 행이 없는 학생이면 **"저장되었습니다" 를 띄운 뒤 편집이 사라졌다**. → 0행을 실패로 취급
- **학년/반 전환 시 미저장 편집 유실 수정**(`PageStudentInfo`): 학생을 바꿀 때는 먼저 저장했지만,
  학년/반·학기 필터를 바꾸는 경로는 곧장 목록을 다시 읽어 `StudentCard.ViewModel.Clear()` 로
  편집을 버렸다. → 목록 재로드 진입점에서 항상 먼저 내려쓰고, 저장에 실패하면 사용자에게 알린다.
  학생 전환 중 저장이 실패하면 목록 선택도 원래 학생으로 되돌린다(재진입 가드 포함)
- **누가기록 신규 항목이 저장되지 않던 문제 수정**(`LogListViewer`): 저장 대상이 "체크된 항목" 뿐인데
  **새로 추가한 기록은 체크가 안 된 상태**라 저장 버튼을 눌러도 삽입되지 않았다. 그런데도
  "누가기록이 저장되었습니다" 가 떠서, 학생을 바꾸는 순간 조용히 사라졌다.
  → 아직 DB 에 없는 항목(No ≤ 0)을 저장 대상에 포함하고, 실제 반영 건수를 세어 보고한다
  (저장할 것이 없으면 그렇게 안내). 삭제도 같은 방식으로 정정
- **누가기록 삭제 시 표시/DB 불일치 수정**(`LogListViewer`): DB 삭제 결과와 무관하게 목록에서
  행을 지워, 실패해도 화면에서만 사라졌다가 새로고침에 되살아났다. → 실제 삭제된 행만 제거
- **연결 누수 수정**(`LogListViewer`): `StudentLogService` 를 `using` 없이 만들고 마지막 줄에서
  `Dispose()` 했다. 루프 중간에 예외가 나면 SQLite 연결이 그대로 남았다. → `using` 으로 전환
- **학생 삭제의 표시/DB 불일치 수정**(`StudentManagementPage`): 저장 경로는 결과를 확인하는데
  삭제 경로만 결과를 버려, 0행이어도 목록에서 지우고 성공 집계에 넣었다. → 실패 시 목록 유지,
  저장·삭제 모두 대상 건수와 성공 건수가 다르면 그대로 알린다
- **진도 처리의 거짓 건수 수정**(`ProgressMatrixPage`·`LessonProgressRepository`):
  `MarkAsCompletedAsync`/`MarkAsIncompleteAsync`/`UpdateAsync` 가 갱신 행 수를 버려서,
  한 건도 반영되지 않아도 "N개 단원 완료 처리됨" 이라고 알렸다(특히 진도 기록이 없는 단원의
  미완료 처리는 항상 무반응). → 세 메서드가 반영 여부를 반환하고, 페이지는 실제 건수로 보고한다.
  일괄 처리 도중 예외가 나면 일부만 반영된 상태이므로 매트릭스를 다시 읽어 화면을 DB 와 맞춘다.
  회귀 테스트 2건

### 21차-1단계 — 스키마 소유권 정리 (2026-07-23)
21차에서 "관찰" 로만 남겼던 항목을 조사한 결과 예상보다 상태가 나빠 정리했다. `school.db` 33개 테이블 중
8개는 해당 리포지토리 생성자에서만 만들어졌고, 2개(`Lesson`·`LessonLog`)는 초기화기와 리포지토리가
**서로 다른 정의를 중복 보유**하고 있었다. 테스트 279개(+11), 빌드 경고 0.

- **`LessonLog` 스키마 분기 제거**: 같은 테이블을 두 곳이 다르게 정의했고 `CREATE TABLE IF NOT EXISTS`
  특성상 먼저 실행한 쪽이 이겼다. 초기화기 정의는 FK 2개를 갖고 `TeacherID` 가 NULL 허용,
  리포지토리 정의는 FK 가 없고 `TeacherID`·`Subject` 가 NOT NULL 이며 확장 컬럼 7개를 더 갖는다.
  실사용 DB 는 초기화기가 먼저 돌아 전자로 만들어진 뒤 `TryAddColumn` 이 컬럼을 덧붙여 굴러갔지만,
  초기화기를 타지 않는 경로에서는 **FK 없는 다른 테이블**이 만들어질 수 있었다.
  → 정의를 `LessonLogRepository.SchemaSql` 하나로 통합. 정본은 **실사용 DB 의 현재 실효 스키마**
  (초기화기의 컬럼 순서·제약 + ALTER 로 붙던 7개 컬럼을 같은 순서로 이어 붙임)로 맞춰,
  새로 만든 DB 와 기존 DB 의 스키마가 일치하게 했다. `Lesson` 도 같은 방식으로 통합
  (`DayOfWeek NOT NULL`·`Class DEFAULT 0`·FK `ON DELETE CASCADE` = 실사용 DB 기준)
- **FK 부모 테이블의 순서 의존 제거**: `LessonProgress`·`ScheduleUnitMap`·`WeeklyLessonHours`·
  `WeeklyUnitPlan` 이 `Schedule`·`CourseSection`·`SubjectYearPlan` 을 FK 부모로 선언하는데,
  그 부모들이 지연 생성이었다. `foreign_keys=ON` 에서 부모 테이블이 없으면 자식에 대한
  INSERT/UPDATE 가 준비 단계에서 `no such table` 로 실패한다. 지금까지는 화면들이 우연히 부모
  리포지토리를 먼저 열어 가려져 있었을 뿐이다(설계가 아니라 호출 순서 관습에 의존).
  → 리포지토리가 소유한 8개 테이블을 `DatabaseInitializer` 가 FK 부모→자식 순서로 미리 생성.
  정의는 각 리포지토리의 `SchemaSql` 상수 하나뿐이라 중복이 생기지 않는다
- **기존 DB 영향 없음**: 추가된 것은 전부 `CREATE TABLE IF NOT EXISTS` 라 이미 있는 테이블에는
  아무 동작도 하지 않는다. 각 리포지토리의 컬럼 추가·테이블 재작성 마이그레이션도 종전대로
  생성자에서 돈다. 이미 만들어진 DB 의 제약 차이를 소급 교정하려면 테이블 재작성과
  `PRAGMA user_version` 기반 마이그레이션이 필요한데, 얻는 것 대비 위험이 커서 하지 않았다
- **회귀 테스트 11건**: 초기화기만 돌린 DB 에서 8개 테이블 존재 확인, 부모 리포지토리 없이
  `LessonProgress` 갱신 동작, `LessonLog` 의 확장 컬럼 7개와 FK 2개 확인, 리포지토리를 나중에
  만들어도 컬럼 집합이 변하지 않음. 수정을 되돌리면 10건이 실패하는 것으로 유효성 확인
- **스키마 버전 표식 도입**(`PRAGMA user_version`): 지금까지 값이 0 이라 "이 DB 파일이 어느 시점
  스키마인지" 판별할 방법이 없었다. 초기화 끝에 `DatabaseInitializer.SchemaVersion`(현재 1)을
  기록해, 향후 제약 변경 등으로 테이블 재작성이 필요할 때 버전 기반 마이그레이션
  (`if (user_version < N) { …변환…; }`)의 기반을 마련했다. 데이터를 건드리지 않는 메타값이라
  기존 DB·새 DB 모두 무해. 실사용 `school.db` 를 사본으로 실측한 결과 `Lesson`·`LessonLog` 이
  이미 통합 정본과 일치(FK 정상·`foreign_key_check` 위반 0)해, 제약 소급 교정(2단계)은
  고칠 대상이 없어 하지 않기로 확정

### 20차 — 전수 조사: 로그 유실·표시 불일치·무음 실패 (2026-07-22)
소스 275개(.cs)·82개(.xaml) 약 104,000줄 전체 재점검. 19차까지 "패턴 스캔만 통과" 로 남아 있던
`ProgressMatrixPage`·`PageStudentInfo`·`PageSeats`·`DayCell` 4개 파일을 정독했다.
공통 주제는 **실패와 상태 변화가 사용자에게 보이지 않던 지점** — 크래시 로그가 디스크에 닿기 전에
사라지고, 저장 실패가 조용히 되돌아가고, 조건을 못 맞춘 자동 배치가 성공한 척하던 문제들이다.
테스트 263개(+4), 빌드 경고 0.

- **크래시 로그 유실 수정**(`FileLogger`): `Dispose()` 가 어디서도 호출되지 않아 종료 시 큐에 남은 로그가
  통째로 버려졌다. 게다가 호출되더라도 `_cts.Cancel()` 을 먼저 걸어 라이터가 `WaitAsync(token)` 에서
  즉시 예외로 빠지는 바람에 큐를 비우지 못했다(= 뒤이은 `_signal.Release()` 는 무의미). 하필 이 경로가
  진단을 정확히 때려서, `UnhandledException` 이 기록한 **원인 로그가 가장 확실하게 사라졌다**.
  → 큐를 먼저 비우고 라이터를 정지하도록 순서를 뒤집고, 동기 기록용 `Flush()` 를 추가해
  치명적 예외 처리기·`ProcessExit`·창 닫기에서 호출. 백그라운드 라이터와 `Flush()` 가 같은 파일에
  동시에 쓰지 않도록 파일 잠금으로 직렬화하고, Dispose 이후의 로깅도 동기 기록으로 흘려보낸다
- **할 일 완료 체크 시 표시 불일치 수정**(`DayCell`): 셀을 갱신하는 두 경로의 로직이 갈라져 있었다.
  `UpdateTasksDisplay` 는 `Settings.ShowTasks`(완료 항목 표시) 필터를 적용하는데
  `UpdateDayDisplayAsync` 는 `dayInfo.Tasks` 를 그대로 뿌리고 배지도 전체 개수로 찍었다.
  완료 토글이 후자를 호출하므로, **"할 일 표시" 를 꺼둔 상태에서 완료 체크하면 사라져야 할 항목이
  그대로 남고 배지 숫자도 줄지 않았다**(달력 전체 새로고침 전까지). 필터 적용 경로로 통일
- **할 일 상태 저장 실패의 무음 처리 수정**(`DayCell`): 메모리 상태를 먼저 뒤집고 DB 에 쓰는데
  실패하면 `Debug.WriteLine` 만 남겨, 화면은 완료·DB 는 미완료로 갈라진 채 다음 새로고침에 조용히
  되돌아갔다. → 실패 시 이전 상태로 복구하고 `UserErrorReporter` 로 알린다(프로젝트 자체 규약대로)
- **동시 대화상자 예외 방지**(`MessageBox`): WinUI 3 는 ContentDialog 를 동시에 하나만 허용하는데
  직렬화 가드가 없었다. `await` 없이 던지는 호출이 겹치면(예: **좌석 미사용 토글 연타 →
  `CheckSeat` 경고 2회**) 두 번째가 예외로 터지면서 정작 안내는 사라지고 오류창이 떴다.
  → 세마포어로 직렬화. 이 클래스를 거치지 않는 대화상자가 열려 있는 경우엔 닫힐 때까지 재시도하며,
  직접 만든 ContentDialog 도 태울 수 있도록 `ShowDialogAsync(ContentDialog)` 를 공개
- **좌석 자동 배치의 조건 충족 실패를 안내**(`PageSeats`): 시도 횟수(기본 500회)를 모두 소진해도
  제약을 만족하지 못하면 **마지막 시도의 위반 배치를 아무 말 없이 확정**했다. 분리·고정·남녀 교차·
  이력 회피를 동시에 걸어 해가 없을 때 교사는 조건이 지켜진 줄 알고 그대로 저장하게 된다.
  → 실패 시 적용 중인 조건 목록과 함께 경고
- **학생 기록 조회 N+1 제거**: `StudentLogViewModel` 이 기록 1건마다 학적·기본정보를 2회씩 조회했다.
  특히 **단일 학생**의 기록 목록에서도 같은 학생을 기록 수만큼 재조회해 결과가 전부 동일한 순수
  낭비였다(기록 50건 → 쿼리 100회). → 배치 팩터리 `CreateManyAsync` 추가(학생 ID 를 모아 조회 2회로
  종료)하고 호출부 4곳 교체: 학생 정보 페이지, 학급 일지 페이지 2곳, `ClassDiaryService`.
  배치 조회가 단건 조회와 같은 "현재 학적"(최신 학년도·학기)을 고르는지 회귀 테스트 4건 추가
- **창 축소 후 자동 배치 시 예외 방지**(`PageSeats`): `GridBody_SizeChanged` 가 비어 있어 창 크기가
  바뀌어도 카드가 재계산되지 않는데, 흩뿌리기 애니메이션이 `Random.Next(0, Room너비 - 카드너비)` 를
  그대로 호출해 창을 충분히 줄이면 상한이 음수가 되어 터졌다. → 0 으로 하한 처리
- **이벤트 핸들러 누적 제거**(`DayCell`): `TasksRepeater` 미로드 시 구독하는 지역 함수가 호출마다
  다른 델리게이트 인스턴스로 쌓였다(내부 `-=` 는 자기 것만 제거). 로드 시 전부 발동하며 오래된
  `dayInfo` 로 덮어쓸 수 있었다. → 대기 중인 대상만 갱신하고 구독은 1회로 고정

## 개발 이력 — 2026-07-19 ~ 07-22 (구 v1.2.0)

전수 코드 검사 1~19차의 결과를 모은 릴리스. **핵심은 시간표 자동 배치의 신뢰성 확보** —
배치 전 과정을 단일 트랜잭션으로 원자화하고, 그동안 막혀 있던 자동 배치 **다시 실행(Redo)** 을
슬롯 스냅샷 방식으로 구현했다. 밀기/당기기 미리보기와 배치 검사 버튼이 추가되어 배치 결과를
적용 전에 확인할 수 있고, 통합 내보내기는 좌석·학생카드 Excel 과 학생정보 명렬 타입까지 넓어졌다.
그 밖에 반복 일정 월말·윤년 드리프트, 수업 당기기 이중 배치, CSV 왕복 손상 등 데이터 정합성
버그를 다수 바로잡았다. 테스트 259개, 빌드 경고 0.

### 19차 — 자동 배치 다시 실행(Redo) 지원 (2026-07-21)
- **자동 배치 Redo 구현**: 그동안 `NotSupportedException` 으로 막혀 있던 자동 배치의 다시 실행을 지원.
  Undo 가 생성 스케줄을 하드 삭제하므로 ID 로는 복원이 불가능해, 배치 직후 각 슬롯의 내용
  (날짜·교시·고정여부·단원 매핑)을 `BulkGenerateActionData.Slots` 스냅샷으로 기록하도록 확장.
  Undo/Redo 를 스케줄 ID 가 아닌 (날짜·교시) 기준으로 대칭 처리해, Redo 재생성으로 ID 가 바뀌어도
  Undo→Redo→Undo 반복이 정확히 동작한다. 스냅샷이 없는 구 기록은 Undo 는 종전대로(생성 ID),
  Redo 는 안내 메시지와 함께 미지원. 회귀 테스트 2건 추가(총 259). UI(Redo 버튼)는 기존 배선 그대로 활성화

### 18차 — 학교 정보 설정 노출 확대 (2026-07-21)
- **NEIS 추가 정보 표시**: 학교 검색 시 NEIS 에서 이미 받아와 School DB 에는 저장되지만 설정 화면에는
  없던 5개 필드(학교 종류·개교기념일·전화번호·팩스번호·홈페이지)를 설정 > 학교 정보에 표시 전용으로 추가.
  Settings.db 중복 저장 없이 School 테이블(`GetSchoolByCodeAsync`)에서 로드 — 학교 검색 저장 직후와
  페이지 로드 시 모두 채워지며, 개교기념일은 8자리(YYYYMMDD)를 `YYYY-MM-DD` 로 정리해 표시

### 17차 — 죽은 코드 정리 (2026-07-21)
- **미구현·미사용 코드 제거**(호출처 0곳 전수 확인): `SchoolService.ImportFromNEISAsync`(NEIS 연동
  자리표시자 — 학교 검색은 `SchoolSearchDialog` 가 이미 NEIS 를 직접 호출), `TeacherService.LoginAsync`
  (비밀번호 검증이 TODO 로 비어 있던 로그인 — 단일 교사용이라 미사용) + 그로 인해 고아가 된
  `TeacherRepository.UpdateLastLoginAsync`, `AnnualLessonPlanViewModel.ConfirmPlanAsync`·`CanConfirm`
  (SchedulingEngine 전환으로 폐기된 구 "계획 확정" 로직)
- **주석 처리된 UI·유물 주석 제거**: 연간 수업 계획 XAML 의 주석 처리된 "계획 확정" 버튼,
  관련 code-behind 의 `// OnConfirmClick 제거됨` 유물 주석, `ClubService` 의 VS 자동생성 종료자 보일러플레이트

### 16차 — 시스템 인쇄 다이얼로그 연동 시도 → Native AOT 제약 확인 (2026-07-21)
- **결론: 인쇄 동작은 종전과 같다**(PDF 렌더 → 기본 뷰어 열기 → Ctrl+P). 아래 연동 코드는 들어가 있으나
  현재 배포 구성(Native AOT)에서는 비활성이며, AOT 를 끄면 코드 수정 없이 인쇄 대화상자가 살아난다
- **막힌 지점**: `PrintManagerInterop.ShowPrintUIForWindowAsync` 가 결과를 `IAsyncOperation<bool>` 로
  동적 캐스팅하는데, CsWinRT 2.2 는 동적 코드가 꺼진 호스트에서 이 ABI 헬퍼 조회를 거부하고
  `NotSupportedException` 을 던진다. `PublishAot=True` 가 csproj 전역이라 **Debug 빌드의 runtimeconfig 에도**
  `IsDynamicCodeSupported=false` 가 박힌다. 네이티브 호출은 성공해 대화상자는 뜨고 마샬링에서만 실패하므로,
  증상은 "인쇄창은 뜨는데 미리보기가 영영 로드 중 + PDF 뷰어가 같이 뜸" 이었다.
  · CsWinRT AOT 최적화기(`CsWinRTAotOptimizerEnabled`) 활성화 → CCW vtable 만 생성, 해당 헬퍼는 미생성
  · `WinRT.TypeExtensions.RegisterHelperType` 수동 등록 → public 아님, ABI 타입도 internal
  ※ 같은 `IAsyncOperation<bool>` 을 쓰는 `Launcher.LaunchFileAsync`(PDF 열기)가 멀쩡한 이유는
    정적 인스턴스화라 동적 조회를 타지 않기 때문 — Interop 클래스 특유의 문제
- **적용한 방어**: 라이브러리가 `RuntimeFeature.IsDynamicCodeSupported` 를 확인해 **대화상자를 띄우기 전에**
  물러난다. 죽은 인쇄창이 뜨거나 PDF 뷰어와 겹치는 일이 없어, 사용자 눈에는 종전 동작 그대로다
- **함께 고친 WinUIRichEditor 결함**(AOT 가 아닌 호스트에서 이 경로가 실제로 동작하도록):
  ① `PrintDocument`·`IPrintDocumentSource` 를 지역 변수로만 들고 있어 await 시점에 GC 되며 인쇄 콜백이
  통째로 사라지던 문제 → 작업이 끝날 때까지 static 필드로 유지하고 `PrintTask.Completed` 에서 해제
  ② 대화상자가 열린 뒤에도 `false` 를 반환해 호출부 폴백이 겹쳐 뜨던 문제
  ③ 페이지 래스터를 CsWinRT 미지원 경로(`PixelBuffer.AsStream()`)로 복사하던 것을 Win2D PNG 인코딩 +
  `BitmapImage` 로 교체(기본 dpi 300→150, A4 1장 ≈ 8MB)
- **호스트 창 해석**: 대화상자는 소유 창 HWND 가 필요하고 에디터 어댑터는 MainWindow 와
  RichTextEditorWin(전체화면 편집) 양쪽에 얹히므로, `XamlRoot.ContentIslandEnvironment.AppWindowId` 로
  실제 호스트 창을 역추적한다(실패 시 MainWindow 폴백)
- 인쇄 작업 이름(게시글 제목·편집창 제목·"학생 정보")도 함께 전달. 도움말(게시판 인쇄)은 실제 동작대로 정정

### 15차 — 배치 도구 완성 + 통합 내보내기 확장 (2026-07-20)
- **자동 배치 실행 취소**: 자동 배치가 Undo 기록(BulkGenerate)을 남기지 않아 되돌릴 수 없던 것을,
  생성된 스케줄 ID 를 기록해 Undo 버튼으로 일괄 취소 가능하게 함(Undo 로직은 기존에 있었으나 저장처 부재)
- **배치 검사 버튼**: 기존에 구현만 되어 있던 `ValidateScheduleAsync`(미배치·시수 초과 검사)를
  연간 수업 계획의 수업 배치 패널에 돋보기 버튼으로 노출 — 결과를 대화상자로 표시
- **수업 밀기/당기기 UI + 미리보기**: 서비스만 있고 UI 가 없던 Push/Pull 을 배치 목록에 버튼으로 연결.
  실행 전 "어떤 수업이 어디로 이동하는지" 목록을 미리보기로 확인 후 적용 —
  계획 산출(BuildPush/PullPlanAsync)과 적용을 분리해 미리보기·실행이 같은 계획을 공유
- **배치 목록 로드 N+1 제거**: 배치 결과 목록이 스케줄마다 매핑을 개별 조회하던 것을
  IN 절 일괄 조회로 교체(단일/전체 학급 로드 양쪽)
- **통합 내보내기 확장**: ① 좌석배정 Excel(좌석 그리드 + 명단 2시트) ② 학생카드 Excel(학생당 1행
  전체 항목) ③ 학생정보 명렬 타입 신설(번호·이름·성별·생년월일·연락처·주소·보호자 —
  Excel/PDF/HTML/CSV + 클립보드 복사). 항목 선택형 상세 출력은 기존 '학생 정보 출력' 페이지 유지.
  도움말(통합 내보내기·연간 수업 계획) 갱신

### 전수 검사 14차 — 자동 배치 원자화 + 시수 부족 경고 (2026-07-20)
- **자동 배치 단일 트랜잭션화**: 자동 배치가 삭제→고정 배치→순차 배치를 건별 자동커밋으로
  수백 회 수행하던 것을, schedule/map 리포지토리가 연결을 공유할 때 단일 트랜잭션으로 묶음 —
  중간 실패 시 "기존 배치만 지워지고 새 배치는 일부만 남는" 문제 방지 + 건당 fsync 제거로 배치 속도 개선.
  `ScheduleRepository`·`ScheduleUnitMapRepository` 에 공유 연결 생성자 추가
- **검증 전 삭제 제거**: 기존 배치 삭제가 유효성 검증(단원 없음 등)보다 먼저 실행돼, 검증 실패로
  조기 반환해도 기존 배치가 사라지던 문제 — 검증 통과 후에만 삭제하도록 순서 교정.
  미사용 `clearExisting` 파라미터(false 시 기존 스케줄 점유를 무시하고 겹쳐 배치되는 잠재 결함)도 제거
- **시수 부족 배치 경고**: 슬롯 부족으로 필요 시수의 일부만 배치된 단원이 "배치됨"으로만 집계되던 것을
  단원별 경고(FillWarnings)로 명시 — 배치 결과 대화상자에 "⚠️ N절: 시수 부족 배치: 5/8차시" 표시
- **밀기/당기기 트랜잭션화**: Push/Pull 의 이동 UPDATE 루프를 단일 트랜잭션으로 — 중간 실패 시
  일부만 이동된 상태로 남는 것을 방지
- **배치 검증 N+1 제거**: `ValidateScheduleAsync` 가 스케줄마다 매핑을 개별 조회하는 루프를
  두 번 돌던 것(2N회 쿼리)을 IN 절 일괄 조회(`GetBySchedulesAsync`, 500개 청크) 1회 + 단일 패스 집계로 교체
- **명부표 조회 최적화**: RosterTableDialog 의 학생 필터가 `List.Contains`(O(N×M))로 동작하던 것을
  HashSet 으로 교체(5곳). 엔진 회귀 테스트 3건 추가(총 257)

### 전수 검사 13차 — 수업 당기기 이중 배치 수정 + 캐시 회계 보정 (2026-07-20)
- **당기기(Pull) 이중 배치 수정**: 수업 당기기가 이동하지 않는 스케줄(고정 수업, 기준일 이전 수업)의
  슬롯을 점유로 인식하지 않아, 당겨지는 수업이 고정 수업이나 기존 수업 위로 이동해 겹치던 문제 —
  밀기(Push)와 동일하게 비이동 스케줄 슬롯을 점유로 시딩. 이동 불가 건수도 결과 메시지에 표시
  ("N개는 빈 슬롯이 없어 이동 불가"). 회귀 테스트 2건 추가(총 254)
- **게시판 캐시 크기 회계 보정**: 같은 키를 교체 저장할 때 옛 항목의 크기만 차감하고 항목은 남겨,
  직후 LRU 정리에서 같은 항목이 다시 뽑히면 크기가 이중 차감되어 15MB 상한이 실제보다 커지던 문제 —
  차감 시점에 즉시 제거. 상한(15MB)보다 큰 단일 항목은 캐시를 다 비우고도 저장돼 상한을 넘던 것도
  캐시 생략으로 변경(미스 시 로컬 SQLite 재조회로 충분)

### 12차 — 앱 아이콘 교체 (2026-07-20)
- **아이콘 리디자인**: 기존 선형 아이콘(펼친 책 + 새싹)은 선이 가늘고 여백이 커서 작업표시줄·16px
  크기에서 형태가 뭉개졌다. 코발트(`#1B45C4`) 라운드 타일에 흰색 대문자 T 를 얹은 모노그램으로 교체 —
  요소가 둘뿐이라 16px 까지 형태가 유지되고, 밝은/어두운 작업표시줄 양쪽에서 대비가 확보된다
- **교체 범위**: `newschool.ico`(16~256 10종)·`newschool.png`, 타일/스플래시 에셋 7종 전체.
  타일 계열은 Windows 권장에 맞춰 심볼 주위 여백을 별도로 부여(Square150 8%, 와이드 14%, 스플래시 26%)
- **패키지 아이콘 경로 수정**: `PackageIcon` 항목이 리포 밖 `..\..\..\..\Downloads\newschool.png` 를
  참조해 다른 환경에서 팩이 깨지던 문제 — 리포 내 `newschool.png` 로 교정

### 11차 — CSV 파서 공용화 + TimePicker 컴팩트화 (2026-07-19)
- **CSV 인용 필드 파싱 수정**: `ConvertCsvToExcel` 이 `Split(',')` 로 나눠 따옴표 안 쉼표·줄바꿈이 든
  필드를 깨뜨리던 문제 — RFC 4180 파서를 `CsvExportService.ParseRecords` 로 공용화하고
  `ConvertCsvToExcel`·`AnnualLessonPlanPage` 가 함께 사용(중복 제거). 파서 테스트 6건 추가
- **시간 선택 컴팩트화**: 기본 TimePicker 는 시/분을 넓은 별도 칸으로 벌려 표시해(콜론 없이) 과하게
  넓었다. 일정 대화상자는 "07:35" 한 줄로 표시하고 누르면 표준 시간 플라이아웃(24시간제)이 뜨는
  컴팩트 버튼 컨트롤(`CompactTimeButton`)로 교체 — 여백·폰트 최소화, 고정 Width 미사용으로 배율 안전.
  설정 화면 TimePicker 2개는 `CompactTimePickerStyle`(24시간제 + MinWidth 0 + 좌측정렬) 적용.
  두 NumberBox 방식의 옛 `CompactTimePicker` 는 제거

### 전수 검사 10차 — 반복 일정 월말·윤년 드리프트 수정 (2026-07-19)
- **월간/연간 반복 날짜 고착 수정**: 반복 할 일 생성이 직전 값에 `AddMonths`/`AddYears` 를 누적해,
  31일 시작 월간 반복이 2월(28일)을 지난 뒤 계속 28일에 고착되던(1/31→2/28→3/28…) 문제 —
  항상 앵커(원본 시작일) 기준으로 계산하도록 `RecurrenceHelper` 로 분리(1/31→2/28→3/31→4/30…,
  2/29 는 다음 윤년에 29일 복귀). 순수 로직 회귀 테스트 7건 추가(총 246)
  ※ 반복 항목은 task(시점) 성격이라 duration(End=Start)은 의도된 동작으로 유지

### 전수 검사 9차 — 과목+시간표 생성/삭제 원자화 (UnitOfWork) (2026-07-19)
- **고아 과목 방지**: `CreateCourseWithScheduleAsync`·`DeleteCourseAsync` 가 Course·Lesson 을
  서로 다른 연결로 처리해, 과목 생성 후 시간표 생성이 실패하면 시간표 없는 고아 과목이 남던 문제 —
  하나의 연결·트랜잭션을 공유하는 `TimetableUnitOfWork` 를 도입해 원자적 커밋/롤백 보장
  (Scheduler·Board 의 기존 UnitOfWork 패턴과 동일). `CourseRepository`·`LessonRepository` 에
  공유 연결 생성자 추가. 원자성 회귀 테스트 2건 추가(성공 시 함께 생성 / 실패 시 함께 롤백, 총 238)

### 전수 검사 8차 — 수업 명부표 생성 크래시 수정 (2026-07-19)
- **명부표 생성 중복 키 크래시**: 수업 명부표(RosterTableDialog)가 학적 조회 시 학기를 지정하지 않아
  1·2학기 학적이 모두 반환되고, 양 학기 재적 학생(연속 재학생)에서 `ToDictionary(StudentID)` 가
  중복 키 예외로 크래시하던 문제 — 현재 학기로 한정 + GroupBy 로 학생당 1행 보장(간이/상세 양쪽 경로)

### 전수 검사 7차 — DatePicker 날짜 변환 범위 방어 (2026-07-19)
- **생년월일 DatePicker 잠재 크래시 방어**: `DateTimeToDateTimeOffsetConverter` 가 기본값
  `DateTime.MinValue`(0001-01-01)를 그대로 `DateTimeOffset` 으로 변환하면 KST(UTC+9) 오프셋 적용 시
  범위를 벗어나 예외가 나던 문제 — 해당 값은 미설정으로 간주해 null 을 반환하도록 가드 추가

### 전수 검사 6차 — 특기사항 일괄 저장 실패 항목 유실 방지 (2026-07-19)
- **일괄 저장 실패 편집 유실 수정**: 학생 특기사항 일괄 저장 시 일부가 실패해도 변경 목록(_modifiedIds)을
  통째로 비워, 실패한 학생의 "수정됨" 표시가 사라지고 재저장에서 건너뛰어 편집 내용이 조용히
  유실되던 문제 — 성공한 항목만 목록에서 제거하고 실패 항목은 수정 상태로 유지하도록 수정

### 전수 검사 5차 — 시간표 저장 원자성 (2026-07-19)
- **Course 시간표/학급 시간표 저장 데이터 유실 방지**: `UpdateCourseScheduleAsync`·
  `CreateClassTimetableAsync` 가 기존 시간표를 삭제한 뒤 새로 생성하는데 트랜잭션이 없어,
  중간 실패 시 기존 시간표만 사라지고 새 시간표는 일부만 남던 문제 — 각 저장을 단일 트랜잭션으로
  감싸 실패 시 원자적으로 롤백되도록 수정 (Fix E 와 동일 유형)

### 전수 검사 4차 — 좌석 초기화 0 나눗셈 방어 (2026-07-19)
- **좌석 초기화 방어 가드**: 학생이 없거나 배열(줄·짝)이 설정되지 않은 상태에서 좌석 초기화 시
  `TotalStudents/(줄×짝)` · `/TotalRows` 가 0 나눗셈이 되어 카드 크기가 NaN 이 되고 좌석 배치가
  깨지던 문제 — 해당 상태에서는 빈 좌석만 표시하고 종료하도록 선행 가드 추가

### 전수 검사 3차 — 단원 일괄 생성 원자성 (2026-07-19)
- **CSV 가져오기 데이터 유실 방지**: `CourseSection.BulkCreateAsync` 가 기존 단원(+진도·매핑 연쇄삭제)을
  먼저 지운 뒤 새 단원을 한 건씩 삽입하는데 트랜잭션이 없어, 중간 실패 시 기존 데이터만 사라지고
  새 단원은 일부만 남던 문제 — 전체를 단일 트랜잭션으로 감싸 실패 시 원자적으로 롤백되도록 수정

### 전수 검사 2차 — 일정 이동 버그 + 조회 성능 (2026-07-19)
- **수업 밀기 이중 배치 수정**: 이동 대상에서 제외된 고정(pinned) 수업의 슬롯 위로 일반 수업이
  밀려 같은 날·교시에 겹치던 문제 — 밀기에도 당기기와 대칭으로 점유 슬롯(occupiedSlots) 추적을
  추가하고, 다음 슬롯 탐색이 점유된 슬롯을 건너뛰도록 수정
- **밀기/당기기 공휴일 누락 수정**: 슬롯 생성 휴일 필터가 "공휴"를 빠뜨려, 최초 자동배치 땐
  제외됐던 공휴일 슬롯으로 수업이 이동될 수 있던 문제 — SchedulingEngine 과 동일 기준으로 일치
- **학적 다건 조회 성능**: ReaderColumnCache 매퍼가 단건 조회에만 연결돼 있던 것을 반별·학년별·
  학교별·학기별 등 다건 조회 전반에 적용 — 행마다 반복되던 GetOrdinal 호출 제거

### 전수 검사 1차 — 결함/성능 4건 정리 (2026-07-19)
- **캐시 크기 회계 불일치**: `CacheManager.Set` 이 기존 항목 교체 시 크기를 재계산(`EstimateSize`)해
  차감하던 문제 — 컬렉션 변형·1000개 순회 상한으로 저장값과 달라져 총 점유량이 어긋나던 것을
  저장 당시 확정된 `entry.Size` 차감으로 수정(15MB 상한이 정확히 동작)
- **자동배치 앵커 대체 슬롯 정리**: 지정일에 슬롯이 없을 때 `OrderBy(Abs(거리))` 였으나 대상이
  이미 "지정일 이후"로 한정돼 `Abs` 가 무의미했음 — 날짜·교시 순 정렬로 단순화(동작 동일, 의도 명확화)
- **명부 반 번호 파싱 방어**: `RosterTableDialog` 이 반 이름을 `int.Parse` 하여 숫자가 아닌 반 이름에서
  미처리 예외가 나던 문제 — `TryParse` + 안내 메시지로 방어
- **학교 검색 HttpClient 공유**: NEIS 학교 검색이 호출마다 `new HttpClient()` 를 생성하던 소켓 고갈
  안티패턴 — 프로젝트 관례대로 `static readonly` 공유 인스턴스로 전환

## 개발 이력 — 2026-07-01 ~ 07-15 (구 v1.1.0)

v1.0.4 이후 누적 대형 릴리스. **핵심은 에디터 엔진 전면 교체** — 웹 기반 Jodit(WebView2 + ~900KB JS)를
네이티브 Win2D 리치에디터(WinUIRichEditor)로 바꾸고 WebView2 런타임 의존을 완전히 제거했다.
함께 WinAppSDK 1.8→2.2 업그레이드, Post 콘텐츠 `.flow`(BLOB) 저장 전환도 포함(상세는 아래 v1.0.6·v1.0.5).
이번 릴리스에서 게시 파이프라인을 Native AOT 로 전환했다.

### 졸업 처리 제거 + 사소한 결함 4건 정리 (2026-07-15)
- **졸업 처리(GraduateAsync) 제거**: 조회가 전부 학년도 기준이라 별도 졸업 마감이 불필요 —
  UI 미노출 상태에서 초등(1~6학년) 진급을 졸업으로 오처리하던 버그의 온상이어서 삭제.
  진급(`PromoteStudentsAsync`)은 다년 이력 연속성(같은 StudentID 유지)의 핵심이라 유지하되
  최고 학년(maxGrade)은 대상에서 제외하도록 수정 — 진급 UI(반/번호 재배정 미리보기)는 추후 과제
- 댓글 수정 시 Post 캐시도 무효화 — 목록 댓글 수가 최대 5분 구버전으로 남던 문제
- 글 저장/삭제 시 카테고리·주제 목록 캐시(30분) 무효화 — 새 주제가 필터에 늦게 나타나던 문제
- OK 전용 대화상자에서 ESC 로 닫으면 None 대신 OK 반환(버튼 구성별 안전한 기본값으로 통일)
- 사진 캐시 LRU 를 접근 순번 기반으로 교체 — Dictionary 순서 의존으로 사실상 무작위 제거되던 문제
- 테스트 236건: 졸업 테스트 5건 → 진급 회귀 테스트 3건(최고 학년 스킵·StudentID 연속성·휴학 제외)

### 전수 조사 버그 수정 5건 (2026-07-15)
- **백그라운드 작업 오류 알림 미표시**: UnobservedTaskException 핸들러가 UI 스레드가 아닌 곳에서
  ContentDialog 를 직접 생성해 COMException 으로 조용히 실패하던 문제 — DispatcherQueue 로
  UI 스레드에 넘겨 알림이 실제로 표시되도록 수정
- **학적 조회 학기 필터 무시**: `GetEnrollmentsAsync` 의 semester 인자가 리포지토리에 전달되지
  않던 문제 수정 + 학급 명부(`GetClassRosterAsync`)는 1·2학기 학적이 모두 있어도 학생당
  최신 학기 행 하나만 반환(좌석 인쇄·학생카드·통합 내보내기 등 중복 방지). 회귀 테스트 2건 추가(총 238)
- **1인석 자리 이력 고정**: 좌석 저장 Round 를 짝 이력(SeatHistory)만으로 계산해 1인석(Jjak<2)
  학급은 위치 이력이 첫 저장에서 멈추던 문제 — 두 이력 테이블 MAX 로 수정("지난 자리 배제" 정상화)
- **학생 사진 교체 후 옛 사진 표시**: 같은 경로 덮어쓰기 시 이미지 캐시가 무효화되지 않던 문제 —
  저장/삭제 시 해당 경로 캐시 제거 + 확장자가 바뀌면 이전 확장자 고아 파일도 정리
- **주기 동기화 중지 경합**: 동기화 진행 중 StopPeriodicSync() 호출 시 루프가 null 이 된 필드를
  읽어 NRE 가 백그라운드로 새던 문제 — 타이머·토큰을 지역 변수로 캡처

### 시작 게이트 오탐 수정 — 일시 오류를 DB 손상으로 판정하던 문제 (2026-07-15)
- 시작 시 무결성 점검(DbIntegrity)이 **모든 예외를 손상으로 간주**해, 백신·클라우드 동기화의
  파일 점유, 읽기 전용 속성, 다른 인스턴스 잠금 같은 일시 오류에도 "데이터베이스 손상 감지"
  경고를 띄우고 시작을 막던 문제 수정 — 이제 SQLite 손상 코드(CORRUPT·NOTADB)와
  quick_check 비정상 결과만 손상으로 판정, 일시 오류는 로그만 남기고 통과(다음 실행 재점검)
- 점검용 연결 풀링 해제 — 점검 후 풀에 남은 파일 핸들이 백업/복원의 파일 교체를 방해하지 않도록
- 회귀 테스트 2건 추가(독점 잠금·읽기 전용 파일은 손상 아님) — 총 236건

### 메모리 점유 최적화 (2026-07-14)
- **GC 메모리 절약 모드**(`System.GC.ConserveMemory=7`) 적용 — 힙을 적극 압축·OS 반납해
  상주 메모리 축소. 이벤트 구동형 앱이라 GC 압력이 낮아 체감 성능 영향 없음
- 게시판 캐시 상한 50MB→15MB — 캐시 5분 만료 특성상 히트율 차이 없이 최악 점유만 축소

### 연간 수업 계획 버그 수정 — CSV 왕복·Undo 화면 갱신 (2026-07-12)
- **CSV 가져오기 파서 재작성(RFC 4180)**: 내보내기는 따옴표 이스케이프("")와 줄바꿈 포함
  필드를 만들지만 가져오기는 단순 줄 분리라, 여러 줄 메모·따옴표가 있는 단원을
  내보냈다 다시 가져오면 데이터가 깨지던 문제 수정 — 이제 왕복이 정확히 일치
- **실행 취소/다시 실행 후 배치 목록 미갱신 수정**: Undo/Redo 성공 시 버튼 상태만 갱신되고
  화면의 배치 목록은 이전 상태로 남던 문제 — 목록도 함께 재로드
- 시수 셀 탭 핸들러의 죽은 매개변수(루프 변수를 잘못 캡처한 컬럼 인덱스) 제거

### 백업 v2 (ZIP) + 좌석 이력 중복 방지 + 리스트 스크롤 유지 (2026-07-12)
- **백업이 ZIP 단일 파일로**: 각 DB를 `VACUUM INTO`(원자적 스냅샷, WAL 포함, 빈 공간 제거)로
  뜬 뒤 하나의 ZIP 으로 압축 — 폴더 복사 대비 용량 대폭 감소, 실패 시 체크포인트+복사 폴백.
  복원은 ZIP 선택(신규)·폴더 백업의 .db 선택(구버전)·단일 .db(하위호환) 모두 지원,
  보관 개수 정리도 ZIP·폴더 통합 최신순. 스냅샷 동작 회귀 테스트 추가(228→229)
- **좌석 이력 UNIQUE 인덱스**: SeatHistory(짝)·SeatPosHistory(자리)에 라운드 단위 UNIQUE
  인덱스 추가 — 기존 중복 행은 초기화 시 자동 정리, 저장은 INSERT OR IGNORE 로 안전화
- **리스트 소량 변경 시 스크롤 유지**: OptimizedObservableCollection 이 8건 이하 추가/제거에는
  Reset 대신 개별 알림 사용 — 전체 재가상화·스크롤 초기화 회피

### Google 동기화 실패 알림 + 자동 재시도 (2026-07-12)
- 백그라운드(시작 시·주기적) 동기화 실패가 디버그 로그에만 남아 사용자가 알 수 없던 문제 —
  메인 창 하단 우측에 **경고 InfoBar**(첫 오류 + 건수)를 띄우고 **'다시 시도'** 버튼 제공
  (재시도 성공 시 성공 표시 후 3초 뒤 자동 닫힘). 오프라인·미인증 등 사전 체크 탈락은
  주기마다 반복 알림이 되지 않도록 표시하지 않음
- **실패분 자동 재시도**: 동기화에 오류가 있으면 마지막 동기화 시각을 전진시키지 않도록 변경 —
  기존에는 Push 실패한 수정분이 다음 주기 비교 대상에서 빠져 재수정 전까지 유실됐음.
  이제 다음 주기(또는 재시도 버튼)에서 자동으로 다시 올라감

### 요일별 교시 수 설정화 (2026-07-12)
- 현재 교시 판정에 하드코딩돼 있던 "월·수 6교시, 그 외 7교시"를 **설정 → 교시 시간 설정 →
  요일별 교시 수**(월~금, 1~12)로 이동 — 다른 시정을 쓰는 학교에서도 홈 화면 현재 교시
  배지·시간표 행 강조가 정확해짐. 기본값은 기존 동작과 동일(월 6·화 7·수 6·목 7·금 7)
- `PeriodCounts` 타입 신설("6,7,6,7,7" 직렬화, 형식·범위 오류 시 기본값 폴백),
  `GetPeriodAt`은 순수 함수 유지. 4교시 이하 시정에서 점심을 종료 시각에 넣지 않도록
  하교 시각 계산도 일반화. 테스트 17개 추가(211→228)

### 전수 코드 검사 — 데이터 안전성·동기화 버그 수정 (2026-07-12)
- **school.db 외래키 활성화**: 연결 PRAGMA에 `foreign_keys=ON` 누락으로 스키마의
  `ON DELETE CASCADE/SET NULL`이 전부 무시되던 문제 수정(Board·Scheduler는 이미 켜져 있었음) —
  수업/동아리 하드 삭제 시 수강신청·시간표 고아 행이 조용히 쌓이던 원인. DB 초기화 시
  기존 고아 행 일괄 정리(CASCADE 삭제·SET NULL 보정·누락 School 스텁 생성) 단계 추가
- **WAL 백업 데이터 누락 수정**: 백업이 `.db` 파일만 복사해 `-wal`에만 있는 최근 커밋이
  빠질 수 있던 문제 — 복사 전 `wal_checkpoint(TRUNCATE)` 수행. 복원 시에는 연결 풀 정리 +
  잔여 `-wal`/`-shm` 삭제 후 덮어써 복원본 오염 방지 (전체 백업·school.db 백업 공통)
- **학사일정 Google 동기화 크래시 수정**: 같은 행사명이 비연속 날짜에 반복되면(예: 재량휴업일)
  제목 중복으로 동기화 전체가 실패하던 문제 — 재조정 자연키를 "제목+시작일"로 변경
- **홈 자정 롤오버**: 앱을 켜둔 채 날짜가 바뀌면 날짜 헤더·시간표·급식이 어제 것으로 남던 문제 —
  1분 타이머에서 날짜 변경 감지 시 전체 재로드
- **자잘한 수정**: Windows 시작 시 자동 실행 토글이 설정 DB에 저장되지 않던 것, 급식 메뉴명
  띄어쓰기 잘림("친환경 쌀밥"→"친환경"), NEIS API 키가 디버그 로그에 노출되던 것(마스킹)
- **성능**: NEIS 바이트 카운트를 문자별 정규식→범위 비교로 교체(실시간 글자수 카운트),
  날짜 파싱 성공 시마다 찍히던 디버그 로그 제거(대량 로드 병목), 로그 파일 정리를 플러시마다→시간당 1회로,
  `Log.Debug`가 파일 로그에도 기록되도록(레벨 Debug 설정 시 릴리스 진단 가능)

### 홈 현재 교시 행 강조 (2026-07-12)
- 오늘 시간표(**내 수업 / 우리 반**)에서 지금 진행 중인 교시 행에 **은은한 틴트 배경 + '지금' 배지** 표시.
  상단 현재 교시 배지와 같은 1분 주기 타이머로 갱신되며, 쉬는시간·점심·방과후 등 수업 외 시간에는
  아무 행도 강조되지 않음
- 비강조 행은 오버레이가 접혀 레이아웃 변화 없음. 교시 판정은 기존 순수 함수 `Functions.GetPeriodNow`(Index) 재사용

### 오늘 페이지 재설계 + 대시보드 카드 테마 통일 (2026-07-06)
- **대시보드 카드 공용 스타일**(`App.xaml`: `DashboardCard`/`DashboardCardHeader`/`Icon`/`Title`) 신설.
  시간표·학사일정·급식·메모·할일 카드에 흩어져 있던 하드코딩 색(`CornflowerBlue` 테두리, `Lavender`
  헤더, 이모지 제목)을 테마 리소스(`CardStrokeColorDefaultSolidBrush` 등) + 강조색 `FontIcon` + 통일 라운드로
  일괄 교체 → **라이트/다크 자동 대응**, 4벌 중복 마크업을 단일 소스로
- **오늘 페이지**(Avalonia SaemDesk 설계 이식)
  - 상단 **날짜 헤더** 추가: 오늘 날짜 · 요일(강조) · 오늘 학사일정(있을 때만) · **현재 교시 배지**(1분 주기 갱신)
  - 시간표를 정적 주간 그리드 → **내 수업 / 우리 반** 오늘 슬롯 표시로 변경(담임이 아니면 '우리 반' 열 접힘)
  - 교시 배지는 인포 블루 칩, 카드 베이스는 중립 그레이 유지
- 학사일정·할일 항목 글자 12→13px, 할일 카테고리 배지 9→11px

### 창 항상-위에(TopMost) 실동작 + 다이얼로그 z-order + 에디터 API 정리 (2026-07-06)
- **TopMost 실제 구현**: 설정 토글이 저장만 하고 적용되지 않던 걸 `OverlappedPresenter.IsAlwaysOnTop`으로
  실동작화(`MainWindow.SetAlwaysOnTop`), 시작 시 저장값 적용 + 토글 즉시 반영. 기본값 `true`→`false`(옵트인)
- **다이얼로그 숨김 방지**: 메인 창이 항상-위에일 때 별도 `Window` 다이얼로그가 뒤로 숨던 문제 수정 —
  MemoEditDialog·RichTextEditorWin·ClassDiaryListWin·StudentLogDialog·StudentSpecBatchDialog 표시 시
  같은 topmost 레벨로 올림(`ContentDialog`는 인-윈도우라 영향 없음)
- **에디터 API 대응**: WinUIRichEditor 에서 `EditorMode` 프리셋이 제거되어(→ `IsReadOnly` + 툴바) 어댑터가
  빌드되지 않던 문제 수정 — `ApplyMode`를 `IsReadOnly` 매핑으로 전환

### 학생·수업·설정 메뉴 버그·정합성 수정 + 아카이브 메모 확인 표시 (2026-07-05)
- **학생 메뉴 6개 페이지**
  - 학생 관리: 인라인 편집한 이름/비고가 저장되지 않던 문제 수정(이름은 정본 `Student` 경유로 갱신해
    denormalized `Enrollment.Name`까지 동기화), 저장 대상을 `IsModified||IsSelected`로 확장
  - 누가 기록: 특정 학기 선택 시 `semester` 인자 누락으로 학기 필터가 무시되던 버그 수정
  - 학생 정보: 페이지 이탈 시 자동저장을 `await`해 Dispose 경합 방지, 죽은 코드·미사용 서비스·루프 내 N+1 정리
  - 학생부 기록: `Unloaded` 이중 등록 정리 / 학생정보 출력: 사용자정의·비고 컬럼명 중복 예외 방지
  - 학생 추가: **성별 열 추가**(수동입력 콤보 + 엑셀 파싱 + 저장/목록/내보내기) — 전원 "남" 고정 제거,
    중복검사 DB오류 시 중복 간주로 안전화, 엑셀 읽기 비동기화
- **수업 메뉴**: 수업 시간표 `Insert` 인자 뒤바뀜 크래시 수정, MessageBox 제목/메시지 뒤바뀜 19곳 정정
  (수업·동아리 관리·학급 시간표), 학급 시간표 편집 취소 시 뜨던 "저장됨" 오탐 제거, 누가 기록의
  `ConfirmLeave` 반환값 무시(저장 실패 시 전환 강행) 수정
- **설정 메뉴**: 학사일정 인라인 편집(`IsModified`) 항목을 체크 없이도 저장, 학년도 종료일 `2/28`
  하드코딩 → 2월 말일 계산(윤년 2/29 포함)
- **아카이브**: 게시글 보기/아카이브에서 메모의 확인(완료) 상태 표시 + 상세 페이지 컴팩트화

### 달력 UI 정리 + 학사일정 전용 Google 캘린더 + 아젠다 목록 재설계 (2026-07-03)
- **달력 색상 정리**
  - 날짜 숫자와 학사일정(`TbDateName`) 텍스트 색 규칙을 분리 — 날짜는 공휴일(최우선)/일요일/토요일/평일,
    학사일정 텍스트는 공휴일/휴업일만 반영(요일 무관)
  - 일정 항목 배경을 하드코딩 구글 블루에서 캘린더별 색상 점(dot) + 투명 배경으로 교체 — 이벤트 자체
    색상이 없으면 소속 캘린더 색으로 폴백(`KEvent.CalendarColor`)
  - 오늘 날짜 원형 배지(`TodayCircle`) 제거 — 셀 전체 테두리(`TodayHighlight`)만으로 충분해 중복 정리
  - 날짜 숫자·학사일정 텍스트를 `VerticalAlignment="Bottom"`으로 통일해 폰트 크기가 달라도 아랫단 정렬
  - 요일 헤더 굵게, 요일/날짜/년월 선택(`MonthPicker.DisplayFontSize`) 폰트 크기 통일
- **폰트 설정 정리**: 설정창 라벨과 실제 대상이 어긋나 있던 걸 명확히 함 —
  `EventFontSize`="학사일정 폰트"(TbDateName), `TaskFontSize`="할 일 폰트"(DayCell 이벤트+할일 목록 둘 다),
  `DateFontSize`="요일/날짜 폰트". 이벤트/할일 목록 폰트는 `ItemsRepeater` 컨테이너 재활용 타이밍에 기대는
  code-behind(`ElementPrepared`) 대신, `EventColorToBrush(DisplayColor)`와 동일하게 검증된 x:Bind 정적
  메서드 패턴으로 통일해 신뢰성 확보
- **학사일정 → Google 전용 캘린더**: 학사일정을 기존 "수업" 캘린더(수업/학급/업무와 공유)가 아닌
  **학교별 전용 Google 캘린더**에 동기화하도록 재설계
  - `KCalendarList.SchoolCode` 추가 — 학교가 바뀌면 새 캘린더를 생성해 예전 학교 학사일정과 분리
  - `GoogleSyncService.UploadSchoolSchedulesAsync`를 단순 업로드에서 **재조정(reconcile) 동기화**로
    전면 재작성 — 신규/날짜변경/삭제를 모두 비교 처리, `school.db` 수정 후 재실행하면 구글도 맞춰짐
  - 로컬 KEvent(`ItemType="schoolschedule"`)에 `GoogleId`를 저장해두는 근본 수정으로, 향후 어떤 경로로
    Pull이 일어나도 중복 생성되지 않도록 함(기존엔 Push 후 로컬에 흔적을 안 남겨 Pull 시 매번 중복 생성됐음)
  - 빈 학사일정 목록이 들어오면(NEIS 조회 실패 등) 기존 동기화분을 삭제로 오인하지 않도록 안전 가드 추가
- **아젠다 목록(KAgendaControl) 재설계**: 날짜별 그룹 헤더 방식 → 단일 평탄 목록으로 변경
  - 각 행 `[분류 배지][날짜][시간][제목][완료/진행]` — 유형 아이콘(○/●/▶) 대신 캘린더 분류 배지로 교체
  - `AgendaHeader`/`AgendaTemplateSelector`/`TypeIcon`/`AccentColor` 등 더는 안 쓰는 코드 정리
  - 캘린더 조회 실패(레거시 `CalendarId=0` 등) 시 배지가 빈 이름으로 숨겨지던 버그 수정("기타"로 폴백)

### 학년도/학급/과목 공용 필터 컨트롤 + 스케줄러 심층 리뷰 + 메모창 리사이즈 (2026-07-03)
- **공용 필터 컨트롤 도입** — `SchoolFilterPicker`를 `YearSemesterPicker`/`ClassPicker`/`CoursePicker` 3개로 분리해
  9개 페이지(학생관리·학생부·학급일지·학급시간표관리·수업관리·학생정보·학생기록·학생정보출력·통합내보내기)에서 재사용.
  과목·강의실 단위로 교과세특을 조회/작성하는 `CourseSpecPage` 신설
  - CoursePicker가 Native AOT에서 과목명을 못 띄우던 리플렉션 바인딩(`DisplayMemberPath`) → `x:Bind` ItemTemplate 전환
  - YearSemesterPicker+ClassPicker 조합 페이지에서 이중 초기화 로드되던 문제 → `ClassPicker.AutoLoad` 옵션 추가
  - 수강생 조회 학생별 순차 쿼리(N+1) → `EnrollmentRepository.GetCurrentByStudentIdsAsync` 배치 조회로 교체
  - `StudentSpecBox` 저장 실패 시에도 학생이 전환되던 데이터 유실 위험 → `ConfirmLeaveAsync()` 반환값 확인하도록 수정
  - `CourseSpecPage` 일괄 저장 미트랜잭션 처리 → `StudentSpecialService.SaveManyAsync` 트랜잭션 도입
- **스케줄러 심층 리뷰 수정**
  - `KtaskList` 참조로 신규 설치 시 스케줄러 DB 초기화가 항상 실패하던 문제
  - Google Calendar 증분 동기화(`syncToken`)가 `orderBy`와 함께 전송되어 두 번째 동기화부터 영구 실패하던 문제(400 응답 시 전체 동기화로 폴백하는 방어도 추가)
  - DB 초기화/복원 시 풀링된 연결이 파일을 잠가 실패하던 문제 → `SqliteConnection.ClearAllPools()` 추가
  - Google 이벤트 색상 팔레트가 실제 Google Calendar 값과 어긋나 있던 문제
  - 할 일(task)의 `End`가 `Start`보다 앞서 저장되던 문제
  - 학사일정 Google 업로드 재실행 시 중복 등록되던 문제 (기존 이벤트 조회 후 스킵)
  - 최초 동기화(`GoogleLastSyncTime` 미설정) 시 로컬 수정분 Push가 통째로 스킵되던 문제
  - 달력 로드 시 이벤트를 두 번 조회하던 중복 쿼리 제거, 인위적 `Task.Delay` 누적 제거(달력 초기 로드·다이얼로그 오픈)
  - 캘린더 삭제 시 소속 이벤트가 고아로 남던 문제 → 트랜잭션으로 함께 삭제
  - **반복 할 일 시리즈 삭제** — `KEvent.SeriesId` 신설, 반복 생성된 항목을 "이 항목만 삭제 / 이후 반복 항목 모두 삭제"로 선택 삭제 가능
- **메모 편집 창을 `ContentDialog`에서 `Window`로 전환** — 사용자가 자유롭게 드래그로 크기 조절 가능(기존 `RichTextEditorWin` 패턴 재사용)
- **WinUIRichEditor 캐럿 버그 수정**(별도 프로젝트 참조) — 빈 문서에서 캐럿이 에디터 세로 중앙에 뜨던 문제(Win2D 캔버스의
  `Stretch`+고정 `Height` 조합이 원인) → 캔버스를 좌상단(`Top`/`Left`)에 고정, 상단에 좌측 들여쓰기와 동일한 여백 추가

### Native AOT 게시 전환 + 미사용 자산 제외 (2026-07-01)
- **R2R+SingleFile → Native AOT 게시** — `PublishAot` 실활성화(기존엔 SingleFile/R2R 와 상호배타라 무시되던 설정).
  게시본 **~165MB → 46.6MB**(네이티브 exe 32.3MB + 네이티브 dll 소수), 파일 335 → 10개
- **미사용 전이 자산 게시 제외** — WinAppSDK 2.2 가 끌고 오는 WebView2·Windows AI(ML) 자산 제거(`RemoveUnusedWinAppSdkAssets` 타깃):
  managed projection(~2MB) + 네이티브 ML 런타임 `onnxruntime.dll`(20.7MB)·`DirectML.dll`(17.8MB) = **~40MB 절감**.
  Win2D(`Microsoft.Graphics.Canvas`)는 에디터가 사용하므로 유지
- **AOT 안전성 조정** — `ReportExportService` 의 엑셀 저장을 `MiniExcel.SaveAsAsync`(비동기 어댑터 `MakeGenericType`→IL3050
  동적코드 위험) → 백그라운드 동기 `SaveAs` 로 전환. MiniExcel·QuestPDF 는 `TrimmerRootAssembly` 로 보존(IL2104 억제)
- **게시 오케스트레이션 수정** — VS 프로필 게시 시 `PublishProfile` 전역속성이 ProjectReference(WinUIRichEditor)로 전파돼
  라이브러리에 ILLink 단독 실행→IL1034 실패하던 문제 해결(라이브러리 pubxml 의 앱스타일 트림/단일파일 설정 비활성화)
- 검증: 프로필 AOT 게시 성공, IL1034·IL3050·IL2104 0, exe 기동 정상

## 개발 이력 — 2026-06-18 ~ 06-27 (구 v1.0.6)

### Post 콘텐츠 저장 .flow 전환 + 검색 평문 분리 (2026-06-27)
- **에디터 내용을 WinUIRichEditor 네이티브 `.flow` 패키지(BLOB)로 저장** — 압축·무손실, 이미지는 base64 팽창 없이 원본 바이트
  - 스키마: `Post.Content` TEXT → BLOB(.flow), `PlainText TEXT` 컬럼 신설(검색·미리보기·인쇄용)
  - `Post.Content` 모델 `string` → `byte[]`, `PostRepository` BLOB I/O, 게시판 내용검색 `Content LIKE` → `PlainText LIKE`
  - 어댑터에 `GetFlowBytes()`/`LoadFlow(byte[])` 추가, 소비처(메모·게시판·자료) 로드=flow·저장=flow+평문 전환
  - `Controls/FlowText` — 평문→.flow 헬퍼(MaterialEditDialog 등 평문 입력용, 헤드리스 모델/포매터)
  - 미배포 전제로 하위호환/스니핑 없이 .flow 전용. 기존 DB 변환은 별도 콘솔 툴 `MemoFlowMigrator`(저장소 외부)로 1회 수행
  - 실측: 실 DB(이미지 포함 글) 변환 후 VACUUM 시 **3.89MB → 0.51MB(−87%)**
- **할일/일정 다이얼로그(UnifiedItemDialog) 노트** → 리치에디터 대신 일반 멀티라인 `TextBox`(KEvent.Notes 는 평문 위주)

### MemoBoard 재설계 — 최신 메모 고정 에디터 + compact 목록 (2026-06-27)
- WebView2/Jodit 우회책이던 **단일 에디터 reparent 트릭 제거** (1062→465줄)
- 인라인 = 가장 최근 활성 메모 1개 고정 편집(첨부 없음), 나머지 = compact 목록 → 클릭 시 다이얼로그 편집
- 완료 체크 = 숨김(아카이브 조회), 정식 게시물 승격은 게시판에서 별도
- `RichTextEditor.ShowToolbar` 옵션 추가: false=bare `RichEditor`(편집면만, 퀵잡용)/true=`RichEditorView`(툴바+크롬)
- 메모 에디터는 bare 컨트롤 — HTML 붙여넣기·단축키 서식 유지하되 군더더기 UI 제거

### 에디터 메모리 — 호스트 종료 시 즉시 해제 (2026-06-26)
- `RichTextEditor.Dispose()` 가 `Clear()` 로 네이티브 CanvasTextLayout 캐시 + GPU 이미지 비트맵 결정적 반환(공유 D3D 디바이스는 유지)
- 다이얼로그·페이지(MemoEditDialog·UnifiedItemDialog·Export·PostEdit/Detail) 닫힐 때 에디터 해제 연결

### 에디터 교체: Jodit(WebView2) → WinUIRichEditor(Win2D) + WebView2 완전 제거 (2026-06-26)
- **WinUIRichEditor 도입** — 웹 기반 Jodit 에디터(WebView2 + ~900KB JS)를 네이티브 Win2D 리치에디터로 교체
  - `Controls/RichTextEditor` — `RichEditorView` 호스팅 어댑터(UserControl). 기존 `JoditEditor` 와 동일 공개 API
    (`Text`/`Mode`/`PlainText`/`TextChanged`/`GetHtmlAsync`/`InsertHtml`/`PrintAsync`/`IsInitialized`/`IDisposable`) 로 드롭인 교체
  - `Controls/RichTextEditorWin` — 구 `JoditEditorWin`(전체화면 편집 윈도우) 대체
  - WinUIRichEditor 는 `ProjectReference` 로 참조(Win2D 1.4.0 추이 의존)
  - **9개 사용처 전수 교체**: `MemoEditDialog`·`MemoBoard`(인라인 reparent)·`PostEditPage`(표삽입 `InsertHtml`)·
    `PostDetailPage`·`ClassDiaryBox`·`StudentInfoExportPage`·`UnifiedExportPage`·`UnifiedItemDialog`(2)
- **WebView2 의존 완전 제거**
  - 도움말: `HelpPage`(WebView2) 제거 → `MainWindow.OpenHelpInBrowserAsync` 가 `help.html` 을 **기본 웹 브라우저**로 띄움
    (`help.html` 은 Content 로 유지 — CSS·다크모드·TOC·JS 전부 실브라우저에서 동작)
  - 설치 프로그램(`installer.iss`·`Installer/NewSchoolSetup.iss`): WebView2 부트스트래퍼 전부 제거 → 런타임 의존 소멸
  - 제거 파일은 `NewSchool-archive/jodit-webview-20260626/` 에 별도 보관(`JoditEditor*`·`JoditEditorWin*`·`HelpPage*`·`Assets/Jodit/*`)
- **인쇄 UX 변경**: WinUIRichEditor 는 시스템 인쇄 다이얼로그가 없어 `PrintAsync` 가 PDF 렌더 후 기본 뷰어로 열기(추후 PrintManager 검토)
- 검증: 빌드 경고 0/오류 0, 앱 기동·Win2D 에디터 렌더·한글 입력·도움말 브라우저 열기 확인

### Windows App SDK 1.8 → 2.2 업그레이드 (2026-06-26)
- `NewSchool.csproj`·`NewSchool.Tests.csproj`: `Microsoft.WindowsAppSDK 1.8 → 2.2.0`, TFM `net10.0-windows10.0.19041.0 → 10.0.26100.0`
  (`TargetPlatformMinVersion 17763` 유지)
- 설치 프로그램: 런타임 패키지명 `Microsoft.WindowsAppRuntime.1.8 → Microsoft.WindowsAppRuntime.2`(2.x 부터 PFN 이 메이저 버전에 정렬),
  레지스트리·다운로드 URL 갱신
- breaking change 영향 없음 — 제거/변경은 대부분 AI/ML 영역(미사용), deprecated WinUI API(`Window.Current` 등)도 미사용(modern `DispatcherQueue`)

### 테스트 프로젝트 신설 (2026-06-18)
- `NewSchool.Tests` (xUnit 2.9.2) — `NeisHelper.CountByte/GetMaxBytes` 29건 + `CsvExportService.Escape` 21건 = **50 테스트 통과**
- 실행: `dotnet test -p:Platform=x64 -p:RuntimeIdentifier=win-x64`. `CsvExportService.Escape` 를 internal 전환 + `InternalsVisibleTo`

### 버그 수정 (2026-06-18)
- 좌석 복원 시 미사용 경고 오발 수정, `PhotoCard` 비동기 경합 해소, 명렬표 숫자만 표기

## 개발 이력 — 2026-04-19 ~ 04-22 (구 v1.0.5)

### CA1063/CA1001 전수 해결 (2026-04-22)
- **CA1063 — IDisposable 구현 패턴**: 22개 클래스를 `sealed` 로 전환
  - Services 13개: `TeacherService`·`SchoolService`·`StudentDetailService`·`GoogleAuthService`·`GoogleSyncService`·`ClassDiaryService`·`SchedulerService`·`TimetableService`·`SchoolScheduleService`·`StudentSpecialService`·`EnrollmentService`·`StudentService`·`SeatService`
  - Repositories 2개: `LessonProgressRepository`·`UndoHistoryRepository`
  - Infrastructure 3개: `DatabaseInitializer`·`FileLogger`·`UnitOfWork`
  - ViewModels 3개: `StudentLogViewModel`·`ClassDiaryViewModel`·`StudentCardViewModel` (`protected virtual OnPropertyChanged`/`SetProperty` → `private` 로 전환해 봉인 충돌 해결)
  - Controls 1개: `JoditEditor` (UserControl sealed)
- **CA1001 — IDisposable 필드 보유 타입**: 11개 클래스에 IDisposable 구현
  - Pages 7개: `PageSeats`·`PageStudentInfo`·`PageStudentLog`·`SchoolScheduleManagementPage`·`StudentInfoExportPage`·`StudentManagementPage`·`StudentSpecPage` — `Dispose()` 에서 서비스 필드 해제, 생성자에서 `Unloaded += (_,_) => Dispose();` 훅업
  - Controls 3개: `MemoBoard`·`ClassDiaryListWin`(Window·`Closed` 훅)·`LessonLogList`
  - Singleton 1개: `Caching/CacheManager` — `_cleanupCts` 정리 책임 명확화, sealed
- **editorconfig 정리**: CA1063/CA1001 을 `warning` 으로 승격(이전엔 전수 리팩토링 유예 주석), 빌드 경고 0 유지

### 정적 분석 표준화 + Dead code 정리 (2026-04-22)
- **`.editorconfig` 신설** (프로젝트 루트)
  - C# 코드 스타일 규칙 고정 — `var` 사용, 식 본문 멤버, 패턴 매칭, `using` 선언, 파일 범위 네임스페이스
  - 진단 severities 명시
    - **warning**: CS8600~8618(null 가능성), CA1825(빈 배열), IDE0005(불필요 using)
    - **suggestion**: CA1822(static 승격), IDE0051/0052/0060(미사용 멤버), IDE0090(new 간소화), CA1063·CA1001·CA2100(전수 리팩토링 전까지)
    - **none**: CA1304~1310(CultureInfo — 한국어 고정 환경), CA1031(catch Exception — 프로젝트 정책), CA1848(LoggerMessage — FileLogger 구조상 과잉)
  - 인코딩 CRLF/UTF-8, XAML/JSON 2칸, C# 4칸 일관성
  - **빌드 경고 0 유지**(suggestion 은 IDE 에서만 표시)
- **Dead code 2차 정리** — Helpers 계층 호출처 0건 메서드 일괄 제거
  - `Helpers/ExcelReader.cs` (ExcelHelper) — 7개 제거: `GetData`, `ReadSheetAsObjectArray`(private 헬퍼), `ReadWithHeaders`, `GetSheetNames`, `ReadAsDataTable`, `WriteArray`, `CreateExcelStream(DataTable)`, `CreateExcelStream<T>`
  - `Helpers/NeisHelper.cs` — 4개 제거: `GetAreaDisplayName`, `IsOverLimit`, `GetByteInfo`, `GetRemainingBytes`
  - **총 11개 메서드** 제거, 유지되는 공개 API 는 주석으로 명시
  - **제외 사항**: `Helpers/TextBoxDropHelper` 의 `GetEnableTextDrop`/`SetEnableTextDrop` 은 XAML attached property 프레임워크가 호출하므로 유지(초기 스캔 오탐)

### 키보드 단축키 · 다크모드 일관성 (2026-04-21 UI 정리)
- **Ctrl+S 표준화** — 저장 버튼 9곳에 `KeyboardAccelerator` 추가, 툴팁에 `(Ctrl+S)` 표기
  - `Pages/PageStudentInfo` · `Pages/PageStudentLog` (BtnSaveActLog) · `Pages/LessonActivityPage` · `Pages/ClubActivityPage` · `Pages/PageSeats` · `Pages/AddStudentsPage` · `Pages/SchoolScheduleManagementPage` (BtnSaveSelected) · `Board/Pages/PostEditPage` (SaveButton) · `Dialogs/StudentSpecBatchDialog` (BtnSaveAll)
  - 기존 적용 페이지(StudentManagementPage, StudentSpecPage) 합쳐 총 11곳에서 Ctrl+S 통일
- **Ctrl+F 검색창 포커스** — `Page.KeyboardAccelerators` + `Invoked` 핸들러로 검색 TextBox `Focus(Keyboard)` + `SelectAll`
  - `Board/Pages/PostListPage` → `SearchTextBox`
  - `Board/ListViewer` → `TBoxSearch`
- **다크모드 하드코딩 제거**
  - `Scheduler/Kcalendar.xaml` — `Background="White"` / `Foreground="Black"` / `BorderBrush="#E0E0E0"` → `ThemeResource LayerFillColorDefaultBrush` · `TextFillColorPrimaryBrush` · `ControlStrokeColorDefaultBrush`
  - `Scheduler/DayCell.xaml` — BrdBase `Background="White"` + `BorderBrush` → ThemeResource 로 전환
  - `Scheduler/KAgendaControl.xaml` — 외곽 Border `Background="White"` 제거
  - `Board/Controls/CommentBox.xaml` — UserControl `Background="White"` 제거
  - `Controls/MonthPicker.xaml` — Flyout 그리드 `Background="White"` → `FlyoutPresenterBackground`
  - `Board/ListViewer.xaml` — Page 배경 + 상/하/검색바(`#FFF8F8F8`), 테두리(`#E0E0E0`/`#F0F0F0`), 보조/삼차 텍스트(`#666666`/`#999999`) 를 모두 ThemeResource 로 일괄 치환 (13곳)
- **유지 사항**: 인쇄용 HTML/PDF 서비스(`HtmlExportService`/`SeatsPrintService`/`StudentCardPrintService`/`StudentLogPrintService`)의 흰 배경·검은 글자는 **의도적 보존** — 종이 출력물은 항상 라이트

### 이벤트 핸들러 람다 → named method 전환 (2026-04-21 마감)
- **배경**: `dialog.Closed += async (s,args) => { ... await LoadLogsAsync(); }` 형태의 람다가 10여 곳에 분산돼 있었음. 다이얼로그가 GC 되기까지 페이지 메서드를 계속 캡처하고, 핸들러 체인에서 자기 자신을 떼어낼 수 없어 잠재적 누수·테스트 불가 코드였음
- **대응**: 각 Page/Control 에 공용 named method(`OnLogDialogClosedReload`, `OnLogDialogClosedReloadDaily`, `OnLogDialogClosedReloadStudent` 등)를 추가하고 핸들러 내부에서 `sender.Closed -= ...` 로 자기 제거
  - `Pages/PageStudentLog.xaml.cs` — 3건 (개별 추가, 일괄, 컨텍스트 메뉴)
  - `Pages/PageStudentInfo.xaml.cs` — 1건 (학생 로그 재로드)
  - `Pages/LessonActivityPage.xaml.cs` — 3건 (일괄+개별+컨텍스트). 일괄 다이얼로그는 `IsSuccess/SavedLogs` 참조가 필요해 지역 named function 사용
  - `Pages/ClubActivityPage.xaml.cs` — 2건
  - `Pages/ClassDiaryPage.xaml.cs` — 3건. `ClassDiaryListWin.DiarySelected` 람다도 `OnDiarySelected` 로 분리, `listWin.Closed` 에서 해제
  - `Controls/LogListViewer.xaml.cs` — 편집 다이얼로그 1건 (지역 named function + `vm` 캡처 유지)
- **효과**: 핸들러 체인 투명성 회복, 누수 경로 제거, 디버깅 시 스택 트레이스가 람다 메타명 대신 실제 메서드명으로 표시

### 성능·메모리·가상화 개선 2차 (2026-04-21 심화)
- **시작 시간 단축**
  - `App.OnLaunched` 의 자동 백업(`Settings.RunAutoBackupIfNeeded`)을 `Task.Run` 으로 밀어 시작 경로 비차단 — 백업 주기 도래 시 체감되던 1~3초 블로킹 제거
  - `TodayPage.Page_Loaded` 의 시간표·학사일정·할일/일정·급식 4종 로드를 `Task.WhenAll` 로 병렬화 (기존 순차) — 누적 400ms~1.2초 → 가장 느린 하나 시간으로 단축, 개별 실패는 `SafeLoadAsync` 로컬 함수로 격리
- **메모리 누수 해소**
  - `PageSeats` — `PhotoCard` 당 6개 이벤트(StudentChanged·UnUsedChanged·FixedChanged·DragOver·Drop·Tapped) 구독이 `InitSeats` 재호출 시마다 누적되던 문제 해결. `DetachCardEvents` 헬퍼 신설 → `InitSeats` 선두 + `Page_Unloaded` 에서 일괄 해제, `StudentList.StudentSelected` 도 해제
  - `StudentSpecBatchDialog` — `this.Closed` 에서 `StudentList.StudentSelected -= OnStudentSelected` 추가, 다이얼로그 반복 열기 누수 차단
- **ListView/ItemsRepeater 가상화 복구**
  - `StudentManagementPage.xaml` — 구조를 `ScrollViewer > StackPanel > [Header + ItemsRepeater]` → `Grid(Auto + *) > [Header 고정 + ScrollViewer > ItemsRepeater]` 로 재구성. 수백 명 학생도 실 렌더 영역만 DOM 생성
  - `Board/ListViewer.xaml` — 최상위 `ScrollViewer` 제거(내부 `PostsRepeater` 의 ScrollViewer 가상화를 무력화하던 이중 구조). 페이징·검색 행은 Grid RowDefinition 이 이미 `Auto` 라 레이아웃 동일
- **Dead code 정리** — 호출처 0건 확인된 public 메서드 2건 제거
  - `StudentRepository.DeleteByIdAsync` (물리 삭제; 사용처는 논리 삭제 `DeleteAsync` 만)
  - `LessonProgressRepository.MarkAsCancelledAsync` (결강 처리; 형제 메서드 `MarkAsSkipped/Makeup` 만 호출됨)

### 성능·안정성·내보내기 개선 (2026-04-21)
- **TextBox 한글 IME 기본 입력** (`Helpers/KoreanImeHelper.cs` 신설)
  - `App.xaml` 전역 `Style` 에 `helpers:KoreanImeHelper.UseHangul="True"` 첨부 속성 자동 적용 — 모든 `TextBox` 에 별도 설정 없이 한글 입력 활성
  - `GotFocus` → `DispatcherQueue` 한 틱 지연 후 현재 IME 상태를 `ImmGetConversionStatus` 로 조회, 한글이 아니면 `SendInput(VK_IME_ON)` 으로 전환 (레거시 폴백 `VK_HANGUL`)
  - **구현 포인트**: WinUI 3 TextBox 는 TSF(Text Services Framework) 로 입력을 처리하므로 `ImmSetConversionStatus` 는 호출이 수락(`ok=True`)되어도 실제로는 무시됨 — 가상 키 입력 시뮬레이션이 유일하게 동작하는 경로
  - 이미 한글 상태면 skip, 영문 전환은 기존 한/영 키로 자유롭게
- **사용자 행동 경로 silent catch 스윕** — 파일 업로드·저장 등 사용자가 명시적으로 트리거한 작업에서 `Debug.WriteLine` 만 남기던 예외 처리를 모두 `UserErrorReporter.ReportAsync` 알림으로 승격
  - 대상: `StudentSpecBatchDialog` · `CourseEnrollmentDialog` · `CourseSectionDialog` · `PostEditPage` · `MemoEditDialog` · `MaterialEditDialog` · `MemoBoard(자동저장)`
- **Google Calendar 일시 오류 재시도** (`GoogleCalendarApiClient.SendWithRetryAsync`)
  - `HttpRequestException` · `TaskCanceledException`(네트워크 타임아웃) · HTTP 5xx 를 일시 오류로 간주, 지수 백오프(2·4·8초) 로 최대 3회 재시도
  - 사용자 취소(`CancellationToken`) 는 즉시 전달, `MaxTransientRetries = 3` 통일
  - 재시도 시 `CreateAuthRequestAsync` 로 새 요청 생성하여 HttpContent 재사용 문제 회피

- **N+1 쿼리 해소** (`UnifiedExportService`)
  - `StudentLogRepository.GetByStudentIdsAsync` / `StudentSpecialRepository.GetByStudentIdsAsync` 신설 — `WHERE StudentID IN (@id0,@id1,...)` 단일 쿼리로 학급 전체 일괄 조회, `Dictionary<StudentID, List<T>>` 반환
  - `LoadClassLogsAsync`: 학생당 2쿼리 × N명 → **총 2쿼리**(1·2학기 일괄)로 축소
  - `LoadClassSpecsAsync`: 학생당 1쿼리 × N명 → **총 1쿼리**로 축소
  - 30명 학급 기준 DB 라운드트립 약 30배↓, 빈 리스트 기본값으로 키 존재 보장
- **전역 예외 안전망** (`App.xaml.cs`)
  - `Application.UnhandledException` 확장: 기존 Debug·FileLogger 로깅 유지 + 사용자 대화상자 알림 + `e.Handled = true`로 앱 강제 종료 방지
  - `TaskScheduler.UnobservedTaskException` 포착 — `async void` / fire-and-forget Task에서 샌 예외도 사용자에 알림 후 `SetObserved()`
  - `AppDomain.CurrentDomain.UnhandledException` — 최종 안전망(파일 로그)
  - `Controls/UserErrorReporter.cs` 신설 — `ReportAsync(context, ex, title?)` 공용 헬퍼(자체 실패 방어)
- **CSV 내보내기 + 클립보드 복사** (`UnifiedExportPage`)
  - `Services/CsvExportService.cs` 신설 — UTF-8 BOM + RFC 4180 인용 + CSV Injection 방지(`=`,`+`,`-`,`@` 선행 인용)
  - `UnifiedExportService.ExportFormat.Csv` 추가, 누가기록/학생부 분기 연결
  - `BuildClassLogsCsvAsync` / `BuildClassSpecsCsvAsync` — 클립보드용 문자열 반환
  - UI: "CSV (.csv)" 라디오 + "클립보드 복사" 버튼 — `Clipboard.SetContent(DataPackage)`로 엑셀·구글 시트 즉시 붙여넣기
  - 좌석배정/학생카드 선택 시 CSV·Excel·클립보드 버튼 자동 비활성화

### 좌석배정표 인쇄 옵션 강화 (2026-04-20)
- **출력 방향 설정** (`SeatPrintOptionsDialog`)
  - 자동 (좌석 가로 칸수 > 세로 칸수이면 가로 출력) · 세로 · 가로 선택
  - `PrintOrientation` enum 추가 (`SeatsPrintService`)
  - PDF: `PageSizes.A4.Landscape()` 분기, 가용 영역(535×782 ↔ 782×535) 스왑 후 셀 크기·상단 여백 재계산
  - HTML: `@page { size: A4 landscape }` 조건부 출력
- **학급 명렬표 삽입 옵션**
  - 왼쪽 사이드바(82pt)에 번호·이름 2열 표, 헤더 `번호 | 이름`
  - 모든 학생이 한 페이지에 들어가도록 행 높이·폰트(5~11pt) 자동 조정
  - HTML: flex 레이아웃 (`.layout > .sidebar + .main`), `.main` 의 교탁은 column-flex 하단 정렬
- **교탁 라벨 개선** — `"교 탁"` → `"{grade}학년 {classRoom}반"`

### 게시판 명렬표 표 구조 개편 (2026-04-20)
- **그룹 세로 레이아웃** (`RosterTableDialog.BuildGroupedVerticalTable`) — 그룹명을 별도 구분 행이 아닌 **칼럼**으로 이동해 엑셀 복사·정렬·필터 용이
- **학급 전체**(학년 내): `학급 | 번호 | 이름 | 사용자칼럼...` (`1반`, `2반`)
- **수업 전체**(강의실 그룹): `강의실 | 학년 | 학급 | 번호 | 이름 | ...` — 강의실 내 여러 학급 혼재 대응, 학년→학급→번호 순 정렬
- **동아리**: `학년 | 학급 | 번호 | 이름 | ...` (학년·학급은 숫자만 표기)
- `BuildGroupedVerticalTable` 시그니처를 `leadHeaders[]` + flat `rows` 로 일반화
- `LoadCourseStudentsByRoomDetailedAsync`, `LoadClubStudentsDetailedAsync` 신설

### 통합 내보내기 — 학생카드 타입 추가 (2026-04-20)
- `DataType.StudentCard` 추가 (PDF/HTML 전용, Excel 자동 비활성화)
- `StudentCardPrintService.GenerateClassCardsPdfFromDbAsync` — 학급 전체를 단일 PDF(학생당 1 페이지)
- `HtmlExportService.BuildClassCardsHtml` / `ExportClassCardsToHtml` — 학생당 1 섹션 구성(기본 정보·보호자·건강·진로)
- `StudentCardViewModel.LoadFromModels` 공개 메서드 — DB 호출 없이 Enrollment + Student + StudentDetail 주입

### 비밀 정보 관리 구조 개편 (2026-04-20)
- **`secrets.json` 통합 도입** — Google OAuth + NEIS API key 를 하나의 파일로 관리 (`.gitignore`)
- `SecretsService` — 런타임에 `AppContext.BaseDirectory/secrets.json` 로드, 없으면 빈 값
- `secrets.template.json` 커밋 — 새 환경용 템플릿
- 기존 `google_oauth.json` 제거, `GoogleAuthService.ClientId`/`ClientSecret` → `SecretsService` 경유
- `Settings.NeisApiKey` 기본값 → `SecretsService.NeisApiKey` (런타임 주입)
- 과거 커밋에 포함되어 있던 NEIS API 인증키 제거 — `git-filter-repo` 로 히스토리 재작성 및 force-push 완료
- 설치 프로그램(`installer.iss`, `NewSchoolSetup.iss`) — `google_oauth.json` → `secrets.json`

### 좌석 배정 전면 개편
- **DB 영속화**: 학급별 좌석 배치를 DB에 저장/복원 (`SeatService`, 4개 테이블)
  - `SeatArrangement` — 학급별 배치 메타 (Jul/Jjak/Rows/사진표시/메시지/잠금/옵션)
  - `SeatAssignment` — 좌석 셀 (Row/Col/StudentID/미사용/미표시/고정)
  - `SeatHistory` — 짝 이력 (회차별 인접 학생 쌍)
  - `SeatPosHistory` — 위치 이력 (회차별 학생-좌표)
  - 학급 선택 시 저장된 배치 자동 복원 (`PageSeats.TryLoadSavedArrangementAsync`)
- **옵션 다이얼로그** (`SeatOptionsDialog`, Pivot 4개 탭)
  - **이력**: 최근 N회차 짝 배제 / 같은 자리 배제
  - **속성**: 남녀 교차 짝 (짝 모드 우선), 앞자리 우선 학생(허용 범위 지정)
  - **짝 제약**: 분리(🚫) · 고정(📌) 쌍 관리
  - **방식**: 배치 시도 횟수 (100/500/1000/3000)
  - 옵션은 `OptionsJson`으로 직렬화해 배치와 함께 저장 (AOT-safe `SeatOptionsJsonContext`)
- **배치 알고리즘 개선** (`PageSeats.ArrangeSeatAsync`)
  - 앞자리 우선 학생을 범위(Row 0~N) 내에 우선 배치
  - 이력 기반 회피: 최근 짝/위치 기피
  - 유효성 검증: 분리/고정 쌍, 최근 짝, 성별 교차를 만족할 때까지 재시도
- **저장/잠금 버튼** (`PageSeats`)
  - 💾 수동 저장 (회차 누적, 이력 기록)
  - 🔒 배치 잠금 (드래그/재배치 금지)
- **통합 내보내기 연동** (`UnifiedExportService`, `UnifiedExportPage`)
  - `DataType.Seats` 추가 — 좌석배정은 PDF/HTML 전용 (Excel 자동 비활성화)
  - `SeatsPrintService.GenerateSeatsPdfFromDbAsync` — DB에서 직접 로드해 PDF 생성
  - `SeatsPrintService.GenerateSeatsHtmlFromDbAsync` / `BuildSeatsHtmlFromDbAsync` — HTML 파일/문자열 출력
  - 렌더링 코어를 `SeatCellData` DTO로 리팩터링해 PhotoCard 비의존
- **컨텍스트 메뉴 동기화 수정** (`PhotoCard`)
  - 프로그램이 `IsUnUsed`/`IsFixed`/`IsHidden`을 설정할 때 우클릭 메뉴의 `ToggleMenuFlyoutItem.IsChecked`도 함께 갱신 → 복원된 배치에서 메뉴 상태 정상 표시

## 개발 이력 — 2026-04-06 (구 v1.0.4)

### UI 개선
- 달력 시인성 강화: 오늘 날짜에 파란 원형 배경 + 파란 테두리, 마우스 호버 시 회색 배경 효과 추가 (`DayCell`)
- 홈 학사일정 스크롤 수정: `ItemsRepeater` → `ListView`로 교체하여 마우스 휠 스크롤 정상 작동 (`SchoolScheduleListControl`)
- 게시물 보기 레이아웃 개선: `ScrollViewer+StackPanel` → `Grid` 레이아웃으로 변경, JoditEditor 내부 스크롤 지원 (`PostDetailPage`)
  - 첨부파일이 없을 때 첨부 영역 자동 숨김
  - JoditEditor ReadOnly 모드에서 내부 스크롤 활성화 (CSS height chain + overflow 설정)
- 일정 추가 TimePicker 교체: WinUI 3 TimePicker → 커스텀 `CompactTimePicker` (NumberBox 기반)
  - 마우스 휠/스핀버튼/직접 입력으로 시·분 변경 가능
- 교사 시간표 표시 모드 수정: `DisplayMode`를 `Teacher`로 설정하여 과목+강의실 표시 (`TeacherTimetablePage`)

### 기능 개선
- 업데이트 확인 메뉴 추가: 설정 하위에 수동 업데이트 확인 항목 추가 (`MainWindow`)
- 일정 구글 동기화 라벨 수정: 카테고리 "학급"일 때만 구글 동기화 옵션 표시, "담임"은 제외 (`UnifiedItemDialog`)
- 게시물 읽기 인쇄 기능 개선: PDF 생성 방식 → JoditEditor 네이티브 인쇄로 변경 (`PostDetailPage`)
  - HTML 서식(표, 이미지, 글꼴, 색상) 완전 보존
  - 화면 85% 크기 미리보기 창(최대 1024×900)을 중앙에 열고 시스템 인쇄 대화상자 표시
- 좌석배정표 인쇄 개선 (`SeatsPrintService`, `PhotoCard`)
  - 미표시 좌석 옵션 추가: 우클릭 메뉴에서 "미표시 좌석" 체크 시 인쇄에서 해당 좌석 완전 숨김
  - A4 하단 정렬: 좌석+교탁이 용지 아래쪽부터 배치, 상단에 여백
  - 사진 최대화: 셀 내 3:4 비율 유지하면서 가용 공간에 최대 크기로 표시
  - 줄 사이 통로: 짝(jjak)은 붙이고 줄(jul) 사이에 10pt 통로 간격 삽입
  - 클린 레이아웃: 셀 테두리/배경 제거, 이름은 사진 너비 기준 가는 테두리 박스로 표시
  - 1페이지 보장: 모든 요소 높이를 A4 가용 영역에서 역산하여 오버플로 방지
- 좌석배정 짝 분리/고정 설정 (`SeatExclusionDialog`, `PageSeats`)
  - 짝 분리: 특정 학생 쌍이 인접하지 않도록 자동배정 시 제약 적용
  - 짝 고정: 특정 학생 쌍이 반드시 인접하도록 자동배정 시 제약 적용
  - "● 자리 배정" 제목의 불릿 버튼으로 설정 다이얼로그 진입
- 좌석배정 배열 UI 개선 (`PageSeats`)
  - 줄 설정: ComboBox → NumberBox(2~8, "N줄" 접미사 포맷터) 교체
  - 짝 설정: ComboBox → CheckBox 교체 (체크=짝2명, 해제=1명)
  - 인라인 도트 패턴: 배열 시각화 (예: ●● ●● ●●)
  - 좌측 패널 레이아웃 통합: 필터+학생목록 단일 블록

### 성능 개선
- GoogleSyncService 리소스 누수 수정: 정적 필드로 인스턴스 보관, 앱 종료 시 Dispose 호출 (`App.xaml.cs`)
- StudentLogViewModel IDisposable 구현: 3개 서비스(StudentLogService, EnrollmentService, StudentService) Dispose 보장
- ClassDiaryViewModel IDisposable 구현: ClassDiaryService Dispose 보장
- StudentLogDialog 이벤트 누수 수정: `Activated` 핸들러를 1회 실행 후 즉시 해제
- PageStudentLog 이벤트 해제 추가: `Unloaded` 시 `StudentSelected` 핸들러 해제
- 일괄 내보내기 N+1 쿼리 제거: `LoadStudentAsync` (학생당 DB 4회) → `LoadFromEnrollment` (DB 0회)
  - 30명 기준 120회 DB 호출 제거
- StudentLogRepository GetOrdinal 캐싱: `ReadAllLogsAsync` 헬퍼 추가, 첫 row에서 캐시 초기화 후 재사용
- HttpClient 연결 풀 최적화: `SocketsHttpHandler` 적용 (연결 수명 2분, GZip 압축, 쿠키 비활성) (`GoogleCalendarApiClient`)
- FileLogger Dispose 개선: 이미 완료된 Task는 Wait 생략, 타임아웃 5초→2초 단축
- 이미지 캐시 LRU 개선: 히트 시 제거→재삽입으로 최근 접근 항목 보존 (`PhotoService`)
- 일괄 내보내기 중복 정렬 제거: 단일 학기+필터 없음일 때 불필요한 `OrderByDescending` 생략
- 앱 시작 DB 초기화 병렬화: Board/Scheduler/School 3개 DB `Task.WhenAll` 동시 초기화 (`App.xaml.cs`)
- 진도 매트릭스 SolidColorBrush 캐싱: 20개 브러시를 static readonly 필드로 캐시, 셀·호버·선택 시 재생성 제거 (`ProgressMatrixPage`)
- PostListPage PropertyChanged 누수 수정: `NavigationCacheMode.Enabled`에서 `OnNavigatedFrom`/`OnNavigatedTo` 구독 관리로 중복 핸들러 방지
- StudentCard 이중 Unloaded 구독 수정: DI 생성자에서 `-=` 후 `+=`으로 중복 방지 (`StudentCard`)
- N+1 배치 INSERT: `LessonRepository.CreateFromSchedulesAsync` — 개별 INSERT → 트랜잭션+파라미터 재사용 배치
- N+1 배치 INSERT: `CourseEnrollmentRepository.BulkCreateAsync` — 개별 CreateAsync → 트랜잭션+파라미터 재사용 배치
- N+1 배치 INSERT: `SchoolScheduleRepository.CreateBulkAsync` — 행별 중복체크 SELECT+INSERT → 일괄 SELECT 후 메모리 필터+배치 INSERT
- Board ReaderColumnCache 적용: `PostRepository`, `CommentRepository`, `PostFileRepository`에 `ReaderColumnCache`+`ExecuteListAsync` 도입
  - 댓글 100개 기준 GetOrdinal 1,200회 → 9회 (99% 감소)
- StudentCardViewModel IDisposable 인터페이스 선언 추가 (기존 Dispose 본문은 유지)
- StudentLogRepository date() 함수 제거: WHERE 절에서 `date(sl.Date)` → `sl.Date` 직접 비교로 인덱스 활용
- SchoolScheduleConverters 정적 브러시 캐싱: `BoolToVacationColorConverter`, `BoolToTodayBackgroundConverter`에 static readonly 브러시
- DayCell 인스턴스 브러시 캐싱: `_whiteBrush`, `_transparentBrush` 필드 추가
- AnnualLessonPlanViewModel PropertyChanged 등호 검사: 10개 setter에 `if (_field == value) return;` 추가
- PostRepository SELECT 컬럼 명시: GetListAsync에서 `SELECT *` → 14개 컬럼 지정
- COUNT(*) → EXISTS 변환: 11개 파일, 13개 존재 확인 쿼리 최적화
- Board BaseRepository `[Conditional("DEBUG")]`: LogDebug/LogInfo에 적용, Release 빌드에서 문자열 보간 제거
- SQLite PRAGMA 성능 튜닝: `cache_size=10000`, `mmap_size=30000000` — Board.cs, Scheduler, 양쪽 BaseRepository에 추가
- AnnualLessonPlanPage `{Binding}` → `{x:Bind}` 변환 (DataTemplate 내 string 바인딩)
- OptimizedObservableCollection 확대: PostListViewModel, PostDetailViewModel에서 Clear+Add 루프 → ReplaceAll 단일 호출
- DayCell 불필요한 Task.Run(async) 래핑 제거: 직접 await로 스레드풀 홉 제거
- App.xaml.cs fire-and-forget 안전망: TryStartGoogleSyncAsync에 ContinueWith(OnlyOnFaulted) 추가
- COUNT(*) OVER() 윈도우 함수: PostRepository.GetListWithCountAsync 신규, BoardService 페이징에서 DB 2회 → 1회 호출
- AddWithValue boxing 제거: PostRepository, CommentRepository, LessonProgressRepository, StudentLogRepository — 73개 int/bool 파라미터를 typed Add()로 변환

### 정리
- Tools.cs 미사용 코드 삭제 (496줄 → 155줄):
  - 메서드 6개: `WaitAsync`, `IsConnected`, `IsNumeric`, `FindVisualParent`, `FindVisualChild`, `GetWeekOfMonth`
  - 컨버터 8개: `YearToAcademicYearConverter`, `GradeConverter`, `ClassConverter`, `HtmlToPlainTextConverter`, `SizeFormatConverter`, `UtcToLocal`, `NeisByteConverter`, `InverseBooleanConverter`
- StudentLogViewModel 미사용 코드 삭제: `CalculateNeisByte()` 메서드, 주석 처리된 `LogByteCount`/`LogCharCount`/`LogByteInfo` 프로퍼티

## 개발 이력 — 2026-03-30 (구 v1.0.3)

### 보안
- SQL Injection 취약 메서드 제거: `Sqlite.cs`의 `CountRecord`, `GetCondition` (미사용 레거시 코드 삭제)
- XSS 차단: JoditEditor 붙여넣기/드래그앤드롭 시 DOMPurify로 HTML sanitize 적용
  - `<script>`, `<iframe>`, `<embed>`, `onclick=` 등 위험 요소 차단
- 주민번호 DPAPI 암호화: `StudentRepository`에서 저장 시 암호화, 읽기 시 복호화
  - 기존 평문 데이터 호환 (복호화 실패 시 평문으로 간주, 재저장 시 자동 암호화)
- 로그 민감정보 제거: `StudentLogRepository`, `StudentRepository`, `TeacherRepository`
  - 파라미터 값 덤프 삭제, 학생/교사 이름·ID 로깅 제거

### 정리
- 미사용 코드 대규모 삭제 (17개 파일):
  - 기능 체인: TodoItem, WorkLog, SchoolEvent (Model+Repository+Service), Evaluation (Model+Repository)
  - 첨부파일 시스템: AttachmentBox, AttachmentService, AttachmentRepository, Attachment 모델
  - 미사용 개별 파일: StudentDragEventArgs, StudentLogWin, SchoolTextBox, UIHelpers, ValidationHelper, EventAggregator
  - DB의 Attachment/Evaluation 테이블·인덱스 생성 코드 제거
- SchoolService 계층 통일: 3곳의 SchoolRepository 직접 호출 → SchoolService 경유로 변경
  - `SaveSchoolAsync()` Upsert 메서드 추가, 중복 코드 ~85줄 → 9줄로 축소
  - Page → Service → Repository → DB 패턴 통일

### 기능 개선
- 진도 매트릭스 × 연간계획 연동
  - "계획" 열 추가: WeeklyUnitPlan에서 각 단원의 계획 주차 표시
  - 현재 주차 경계선: 오늘 날짜 기준 빨간 가로선으로 "여기까지 진행 예정" 표시
  - 지연 경고: 계획 주차를 지났으나 미완료인 셀에 "!" 표시 및 붉은 배경
  - 하단 요약에 계획 진도/현재 주차 통계 표시
- 수업 배치(단원 배치) 전체 강의실 보기
  - 학급 선택에 "전체" 옵션 추가: 모든 강의실 배치 현황을 통합 표시
  - 강의실명(`[1-3]` 등) 각 슬롯에 표시
  - 자동 배치/실행취소는 개별 학급 선택 시에만 활성화
- 업데이트 서비스 GitHub 사용자명 수정 (Centwons → centwon)

### 버그 수정
- 달력 할일 2일 표시 버그: 할일 생성 시 `End = Start + 1일` → `End = Start`로 수정 (inclusive 방식 통일)
  - `NewTaskEvent`, `TaskDueDatePicker_DateChanged`, 반복 할일 생성 모두 수정
- StudentLogViewModel.Date: `ToUniversalTime()` → `new DateTimeOffset()` 변환 수정 (UTC 변환으로 날짜가 하루 전으로 표시되는 버그)
- GoogleSyncService: `DateTime.Parse` → `RoundtripKind` + `.ToLocalTime()` 추가 (시간대 무시 파싱 수정)

### 정리
- 중복 `DateTimeToDateTimeOffsetConverter` 제거 (Tools.cs 삭제, CommonConverters.cs만 유지)

## 개발 이력 — 2026-03-24 (구 v1.0.1)

### 버그 수정
- 학생 삭제 쿼리 컬럼명 오류 수정 (`WHERE ID` → `WHERE StudentID`)
- SchoolCode 미설정 시 필터 연쇄 실패 방어 처리
- 진도 분석 시 빈 컬렉션 크래시 수정 (`First()` → `FirstOrDefault()`)
- 구글 캘린더 시간대 오차 수정 (`DateTime.Now` → `DateTime.UtcNow`)
- CourseSectionRepository SQL 문자열 보간 → 파라미터 바인딩
- StudentRepository `DateTime.Parse` → `TryParse` (잘못된 DB 값 크래시 방지)
- StudentRepository 내 EnrollmentRepository 연결 누수 수정 (`using` 추가)
- EnrollmentRepository `GetByGradeAsync` 불필요한 파라미터 바인딩 제거
- JoditEditor 붙여넣기 중복 삽입 수정 (`async` → 동기 함수로 `return false` 정상 동작)
- 메모 저장 시 기존 제목 덮어쓰기 방지 (제목이 비어있을 때만 자동 생성)

### 성능 개선
- 진도 매트릭스 조회 O(n²) → Dictionary 인덱싱 O(1)
- HelpPage WebView2 메모리 해제 (`OnNavigatedFrom`에서 `Close()`)
- 메뉴 네비게이션 시 Frame BackStack 정리

### 기능 개선
- 누가기록 일괄 출력: PDF 표 형식으로 전체 학생 한 문서 출력 (동일 학생 번호/이름 행 병합, 가로 A4)
- 누가기록 일괄 출력 버튼 단순화: MenuFlyout 제거 → 필터 다이얼로그에서 형식 선택
- 파일 저장 위치 통일: `문서\NewSchool` → `Settings.UserDataPath` (포터블 모드 호환)
  - 적용: 누가기록 PDF/엑셀, 게시글 PDF, 좌석배정표 PDF, 학생명단 템플릿
- 학생부 일괄 출력: PDF/엑셀로 학급 전체 특기사항 한 문서 출력 (영역/상태/빈항목 필터)
- 학생부 일괄 입력 버튼 추가: 페이지에서 바로 BatchDialog 진입
- 학생부 일괄 입력 시 누가기록 참조 패널 (토글식, 해당 영역 DraftSummary 표시)
- 학생부 초안 자동 생성: 누가기록 DraftSummary 병합하여 특기사항 초안 채움
- 학생부 삭제 로직 변경: 내용 초기화 → DB에서 실제 삭제

### 정리
- Ktask/KtaskList 레거시 테이블 및 인덱스 제거
- KEvent CREATE TABLE에 `ItemType`, `IsDone`, `Completed` 컬럼 포함
- 게시 폴더 정리 강화 (빈 폴더, Installer, prerequisites 제외)

## 개발 이력 — 2026-03-22 (구 v1.0.0)

- 최초 릴리스
- 학급 경영: 학급 일지, 학생 정보, 누가 기록, 학생부, 자리 배정, 게시판, 시간표
- 수업 관리: 교과 등록, 수강 배정, 연간 계획, 진도 관리, 수업 기록, 동아리
- 일정 관리: 캘린더, 구글 캘린더 양방향 동기화
- 업무/아카이브: 카테고리별 게시판
- 설정: 학교 검색(NEIS), 학사일정, 학생 엑셀 가져오기, 백업/복원
- HTML 도움말 페이지 (WebView2)
- GitHub 기반 업데이트 확인
